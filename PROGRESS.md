# PROGRESS.md — Log de execução do `the-light-app`

> Uma linha por tarefa concluída e verde (regra 9 da seção 0 do
> `IMPLEMENTATION_PLAN.md`): ID · data · resumo de 1 linha · commit. O log é
> append-only e serve de trilha auditável do loop autônomo.

| ID    | Data       | Resumo                                                                 | Commit    |
| ----- | ---------- | --------------------------------------------------------------------- | --------- |
| F0.0  | 2026-06-29 | Confirmou toolchain e registrou versões do ambiente (sem código de produto). | `efe334f` |
| F0.1  | 2026-06-29 | Bootstrap da estrutura do repo + docs de processo (DECISIONS/PROGRESS/.gitignore). | `595c70c` |
| F0.2  | 2026-06-29 | Crate `core/` (the-light-app-core) com fronteira UniFFI trivial (`ping`) compilando; uniffi 0.31.2, modo library. | `7b922eb` |
| F0.3  | 2026-06-29 | `parse_reference` na fronteira UniFFI delegando ao `the-light-core` (git dep pinada rev `0888ac0`, core v1.2.0); "Jo 3.16"=="John 3:16". | `9881c72` |
| F0.4  | 2026-06-29 | Script reproduzível `gen-bindings.sh` gera bindings TS via `ubrn` 0.31.0-3 (ADR-0004); `bindings/*.ts` com `parseReference`/`ping`/tipos. | `e19064a` |
| F0.5  | 2026-06-29 | App Expo mínimo (SDK 56, expo-router) em `app/` com tela `TextInput`+resultado (sem core); export web compila, `tsc` limpo. Node 25 compat. SDK 56. | `3262f56` |
| (core) | 2026-06-30 | **the-light**: feature `embedded` (default-on) destrava build wasm da lógica pura (branch `feat/core-wasm-feature-gating`, mesclado na `main`). ADR-0005. | `8f66004`† |
| F0.6a | 2026-06-30 | Re-pin do core p/ `8f66004` + matriz de features por alvo (web=puro / nativo=embedded). Build nativo + 5 testes OK; `cargo tree` confirma 0 deps pesadas no wasm. **Bloqueio SQLite-no-WASM resolvido.** | `9f37bc4` |
| F0.6c | 2026-06-30 | Alinhar `uniffi` da fronteira a `=0.31.0` (compat. runtime web do ubrn); fronteira verde (5 testes) + JSI regenera; ADR-0006. Destrava retry da F0.6b. | `7b98644` |
| F0.6b | 2026-06-30 | **Caminho web/WASM fechado**: `ubrn build web` + `gen-bindings-web.sh` → wasm-bindgen; glue `app/web/`; tela ligada; prova headless `parseReference` PT==EN (43/3/16) pelo Rust; `expo export web` ok. ADR-0007. | `1cdde6c` |
| F0.7 | 2026-06-30 | **Alvo iOS fechado**: `ubrn build ios` → Turbo Module JSI; integração Expo (prebuild+New Arch codegen+CocoaPods); run real no simulador iPhone 17/iOS 26.5; prova headless PT==EN (43/3/16) pelo Rust nativo. ADR-0008. | `a6f6797` |
| F0.8 | 2026-06-30 | **Alvo Android fechado**: `ubrn build android` (cargo-ndk→jniLibs) → Turbo Module JNI/JSI; integração Expo (prebuild+autolink+New Arch); run real no emulador headless; prova headless PT==EN (43/3/16) via `adb logcat`. ADR-0009. **Os 3 alvos provados.** | `36af016` |
| F0.9 | 2026-06-30 | **Store nativo provado**: `get_passage` delega ao `the-light-core` (`Store::open`+`EmbeddedSource::passage`); lê João 3:16 do `assets/data/sample.sqlite` versionado (KJV domínio público, gerado via `scripts/gen-sample-db.sh`→`Store::open`). Teste host verde (texto KJV verbatim + 43/3/16). Gating de **corpo** por alvo (web=stub) mantém wasm puro (sem rusqlite) e `ubrn build web` verde. ADR-0010. | `1d16897` |
| F0.10 | 2026-06-30 | **Store web provado**: `getPassage` web em TS — parseReference PELO RUST (wasm) + `queryPassage` espelhando `EmbeddedSource::passage` num `wa-sqlite@1.0.0` (build sync, sem SharedArrayBuffer); persistência OPFS + leitura em VFS de memória hidratado. Tela web exibe o TEXTO do store. Prova headless node verde (KJV verbatim + 43/3/16, texto do store). `expo export web` 0 com wa-sqlite + sample no bundle; sem regressão (F0.6b/F0.7/F0.8/F0.9). ADR-0011/ADR-0012. | `d6e968d` |
| **F0.11** | 2026-06-30 | **🎯 MARCO 0 APROVADO (sign-off humano)** — viabilidade da ponte Rust→Expo provada nos 3 alvos + store nativo/web; Fase 0 concluída. **Fase 1 liberada.** | `aed825d` |
| F1.1 | 2026-06-30 | **Banco bíblico COMPLETO embarcado**: `scripts/gen-bible-db.sh` roda o `xtask import` canônico do the-light (rev pinado `8f66004`) SEM tocá-lo (checkout do cargo + `CARGO_TARGET_DIR` fora) → `assets/data/bible.sqlite` (~27 MB) com KJV (en) + Almeida 1911 (pt), domínio público. Contagens validadas LENDO do banco: kjv=**31102**, alm1911=**31101**, 66 livros/versão, `verses_fts` 62203 (acento-insensível: `MATCH 'ceus'`→384 "céus"). Idempotente (2× estável). Armazenamento: gerar-ignorado (`bible.sqlite`+`.cache/` no .gitignore; `sample.sqlite` segue versionado). ADR-0013. | `b1a9be4` |
| F1.2 | 2026-06-30 | **Leitura no core**: fronteira expõe `list_translations`/`list_books`/`get_chapter`/`chapter_count` delegando ao `the-light-core` (gating de corpo por alvo + stub web; `list_books` puro via `reference::BOOKS`). Teste host 14/14 (66 livros; `chapter_count(kjv,43)==21`; João 3 numerado, v16 KJV verbatim). Web puro (sem rusqlite). | `e06b0c6` |
| F1.3 | 2026-06-30 | **UI de leitura NATIVA + bundling do banco**: telas expo-router livro→capítulo→texto + seletor de versão (`app/app/read/**` + `components/Reader*`), glue `app/web/reading.ts` delegando à fronteira F1.2 (JSI→core; web=stub F1.13). Bundling (ADR-0014): subset de leitura `reading-sample.sqlite` (~1,8 MB; KJV+Almeida de Gn/Sl/Jo — João KJV 21 caps) extraído do `bible.sqlite` via `gen-reading-sample-db.sh` (texto verbatim do store), empacotado como asset (`assetExts`+symlink) e copiado p/ `documentDirectory` no 1º boot (`expo-asset`+`expo-file-system`, `app/lib/db.ts`). Prova HEADLESS no simulador iPhone 17: `TLA_READ books=66 john3_v16="For God so loved the world…" john_chapters=21` (texto do retorno de `get_chapter`), além de PT/EN (sem regressão); `tsc`+`expo export web` verdes. Bindings nativos regenerados (`ubrn build ios`) — core/the-light intocados. ADR-0014. | `9cf4a9e` |
| F1.4 | 2026-06-30 | **Leitura LADO A LADO + tema claro/escuro (nativo)**: `ReaderParallelView` exibe o mesmo capítulo em 2 traduções (KJV\|Almeida) alinhadas pelo número do versículo (união ordenada dos `Single`; lacuna→coluna vazia), recebendo 2 `Passage` de DUAS chamadas de `get_chapter` (toggle "lado a lado" + 2º seletor em `read/[book]/[chapter].tsx`) — alinhamento é apresentação, SEM SQL/texto em TS, SEM nova função de fronteira. Tema: `app/lib/theme.ts` (tokens light/dark + `useTheme()` via `useColorScheme` + override por SESSÃO/contexto, sem dep nova nem persistência em disco) migra os `Reader*` e a tela do capítulo de hex hardcoded p/ tokens; `ThemeToggleButton` no header. Prova HEADLESS no simulador iPhone 17: `TLA_PARALLEL kjv_john3_16="For God so loved the world…" alm_john3_16="Porque Deus amou o mundo de tal maneira…"` (AMBOS do retorno de `get_chapter`, não hardcoded), além de `TLA_READ`/PT/EN (sem regressão); `tsc`+`expo export web` verdes. core/the-light intocados (`8f66004`). ADR-0015. | `24cd3b8` |
| F1.5 | 2026-06-30 | **Busca FTS5 na fronteira**: `search(db,query,translation,book?,limit?) -> Vec<SearchHit>` delegando a `BibleSource::search` (the-light-core); Record `SearchHit{reference,translation,text,highlighted,score}` via `From`; corpo gated cfg(not wasm32)+stub web. Teste host 20/20 (6 novos): João 3:16 verbatim, acento-insensível `ceus`→`céus`, vazio sem panic, limit, tradução inexistente→CoreError. Web puro (sem rusqlite); sem FTS/SQL reimplementado. | `f006a31` |
| F1.6 | 2026-06-30 | **UI de busca NATIVA**: tela `app/app/search` (TextInput+debounce → `search` da F1.5 → FlatList), `ReaderSearchResultItem` (ref + snippet com HL markers U+0002/U+0003→estilo via `highlight.ts`, controles nunca exibidos), referência clicável → `read/[book]/[chapter]`. `verses_fts` populado no gerador de dados (subset; 9746 linhas FTS). Prova HEADLESS iOS: `TLA_SEARCH query="God" hits=646 first_ref="John 3:16" first_text="For God so loved…"` (do retorno de `search`). Sem FTS/SQL em TS; sem regressão (READ/PARALLEL/parse/web); core/the-light intactos. | `ab75071` |
| F2.3 | 2026-07-01 | **PR ao `the-light-core` — Gemini (D1) + streaming no trait (D4)** *(PR sancionado, ADR-0023)*: implementado no branch `feat/ai-gemini-streaming`, **revisado** (workspace 184 testes, 7 novos; fmt/clippy limpos; não-quebrante) e **mergeado pelo humano** na `main` do `the-light` → rev **`133077a`**. `GeminiProvider: LlmProvider` registrado (`PROVIDERS`/`default_model=gemini-2.0-flash`/`build_provider` exige key/`estimate_cost_usd`); `gemini_body`/`gemini_extract` puros e testados; POST a `generativelanguage.googleapis.com`, key em header `x-goog-api-key` (nunca logada/na URL). `LlmProvider::complete_stream` com **default não-quebrante** (SSE real por provedor = evolução). Driver **re-pinou** `core/Cargo.toml`+`Cargo.lock` (8f66004→`133077a`) nos 2 blocos (web puro + nativo embedded): 37 testes da fronteira verdes, **grafo wasm ainda puro** (sem rusqlite/reqwest do novo `ai`), fmt/clippy limpos. D2 (wasm-decouple p/ IA web) = PR focado separado antes da F2.7. | `133077a`† |
| F2.4 | 2026-07-01 | **BYOK — chave nativa segura (`expo-secure-store`) + stub web**: serviço `app/lib/keystore.ts` (nativo) grava/lê/apaga a **API key por provedor** (anthropic/openai/gemini/ollama) no **Keychain/Keystore** (ids `tla.apikey.<provider>`, valor nunca impresso); `createKeystore(backend)` com backend injetável; `listProviders()` devolve só NOMES; import lazy; trim/rejeita vazio. `keystore.web.ts` = **stub sem chave no web** (D3/ADR-0023; molde `db.web.ts`). Implementa **D3** (BYOK API key indolor; login-de-conta OAuth rejeitado). UI de colar/deep-link fica na F2.5. Prova HEADLESS (backend fake em memória, **sem chave real**): round-trip por provedor, provedores isolados, validação; **chave NUNCA logada** (espião de console = 0; fonte sem `console.*`) **nem versionada** (`git grep` do placeholder só no teste). `tsc`+`expo export web` verdes (stub não puxa secure-store ao bundle); the-light/core intactos (`8f66004`, diff vazio; commit só toca `app/**`). App-side puro, não-bloqueante. A chave só é USADA com rede na F2.6. | `34c831f` |
| F2.2 | 2026-07-01 | **🚦 GATE estratégico — arquitetura da IA (DECIDIDO por sign-off humano)**: **D1** Gemini via **PR ao `the-light-core`** (`GeminiProvider: LlmProvider`; fonte única, precedente ADR-0005) · **D2** IA no web via **PR ao core** desacoplando `ai::study`/`citation` de `research::WebSource`/`reqwest` p/ compilar em wasm (anti-alucinação numa só impl Rust; chamada HTTP via `fetch`, ADR-0011) · **D3** **BYOK API key indolor** (Keychain via `expo-secure-store`, 1×, nunca em git/log) — **login-de-conta OAuth REJEITADO** após pesquisa (2026: OpenAI "Sign in" só identidade; Anthropic e Google **baniram** tokens OAuth de consumidor em apps de terceiros, com risco de banir a conta do usuário) · **D4** **streaming** (tokens incrementais). Re-escopa F2.3 (Gemini no core), F2.4 (chave nativa), F2.7 (IA web). **ADR-0023.** `loop/HALT` removido. | _(sign-off)_ |
| F2.1 | 2026-07-01 | **IA — pergunta ancorada (`ask`) na FRONTEIRA + MOCK (Fase 2 abre)**: Record `AiAnswer{reference,cited_text,interpretation,provider,model}` (puro, todos os alvos) + `ask_anchored(db,translation,reference,question,provider_name,key:Option<String>,model:Option<String>,lang) -> Result<AiAnswer,CoreError>` **delegando** a `the_light_core::ai::{numbered_passage,ask_context,build_provider,ask}` (o core já tem o módulo `ai`, `embedded`-only) + leitura da passagem via `Store::open`/`EmbeddedSource::passage` (rota F1.2); corpo `cfg(not wasm32)` + stub web (IA/rede fora do grafo wasm). Provider `"mock"` (`key=None`, **sem rede/sem chave**). **Anti-alucinação (contrato):** `AiAnswer` separa `cited_text` (verbatim do store) da `interpretation` (LLM). Teste host **37/37** (3 novos `ai_tests`): `cited_text` = KJV verbatim do store; prova anti-fake — 3:16 vs 3:17 o `cited_text` MUDA (segue o store) mas a `interpretation` do mock é INVARIANTE; error paths (ref inválida/tradução/provider) → `CoreError` sem panic. Grafo wasm puro (`cargo tree wasm` sem rusqlite/reqwest); `gen-bindings-web` 0 (`askAnchored`/`AiAnswer` stub web); the-light/core intactos (`8f66004`), `Cargo.toml` inalterado. Gemini/IA-web/chave = gate F2.2. | `357abca` |
| F1.17 | 2026-07-01 | **🎯 MARCO 1 APROVADO (sign-off humano)** — leitura offline completa, multiplataforma. Todas as 16 tarefas da Fase 1 (F1.1–F1.16) aceitas e verdes nos **3 alvos** (iOS + Android + Web): leitura (nav + versões + lado a lado + tema), busca FTS5, referências cruzadas (CC-BY OpenBible visível) e notas/highlights + export — com provas determinísticas (`TLA_*` no device / headless node no web) e review independente por tarefa. Regras não negociáveis mantidas: `the-light` intocado (`8f66004`); offline-first (assets locais, zero rede, sem SharedArrayBuffer); anti-alucinação (texto domínio público do store; xref CC-BY com atribuição); BYOK (sem segredos). ADRs 0013–0022. **Fase 2 (IA BYOK + Gemini) LIBERADA**; corpus completo (~59 MB) permanece backlog transversal. `loop/HALT` removido. | _(sign-off)_ |
| F1.16 | 2026-07-01 | **Paridade WEB de notas/highlights + export (Opção A/A1)**: as 7 funções de userdata (`putNote`/`getNote`/`deleteNote`/`listNotes` + `addHighlight`/`removeHighlight`/`listHighlights`) DESTUBADAS em `app/web/reading.web.ts`, persistindo em **OPFS** num `data_dir` web separado do conteúdo só-leitura. Como o módulo `userdata` do core é `embedded`-only (não está no wasm), o web **reimplementa o I/O como INFRA** (precedente ADR-0011), **espelhando o formato do core** byte-a-byte: slug `notes/<slug>.md` (`John 3:16`→`John_3.16.md`, Range→`Genesis_1.1-3.md`, WholeChapter→`Psalms_23.md`), `.md` só-corpo, `highlights.json` array `{ref,color,tag?}` pretty 2sp (tag omitido), `list_notes` ordenado por ref canônica / `list_highlights` por inserção (add substitui mesma ref). Referência canonicalizada pelo **wasm** (`parseReference` + `listBooks().nameEn`) nas duas direções. Novos glues `app/web/userdata-fs.web.ts` (VFS-agnóstico) + `userdata-opfs.web.ts` (backend OPFS); `userdata.web.ts` retorna sentinela `'web:userdata'` (UI `ReaderVersePanel` funciona no web). EXPORT reusa `buildNotesExport` (idêntico ao nativo). Prova HEADLESS node (OPFS-like em memória): `WEB_NOTES note_ref="John 3:16" note_len=36 highlights=1 persisted=true export_ok=true` (persistência relida de novo handle; slug == nativo F1.11). Leitura/busca/xref web sem regressão; `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). ADR-0022. **Última paridade web — Fase 1 funcionalmente completa nos 3 alvos.** | `7557f0e` |
| F1.15 | 2026-07-01 | **Paridade WEB de xref + atribuição CC-BY (Opção A/A1)**: `crossRefs(book,chapter,verse,minVotes?,limit?)` em `app/web/reading.web.ts` DESTUBADO espelhando o SELECT de `xref::for_verse` (`SELECT to_book,to_chapter,to_verse_start,to_verse_end,votes FROM cross_references WHERE from_*=? AND votes>=? ORDER BY votes DESC, to_book, to_chapter, to_verse_start LIMIT ?`; clamp≥1; Single se `to_verse_start>=to_verse_end` senão Range; xref independe de tradução), sobre o subset, **reusando** `openReadingDbWeb` das F1.13/F1.14 (sem recarregar). Novo glue `app/web/sqlite-xref.web.ts`. Defaults minVotes=1/limit=20 (= core/F1.8); `votes` i64→bigint. Telas `ReaderXrefPanel`/`ReaderVersePanel` funcionam no web; **atribuição CC-BY** EXATA "Cross references courtesy of OpenBible.info (CC-BY)" (ADR-0016) confirmada no bundle. Prova HEADLESS node: `crossRefs(43,3,16)` → 9 xrefs, **João 3:15 1º por votos (439, Single)** = paridade c/ `TLA_XREF` nativo; Range provado (João 11:25-26, 400); verso sem xref→[]; minVotes/limit respeitados. Sem ordenação/filtro reimplementado em TS (só espelho do SQL); leitura/busca web sem regressão; `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). ADR-0021. | `1fe14e6` |
| F1.14 | 2026-07-01 | **Paridade WEB de busca FTS5 (Opção A/A1)**: `search` em `app/web/reading.web.ts` DESTUBADO espelhando o SQL FTS5 do core (`SELECT … highlight(verses_fts,0,?,?) AS hl, bm25(verses_fts) AS score FROM verses_fts JOIN verses … WHERE verses_fts MATCH ? AND translation_id=? [AND book_number=?] ORDER BY score LIMIT ?`; `has_translation` antes → UnknownTranslation; `build_match_query` split→aspas→AND), sobre o subset, **reusando** o store da F1.13 (sem recarregar). Novo glue `app/web/sqlite-search.web.ts`. **RISCO FTS5 resolvido (ADR-0020):** o `wa-sqlite@1.0.0` do npm NÃO traz FTS5 (`no such module: fts5`, 0 strings) → rebuild SYNC com `-DSQLITE_ENABLE_FTS5` da fonte (docker/emscripten, pinado no gitHead `5147454` = release npm 1.0.0) → vendorizado em `app/web/vendor/wa-sqlite-fts5/wa-sqlite.{mjs,wasm}` (asset local VERSIONADO, offline-first, sem SharedArrayBuffer); rebuild reprodutível via `scripts/build-wa-sqlite-fts5.sh`. Prova HEADLESS node: `search("God","kjv",-,1000)` → 646 hits, **João 3:16 localizado** (43/3/16, bm25 score −2.389, KJV verbatim, markers U+0002/U+0003 só em `highlighted`, não em `text`); acento-insensível `ceus`/`alm1911` → 20 hits c/ "céus"; vazio→[]; limit/book/tradução-inexistente→erro. Telas de busca (F1.6) funcionam no web; ref clicável → Reader. Leitura web sem regressão; sem ranqueamento reimplementado (bm25/highlight do FTS5); `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). | `b2f1d8f` |
| F1.13 | 2026-07-01 | **Paridade WEB de leitura (Opção A/A1)**: `app/web/reading.web.ts` DESTUBADO p/ leitura REAL sobre o subset `reading-sample.sqlite` (~4,4 MB, o MESMO do nativo) via `wa-sqlite` (build SYNC) OPFS no browser + MemoryVFS (molde F0.10). Novos `sqlite-reading.web.ts` (queries VFS-agnósticas) + `sqlite-reading-opfs.web.ts` (backend OPFS). Funções espelham os SELECTs do core (`getChapter`→`SELECT verse,text FROM verses … ORDER BY verse` + `has_translation`→UnknownTranslation; `chapterCount`→`max(chapter)`; `listTranslations`→`FROM translations ORDER BY language,id`); `listBooks` vem do **wasm/Rust** (`reference::BOOKS`, 66), não reescrito. As mesmas telas `read/**` (incl. lado a lado da F1.4) funcionam no browser via gate `useWasmReady` (no-op no nativo). Prova HEADLESS node (MemoryVFS): João 3:16 KJV+Almeida **verbatim** do store, `chapterCount=21`, João 3 KJV=36 vv, traduções [kjv,alm1911], 66 livros — **paridade** com `TLA_READ`/`TLA_PARALLEL`. `tsc`+`expo export web` verdes (subset 4.567.040 B + `wa-sqlite.wasm` 558.343 B no bundle). Sem domínio reimplementado; sem regressão nativa (diff nativo vazio); the-light/core intactos (`8f66004`). Busca/xref/notas web = F1.14–F1.16. ADR-0019. | `0479b5b` |
| F1.12 | 2026-06-30 | **🚦 GATE estratégico — store WEB do corpus (DECIDIDO por sign-off humano)**: escolhida a **Opção A** (`wa-sqlite` + OPFS espelhando os SELECTs do core em TS; reaproveita F0.10 + semântica FTS5/xref do SQLite; zero PR ao the-light), começando por **A1 = paridade com o subset `reading-sample.sqlite` (~4,4 MB)** via o MemoryVFS já provado. Corpus completo (~59 MB; 62.203 versículos, FTS5, 344.799 xrefs) + VFS-live em Worker/cota OPFS = decisão transversal separada (não bloqueia a paridade). Habilita F1.13–F1.16. **ADR-0018.** `loop/HALT` removido (motivo resolvido). | _(sign-off)_ |
| F1.11 | 2026-06-30 | **UI de notas/highlights NATIVA + export + persistência**: painel combinado `ReaderVersePanel` (nota markdown + highlight cor/tag + export + xref) aberto pelo gesto de seleção de versículo da F1.9; criar/editar/remover nota e marcar/desmarcar highlight com indicadores no Reader. Tudo lê/grava **pela fronteira F1.10** (glue `app/web/reading.ts` estendido: `putNote`/`getNote`/`deleteNote`/`listNotes`/`addHighlight`/`removeHighlight`/`listHighlights` → JSI → Rust); SEM reimplementar I/O/serialização/ordenação de userdata em TS. **Persistência** em `${documentDirectory}userdata/` (`app/lib/userdata.ts`), separado do `reading-sample.sqlite` só-leitura; sobrevive a restart (provado por 2ª leitura). **Export** = apresentação pura dos Records de `list_notes`/`list_highlights` (`app/lib/notesExport.ts`) via `Share` do react-native (sem `expo-sharing`). Atribuição CC-BY (ADR-0016) preservada no painel. Bindings nativos regenerados; web = stub (notas web = F1.16). Prova HEADLESS iPhone 17: `TLA_NOTES note_ref="John 3:16" note_len=55 highlights=1 persisted=true export_ok=true` (do retorno real), sem regressão READ/PARALLEL/SEARCH/XREF/parse; `tsc`+`expo export web` verdes; the-light intacto (`8f66004`), `core/src/lib.rs`+`Cargo.toml` não tocados. ADR-0017. | `deb3b0c` |
| F1.10 | 2026-06-30 | **Notas/marcações na FRONTEIRA (userdata file-based)**: 7 funções (`put_note`/`get_note`/`delete_note`/`list_notes` + `add_highlight`/`remove_highlight`/`list_highlights`) **delegando** a `the_light_core::userdata::{NoteStore,HighlightStore}` (situação A — o core já entrega a camada file-based); recebem um `data_dir` gravável (deriva `notes/*.md` + `highlights.json`; **NUNCA** o `bible.sqlite` só-leitura) e a referência como String canonicalizada por `parse_reference`. Records `Note{reference,body}`/`Highlight{reference,color,tag}` puros + `From<userdata::*>` gated `cfg(not wasm32)` (tipos-fonte `embedded`-only, molde F1.8); corpo gated + stub web. Teste host **34/34** (8 novos `userdata_tests` num diretório temporário, **relendo de OUTRO handle** = persistência em disco): round-trip (`John_3.16.md`), PT==EN mesma nota, lista ordenada por ref canônica, delete/remove idempotentes, vazio sem panic, ref inválida → `CoreError` antes do I/O. Web puro (`cargo tree wasm` sem rusqlite); `core/Cargo.toml` inalterado. UI/persistência device/export = F1.11; web = F1.16. | `b6381d0` |
| F1.9 | 2026-06-30 | **UI de xref NATIVA + atribuição CC-BY**: ao selecionar um versículo no Reader (`read/[book]/[chapter]`), `ReaderXrefPanel` lista as referências cruzadas vindas da fronteira `crossRefs` (F1.8→glue `app/web/reading.ts`→JSI→`xref::for_verse`, já ordenado por votos DESC) — cada item = referência+votos, clicável → `router.push` p/ `read/[book]/[chapter]` (molde F1.6); SEM SQL/ordenação/filtro de xref em TS (só apresenta o `Vec<CrossRef>`). Atribuição CC-BY EXATA "Cross references courtesy of OpenBible.info (CC-BY)" visível (ADR-0016). Subset bundled: gerador `gen_reading_sample_db.rs` propaga `cross_references` do `bible.sqlite` (ambos os lados em Gn/Sl/Jo) → `reading-sample.sqlite` com **22413** xrefs (João 3:16 → 9). Bindings nativos regenerados (`crossRefs`/`CrossRef`; `votes: i64`→`bigint` formatado via `String()`). Web = stub (xref web = F1.15). Prova HEADLESS iPhone 17: `TLA_XREF verse="John 3:16" count=9 first_ref="John 3:15" first_votes=439` (do retorno real de `cross_refs`), sem regressão READ/PARALLEL/SEARCH/parse; `tsc`+`expo export web` verdes; the-light intacto (`8f66004`), `core/src/lib.rs` não tocado. | `2ff73c5` |
| F1.8 | 2026-06-30 | **Xref na fronteira**: `cross_refs(db,book,chapter,verse,min_votes:Option<i64>,limit:Option<u32>) -> Vec<CrossRef>` delegando a `the_light_core::xref::for_verse(store.conn(),…)`; Record `CrossRef{reference,votes:i64}` via `From<xref::CrossRef>` sob `cfg(not wasm32)` (tipo-fonte `embedded`-only); corpo gated nativo + stub web. Teste host 26/26 (5 novos `xref_tests`, fixture `cross_references` à mão): Rm 5:8 (book 45/cap 5/`Single 8`, 50 votos) em [0] por votos DESC; `Range 8-9`; `min_votes=None` oculta disputada (-5) e `Some(-100)` inclui; `limit`; verso sem xref → vazio sem panic. Web puro (`cargo tree wasm` sem rusqlite); sem SQL/ranking reimplementado. Atribuição CC-BY exibida na F1.9. | `36b2cb0` |
| F1.7 | 2026-06-30 | **Referências cruzadas (DADOS)**: `scripts/gen-bible-db.sh` estendido roda, APÓS o `import` (verses+FTS), o subcomando DEDICADO `xtask import-xref` (≠ `import --xref`) canônico do the-light (rev pinado `8f66004`) SEM tocá-lo (checkout do cargo + `CARGO_TARGET_DIR` fora + `--locked`) → popula `cross_references` no MESMO `assets/data/bible.sqlite` com OpenBible.info/TSK (CC-BY; XREF_URL mirror raw scrollmapper, baixado 8.293.834 bytes). Contagem validada LENDO do banco: **344799** (guarda do xtask aborta <300k); João 3:16 (`from_book=43,ch=3,v=16`)→**23** xrefs; votos negativos preservados (1166); verses intactos (62203). Idempotente (2× estável; DELETE+reinsert). Banco ~51 MB (gerar-ignorado, ADR-0013 mantém-se; `.gitignore` inalterado). Atribuição CC-BY obrigatória: "Cross references courtesy of OpenBible.info (CC-BY)" (exibição visível = F1.9). ADR-0016. | `4d2d3bf` |

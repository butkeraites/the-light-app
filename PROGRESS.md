# PROGRESS.md — Log de execução do `the-light-app`

> Uma linha por tarefa concluída e verde (regra 9 da seção 0 do
> `IMPLEMENTATION_PLAN.md`): ID · data · resumo de 1 linha · commit. O log é
> append-only e serve de trilha auditável do loop autônomo.

| ID    | Data       | Resumo                                                                 | Commit    |
| ----- | ---------- | --------------------------------------------------------------------- | --------- |
| F0.0  | 2026-06-29 | Confirmou toolchain e registrou versões do ambiente (sem código de produto). | `efe334f` |
| F0.1  | 2026-06-29 | Bootstrap da estrutura do repo + docs de processo (DECISIONS/PROGRESS/.gitignore). | `595c70c` |
| F0.2  | 2026-06-29 | Crate `core/` (the-light-app-core) com fronteira UniFFI trivial (`ping`) compilando; uniffi 0.31.2, modo library. | `7b922eb` |
| F0.3  | 2026-06-29 | `parse_reference` na fronteira UniFFI delegando ao `the-light-core` (git dep pinada rev `0888ac0`, core v1.2.0); "Jo 3.16"=="John 3:16". | `9881c72` |
| F0.4  | 2026-06-29 | Script reproduzível `gen-bindings.sh` gera bindings TS via `ubrn` 0.31.0-3 (ADR-0004); `bindings/*.ts` com `parseReference`/`ping`/tipos. | `e19064a` |
| F0.5  | 2026-06-29 | App Expo mínimo (SDK 56, expo-router) em `app/` com tela `TextInput`+resultado (sem core); export web compila, `tsc` limpo. Node 25 compat. SDK 56. | `3262f56` |
| (core) | 2026-06-30 | **the-light**: feature `embedded` (default-on) destrava build wasm da lógica pura (branch `feat/core-wasm-feature-gating`, mesclado na `main`). ADR-0005. | `8f66004`† |
| F0.6a | 2026-06-30 | Re-pin do core p/ `8f66004` + matriz de features por alvo (web=puro / nativo=embedded). Build nativo + 5 testes OK; `cargo tree` confirma 0 deps pesadas no wasm. **Bloqueio SQLite-no-WASM resolvido.** | `9f37bc4` |
| F0.6c | 2026-06-30 | Alinhar `uniffi` da fronteira a `=0.31.0` (compat. runtime web do ubrn); fronteira verde (5 testes) + JSI regenera; ADR-0006. Destrava retry da F0.6b. | `7b98644` |
| F0.6b | 2026-06-30 | **Caminho web/WASM fechado**: `ubrn build web` + `gen-bindings-web.sh` → wasm-bindgen; glue `app/web/`; tela ligada; prova headless `parseReference` PT==EN (43/3/16) pelo Rust; `expo export web` ok. ADR-0007. | `1cdde6c` |
| F0.7 | 2026-06-30 | **Alvo iOS fechado**: `ubrn build ios` → Turbo Module JSI; integração Expo (prebuild+New Arch codegen+CocoaPods); run real no simulador iPhone 17/iOS 26.5; prova headless PT==EN (43/3/16) pelo Rust nativo. ADR-0008. | `a6f6797` |
| F0.8 | 2026-06-30 | **Alvo Android fechado**: `ubrn build android` (cargo-ndk→jniLibs) → Turbo Module JNI/JSI; integração Expo (prebuild+autolink+New Arch); run real no emulador headless; prova headless PT==EN (43/3/16) via `adb logcat`. ADR-0009. **Os 3 alvos provados.** | `36af016` |
| F0.9 | 2026-06-30 | **Store nativo provado**: `get_passage` delega ao `the-light-core` (`Store::open`+`EmbeddedSource::passage`); lê João 3:16 do `assets/data/sample.sqlite` versionado (KJV domínio público, gerado via `scripts/gen-sample-db.sh`→`Store::open`). Teste host verde (texto KJV verbatim + 43/3/16). Gating de **corpo** por alvo (web=stub) mantém wasm puro (sem rusqlite) e `ubrn build web` verde. ADR-0010. | `1d16897` |
| F0.10 | 2026-06-30 | **Store web provado**: `getPassage` web em TS — parseReference PELO RUST (wasm) + `queryPassage` espelhando `EmbeddedSource::passage` num `wa-sqlite@1.0.0` (build sync, sem SharedArrayBuffer); persistência OPFS + leitura em VFS de memória hidratado. Tela web exibe o TEXTO do store. Prova headless node verde (KJV verbatim + 43/3/16, texto do store). `expo export web` 0 com wa-sqlite + sample no bundle; sem regressão (F0.6b/F0.7/F0.8/F0.9). ADR-0011/ADR-0012. | `d6e968d` |
| **F0.11** | 2026-06-30 | **🎯 MARCO 0 APROVADO (sign-off humano)** — viabilidade da ponte Rust→Expo provada nos 3 alvos + store nativo/web; Fase 0 concluída. **Fase 1 liberada.** | `aed825d` |
| F1.1 | 2026-06-30 | **Banco bíblico COMPLETO embarcado**: `scripts/gen-bible-db.sh` roda o `xtask import` canônico do the-light (rev pinado `8f66004`) SEM tocá-lo (checkout do cargo + `CARGO_TARGET_DIR` fora) → `assets/data/bible.sqlite` (~27 MB) com KJV (en) + Almeida 1911 (pt), domínio público. Contagens validadas LENDO do banco: kjv=**31102**, alm1911=**31101**, 66 livros/versão, `verses_fts` 62203 (acento-insensível: `MATCH 'ceus'`→384 "céus"). Idempotente (2× estável). Armazenamento: gerar-ignorado (`bible.sqlite`+`.cache/` no .gitignore; `sample.sqlite` segue versionado). ADR-0013. | `b1a9be4` |
| F1.2 | 2026-06-30 | **Leitura no core**: fronteira expõe `list_translations`/`list_books`/`get_chapter`/`chapter_count` delegando ao `the-light-core` (gating de corpo por alvo + stub web; `list_books` puro via `reference::BOOKS`). Teste host 14/14 (66 livros; `chapter_count(kjv,43)==21`; João 3 numerado, v16 KJV verbatim). Web puro (sem rusqlite). | `e06b0c6` |
| F1.3 | 2026-06-30 | **UI de leitura NATIVA + bundling do banco**: telas expo-router livro→capítulo→texto + seletor de versão (`app/app/read/**` + `components/Reader*`), glue `app/web/reading.ts` delegando à fronteira F1.2 (JSI→core; web=stub F1.13). Bundling (ADR-0014): subset de leitura `reading-sample.sqlite` (~1,8 MB; KJV+Almeida de Gn/Sl/Jo — João KJV 21 caps) extraído do `bible.sqlite` via `gen-reading-sample-db.sh` (texto verbatim do store), empacotado como asset (`assetExts`+symlink) e copiado p/ `documentDirectory` no 1º boot (`expo-asset`+`expo-file-system`, `app/lib/db.ts`). Prova HEADLESS no simulador iPhone 17: `TLA_READ books=66 john3_v16="For God so loved the world…" john_chapters=21` (texto do retorno de `get_chapter`), além de PT/EN (sem regressão); `tsc`+`expo export web` verdes. Bindings nativos regenerados (`ubrn build ios`) — core/the-light intocados. ADR-0014. | `9cf4a9e` |
| F1.4 | 2026-06-30 | **Leitura LADO A LADO + tema claro/escuro (nativo)**: `ReaderParallelView` exibe o mesmo capítulo em 2 traduções (KJV\|Almeida) alinhadas pelo número do versículo (união ordenada dos `Single`; lacuna→coluna vazia), recebendo 2 `Passage` de DUAS chamadas de `get_chapter` (toggle "lado a lado" + 2º seletor em `read/[book]/[chapter].tsx`) — alinhamento é apresentação, SEM SQL/texto em TS, SEM nova função de fronteira. Tema: `app/lib/theme.ts` (tokens light/dark + `useTheme()` via `useColorScheme` + override por SESSÃO/contexto, sem dep nova nem persistência em disco) migra os `Reader*` e a tela do capítulo de hex hardcoded p/ tokens; `ThemeToggleButton` no header. Prova HEADLESS no simulador iPhone 17: `TLA_PARALLEL kjv_john3_16="For God so loved the world…" alm_john3_16="Porque Deus amou o mundo de tal maneira…"` (AMBOS do retorno de `get_chapter`, não hardcoded), além de `TLA_READ`/PT/EN (sem regressão); `tsc`+`expo export web` verdes. core/the-light intocados (`8f66004`). ADR-0015. | `24cd3b8` |
| F1.5 | 2026-06-30 | **Busca FTS5 na fronteira**: `search(db,query,translation,book?,limit?) -> Vec<SearchHit>` delegando a `BibleSource::search` (the-light-core); Record `SearchHit{reference,translation,text,highlighted,score}` via `From`; corpo gated cfg(not wasm32)+stub web. Teste host 20/20 (6 novos): João 3:16 verbatim, acento-insensível `ceus`→`céus`, vazio sem panic, limit, tradução inexistente→CoreError. Web puro (sem rusqlite); sem FTS/SQL reimplementado. | `f006a31` |
| F1.6 | 2026-06-30 | **UI de busca NATIVA**: tela `app/app/search` (TextInput+debounce → `search` da F1.5 → FlatList), `ReaderSearchResultItem` (ref + snippet com HL markers U+0002/U+0003→estilo via `highlight.ts`, controles nunca exibidos), referência clicável → `read/[book]/[chapter]`. `verses_fts` populado no gerador de dados (subset; 9746 linhas FTS). Prova HEADLESS iOS: `TLA_SEARCH query="God" hits=646 first_ref="John 3:16" first_text="For God so loved…"` (do retorno de `search`). Sem FTS/SQL em TS; sem regressão (READ/PARALLEL/parse/web); core/the-light intactos. | `ab75071` |
| F6.11 | 2026-07-04 | **Corrigir import de backup em OPFS vazia / fresh install (Fase 6 — bug de produto achado pelo harness)** — o smoke da F6.2 pegou: importar backup numa OPFS VAZIA (instalação limpa) lançava `NotFoundError` ("A requested file or directory could not be found") porque `userdata-opfs.web.ts` `resolveParent` chamava `getDirectoryHandle({create:false})` SEM guarda no caminho de leitura-antes-de-escrita. Fix: envolver os `getDirectoryHandle` da raiz/`userdata` no MESMO padrão `try/catch→null` que `readFile`/`deleteFile`/o laço de segmentos já usam — leitura (`create=false`) de dir ausente retorna `null`/vazio; escrita (`create=true`) segue criando dirs; comportamento com dir existente inalterado. **Nada destrutivo** (sem `removeEntry`/overwrite; dado do usuário nunca apagado). Novo fluxo no smoke `TLA_WEB_import-fresh`: exporta no contexto principal → abre `browser.createBrowserContext()` FRESCO (OPFS limpa) → importa → assert "1 notes, 1 highlights applied" + nota visível em João 3:16. **Revert-provado** (reviewer reverteu o guard → `import-fresh` VERMELHO com o `NotFoundError` original → restaurou → verde). tsc 0; `test:web:notes`/`snapshot` verdes; smoke dev+dist verde (todos os fluxos F6.2 + import-fresh); the-light `225b8c9`. Bug de PRODUTO real, pego só por rodar em browser real. | `23bdc68` (impl) · `<accept>` (loop) |
| F6.5 | 2026-07-04 | **Consertar/nivelar o self-test on-device (search PROOF_LIMIT stale + DEVICE_NAME + paridade Android) (Fase 6 — verificação)** — a F6.4 revelou `run-ios-selftest.sh` saindo 1 por `TLA_SEARCH ERROR john_3_16_not_found hits=1000` — bug PRÉ-EXISTENTE da F5.36 (Bíblia completa → "God" >1000 hits, João 3:16 de BM25 baixo cai fora do top-1000). Fix: **escopar a busca do self-test ao livro 43** (João) → João 3:16 determinístico (não aumentar cegamente o limite); anti-alucinação intacta (texto do store, constante só na asserção); produto (`reading.ts`/`sqlite-search.web.ts`) INTOCADO. Defaults `DEVICE_NAME`(iOS)/`AVD`(Android) resilientes (resolvem o 1º sim/AVD disponível; `iPhone 17` saiu do catálogo). `run-android-selftest.sh` trazido à **paridade `TLA_*` completa** com o iOS (+`TLA_DBUP`, +pre-check a11y). **Prova on-device iOS FULL-GREEN (exit 0), reproduzida pelo reviewer:** `TLA_SEARCH ...ok hits=69 first_ref="John 3:16"` + `TLA_DBUP adopted=true` + bateria completa (READ 66/PARALLEL/XREF/NOTES/ASK/STUDY/CHAT/COMPARE/EXPORT/PLANS). Cross-check: headless `search.web.test.mjs` também retorna 69 hits ao filtrar livro 43. tsc 0; test:web:search/reading/xref/smoke verdes; the-light `225b8c9`. **Android on-device = PENDENTE device-gated** (precisa build gradle fresco; APK stale c/ módulo nativo incompatível — alheio ao código; script pronto). | `ed2654d` (impl) · `<accept>` (loop) |
| F6.4 | 2026-07-04 | **Guard de staleness do DB nativo no upgrade (`ensureReadingDb`) + self-test `TLA_DBUP` (Fase 6 — verificação)** — `ensureReadingDb` (`app/lib/db.ts`) copiava o DB só `if (!exists)` sem check de versão → quem ATUALIZA o app mantinha o DB ANTIGO ("Mateus 404 p/ quem atualiza"); os self-tests só rodam em instalação limpa, nunca pegavam. Identidade do asset = `${DB_VERSION}:${Asset.fromModule(DB_MODULE).hash}` (MD5 do conteúdo, muda determinísticamente); sidecar `reading-sample.sqlite.version` guarda a identidade da cópia local. No boot: fresh OU sidecar ausente(=upgrader pré-F6.4)/diferente → apaga a cópia stale (+ `-wal/-shm/-journal`) → re-copia → grava o sidecar **só após a cópia OK** (crash-safe: crash no meio deixa mismatch → re-copia no próximo boot); match → reusa. **Userdata NUNCA versionado-apagado** (notas vivem no subdir separado `documentDirectory/userdata/`). Self-test (`reading-selftest.ts`): pré-semeia DB stale + sidecar velho → `ensureReadingDb` força re-cópia → consulta Mateus/40 → `adopted` derivado do retorno REAL de `get_chapter`. **Prova on-device iOS: `TLA_DBUP adopted=true matt1_verses=25 matt1_v1_len=81`** (iPhone 17 Pro sim; + `TLA_READ books=66`/`TLA_PARALLEL`). tsc 0; web de leitura intacto (`db.web.ts` stub; smoke+headless verdes). Reviewer independente confirmou crash-safe, userdata-safe, self-test não-falsificável (triangulado). **Achado (pré-existente, não-F6.4):** `TLA_SEARCH` vermelho on-device por `PROOF_LIMIT=1000` stale pós-F5.36 → F6.5. the-light `225b8c9`. | `bf3d663` (impl) · `<accept>` (loop) |
| F6.3 | 2026-07-04 | **Endurecer o gate do wasm da fronteira — erro visível + retry, nunca spinner infinito (Fase 6 — verificação)** — `wasm.web.ts:36-39` CAPTURAVA a falha de `uniffiInitAsync()` e a DESCARTAVA, deixando `ready=false` p/ sempre → `WasmGate` = **spinner infinito sem erro** (o mesmo padrão "erro engolido parece carregando" que escondeu a leitura quebrada 3 ciclos). `useWasmReady()` agora retorna `{ready, error, retry}` (não engole); `WasmGate.tsx` renderiza **mensagem de erro visível + botão "Tentar de novo"** (`t('wasm.loadError')`/`wasm.retry`, `colors.error`, a11y role/label, testIDs) em vez do `ActivityIndicator` infinito; `retry()` reseta a `initPromise` memoizada → re-instancia de verdade. **Provado no smoke** (`SMOKE_WASM_WRONG_MIME=1`, dist): corrompe SÓ o corpo do frontier `index_bg.wasm` (MIME sozinho não basta — wasm-bindgen cai em `arrayBuffer()`) → `TLA_WEB_wasm-error-ui ok` (erro visível, sem spinner, children bloqueados); rota `/__smoke/fix-frontier-wasm` + clique em retry → **João 3:16 renderiza** (recuperação end-to-end). Caminho feliz inalterado (7 fluxos F6.2 verdes dev+dist; wasm-error só sob o flag). `generated/*` intocado; nativo `wasm.ts` segue `ready=true`. tsc 0; i18n(235)/i18n-coverage/a11y-scan verdes; the-light `225b8c9`. Reviewer independente reproduziu erro-UI + recuperação. | `4316a22` (impl) · `<accept>` (loop) |
| F6.2 | 2026-07-04 | **Asserções de fluxos críticos no smoke em browser real, dev E dist (Fase 6 — verificação)** — estende `assertions.mjs` p/ os 6 fluxos que só tinham cobertura headless: **paralelo** (KJV+Almeida lado a lado), **busca+xref** ("God"→10 hits + João 3:16 + 20 xrefs), **notas persistem no reload** (OPFS real) **+ anônimo** (gracioso, sem hang silencioso sob quota OPFS), **planos persistem** (`completed=1/365`), **export→download→import round-trip** (Blob/`createObjectURL`/`<a>` capturado por CDP `setDownloadBehavior` + re-injetado no `<input>` via `fileChooser` → contagens 1→1), **IA-reachability** (chave DUMMY). Todos verdes em dev E dist (`TLA_WEB_* ok`). **Achados reais (o valor do guard):** (a) **CORS de IA no browser** — OpenAI✓ e Gemini✓ alcançam o provedor; **Anthropic = parede CORS** (`net::ERR_FAILED`, esperado até a F6.8 — registrado, não reprovado) → âncora de regressão p/ a F6.8; (b) **bug de produto** — import de backup em OPFS VAZIA (fresh install) lança `NotFoundError` (`userdata-opfs.web.ts` L37-38 `getDirectoryHandle({create:false})` sem guarda) → follow-up **F6.11**. Harness bug corrigido (CDP `page.click` trava em Pressables RNW → `el.click()` sintético). Só código de teste/harness; determinístico (sem órfãos); the-light `225b8c9`. Reviewer independente re-rodou os 7 fluxos dev+dist + confirmou os achados. | `16e998f` (impl) · `<accept>` (loop) |
| F6.1 | 2026-07-04 | **Harness de smoke em browser REAL (puppeteer dev+dist) + auto-prova (Fase 6 — fundação de verificação)** — fecha o buraco que deixou a leitura web quebrada por 3 ciclos (F5.36/38/39) sem gate reprovar: a suíte `test:web:*` roda sobre `MemoryVFS`/in-memory e NUNCA toca o runtime real do browser. Novo `app/web/__browser__/`: `chrome.mjs` (resolver do binário; ausente→vermelho, só pula com flag explícito imprimindo NOT VERIFIED), `server.mjs` (ciclo dev=`expo start` porta própria 8099 + dist=`expo export`+static-server, trap-teardown), `static-server.mjs` (resolve templates `[param].html` do expo-router + serve `.wasm` como `application/wasm`), `smoke.browser.mjs` (puppeteer-core, userDataDir limpo, `waitForFunction` em sinais estáveis, hooks pageerror/requestfailed + wrap de URL), `assertions.mjs` (fluxo abrir-capítulo). Scripts `test:web:smoke-dev/dist/smoke` + `puppeteer-core` devDep (SEM download de browser). **AUTO-PROVA (reproduzida pelo executor E pelo reviewer):** reverter o fix `locateFile` da F5.39 → `test:web:smoke` VERMELHO com a falha real do browser ("Failed to construct 'URL': Invalid base URL", versículo não renderiza) enquanto `test:web:reading` headless VERDE → restaurar → smoke VERDE (dev E dist). Prova que o guard pega o que o headless estruturalmente não vê. Determinístico (sem órfãos em 8099/8100); Chrome-ausente→vermelho (nunca falso verde). tsc 0; the-light `225b8c9`. Reviewer independente re-rodou a auto-prova. Escopo: só abrir-capítulo (demais fluxos=F6.2). | `ab523d6` (impl) · `<accept>` (loop) |
| F5.39 | 2026-07-03 | **CAUSA RAIZ REAL: wa-sqlite "Invalid base URL" — leitura web SEMPRE esteve quebrada (fix + prova em Chrome real)** — Mateus (e qualquer capítulo) não abria no WEB. **Diagnóstico com Chrome headless REAL** (puppeteer-core sobre o Chrome do sistema, dirigido pelo Driver): stack `openReadingDbWeb`→`SQLiteESMFactory`→o glue Emscripten do `wa-sqlite.mjs` avalia EAGERMENTE `new URL("wa-sqlite.wasm", import.meta.url)` e, sob o bundler **DEV do Metro, `import.meta.url` é a string "null"** → `new URL("...","null")` lança "Failed to construct 'URL': Invalid base URL" ao abrir o SQLite. **NÃO era tamanho do DB (F5.36) nem OPFS (F5.38)** — a leitura web SEMPRE esteve quebrada; só passava nos testes headless (Node, onde `import.meta.url` é `file://` válido). O humano foi o 1º a LER no browser. As 3 camadas do bug: livros faltando (F5.36) → cache de asset do Metro (restart --clear) → este URL (F5.39). **Fix:** `locateFile:(path)=>path` ao factory nos 2 stores de produção (`sqlite-reading-opfs.web.ts:104`, `sqlite-lexicon-opfs.web.ts:99`) → o glue não toma o branch do `new URL` inválido (`Ca ||= f.locateFile ? ... : new URL(...)`); os bytes do wasm já vêm por `wasmBinary`. 28 factories de TESTE (Node) inalterados. **Verificação: DUAS provas em Chrome real** (Driver + reviewer): Mateus 1 renderiza "The book of the generation of Jesus Christ..."; `SQLITE_URL_ERROR_PRESENT=false`. tsc 0; reading(João+Mateus)/coverage/search/xref/lexicon/perf-budget(841) verdes; the-light `225b8c9`. **Follow-up:** guard de smoke em browser real (o loop só testava headless → "passa headless, quebra no browser"). | `5e88fc5` (impl) · `<accept>` (loop) |
| F5.38 | 2026-07-03 | **Leitura web via `fetch→MemoryVFS` direto — corrige Mateus não abrir no WEB (continuação do bug)** — após a F5.36 (DB 38MB) Mateus não abria no browser mesmo com o Metro servindo o asset certo. **Repro Node isolou:** DB+`MemoryVFS`+wa-sqlite funcionam (abre 38MB, lê Mateus verbatim, 66 livros); a falha era o passo BROWSER-ONLY em `sqlite-reading-opfs.web.ts`: `fetch(38MB)→grava em OPFS→lê de volta`; a gravação monolítica de 38MB em OPFS é frágil e falha sob a quota RESTRITA do modo anônimo (que eu havia sugerido — contraproducente). **Fix:** `openReadingDbWeb` carrega os bytes **direto do asset para o `MemoryVFS`** (`fetchBytes(readingDbUri)`), SEM round-trip OPFS (removidos `readSubsetBytesFromOpfs`/`OPFS_DIR`/`OPFS_FILE`; grep confirma zero outros importadores); léxico/userdata OPFS INTOCADOS. Funciona em janela normal e anônima; some o problema de cache stale. Teste `reading.web.test.mjs` passa a asserir **Mateus 1 (KJV) verbatim** ("The book of the generation of Jesus Christ...") além de João. ADR-0057. tsc 0; reading/search/xref/coverage/perf-budget/expo web verdes. Reviewer independente confirmou o fix + o **CAVEAT: browser-only, não exercitado headless** — o humano confirma no browser real. **Junto:** re-baseline perf da F5.37 (débito: moduleCount 840→841 + bytes do `AiProviderNotice` eager; a revisão da F5.37 não rodou perf-budget). the-light `225b8c9`. | `52de887` (impl+re-baseline) · `<accept>` (loop) |
| F5.37 | 2026-07-03 | **Clareza de UX — recursos de IA (BYOK) vs. 100% offline (pedido do humano)** — sem provedor de IA configurado, os 4 painéis (Ask/Study/Compare/Chat) mostravam estado vazio/erro que parecia "app quebrado". Novo `app/components/AiProviderNotice.tsx` (+ hook `useConfiguredAiProviders` lendo só NOMES de provedor via `listProviders`, nunca valores, sem rede): card claro via `t()` — "Este recurso usa IA. Configure um provedor (Claude, GPT, Gemini ou Ollama local)" + reassurance "Leitura, busca, referências, notas e planos funcionam 100% offline, sem provedor" + CTA (a11y `button`) → `/about`. Seção offline-vs-IA na About (F5.35). Removido jargão interno "F3.10" de `ai.mockProviderNote`/`compare.columnNoKey`. **Offline NÃO gated** (nenhum key-check em leitura/busca/xref/notas/planos). Anti-alucinação intacta (cited_text/interpretation separados; sem texto bíblico via `t()`). +8 chaves i18n (pt/en, 233 total). tsc 0; i18n/i18n-coverage/a11y-scan(74)/contrast/web:ai/study/compare/expo web verdes; the-light `225b8c9`. Reviewer independente confirmou aviso claro/acionável, offline não-gated, honesto (mock ainda responde). **Achados:** não há tela Settings dedicada (CTA→About, o hub BYOK); Study/Chat ainda MOCK-only (BYOK real = follow-up). **Débito:** +1 módulo eager (perf-budget não estava na verificação da F5.37) → re-baseado na F5.38. | `79fb114` (impl) · `07fa45d` (loop) |
| F5.36 | 2026-07-03 | **Bíblia COMPLETA (66 livros) no banco de leitura — corrige "Mateus 1 indisponível" (bug do humano)** — o app embarcava um SAMPLE de dev de 3 livros (Gên/Sal/João); `core/examples/gen_reading_sample_db.rs` fixava `BOOKS=[1,19,43]` copiando só esses do `bible.sqlite` (que tem os 66). Removido o filtro de livros nas tabelas de LEITURA (`books`/`verses`/`cross_references`/`verses_fts` = todos os 66 livros × KJV+Almeida, texto VERBATIM do store — `INSERT..SELECT FROM src`, anti-alucinação); `LEXICON_BOOKS=[1,19,43]` mantém o léxico como sample on-demand (completo = F5.38). Regenerado `reading-lite.sqlite` **4,3→38,4MB** (web) + `reading-sample.sqlite` (nativo) + `lexicon-sample` inalterado; `.sqlite` são gerados-ignorados. Novo guard `test:web:coverage` (exige 66 livros, Mateus/Marcos/Lucas/Romanos, hit de busca fora de Gên/Sal/João — falha se voltar a ser sample). Perf re-baseline cirúrgico (só `readingLiteDbBytes`; moduleCount **840 inalterado** — DB carrega on-demand em OPFS, não no first-paint). Constantes de teste search/xref atualizadas p/ valores reais da Bíblia completa (God/kjv 646→3892; xref top Rom 5:8/871). ADR-0056. tsc 0; coverage/reading/search/xref/notes/plans/perf-budget/expo web verdes; the-light `225b8c9`. Reviewer independente verificou contra o DB vivo: Mateus real ("The book of the generation of Jesus Christ..."), **testes NÃO enfraquecidos** (re-queriou cada constante), guard não-vacuoso. | `b9b567e` (impl) · `7a524b2` (loop) |
| F5.35 | 2026-07-03 | **Tela in-app Sobre / Créditos / Licenças (Refinamento — varredura, abertura; ÚLTIMA)** — não havia tela central de "Sobre"; atribuições só apareciam contextualmente e os textos bíblicos (KJV, Almeida 1911) não tinham atribuição exibida. Nova rota `app/app/about.tsx` alcançável da Home (`Link testID="open-about"`) + registrada no Stack (`_layout.tsx`): lista as **4 fontes + licenças** (KJV domínio público, Almeida 1911 domínio público, cross-refs OpenBible CC-BY, léxico STEP/Tyndale CC BY 4.0) com as strings CC-BY **byte-exatas** das constantes in-app (`XREF_ATTRIBUTION`/`STEP_ATTRIBUTION`), declara offline-first + BYOK + anti-alucinação, serve de explicador de primeiro uso, e linka backup/export reusando o MESMO chunk lazy `SyncSettings` (sem superfície duplicada). Providers listados = `SUPPORTED_PROVIDERS` reais (Claude/GPT/Gemini/**Ollama** local). **Novo drift-guard `test:about-attr`**: byte-compara as strings do About (pt E en) contra as constantes fonte + exige pt==en (falha em drift). Cromo via `t()` (paridade, +chaves `about.*`, 225 total); tokens de tema (sem hex). tsc 0; `test:i18n-coverage`(225)/`a11y-scan`(73)/`contrast`(AA)/`about-attr`/`i18n`/expo web verdes; perf **re-baseline sancionado** (839→840 = +1 rota eager `about`; SyncSettings/driveAuth seguem async; tolerâncias inalteradas; ADR-0055; precedente F5.10/F5.19/F5.26); único toque em `loop/` = `web-bundle-budget.json`. Reviewer independente confirmou drift-guard não-vacuoso, providers exatos, backup reusa chunk, re-baseline legítimo, the-light `225b8c9`. ADR-0055. | `89c34ad` (impl) · `<accept>` (loop) |
| F5.34 | 2026-07-03 | **README real (propósito · offline-first/BYOK · anti-alucinação · licenças · como rodar) (Refinamento — varredura, abertura)** — `README.md` tinha 15 bytes; escrito README completo (docs-only) fundamentado em `VISION_AND_ARCHITECTURE.md`/`IMPLEMENTATION_PLAN.md`/`package.json`/`scripts/` — SEM inventar recursos. Seções: intro (offline-first, PT/EN, Rust+UniFFI+Expo web/iOS/Android); princípios não-negociáveis + subseção **anti-alucinação** (texto do versículo sempre do store; LLM só interpreta; `ai::citation` em todos os alvos); features (leitura/paralelo, busca FTS5, xref, notas/marcações, IA BYOK Claude/GPT/Gemini+compare, estudo+léxico, export acadêmico, planos, i18n PT/EN + tema claro/escuro, sync Google Drive opt-in/OFF-por-padrão); **tabela de atribuição** autoritativa (subsume NOTICE: KJV & Almeida 1911 domínio público, OpenBible CC-BY, STEP/Tyndale CC BY 4.0) com as strings **byte-exatas** das constantes in-app (`XREF_ATTRIBUTION`/`STEP_ATTRIBUTION`, grep -F confirma identidade); build/run reais (gen-bible-db, gen-bindings-{web,ios,android}, expo start/run, testes). Só `README.md` commitado; tsc 0. Reviewer independente auditou feature-a-feature — **nenhuma alegação falsa** (PT/EN only, tema claro/escuro only, providers reais); strings byte-exatas; observação não-bloqueante: README omite o 4º provider `ollama` (modelos locais) — under-statement, não falso (endereçado na F5.35). README em inglês (convenção de repo público). the-light `225b8c9`. Sem ADR (docs). | `838cf45` (impl) · `<accept>` (loop) |
| F5.33 | 2026-07-03 | **Seletor tri-estado de tema (claro/escuro/seguir sistema) (Refinamento — varredura, fecha lacuna ADR-0043)** — a ADR-0043/F5.14 entregou `setMode(mode|null)` + flag `isSystem` no `ThemeProvider` (null → `removePref` → segue `useColorScheme`), mas o único controle era o `ThemeToggleButton` binário → "seguir sistema" era inalcançável. Novo `ThemeModeSelector.tsx`: **3 chips segmentados no header** (claro ☀ / escuro ☾ / sistema ◐) **substituindo** o toggle binário; cada chip chama `setMode('light'|'dark'|null)`; "sistema" → `setMode(null)` → `removePref(THEME_PREF_KEY)` → limpa `tla.pref.theme.mode` → segue `useColorScheme`; chip ativo derivado de `isSystem`/`mode`. **Reusa a persistência da F5.2 — sem 2ª camada** → cada escolha (incl. sistema) sobrevive a reinício pela re-hidratação existente. `ThemeToggleButton` aposentado (deletado; grep repo = zero ref de código); chaves i18n `theme.switchTo*`→`theme.{light,dark,system}` (pt/en); firstpaint stub renomeado. tsc 0; `test:web:theme`(limpa a pref)/`test:i18n`(204)/`test:i18n-coverage`/`test:a11y-scan`(chips com role+label+hitSlop)/`test:web:firstpaint`/expo web verdes; the-light `225b8c9`. Reviewer independente confirmou system→removePref não-cosmético, sem 2ª camada, aposentadoria limpa, paridade. Sem ADR (só expõe capacidade existente). | `6f8186f` (impl) · `<accept>` (loop) |
| F5.32 | 2026-07-03 | **Rolar/destacar o versículo alvo com `?verse=N` no Reader (Refinamento — varredura, UX)** — busca (`openHit`) e xref (`openXref`) já empurravam `/read/[book]/[chapter]?verse=N`, mas a tela de capítulo lia só `{book,chapter}` e descartava `verse` → tocar um hit de João 3:16 caía no TOPO de João 3. `[chapter].tsx` agora lê o `verse` opcional (normaliza `string|string[]`→primeiro, valida finito/>0 senão null) e passa `anchorVerse` ao `ReaderChapterView`; o componente mapeia `y` por versículo via `onLayout` (`offsetsRef`), rola via `scrollTo({y})` com fallback `pendingRef` (completa quando o `onLayout` da linha alvo dispara — resolve o timing de render async) e aplica `flashVerse` (estilo `verseSelected` existente, auto-limpa ~2.5s, separado de `selectedVerse` → não abre painel de ação). Sem `verse` = no-op idêntico ao de hoje (React bail-out); N fora de faixa/lixo = ignorado sem crash. Componente compartilhado → web+nativo. tsc 0; expo web 0; `test:i18n-coverage`/`test:web:reading`/`xref`/`search` verdes; texto do versículo segue do store. Reviewer independente confirmou no-param regression (crítico), robustez, cross-target (só `ScrollView`/`scrollTo`/`onLayout`), anti-alucinação. xref/busca já passavam `?verse=N` → só o lado receptor precisou. Sem ADR. | `4bbe855` (impl) · `<accept>` (loop) |
| F5.31 | 2026-07-03 | **Seletor de tradução na Busca (Refinamento — varredura, feature-gap)** — `app/app/search/index.tsx` fixava `TRANSLATION='kjv'`, então a busca só consultava KJV mesmo com KJV+Almeida 1911 no store. Adicionado seletor **reusando `ReaderVersionPicker`** (`testIDPrefix="search-version"`): lista via `listTranslations(dbPath)` (store, nunca hardcoded), `DEFAULT_TRANSLATION='kjv'`, e `translation` entrou nas deps do efeito de debounce `[query, translation]` → selecionar Almeida 1911 re-executa a query atual via o parâmetro existente `search(dbPath, term, translation)` e atualiza os resultados (não é relabel). Só o rótulo novo "Tradução"/"Translation" (`search.translationLabel`) passa por `t()`; snippet/referência dos resultados seguem VERBATIM do store (`ReaderSearchResultItem` não usa `t()`). tsc 0; `test:web:search`(KJV+alm1911)/`test:i18n`(203)/`test:i18n-coverage` verdes; the-light `225b8c9`. Reviewer independente confirmou re-consulta real (não estado morto), fonte-store, reuso sem duplicação, anti-alucinação. Sem ADR. | `0e4f522` (impl) · `<accept>` (loop) |
| F5.30 | 2026-07-03 | **Expor links /read, /search, /plans na home web (Refinamento — varredura, descobribilidade)** — `app/app/index.tsx` escondia os 3 links atrás de `Platform.OS !== 'web'` embora as rotas funcionem no web (leitura/busca DESTUBADAS F1.13/F1.14; planos F5.10/ADR-0050). Un-gated os 3 (verificado que cada rota web é REAL, não stub: `reading.web.ts` faz `getChapter` wa-sqlite/OPFS, `search` FTS5 real, planos wasm+OPFS; nenhum arquivo de rota tem bloco "indisponível no web") + corrigidos os comentários obsoletos em `index.tsx` e a nota "esta tela não é alcançada no web" em `search/index.tsx`. `dist/index.html` contém `href="/read|/search|/plans"` + testIDs. tsc 0; expo web 0; `test:web:perf-budget` moduleCount **839 EXATO** (Link já era import estático → sem módulo eager novo, sem re-baseline); `test:web:reading`/`search`/`i18n-coverage` verdes; the-light `225b8c9`. Reviewer independente confirmou rotas reais (66 livros, 646 hits "God", João 3:16), nativo inalterado, comentários corretos. Sem ADR (ADR-0050 já cobre planos web). | `ea22a77` (impl) · `<accept>` (loop) |
| F5.29 | 2026-07-03 | **Corrigir o `Text` de resultado da Home que escondia o conteúdo do leitor de tela (Refinamento — varredura, a11y)** — o `<Text>` de resultado em `app/app/index.tsx` tinha `accessibilityLabel={t('a11y.result')}`, que sobrescreve os filhos → o leitor de tela anunciava só "Resultado da interpretação", nunca o `resultText` real (referência/passagem resolvida ou erro). **Removido** o `accessibilityLabel` que sobrescrevia (mantido `accessibilityRole="text"`) → agora anuncia o conteúdo real; `resultText` cobre os 4 `Outcome` kinds (passage/reference/error/idle). Chave `a11y.result` ficou órfã (só usada aqui) → removida do union `MessageKey` + catálogos pt/en (203→202, paridade preservada). tsc 0; `test:a11y-scan`(71)/`test:i18n-coverage`(202)/`test:i18n` verdes; the-light `225b8c9`. Reviewer independente confirmou fix nos 4 outcomes, remoção de chave sem referência pendente (grep repo=NONE), sem over-reach. Guardas F5.20/21 só checavam presença de label → resíduo fechado. Sem ADR. | `d97ca2c` (impl) · `<accept>` (loop) |
| F5.28 | 2026-07-03 | **Localizar nomes das cores de destaque via `t()` (Refinamento — varredura, follow-up F5.16)** — os 4 nomes das cores (`Amarelo/Verde/Azul/Rosa`) estavam hardcoded em PT em `app/lib/highlightColors.ts` e fluíam para o `accessibilityLabel` do swatch via `t('versePanel.highlightWith',{color:c.label})` → em EN o leitor de tela anunciava "Highlight with Amarelo". Removido o campo `label` PT; +4 chaves `highlight.{yellow,green,blue,pink}` (pt/en) em `i18n.ts`; `ReaderVersePanel.tsx` resolve via `t(`highlight.${c.name}`)` (EN "Highlight with Yellow", PT "Marcar com Amarelo"). `name` estreitado p/ união literal `'yellow'|'green'|'blue'|'pink'` (type-check sem cast); `resolveHighlightColor(name:string)` mantém string livre p/ highlights persistidos. `highlight` adicionado ao allowlist CHROME_NAMESPACES dos 2 guardas. tsc 0; `test:i18n`(203 chaves, paridade)/`test:i18n-coverage`/`test:a11y-scan`(71 interativos)/`test:web:notes` verdes; the-light `225b8c9`. Reviewer independente confirmou bug corrigido nos 2 locais (asserts não-vazios), paridade real, remoção de `label` segura. Fecha a última lacuna i18n. Sem ADR (extensão F5.16). | `32d06f2` (impl) · `<accept>` (loop) |
| F5.26 | 2026-07-03 | **UI opt-in de sync + wiring do `SnapshotStore` ao store real (Sync opcional — etapa 4/ÚLTIMA construível, ADR-0036)** — tela de Sync como **seção lazy na Home** (não rota eager, p/ não regredir o entry graph). **Toggle opt-in OFF por padrão** (`app/lib/syncPrefs.ts` sobre o KV F5.2; `getSyncOptIn()`=`false` em KV vazio; desligar REMOVE a chave). **Aviso de privacidade honesto:** o QUE sincroniza (notas+marcações+progresso) vs. o que **NUNCA** sai do device (sessões de IA, banco bíblico, chaves/segredos, texto de versículo além da referência canônica), "sem telemetria". **"Funciona 100% offline sem isto"** proeminente. **Export/import manual** (todos os alvos: export via Share-sheet, import por colar universal + por **arquivo web-only**). **Google Drive (web, opt-in):** link/unlink (F5.24) + "Sincronizar agora" (F5.25) **gated** — sem `EXPO_PUBLIC_GOOGLE_DRIVE_CLIENT_ID` configurado, Connect fica disabled e **opt-in OFF ⇒ zero rede**; token só em `sessionTokenStore` (memória), nunca logado/commitado. **Adaptador `SnapshotStore`→store REAL:** `app/lib/snapshotStore.ts` (nativo, fns de fronteira via `reading.ts`) + `app/lib/snapshotStore.web.ts` (web, OPFS via `reading.web.ts`), injetando as fns reais de notas/marcações/progresso + `parseReference` do core; round-trip export→import provado via o MESMO `createSnapshotStore` de produção (só o backend de storage é in-memory no teste). a11y (roles/labels) + i18n (+~40 chaves `sync`/`home`/`a11y`, paridade PT/EN) + contraste AA. Prova headless `syncui.web.test.mjs`: `SYNC_UI store=ok optin_default_off=ok optin_persist=ok`. tsc 0; `test:i18n-coverage`(199)/`test:web:contrast`(18 pares AA)/expo web 0; perf-budget **moduleCount 839 INALTERADO** (SyncSettings/snapshotStore/driveAuth/driveSync em chunks async; só cresceu cromo i18n eager → re-baseline **bytes-only** do contrato `measure-web-bundle.sh`+`web-bundle-budget.json`, tolerâncias inalteradas — análogo F5.10/F5.19); snapshot/driveauth/drivesync sem regressão; the-light `225b8c9`. Reviewer independente re-rodou tudo + 8 checagens (opt-in-OFF real, OFF⇒zero-rede, wiring ao store real não-mock, privacidade honesta, sem chamada/segredo Google reais, escopo só-snapshot, re-baseline legítimo bytes-only, não-negociáveis). **Único arquivo sob `loop/` no commit = `loop/perf/web-bundle-budget.json`** (contrato de perf, permitido pela task; nenhum STATUS/JOURNAL/queue/HALT tocado). Limitações registradas (ADR-0054): import por arquivo web-only, validação com conta real diferida à **F5.27 (⛔ gate humano)**. ADR-0054. | `c2dc1f5` (impl) · `<accept>` (loop) |
| F5.25 | 2026-07-03 | **Push/pull do snapshot na pasta app-data do Drive + merge no pull (Sync opcional — etapa 3, ADR-0036)** — motor PURO/injetável `app/lib/driveSync.ts` (injeta `fetch`, `getToken()` da F5.24, `SnapshotStore` da F5.23). **`pushSnapshot`**: `exportSnapshot(store)` → upload/replace de **1 arquivo canônico** (`the-light-app.snapshot.json`) na pasta **`appDataFolder`** (multipart; cria se não existe, senão PATCH por id). **`pullSnapshot`**: `findSnapshotFile` (list por nome em `spaces=appDataFolder`) → download (`alt=media`) → delega 100% a `importSnapshotIntoStore` (parse + `assertValidReference` via core **ANTES** de qualquer write + `mergeSnapshots` + aplica só o diff). **`syncNow`**: pull-merge-push (convergência). **Reuso genuíno** — sem reimplementar snapshot/merge (F5.23) nem OAuth (F5.24); token só via `getToken()` injetado, enviado como `Authorization: Bearer`, **nunca logado**. **Garantias provadas** (2 stores in-memory + 1 "nuvem" dict compartilhada): (a) **convergência entre 2 devices** — A.syncNow então B.syncNow ⇒ ambos = união {X,Y}, progresso `max(completed)`; (b) **merge nunca apaga local** — pull de remoto MENOR preserva nota só-local; (c) **idempotência** — 2ª `syncNow` = 0/0/false, arquivo estável; (d) push cria/replace (1 arquivo, id estável). **Privacidade/anti-alucinação:** só notas+marcações+progresso sobem (assert de chaves permitidas; "For God so loved" ausente; regex negando token/sessão/chave). **MOCK apenas** (fetch injetado; ZERO rede real; sem conta Google — validação real = F5.27 gate humano). Prova headless `driveSync.web.test.mjs`: `DRIVE_SYNC push=ok pull=ok converge=ok idempotent=ok notoken=ok`. tsc 0; expo web 0; perf-budget moduleCount **839 inalterado** (motor puro fora do entry); snapshot/driveauth sem regressão; the-light `225b8c9`. Reviewer independente re-rodou tudo + 8 checagens adversariais (reuso real via única import de `userdataSnapshot`, pull-valida-antes-de-escrever, merge-não-apaga, idempotência, upload só-snapshot, token-não-vaza real, mock/pureza, escopo+não-negociáveis). Escopo: push/pull/merge (UI+wiring nativo=F5.26). ADR-0053. | `7f4fac2` (impl) · `<accept>` (loop) |
| F5.24 | 2026-07-03 | **Link Google Drive no WEB — OAuth 2.0 PKCE client-side, escopo `drive.appdata` (Sync opcional — etapa 2, ADR-0036)** — motor PURO/injetável `app/lib/driveAuth.ts` (injeta `fetch`/`crypto`/`TokenStore`/`clientId`/`redirectUri`/`now`). **`generatePkce`**: `code_verifier` + `code_challenge`=BASE64URL(SHA-256(verifier)) via webcrypto real. **`buildAuthUrl`**: `accounts.google.com/o/oauth2/v2/auth`, `response_type=code`, `code_challenge_method=S256`, escopo mínimo **`drive.appdata`** (pasta app-private oculta — NÃO lê o resto do Drive do usuário), `state`, redirect. **`exchangeCode`**: POST a `oauth2.googleapis.com/token` com `grant_type=authorization_code`+`code_verifier`+`client_id` (**SEM `client_secret`** — cliente público + PKCE, reconcilia ADR-0023: banido era OAuth essencial/com-servidor; aqui é transporte opt-in de dados DO usuário, na conta DELE, sem infra do app). **`link`/`unlink`/`isLinked`/`currentToken`/`getLinkState`** sobre `TokenStore` injetável (access token em sessão; refresh em secure storage do alvo). **Privacidade/segurança:** token/verifier/challenge **NUNCA logados** (0 `console.*` no fonte; sem `client_secret` no repo); a prova espiona os 6 métodos de console + grepa o fonte e assevera ausência (`notoken=ok`). **MOCK apenas** (`fetch`/`crypto` injetados; ZERO rede real; sem conta Google — validação real diferida à F5.27, gate humano). Prova headless `driveAuth.web.test.mjs`: PKCE (vetor RFC 7636 Ap. B, challenge por SHA-256 real), buildAuthUrl (scope/S256/state/redirect), exchangeCode→link→isLinked→unlink, e a asserção `notoken` — `DRIVE_AUTH pkce=ok url=ok exchange=ok link=ok unlink=ok notoken=ok`. tsc 0; expo web 0; perf-budget moduleCount **839 inalterado** (motor puro fora do entry graph); snapshot/notes sem regressão; the-light `225b8c9`. Reviewer independente re-rodou tudo + as 7 checagens adversariais (PKCE real, sem segredo, notoken real, mock-only, pureza, escopo só-autorização, não-negociáveis). Escopo: só o fluxo de autorização (push/pull=F5.25, UI+wiring nativo=F5.26). ADR-0052. | `a4397c1` (impl) · `<accept>` (loop) |
| F5.23 | 2026-07-03 | **Snapshot JSON round-trippável dos dados do usuário + import-com-merge (Sync opcional — fundação, ADR-0036)** — motor PURO `app/lib/userdataSnapshot.ts` (molde `notesExport.ts`, backend `SnapshotStore` injetável). **Formato versionado:** `{app,version:1,exportedAt?,notes:{reference,body}[],highlights:{reference,color,tag?}[],planProgress:{planId,startDate,completed}|null}` — `reference` = string canônica do core (via `formatReference` injetado); ordenado por referência → serialização determinística/byte-estável. **Export** dos Records (`listNotes`/`listHighlights`/`readingPlanProgress`) — sem reimplementar o store. **Import** valida (app/version/tipos) + `assertValidReference` (core `parseReference`) ANTES de qualquer escrita; aplica só o diff via fns do core (`putNote`/`addHighlight`/`startReadingPlan`+`setReadingPlanCompleted`). **Merge determinístico (ADR-0051):** notas/marcações = união por referência (colisão → **imported wins**, pois os Records NÃO têm timestamp → "last-modified" do ADR-0036 impossível; documentado); progresso = LWW/`max(completed)` (nunca regride); **idempotente** (`merge(s,s)==s`; re-import = 0/0/false). **Anti-alucinação/privacidade:** só notas+marcações+progresso; NÃO sessões de IA/banco/chaves; nenhum texto bíblico (só a referência). Prova headless `snapshot.web.test.mjs` (mock OPFS): export, round-trip (export→clear→import→`deepEqual`), merge (divergentes→união), idempotente, import inválido/irreal rejeitado sem corromper — `WEB_SNAPSHOT notes=2 highlights=2 plan=gospels:3 roundtrip=ok merge_notes=3 idempotent=ok`. tsc 0; expo web 0; notes/plans/perf-budget (moduleCount 839 exato — motor puro fora do entry) verdes; the-light `225b8c9`. Reviewer independente confirmou round-trip/merge/anti-alucinação + a regra de colisão. Escopo: motor + prova (Drive=F5.24/25, UI+wiring nativo=F5.26). ADR-0051. | `be9264d` (impl) · `<accept>` (loop) |
| F5.10 | 2026-07-03 | **Paridade WEB dos planos de leitura (geração cfg-free via wasm + progresso OPFS + rota /plans) — gate estratégico, opção A** — completa o workstream de planos (nativo F5.1/4/7/13 + web). **Core (ADR-0037/PR #3 `225b8c9`):** `pub mod userdata` `#[cfg(embedded)]`→`#[cfg(any(embedded,ai-pure))]`; geração pura (`available_plans`/`plan_by_id`/`PlanProgress`/`day_index_for`/`chunk`) wasm-safe; `PlanStore`/fs seguem embedded; +36/−3, não-quebrante, revisão independente (available_plans REACH_OK; PlanStore→E0433). Re-pin 2 linhas. **App-side (`d670ee6`):** as 3 fns de geração da F5.1 viram **cfg-free** (web = impl wasm real, não mais stub; zero-drift); novo `app/web/plans-fs.web.ts` persiste o progresso em OPFS (`reading-plans/active.json` snake_case `{plan_id,start_date,completed}`, MESMO formato do core; VFS F1.16; plan_id validado no CATALOG via `listReadingPlans`, data via `readingPlanDayIndex` do core — NADA reimplementado em TS); rota `/plans` no web (removido `PlansWebNotice`; `ReminderControls` gated nativo); prova headless `plans.web.test.mjs` (`WEB_PLANS plan_id="gospels" days=30 today_index=0 completed=1 persisted=true`: 3 planos com nomes PT do core, gospels dia 0 = Mt 1, round-trip OPFS start→marca→reabre, plan_id/data inválidos rejeitados sem I/O). **Perf re-baseline (`1b1df43`):** wasm 1198888→1223324 (+24KB, geração no grafo ai-pure), moduleCount 838→839; budget/contrato re-centrados, `test:web:perf-budget` verde+byte-reproduzível; ADR-0050+BUDGET.md. cargo test 80; tsc 0; expo web 0; the-light `225b8c9`/`core/Cargo.toml` intactos. Reviewer independente confirmou zero-drift + OPFS + re-baseline honesto. ADR-0037+0050. | `225b8c9`/`d670ee6`/`1b1df43` (impl) · `<accept>` (loop) |
| F5.21 | 2026-07-03 | **Dynamic type + semântica de modal + ordem de foco dos painéis de leitura (i18n/a11y) — ÚLTIMA task não-gate da Fase 5**. Reality-check: `allowFontScaling={false}`/`accessibilityViewIsModal` só existiam em `ios/Pods` (headers nativos), NUNCA no source → dynamic type já respeitado (RN escala por padrão; sem hard-cap); a lacuna real era semântica de modal. Novo hook `app/lib/useReaderModalA11y.ts`: no open (`visible` false→true) move o foco do leitor de tela p/ o `<Text>` do título via `AccessibilityInfo.setAccessibilityFocus(findNodeHandle(ref))` (150ms iOS/350ms Android) → ordem de foco lógica (header→conteúdo→ações) + anúncio idiomático de abertura (sem double-speak); close delegado ao `<Modal>` nativo. **6 painéis** (Verse/Ask/Study/Compare/Chat/Xref): sheet com `accessibilityViewIsModal` (prende foco), título `role="header"`+`ref`+hook. Sem mudança visual; versículo verbatim do store. **Guarda** `reader-modal-a11y.test.mjs` (`test:a11y-modals`, scan estático): cada `Reader*Panel` com `<Modal>` declara `accessibilityViewIsModal`+hook+título `role=header`, e 0 `allowFontScaling={false}` travando escala (escape `a11y-allow-fontscale-lock`); self-test embutido; emite `TLA_A11Y modal=true labels=ok scale=ok focus=ok panels=6 locks=0`. `run-ios-selftest.sh [0/5]` roda a guarda e assevera `TLA_A11Y` sem regredir os `TLA_*` de fronteira do device. **Prova honesta:** headless/estático (props JSX estáticos; OS honra em runtime — device não adiciona fato de fronteira p/ props); check manual VoiceOver/fonte-grande documentado (procedimento). tsc 0; a11y-modals/a11y-scan/contrast/i18n-coverage/reading/notes/theme verdes; expo web 0; the-light `2fc2dab`. Reviewer independente confirmou modal semantics corretos (RN `<Modal>` real, ref no título, hook só no open), re-provou a guarda (2 probes→exit 1) e julgou o headless ACEITÁVEL/honesto. ADR-0049. | `84f8f46` (impl) · `<accept>` (loop) |
| F5.20 | 2026-07-03 | **Varredura de a11y dos interativos + guarda + follow-up do `faint` (i18n/a11y)** — guarda headless `a11y-scan.test.mjs` (`test:a11y-scan`, scan estático determinístico, lexer de comentários da F5.16): p/ cada interativo (`Pressable`/`Touchable*`/`Switch`/`TextInput`/`Link`) política POR-TAG — role só p/ touchables genéricos (View sem role); TextInput/Switch/Link têm role nativa → isentos; label via `accessibilityLabel`/placeholder/child-text (valor de store é label válido — checa presença, não idioma); alvo ≥44 (exige hitSlop/flex/size; falha em dim fixa <44 sem hitSlop, resolvendo `styles.NAME` contra `StyleSheet.create`). Self-test embutido (rejeita sem-role/sem-label/fixo<44/sem-size; sem falso-positivo). **Lacunas corrigidas (não-visuais):** 6 backdrops de modal (role+label), 6 close (hitSlop 12), swatch 34×34 (hitSlop→≥44), chips de baixo padding (hitSlop), VersionPicker chip (role+state+label do store), home readLink (padding), link CC-BY xref (hitSlop+label). **Follow-up F5.18 (ADR-0048):** o em-dash `—` de versículo ausente CONVEIA info (não existe na tradução) → **não-decorativo** → `faint`(1.61:1)→`muted`(AA 5.33/6.75); `faint` fica SÓ no chevron redundante do `ReaderBookList` (decorativo) → exclusão 1.4.11 da ADR-0046 agora exata; nenhum valor de token mudou. tsc 0; test:a11y-scan (61 interativos/22 arquivos) + contrast + i18n-coverage + reading/search/notes verdes; expo web 0; the-light `2fc2dab`. Reviewer independente re-provou a guarda (strip role→FALHA; height:34→FALHA; revert→verde) + validou os hitSlop reais + a decisão do em-dash. ADR-0048. | `b8538ce` (impl) · `<accept>` (loop) |
| F5.19 | 2026-07-03 | **Travar o orçamento de performance web como guarda de regressão (Performance web — SAÍDA do workstream)** — `web-bundle-budget.json` (novo, contrato CONGELADO: bytes+transfer exatos dos assets estáveis, `moduleCount` eager, entry-JS nominal±tolerância, first-paint transfer, guards `removed`, `reBaselinePolicy`) + `check-web-bundle-budget.sh` (guarda oficial, 2 camadas: enforcer roda `measure-web-bundle.sh`; cross-check compara a baseline vs o contrato congelado → detecta re-baseline não-documentado). `test:web:perf-budget` wired no suite web. `BUDGET.md` (limites finais + jornada: wasm −71,8%, leitura −9,88MB, dead assets −689KB, eager moduleCount 856→838, transfer) + ADR-0047. **ACHADO (o guarda cumpriu seu propósito):** a árvore estava RED antes da F5.19 — a **F5.18** (`d4ca8e5`) extraiu `themePalettes.ts` (módulo eager novo, importado por `theme.ts` no first-paint) → `moduleCount` 837→838, e a F5.18 (task de a11y) NUNCA rodou a métrica → a deriva estrutural escapou. **Resolvido honestamente:** re-baseou a ÚNICA constante `moduleCount` 837→838 (mudança já aceita da F5.18/ADR-0046; ~600B absorvidos pela banda ±1024; app NÃO tocado; documentado em ADR-0047+BUDGET.md). **Provado tripar** (reviewer reconfirmou 2 camadas: contrato −1 byte → exit 1; enforcer moduleCount 837 → exit 1; ambos revertidos byte-a-byte). tsc 0; expo web 0; self-tests(reading/firstpaint/study/search/lexicon) verdes; baseline reproduzível; só tooling/perf/docs + 1 linha de script (sem `core/`/app source); the-light `2fc2dab`. ADR-0047. **Lição de processo:** tasks que tocam módulos eager devem rodar a métrica — o wire da F5.19 fecha a lacuna. Perf workstream COMPLETO. | `fc45084` (impl) · `<accept>` (loop) |
| F5.18 | 2026-07-03 | **Auditoria de contraste WCAG AA das paletas de tema + guarda (i18n/a11y)** — `themePalettes.ts` (novo, puro: `ThemeColors`/`LIGHT`/`DARK`/`PALETTES` extraídos de `theme.ts` que importa `react-native`; re-exportados → 24 consumidores intactos, molde `themePrefs.ts`) + `contrast.ts` (novo: `relativeLuminance` sRGB + `contrastRatio`, `AUDITED_PAIRS` 9 bloqueantes, `DECORATIVE_PAIRS` só-report, `auditPalettes()`). **3 tokens LIGHT reprovaram AA (on `#fff`) e foram corrigidos minimamente (hue preservado):** `muted` `#888888` 3.54→`#6b6b6b` 5.33; `accent` `#b08400` 3.42→`#916c00` 4.83 (mesmo dourado); `chipLang` `#999999` 2.85→`#737373` 4.74. DARK já passava (inalterado). **Guarda** `contrast.test.mjs` (`test:web:contrast`): 9 pares × 2 modos ≥AA; falha em regressão; não-vacuosa (injeta cor reprovada → `pass:false`); valida a math contra âncoras (preto↔branco 21:1, `#767676/#fff` 4.54). **Exclusão decorativa (WCAG 1.4.11):** `faint`(chevron redundante)/`divider`(hairline)/`border`(borda cosmética) reportados-não-bloqueantes (trade-off de identidade, ADR-0046). tsc 0; test:web:contrast + theme + 18 self-tests + i18n-coverage verdes; expo web 0; the-light `2fc2dab`. Reviewer RECOMPUTOU cada razão a 4 casas (bateu) + re-provou a guarda no source real + validou a exclusão inspecionando o uso. ADR-0046. **Follow-up (F5.20):** `faint` também colore o em-dash de "versículo ausente" (1.61:1) — defensavelmente decorativo, candidato a rever a 3:1. | `d4ca8e5` (impl) · `<accept>` (loop) |
| F5.17 | 2026-07-03 | **Precompress .wasm/.sqlite + estratégia de serving + medir transfer (Performance web)** — `scripts/lib/web-compress.cjs` (novo, gzip-9 + brotli-11 via `zlib` embutido do Node, sem dep externa; `emitVariantsVerified` descomprime+compara byte-a-byte no emit) + `compress-web-assets.sh` (emite `<asset>.gz`+`.br` ao lado dos `.wasm`/`.sqlite` + texto ≥1KB). `measure-web-bundle.sh` agora 3 passos (export→precompress+verify→budget+transfer): grava `brotliBytes` por asset estável + `firstPaintTransferBytes` (entry-JS gzip = headline) + brotli. `SERVING.md` + ADR-0045: content-hashed→`Cache-Control: immutable`; variantes servidas via `Content-Encoding` (config nginx/Netlify/CF copy-paste). **Honesto:** `expo export` estático NÃO seta `Content-Encoding` sem servidor → o ganho over-the-wire é build-tracked+medido, NÃO runtime-wired (não superestimado). **Transfer (byte-exato):** frontier wasm 1198888→br 311729 (−74%); reading-lite 4,53MB→br 1,09MB (−76%); lexicon 9,5MB→br 1,84MB (−80,6%); estáveis 15,16MB→3,36MB br (−77,8%). **Zero-drift:** `gunzip -c|cmp` + `brotli -dc|cmp` + Node → byte-idêntico (reviewer reconfirmou 3 vias); app importa o descomprimido. tsc 0; expo web 0; 20/20 self-tests; measure BUDGET OK byte-reproduzível; the-light `2fc2dab`; só scripts/perf/docs (moduleCount 837 inalterado; shift de ~2,3KB no entry = flutter do ambiente). ADR-0045. Perf budget lock = F5.19. | `d81418d` (impl) · `<accept>` (loop) |
| F5.16 | 2026-07-03 | **i18n dos componentes restantes + GUARDA de cobertura (i18n/a11y)** — fecha a cobertura i18n (após F5.5/8/11). `ReaderVersePanel` (maior: seções, botões, placeholder, Share, 6 a11y labels), `ReaderXrefPanel` (título/close/empty/"N votos"; **`XREF_ATTRIBUTION` OpenBible CC-BY verbatim**), `ReaderParallelView` (empty reusa `read.chapterNotFound`) → `t()`. VersionPicker/toggles/painéis-IA já migrados. +24 chaves (namespaces `common`/`xref`/`versePanel`; `versePanel` evita o guard anti-alucinação `verse`; 162 total, paridade compile-time). **Guarda de cobertura** (`i18n-coverage.test.mjs` + `test:i18n-coverage`): scanner headless sobre 22 arquivos (`components/*.tsx`+`app/**/*.tsx`) que FALHA em (1) quebra de paridade/chave fora de namespace de cromo, (2) char acentuado/palavra PT em string/JSX/label fora de `t()`, (3) hex hardcoded; lexer que remove comentários (PT/URL não falso-positivam), allowlist CC-BY/STEP, self-test. **Prova:** probe `<Text>Configurações</Text>`+`#ff3366` → guard FALHA (exit 1, linha reportada); comentário PT ignorado; remove probe → PASSA. **Anti-alucinação:** versículo/refs/nomes de livro do STORE, CC-BY verbatim, nunca via `t()`; `{source}`/`{count}`/`{color}` são data. tsc 0; test:i18n(162)/test:i18n-coverage/20 self-tests/expo-web verdes; the-light `2fc2dab` (7 arquivos). Reviewer independente re-provou o guard. **Trade-off (follow-up):** nomes das cores de destaque (`lib/highlightColors.ts`) não localizados (a11y label "Highlight with Amarelo" em EN) — fora do escopo de cromo; tarefa i18n/a11y futura. Sem ADR. | `a880e84` (impl) · `<accept>` (loop) |
| F5.15 | 2026-07-03 | **Separar o léxico de ~9MB do caminho de leitura (on-demand) + UX (Performance web)** — maior ganho de perf da fase. Antes: o léxico (~9,4MB) vivia DENTRO do `reading-sample.sqlite` (14,4MB); todo reader web baixava os 9MB (F5.9 splitou só o GLUE; o DADO seguia acoplado). `gen-reading-sample-db.sh` deriva 2 subsets web do DB combinado (fonte da verdade nativa) via `sqlite3` CLI (`DROP`+`VACUUM` deterministas): `reading-lite.sqlite` (4.530.176 B, sem léxico) + `lexicon-sample.sqlite` (9.502.720 B, só léxico) — **app-side, SEM tocar `core/`** (honrou a regra dura sobre o passo sugerido no core). `sqlite-reading-opfs.web` usa reading-lite; novo `sqlite-lexicon-opfs.web` abre lexicon-sample on-demand (só `import()`-ado por `deepStudy`/`lexicalEntries` → chunk async). `study.web`: 2 handles (reading-lite + lexicon-sample; SQL/linhas idênticos → zero drift). UX: `ReaderStudyPanel` mostra `study.loadingLexicon` (pt/en) no fetch on-demand. **Resultado: caminho de leitura 14,41MB→4,53MB (−9,88MB)**; 9,5MB só carregam ao abrir estudo. **Zero-drift:** SHA-256 de cada tabela (léxico + leitura) IDÊNTICO entre combinado e split (reviewer reconfirmou). baseline re-centrada (`readingDb`→`readingLiteDb`+`lexiconDb`, combinado no guard `removed`) → BUDGET OK; entry moduleCount 834→837. tsc 0; expo web 0; 17 self-tests (lexicon João 3:16=21 Strong STEP CC-BY; study 22 citações) verdes; the-light `2fc2dab`. Split DBs gitignored; symlinks commitados. ADR-0044. Nativo segue no DB combinado (follow-up documentado). | `290057d` (impl) · `<accept>` (loop) |
| F5.14 | 2026-07-03 | **Persistir o modo de tema entre reinícios (i18n/a11y)** — fecha a lacuna do ADR-0015. `themePrefs.ts` (novo, puro, sem `react-native`: `ThemeMode`, `THEME_PREF_KEY='theme.mode'`, guard `isThemeMode`). `theme.ts` `ThemeProvider`: hidrata no boot via `getPref('theme.mode')` (aplica só se `isThemeMode`) + persiste on-change (`setPref`/`removePref`; `null`→segue sistema). REUSA o KV da F5.2 sob `tla.pref.theme.mode` (junto de `ui.locale`/`plans.reminder`) — SEM 2ª camada, SEM dep nova; base ainda `useColorScheme()` (F1.4 preservado); 22 consumidores de `lib/theme` intactos (re-export de `ThemeMode`). Prova headless `theme.test.mjs` (molde i18n): round-trip `set 'dark'` sobrevive a instância nova (reopen) do MESMO storage sob chave namespaced; sem override→segue sistema; valor corrupto ignorado pelo guard. tsc 0; test:web:theme + i18n(138) + 17 self-tests + expo-web verdes; the-light `2fc2dab`; commit 6 arquivos (só `test:web:theme` script, sem dep). ADR-0043 (decisão) + emenda em ADR-0015. Flash de boot async (= I18nProvider F5.2) documentado, aceitável. Reviewer independente confirmou persistência-ao-storage real. | `ea7ac0a` (impl) · `<accept>` (loop) |
| F5.13 | 2026-07-03 | **Lembretes locais offline do plano ativo (Planos de leitura)** — fecha o workstream de planos (F5.1/4/7/13). `planReminders.shared.ts` (puro, backend `NotificationsBackend` injetável + KV F5.2; `parseHHMM`/`formatHHMM`) + `planReminders.ts` (nativo: `expo-notifications` `SchedulableTriggerInputTypes.DAILY`, permissão, cancel; SEM método de push token) + `planReminders.web.ts` (no-op). `plans/index.tsx`: `ReminderControls` (Switch OFF por padrão + chips de horário 06/07/08/12/18/21h) — enable valida horário, cancela prévio, pede permissão LOCAL **só aqui**, agenda 1 diária, persiste; disable/`clearReadingPlan` cancelam. Pref `{enabled,time,id}` no KV F5.2 sob `tla.pref.plans.reminder` (separada do `active.json` do core; `PlanProgress` intacto). Corpo da notificação = cromo i18n + nome do plano do CATALOG. +8 chaves i18n. `expo-notifications ~56.0.19` (SDK 56) + plugin `app.json`. Web degrada (no-op; export 0; **0 refs no dist**). **Offline-first/local-only (crux):** grep estrutural confirma SEM `getExpoPushTokenAsync`/`getDevicePushTokenAsync`/`fetch`/rede; permissão só no opt-in. Prova headless `planReminders.web.test.mjs` (mock do backend, sem disparar notificação): OFF por padrão, enable=1 schedule+pref, change-time cancela+reagenda, disable cancela+remove, permissão negada→nada, pref sobrevive reopen. tsc 0; expo web 0; test:i18n(138)/15 self-tests+keystore verdes; the-light `2fc2dab`. ADR-0042. Reviewer independente confirmou local-only + mock 1:1. | `93c05f3` (impl) · `<accept>` (loop) |
| F5.12 | 2026-07-03 | **Remover assets duplicados mortos + split `passage.web` + corrigir ADR-0040 (Performance web)** — inclui os 2 follow-ups da F5.9. Grep provou mortos (1 só importador de produção cada, `sqlite-opfs.web.ts`): removidos o wasm binário do npm `wa-sqlite` (558KB) e o `sample.sqlite` de 1 versículo (131KB; João 3:16 KJV byte-idêntico ao `reading-sample.sqlite`). `passage.web.ts` re-aponta `getPassage` ao reading store (`await import('./sqlite-reading-opfs.web')`+`./sqlite.web`); `index.tsx` importa `getPassage` dinâmico no submit (não no mount) → 2ª factory wa-sqlite OFF do entry eager (**follow-up A**). Deletado `sqlite-opfs.web.ts` (único importador do npm factory+sample). ADR-0040 retratada ("única porta/~66KB irredutível") + ADR-0041 (**follow-up B**). `measure-web-bundle.sh`+baseline re-centrados (`waSqliteNpm`/`sampleDb` → guard `removed` que falha se reaparecerem). **Resultado:** entry moduleCount 844→834; eagerBytes −74.678; stableAssetsBytes −689.415; novo chunk `passage-*.js` lazy. **Zero-drift:** 15 self-tests + getPassage(João 3:16 verbatim) + i18n + keystore verdes idênticos; measure BUDGET OK. tsc 0; expo web 0; the-light `2fc2dab`. `wa-sqlite` npm segue devDep (API JS usada); `assets/data/sample.sqlite` fica (não importado, não bundlado). **Nuance (ADR-0041):** home demo agora resolve refs além de João 3:16 (usa o reading subset) — superset estritamente-melhor, João 3:16 idêntico; reviewer confirmou aceitável. | `8ecd08a` (impl) · `<accept>` (loop) |
| F5.11 | 2026-07-03 | **i18n dos 4 painéis de IA (Ask/Chat/Compare/Study) + a11y (i18n/a11y)** — migra o CROMO dos painéis de IA. `i18n.ts` +84 chaves (catálogo 25→130): namespaces `ai/ask/chat/compare/study` + 9 `a11y`. Os 4 componentes (`ReaderAskPanel/ChatPanel/ComparePanel/StudyPanel`): header/close, seções de provedor, badges BYOK, campos, submit, títulos cited/interp, meta/estimate/disclaimer, warnings/citações/léxico, export/share → via `t()`. `ReaderStudyPanel`: `Option<T>` agora carrega `labelKey: MessageKey` (enum `value`/`key`/`testID` inalterados — fronteira = fonte única). `[chapter].tsx`: `bookLabel(bookNum)` (`useCallback[locale,t]`) localiza o `sourceLabel` dos 4 painéis (item deferido da F5.5) — nome do STORE (`namePt`/`nameEn`, `locale` só seleciona campo); `reference` fica `bookNameEn` (âncora EN). **Anti-alucinação (reviewer escrutinou):** `cited_text`/`interpretation`/`reference` EN/atribuições STEP CC-BY (`c.license`/`c.attribution`) NUNCA via `t()` — só cromo + dados interpolados (nome de livro do store, ids provider/model, contagem de tokens). Teste i18n estendido (guard c/ ai/ask/chat/compare/study; divergência PT≠EN + interpolação verbatim). tsc 0; test:i18n(130)/6 self-tests de IA (cited_text verbatim)/expo-web verdes; the-light `2fc2dab` (7 arquivos). Sem ADR (extensão F5.2). | `8b8cd74` (impl) · `<accept>` (loop) |
| F5.9 | 2026-07-03 | **Code-split web (Performance web)** — move o subgrafo pesado web-only para chunks lazy. `app/web/reading.web.ts` (a ÚNICA porta estática p/ o subgrafo pesado): imports estáticos → `await import()` no corpo de cada fn async; enums/fns wasm-bindgen ficam eager; `AiFetch`→`import type`. Deferidos p/ on-demand: factory wa-sqlite FTS5 (`sqlite-reading-opfs`), `sqlite-reading/search/xref`, `ai-anchored`, `study`(+`sqlite-lexicon`+`research`), `session`, `userdata-fs/opfs`. Metro emite chunks async (`web.output:static`). `measure-web-bundle.sh` re-centrado (moduleCount 854→844, nominais entry-JS) + `web-bundle-baseline.json` regenerado → **BUDGET OK**. **Resultado:** 1 bundle→**10 bundles**; entry moduleCount 856→844 (−12), bytes 1.448.032→1.381.059 (−4,6%), gzip −5,4%. **Zero-drift:** 15 self-tests + i18n + keystore PASS idênticos (lazy só muda QUANDO carrega). tsc 0; expo web 0; wasm puro; the-light `2fc2dab` intacto (4 arquivos). ADR-0040. **Honesto:** alvo aspiracional −20% inalcançável (movível ~106KB; resto ~1,38MB = RN Web+React+expo-router+wasm-bindgen glue, first-paint). **Reviewer flag (→F5.12):** 2ª factory wa-sqlite npm (~40KB via `index.tsx→passage.web`) ainda eager mas splittable (`getPassage` só no submit); corrigir "irredutível/~66KB" da ADR-0040. | `cd4323e` (impl) · `<accept>` (loop) |
| F5.8 | 2026-07-03 | **i18n da busca + componentes de navegação (i18n/a11y)** — estende F5.2/F5.5 à busca+nav. `i18n.ts` +9 chaves (search.inputPlaceholder/hintEmpty/noResults, read.emptyChapters/chapterNotFound, a11y.searchTextInput/openBook/openChapter/verseOptions; paridade pt/en compile-time). `search/index.tsx`: placeholder/hints/no-results via `t()` (`{term}`=query do usuário), TextInput a11y label, nome de livro na ref via `namePt`/`nameEn` por `locale` (era hardcoded `nameEn`) + fallback `t('read.bookFallback')`. `ReaderBookList`/`ReaderChapterGrid`/`ReaderChapterView`: empty-states via `t()`, a11y role+label nas células; **ChapterView usa `accessibilityHint`** (não label) p/ preservar o versículo verbatim como conteúdo do screen-reader. `ReaderSearchResultItem` intocado (já tinha role+label da ref do store). **Anti-alucinação (verificado):** snippet/refs/nomes de livro do STORE, nunca via `t()`; CC-BY intacto. Teste i18n estendido (search no guard anti-alucinação; divergência PT≠EN + interpolação). tsc 0; test:i18n(53 chaves)/search/reading/expo-web verdes; the-light `2fc2dab` intacto (6 arquivos). Reviewer independente confirmou aceites + anti-alucinação. Sem ADR (extensão F5.2). **Follow-up:** +2 módulos eager → baseline perf a re-centrar em F5.9. | `87d95b4` (impl) · `<accept>` (loop) |
| F5.7 | 2026-07-03 | **UI nativa de Planos de leitura (lista→iniciar→dia→marcar) + self-test on-device (Planos de leitura)** — fecha o chão NATIVO do workstream de planos (F5.1 geração + F5.4 progresso + F5.7 UI). Rota `app/app/plans/index.tsx` (556 linhas): sem plano ativo → `PlanChooser` (3 planos + contagem de dias, `start-plan-<id>`); com plano → `ActivePlanView` (barra de progresso `completed/len` + streak, `FlatList` de dias com HOJE destacado, `mark-day-done`/`change-plan`). Orquestra SÓ as fns de fronteira (`listReadingPlans`/`readingPlanDay`/`readingPlanDayIndex` F5.1 + `readingPlanProgress`/`startReadingPlan`/`setReadingPlanCompleted`/`clearReadingPlan` F5.4) — nada reimplementado em TS. Dia → navega `/read/${book}/${chapter}` (leitura existente). `dataDir` via `ensureUserDataDir()`. `_layout` registra `plans/index`; home `open-plans` (gated nativo); web → `PlansWebNotice` (degrada sem chamar stubs). i18n: novas chaves `nav.plans`/`home.readingPlans`/`plans.*`/`a11y.*` (pt+en, guard anti-alucinação c/ `plans`). **Anti-alucinação:** nomes do CATALOG, refs/dias de `readingPlanDay`, versículo pelo Reader (store) — nunca via `t()`. **Prova ON-DEVICE** (iPhone 17 sim/iOS 26): `TLA_PLANS plan_id="gospels" days=30 today_index=0 completed=1 persisted=true` (composto de retornos reais; sem regressão dos outros TLA_*). Corrigiu xcframework iOS stale (predava F5.1/F5.4 → 0 símbolos de plano) via `gen-bindings-ios.sh`. tsc 0; expo export web 0 (8 rotas incl `/plans`); test:i18n(44 chaves)+15 self-tests web PASS; the-light `2fc2dab` intacto; 11 host tests de plano intactos. ADR-0039 (streak=`completed`; native-first; web=F5.10). **Follow-up (reviewer):** `/plans` +2 módulos → `measure-web-bundle.sh` RED → F5.9 re-centra a baseline. | `ea74908` (impl) · `<accept>` (loop) |
| F5.6 | 2026-07-03 | **Build do frontier wasm em release + wasm-opt (Performance web)** — 3ª tarefa do workstream perf (segue F5.3). `scripts/gen-bindings-web.sh`: `cargo build`→`--release` (step 2), WASM_FILE lê `.../release` (step 4), novo passo `wasm-opt -Oz --strip-debug --strip-producers` (guarda como a do wasm-bindgen: `cargo install wasm-opt 0.116.1` se ausente, NÃO-fatal → cai p/ release-only). `wasm-crate.patch.toml` `[profile.release]` agora aplica: `opt-level="s"`+`lto=true`+`codegen-units=1`+`strip="debuginfo"`; **NÃO** adicionado `panic="abort"` (preserva `catch_unwind` FFI da UniFFI — zero-drift). **Resultado: frontier wasm 4.244.884→1.198.888 B (−71.8%)**, gzip 1.136.449→430.849; `measure-web-bundle.sh`/baseline atualizados. **Zero-drift:** 15 self-tests PASS (reading/ai/study idênticos), wasm-bindgen `index.js` byte-idêntico (sem mudança de API/assinatura). Reviewer independente rebuildou → mesmo sha256 (`2e6059e0…`) + baseline byte-reproduzível. tsc 0; expo export web 0; the-light `2fc2dab` intacto; `core/` não tocado. Entry-JS nominal re-centrado (drift pré-F5.6 de código TS das F5.x anteriores; F5.6 não toca TS) — manutenção intencional da métrica. `perf(F5.6)`. Sem ADR (build-config). | `3e7e5e8` (impl) · `<accept>` (loop) |
| F5.5 | 2026-07-02 | **i18n do fluxo de leitura (i18n/a11y)** — estende a camada F5.2 ao fluxo de leitura. `i18n.ts` +9 chaves (nav.*, read.parallel/bookFallback, theme.*, paridade PT/EN em compile-time). `_layout.tsx` consome `useI18n()` → títulos das 5 `Stack.Screen` via `t('nav.*')` e re-render ao trocar idioma (sem restart). Telas `read/index`/`[book]/index`/`[book]/[chapter]` com `setOptions` reativos ao `locale`; `"Lado a lado"`→`t('read.parallel')` + a11y. **`lang="pt"`→`lang={locale}` nos 4 painéis de IA** (Ask/Study/Chat/Compare) → resposta da IA segue o idioma da UI. `ThemeToggleButton` a11y label via `t()`. **Anti-alucinação (verificado):** versículo NUNCA via `t()` (vem do `get_chapter`/store); nomes de livro do STORE (`namePt`/`nameEn`, `locale` só SELECIONA o campo, nunca traduz); `reference` canônica permanece EN (âncora); `citedText` intacto; CC-BY não tocado; `locale`(UI)≠`translation`(versão). Teste headless estendido (nav.read/read.parallel divergem PT≠EN; bookFallback interpola). tsc 0; i18n(25 chaves)/reading/search/expo-web verdes; the-light `2fc2dab` intacto. Escopo deferido (sourceLabel dos painéis→F5.11; empty-state do ChapterGrid) razoável. Sem ADR (extensão da F5.2/ADR-0038). Reviewer independente confirmou aceites + anti-alucinação. | `694ca88` (impl) · `<accept>` (loop) |
| F5.4 | 2026-07-02 | **Persistência nativa do progresso do plano (PlanStore fs) — Planos de leitura** — 2ª tarefa do workstream de planos (segue F5.1). 4 fns de fronteira em `core/src/lib.rs` (`reading_plan_progress`/`start_reading_plan`/`set_reading_plan_completed`/`clear_reading_plan`) com impl nativa `#[cfg(not wasm32)]` delegando ao `the_light_core::userdata::plans::PlanStore` (persiste `<data_dir>/reading-plans/active.json`) + stub web `#[cfg(wasm32)]` (throw CoreError; OPFS = F5.10). Record `ReadingPlanProgress{plan_id,start_date,completed}` (puro) + `From<PlanProgress>` nativo (NaiveDate↔ISO por inferência, sem `use chrono`). **Anti-alucinação:** `start_reading_plan` valida `plan_id` contra o CATALOG (`plan_by_id`) + parse ISO ANTES de qualquer I/O; nada reimplementado em TS/Rust (delega ao core, zero drift). 5 host tests (temp dir isolado, molde notes F1.10): round-trip start→marca→reload de handle independente; id desconhecido/ISO inválida/sem-plano-ativo → CoreError sem escrever arquivo. Glue TS nativo+stub async (I/O). 80 testes; fmt/clippy(-D)/tsc/gen-bindings(nativo+web)/wasm-puro verdes; the-light `2fc2dab` intacto (`core/Cargo.toml` fora do commit). **Desvio aceito pelo reviewer:** retornos `Result<Option<…>>`/`Result<bool>` (molde notes F1.10, propaga erro de I/O/JSON) em vez de `Option`/`bool` crus — estritamente melhor. Reading-plans nativo (geração F5.1 + progresso F5.4) pronto → F5.7 (UI) desbloqueada. | `f52e16e` (impl) · `<accept>` (loop) |
| F5.3 | 2026-07-02 | **Tracer perf web: baseline de bundle + defer do wasm de 4MB do first-paint (Performance web)** — abre o workstream de performance; aceita em 2 tentativas (auto-correção do loop). **Defer (`675ca64`):** removido o gate global de wasm do `_layout.tsx` (shell pinta imediatamente; wasm aquece em `useEffect` não-bloqueante); novo `WasmGate.tsx` por-rota monta leitura/busca só após o wasm pronto (nativo transparente); prova `firstpaint.web.test.mjs` (react-test-renderer: 5 `Stack.Screen` montam SÍNCRONO, 0 spinner render-blocking, wasm warm 1× com promessa não-resolvida). **Métrica (`26b29ec`, attempt 2 após reviewer reprovar a v1 por não-reprodutibilidade):** `scripts/measure-web-bundle.sh` → `loop/perf/web-bundle-baseline.json`; raiz do problema = Metro renumera ~854 módulos em ordem de grafo assíncrono (entry-JS flutua ~122B/hash muda, upstream/inevitável sem inflar o bundle); solução honesta = gravar assets content-addressed EXATOS (wasm 4.24MB, DBs — 93% dos ~20MB) + `moduleCount=854` (exato, order-independent) + entry-JS raw/gzip como nominal±tolerância re-verificada; header reescrito (a afirmação falsa de determinismo removida); script sai ≠0 em violação de orçamento. **Byte-reproduzível:** 5 runs → mesmo SHA `bb38714…`. **Anti-regressão:** 16 self-tests + tsc + expo export web verdes; the-light `2fc2dab` intacto; `react-test-renderer` só devDep (não entra no bundle). Reviewer independente confirmou attempt-1 FAILED e attempt-2 PASSED. Escopo tracer (defer + baseline; code-split/lexicon-split/precompress = F5.6/9/12/15/17). Sem ADR (infra de build/métrica). | `675ca64`+`26b29ec` (impl) · `<accept>` (loop) |
| F5.2 | 2026-07-02 | **Tracer i18n PT/EN + prefs offline + seletor + home migrada (i18n/a11y)** — abre o workstream de i18n. `app/lib/i18n.ts` (novo, dependency-free, molde `theme.ts`): `Locale='pt'|'en'`, catálogos `pt`/`en` tipados `Record<MessageKey,string>` (paridade garantida em compile-time), `translate()` puro com interpolação `{param}`, `detectDeviceLocale()` offline, `I18nProvider`/`useI18n()`. `app/lib/prefs.ts`+`prefs.web.ts` (novo): KV offline com `PrefsBackend` injetável (molde `keystore.ts`) — nativo=`prefs.json` em documentDirectory, web=`localStorage`, namespaced `tla.pref.*`, nunca logado (reusável p/ theme-persist F5.14). `LanguageToggleButton.tsx` (PT⇄EN, `testID='language-toggle'`, `role='switch'`) no header; `_layout.tsx` envolve em `I18nProvider`; `index.tsx` (home) migrada 100% via `t()`, a11y, ZERO hex. **Anti-alucinação:** i18n só traduz CHROME — versículo/refs/citações verbatim do store; CC-BY inalterado; `locale`(UI)≠`translation`(versão bíblica). Prova headless `i18n.test.mjs` (16 chaves paridade pt↔en, translate+interpolação, round-trip de persistência sobrevive a instância nova/reopen, detecção pt-BR→pt/en-US→en/desconhecido→pt, sem `console.*`). tsc 0; expo export web 0 (7 rotas, Metro pega `prefs.web.ts`); self-tests reading/search/keystore sem regressão; eslint N/A (repo sem config). SEM nova dep de runtime. the-light `2fc2dab` intacto (`core/` fora do commit). Reviewer independente confirmou aceites + não-negociáveis + ADR-0038 sem colisão. Escopo tracer (só home migrada; demais telas = F5.x seguintes). ADR-0038. | `05f8102` (impl) · `<accept>` (loop) |
| F5.1 | 2026-07-02 | **Fronteira NATIVA de planos de leitura (tracer, re-escopo native-first)** — abre a Fase 5. Expõe a geração de planos JÁ EXISTENTE no core (`the_light_core::userdata::plans`) pela fronteira do app (`core/src/lib.rs`): Records puros `ReadingPlanSummary{id,name,days}`/`ReadingPlanDay{label,references}` (reusa `Reference`) + 3 fns `list_reading_plans`/`reading_plan_day`/`reading_plan_day_index` com **impl nativa `#[cfg(not(target_arch="wasm32"))]` + stub web** (molde `deep_study`). `reading_plan_day_index` delega a `PlanProgress::day_index_for` (sem clamp reimplementado); `NaiveDate` por inferência de tipo (sem `use chrono`, sem editar `core/Cargo.toml`). 6 host tests deterministas (3 planos com nomes PT verbatim do core; gospels dia 0 = Mt 1 book=40/ch=1; annual soma 1189 caps; clamp; data inválida→CoreError). **Zero drift** (TS só reexporta bindings; geração/chunking só no core). **RE-ESCOPO:** a versão cfg-free original era impossível — `userdata` é `#[cfg(feature="embedded")]` no core `2fc2dab` (executor comprovou por probe E0433; Driver verificou `lib.rs:38-39`) → seguido o padrão native-first; **paridade web (F5.10) exigirá PR `ai-pure` ao core** (gate futuro, ADR-0037). 75 testes; fmt/clippy(-D)/tsc/gen-bindings(nativo+web)/wasm-puro verdes; the-light `2fc2dab` intacto (`core/Cargo.toml` fora do commit); sem drift/segredos. Reviewer independente confirmou 5/5 aceites. | `eb81a72` (impl) · `<accept>` (loop) |
| F4.7 | 2026-07-02 | **🎯 MARCO 4 APROVADO (sign-off humano)** — **Fase 4 (Polimento da IA) completa** nos 3 alvos (web/iOS/Android). F4.1–F4.6 aceitas e verdes: streaming WEB token-a-token (F4.1/ADR-0033), transporte web multi-provedor anthropic/openai/ollama (F4.2/ADR-0034), Tavily BYOK nativo (F4.3) + web session-only (F4.4), validação REAL Gemini+Tavily com WARNINGS=[] (F4.5), e **streaming NATIVO real por provedor** via PR sancionado ao `the-light` (F4.6/ADR-0035 — override de `complete_stream` SSE/NDJSON, PR #2 merged → rev `2fc2dab`, re-pin revalidado). Não-negociáveis mantidos: anti-alucinação zero-drift (mesma impl Rust `ai-pure` nativo↔web; concat dos deltas == `complete`), BYOK (chave nunca em git/log), offline-first (IA/pesquisa opt-in; Fase 1 segue 100% offline), `the-light` só via PR+ADR (`2fc2dab`, nunca forkado; wasm puro), STEP/OpenBible CC-BY visível. **Próxima: Fase 5 — Refinamento e abertura** (plano §FASE 4: planos de leitura, i18n/acessibilidade/temas, performance web, sync opcional), decomposta em `loop/backlog/PHASE-5.md` e semeada na `queue/`. `loop/HALT` removido. | _(sign-off)_ |
| F4.6 | 2026-07-02 | **PR ao `the-light-core` — streaming NATIVO real por provedor (SSE/NDJSON) — MERGEADO**: sobrescreve `LlmProvider::complete_stream` nos 4 provedores reais para streamar token-a-token direto do provedor — **SSE** (Anthropic `content_block_delta`/`text_delta`; OpenAI `choices[0].delta.content`+`[DONE]`; Gemini `:streamGenerateContent?alt=sse`, `candidates[0].content.parts[].text`) e **NDJSON** (Ollama `message.content` por linha) —, fechando o último caminho de IA ainda não-streaming no núcleo. Duas camadas: parsers de delta PUROS/un-gated (`sse_data`/`with_stream`/`*_stream_delta`, cobertos pelo `allow(dead_code)` sob `ai-pure`) + `stream_reader` genérico sobre `std::io::BufRead` (testável por `Cursor`, sem rede) + `stream_response`/`post_stream` `#[cfg(feature="embedded")]` (I/O `reqwest::blocking`, sem feature async). Cada override **reusa o `*_body`** não-streaming (só liga o flag/endpoint) e o parser **espelha o `*_extract`**. **Revisão adversarial (workflow 5 lentes)** aprovou zero-drift/não-quebrante/BYOK/wire-format e achou 1 defeito real — frames de **erro in-band** (após o 200: overload/rate-limit) eram engolidos → truncavam a resposta; **corrigido no mesmo PR** (cada `*_stream_delta` devolve `AiError::Http`, espelhando os `*_extract`) +4 testes. **Não-quebrante** (default de `complete_stream` preservado; mock/CLI/TUI em 1×; `complete`/`chat` intactos; `default=["embedded"]`), **zero-drift** (concat dos deltas == `complete`; texto/citações do store/`ai-pure`), **embedded-only** (grafo `ai-pure`/wasm puro), **assinatura de `complete_stream`/fronteira/bindings INALTERADA** (`ask_anchored_stream`/`AiTokenCallback` só recebem N tokens reais). **the-light:** branch `feat/native-sse-streaming` de `04b9b24` → PR #2 → **MERGEADO** (ff na `main`) → rev **`2fc2dab`**; `fmt`/`clippy -D warnings`/`test --workspace` (196 no core) + wasm `ai-pure` puro verdes. **Re-pin do app** (`core/Cargo.toml` 2 linhas → `2fc2dab`) revalidado: fronteira 69 testes; bindings web regen contra o novo rev; `tsc`/`expo export web`/15 self-tests web verdes; grafo wasm puro. Chave só em header (ollama sem chave); secret-scan vazio. Streaming nativo com chave real = etapa humana (base F4.5 aceita). ADR-0035; próximo ADR livre = ADR-0036. **Fase 4 tecnicamente completa → F4.7 ⛔ Marco 4 (sign-off).** | `5cffd32` (app) · the-light `2fc2dab` |
| F4.5 | 2026-07-02 | **⛔ Validação REAL da IA polida (streaming · multi-IA · Tavily) — sign-off humano**: o humano rodou `core/examples/study_real.rs` com as PRÓPRIAS chaves (Gemini `gemini-2.5-flash` + **Tavily**, ambas por env var, nunca no chat/git/log) → estudo profundo REAL de João 3:16 com pesquisa web REAL via **Tavily (BYOK)**: PASSAGE_TEXT = João 3:16 KJV verbatim do STORE; interpretação real em 7 seções (Baptist/Academic/Exegetical) com âncoras `[V:G####]`+`[W:n]`; **26 citações** (22 léxico STEP CC-BY TBESG + **4 web REAIS do Tavily** — `[W:1]` Wikipedia, `[W:2]` Bible App, `[W:3]` Come unto Christ, `[W:4]` outro site; domínios variados da web aberta, provando o backend Tavily real), NUNCA do modelo; **`WARNINGS=[]`** (o core validou TODAS as âncoras MESMO com pesquisa web real ligada — prova central do anti-alucinação sob rede); ACADEMIC_MARKDOWN 22982 chars (SBL). Chaves nunca exibidas/logadas. Streaming web token-a-token (F4.1) e comparação multi-IA web (F4.2/F3.7) já provados por MOCK com zero-drift; sign-off humano ('Aceitar F4.5 e seguir') aceitou a F4.5 nesta base sem exigir re-execução web ao vivo. Rede só nesta validação opt-in; offline-first intacto; the-light `04b9b24`. HALT removido → segue F4.6 (PR streaming NATIVO SSE, BLOQUEANTE). | sign-off humano |
| F4.4 | 2026-07-02 | **Tavily no WEB (`fetch` + chave session-only + toggle UI opt-in)**: `app/web/research.web.ts` ganha `tavilySearch(fetch,key,query,lang,limit)` (molde de `wikipediaSearch`) → `POST https://api.tavily.com/search` com a chave `api_key` **NO CORPO** (nunca header/URL/log), `{query, max_results: limit.clamp(1..10), search_depth:"basic"}`, parse `results[]`→`StudyWebSourceInput{title,url,snippet,site,fetchedAt}` (pula sem `url`). `study.web.ts::deepStudyOnHandle` passa a HONRAR `researchBackend="tavily"`+`researchKey` (antes `void researchKey`): `resolveWebSources` chama `tavilySearch` e injeta os `web_sources`; sem chave→erro citando só "tavily" (0 fetch); `"wikipedia"` keyless e `off` inalterados. UI `ReaderStudyPanel.tsx`: seletor 3-vias off/Wikipedia/**Tavily** + campo de chave **session-only** (`useState`, `secureTextEntry`, nunca persistida/logada) + aviso de privacidade/atribuição. **Anti-alucinação ZERO-DRIFT:** as citações `kind="Web"`/`[W:n]` seguem montadas pelo Rust `study_web_finalize` (ai-pure) das URLs; o TS só faz o HTTP e entrega fontes cruas — nenhuma citação/prompt em TS (ADR-0029). `passage_text` João 3:16 do store, separado da interpretação. **NÃO toca o Rust `ai-pure` nem o the-light** (`04b9b24`; TavilyProvider/build_research_provider seguem embedded-only intactos; `core/Cargo.toml` e o pin não mudam; grafo wasm puro). Prova por MOCK (headless node, sem chave/rede real): `fetch` MOCK de `api.tavily.com/search`→`results[]` fixos → `deepStudy` web `researchBackend="tavily"`,`researchKey="dummy"` → assert URL, chave NO CORPO (parse do body, nunca URL), citação `kind="Web"` (britannica.com) pelo Rust, `academic_markdown` cita `[W`, João 3:16 do store ≠ interpretação; tavily-sem-chave→erro só "tavily"/0 fetch; Wikipedia preservado; off→0 pesquisa. `tsc`/`expo export web`/self-tests(research/study/compare/export) verdes, sem regressão; grafo wasm puro; the-light `04b9b24`/`core/**` intactos; chave session-only nunca em git/log (scan vazio). Sem ADR (transporte=infra). Validação real com chave Tavily = F4.5 (gate). | `69b369f` |
| F4.3 | 2026-07-02 | **Tavily BYOK nativo no `deep_study` (threading de `research_key`)**: a fronteira `deep_study` (`core/src/lib.rs`, corpo `cfg(not wasm32)` + stub web) ganha `research_key: Option<String>` como ÚLTIMO parâmetro (depois de `research_backend`), passado a `build_research_provider(&backend, research_key, lang.code())` — o `None` HARD-CODED (`lib.rs:1438`) foi removido. Assim `research_backend="tavily"` + `research_key=Some(...)` usa o `TavilyProvider` (BYOK, embedded); `"wikipedia"` ignora a chave (keyless, F3.9a preservado); `"mock"` sem rede; `None`→offline. **NÃO toca the-light** (`TavilyProvider`/`build_research_provider` já `pub` sob `embedded` em `04b9b24`; só threading da chave na fronteira; `core/Cargo.toml` intacto). Grafo wasm puro (Tavily/`research` embedded-only ficam no bloco `cfg(not wasm32)`; stub web recebe o param e segue stub). Anti-alucinação: citações Web vêm das URLs (mock/Tavily/Wikipedia), NUNCA do modelo. BYOK: `research_key` nunca em git/log. Prova por MOCK (sem chave/rede real): teste novo `deep_study_tavily_without_research_key_errors_without_network` (`tavily`+key=None → CoreError NoKey ANTES de qualquer HTTP) + invariância do mock com key `None` vs. fictícia (citações Web idênticas) + `wikipedia` ignora a chave + `None`→offline; 9 call sites de teste atualizados. Exemplo host `study_real.rs` ganha `AI_RESEARCH_KEY` (opc., impressa só como "(presente — não exibida)", nunca o valor). Glue TS (`reading.ts`/`reading.web.ts`/`study.web.ts`) ganha `researchKey?: string` opcional final; bindings web+ios regenerados expõem `researchKey`. **69 testes** (68+1); fmt/clippy(-D warnings)/tsc/self-tests(study/research/export)/expo export web verdes; grafo wasm puro (sem rusqlite/reqwest); the-light `04b9b24` e `core/Cargo.toml` intactos; chave nunca vazou (secret-scan vazio). Sem ADR (infra de threading). Chave real Tavily = F4.5 (gate). | `0141f0f` |
| F4.2 | 2026-07-02 | **Transporte web MULTI-PROVEDOR (anthropic/openai/ollama) com streaming**: `webLlmTransport` (app/web, TS) estendido além de gemini/mock — por provedor: endpoint + método + header da chave corretos (anthropic `x-api-key`+`anthropic-version` `/v1/messages`; openai `authorization: Bearer` `/v1/chat/completions`; ollama SEM chave `/api/chat` localhost), body espelhando `*_body` do core (model resolvido pelo `ai_web_prepare`, max_tokens 8192), extract streaming (SSE anthropic/openai; **NDJSON** ollama) + não-streaming; helpers `postJson`/`readLineStream`/`parseSseData`. **Transporte = infra TS** (os `*_body`/`*_extract` do core são privados, ADR-0023/0025); o Rust `ai_web_prepare`/`ai_web_finalize` NÃO muda. Anti-alucinação ZERO-DRIFT: system/user/model do prepare (Rust); `cited_text`/citação/finalize do `ai_web_finalize` (Rust). Prova HEADLESS por provedor: streaming `onToken 5×` na ordem → `concat==interpretation`; âncora Strong espúria `[V:G9999]` removida pela MESMA `ai_web_finalize` (Rust); `cited_text` João 3:16 do store inalterado; chave DUMMY nunca na URL; chave ausente → erro cita só o provedor (0 fetch); ollama sem chave. `tsc`+`expo export web` verdes; sem regressão; grafo wasm puro; the-light/`core/**` intactos (`04b9b24`). ADR-0034. | `9ba3d52` |
| F4.1 | 2026-07-02 | **Streaming WEB real (token-a-token via fetch/SSE) — Fase 4 (Polimento da IA) abre**: o `webLlmTransport` (app/web, TS) passa a fazer **streaming** da resposta do provedor — `geminiCompleteStream` lê `res.body.getReader()`+`TextDecoder`, parseia SSE `data:{…}` (`streamGenerateContent?alt=sse`), chama `onToken(delta)` na ordem e acumula; `askAnchoredStream` web destubado (assinatura pública + `AiAnswer` final inalterados). **O Rust `ai-pure` (`ai_web_prepare`/`ai_web_finalize`) NÃO muda — streaming muda SÓ o transporte TS.** Anti-alucinação **ZERO-DRIFT**: `cited_text` do `ai_web_prepare` (store, verbatim, separado da interpretação); a interpretação completa (concat dos deltas) passa pela MESMA `ai_web_finalize` (`rewrite_anchors` do Rust). Prova HEADLESS node (3 cenários): (A) 6 tokens na ordem → `concat==interpretation`, 1 fetch, chave só no header; (B) âncora Strong espúria `[V:G9999]` + bytes fatiados (SSE quebrado entre chunks) → **removida pela `ai_web_finalize` (Rust)**; (C) `"mock"` 15 tokens OFFLINE, 0 fetch. `tsc`+`expo export web` verdes; 12 testes web sem regressão; grafo wasm puro inalterado; chave session-only nunca logada; the-light/`core/**` intactos (`04b9b24`). ADR-0033. | `882bdb5` |
| F3.13 | 2026-07-02 | **🎯 MARCO 3 APROVADO (sign-off humano)** — estudo profundo assistido por IA, multiplataforma. Fase 3 (F3.1–F3.12b) aceita e verde: léxico Strong (STEP CC-BY), estudo profundo, conversa/refinamento, comparação multi-IA, export acadêmico SBL, pesquisa web (Wikipedia keyless opt-in) — **nativo + web (zero drift)**, com **validação REAL** (F3.10: Gemini + Wikipedia; 26 citações de fontes locais/verificadas; WARNINGS vazio = zero âncoras inventadas). Regras não negociáveis mantidas: anti-alucinação zero-drift (mesma impl Rust ai-pure nativo↔web, `assert_eq!`); BYOK (chave nunca em git/log; OAuth rejeitado); offline-first (IA/pesquisa opt-in); `the-light` só via PR+ADR (**3 PRs sancionados**, `04b9b24`, nunca forkado; wasm puro); STEP/OpenBible CC-BY + domínio público. ADRs 0026–0032. **Roteiro planejado (Fases 0–3) COMPLETO. Próximo: Polimento da IA (Fase 4).** `loop/HALT` removido. | _(sign-off)_ |
| F3.12b | 2026-07-02 | **Paridade WEB de CONVERSA + pesquisa Wikipedia (opt-in) + comparação multi-IA (ZERO DRIFT)**: 1 fronteira web nova `session_web_prepare(...)` em `core/src/lib.rs` (cfg-free/ai-pure) — dirige `ai::ask_session(&cap, lang, &context, &turns, study?)` por um `CaptureProvider` (o `chat` default dobra o transcript→`complete` → captura o `(system,user)` EXATO do nativo) → devolve `AiWebRequest`; **finalize = reuso puro do `ai_web_finalize`**. Glue `session.web.ts` destuba `askSessionAnchored` web (→ `ReaderChatPanel` no browser). **Wikipedia web** (`research.web.ts`): `fetch` keyless opt-in (padrão OFF + aviso de privacidade) → `WebSource[]` → `web_sources` do `study_web_*` → citações `[W:n]`/`kind="Web"` montadas pelo Rust (das URLs, nunca do modelo). **Comparação web**: un-SKIP (`askAnchored` já web-ok). Teste host **68/68** (3 novos): `session_web_prepare_matches_native_ask_session_zero_drift` (`assert_eq!` de system/user/cited_text vs nativo = ZERO DRIFT); follow-up de estudo ≠ ask simples. Provas HEADLESS node (fetch MOCK): conversa (João 3:16 do store × interpretation mock, multi-turno, chave só no header, mock=0 fetch); estudo com `researchBackend="wikipedia"` → citação Web wikipedia + `[W` no markdown / sem → 0 Web (F3.12a preservado); comparação `cited_match=true`. Grafo wasm puro; `tsc`+`expo export web`+`gen-bindings-web` verdes; chave session-only nunca logada; sem regressão nativa/web; the-light/`Cargo.toml` intactos (`04b9b24`). ADR-0032. **Paridade web da Fase 3 COMPLETA.** | `e4da90a` |
| F3.12a | 2026-07-02 | **Paridade WEB do ESTUDO PROFUNDO + léxico + export (ai-pure prepare→fetch→finalize; ZERO DRIFT)**: fronteira web nova em `core/src/lib.rs` (só `ai-pure`, cfg-free, NÃO toca the-light/Cargo.toml): `study_web_prepare(...) -> StudyWebRequest{passage_text,system,user,...}` (monta `StudyRequest` do store — versos + léxico do subset — via `numbered_verses`/`VerifiedLexicon`/`system_prompt_in`/`user_prompt`) + `study_web_finalize(...,raw_llm_response,...) -> StudyResultOut` (`split_sections`+`verify`+`cited_web_indices`+`CitationCollector{from_verified_lexicon+from_web_results}`+`to_academic_markdown`). Glue `study.web.ts` destuba `deepStudy` web (prepare→**fetch**→finalize; transporte `webLlmTransport`, chave só no header); `sqlite-lexicon.web.ts` lê o léxico do subset (SELECT + shaping, ADR-0011); `lexicalEntries` web destubado; export web reusa `buildStudyExport`. Chave web **session-only** (nunca logada). Teste host **65/65** — **paridade nativo↔web `study_web_prepare_and_finalize_match_native_study_zero_drift`** (`assert_eq!` de `ai::study::study` nativo vs prepare+finalize web em passage_text/user/system/sections/warnings/citations/academic_markdown = ZERO DRIFT). Prova HEADLESS node (fetch MOCK): João 3:16 verbatim do store, 22 citações do léxico STEP CC-BY, academic_markdown 1276 chars; lexicalEntries web = 21 Strong; export = markdown+sidecar reais. Grafo wasm puro (sem rusqlite/reqwest/wasm-bindgen); `tsc`+`expo export web`+`gen-bindings-web` verdes; sem regressão nativa/web; the-light/`Cargo.toml` intactos (`04b9b24`). ADR-0031. | `c340d7a` |
| F3.11 | 2026-07-02 | **PR ao `the-light-core` — feature `ai-pure` ESTENDIDA p/ a superfície pura do ESTUDO** *(3º PR sancionado, ADR-0029/0030)*: implementado no branch `feat/ai-pure-study`, **revisado** (portão D2 verde; não-quebrante; workspace core 184) e **mergeado pelo humano** na `main` do `the-light` → rev **`04b9b24`**. Widening de `#[cfg]` (`embedded`→`any(embedded,ai-pure)`) na lógica pura do estudo (`StudyRequest`/`StudyResult`/`to_academic_markdown`/`to_markdown`/`cited_web_indices`/`user_prompt` + `WebSource`/`from_web_results`) → compila em `wasm32`; **1 `pub` novo** (`user_prompt`) + `cited_web_indices` pub; `ai-pure` liga **chrono clock-free** (nativo mantém o clock via `chrono/clock`) → `default` byte-a-byte. `study()` fica embedded; a paridade web (F3.12) usa **prepare→fetch→finalize** com as peças puras (zero drift). Driver **re-pinou** `core/Cargo.toml`+`Cargo.lock` (c8ecb2f→`04b9b24`, linha web já com `ai-pure`): 63 testes fronteira verdes, **grafo wasm ainda puro** (sem rusqlite/reqwest/wasm-bindgen), `gen-bindings-web` 0, fmt/clippy limpos. ADR-0030. | `04b9b24`† |
| F3.10 | 2026-07-02 | **🎯 Validação REAL do ESTUDO PROFUNDO (VALIDADA por sign-off humano)**: o humano rodou `core/examples/study_real.rs` (harness que lê a chave de **env var**, **nunca a imprime/commita**) com `AI_PROVIDER=gemini AI_MODEL=gemini-2.5-flash AI_RESEARCH=wikipedia` → estudo profundo **REAL** de João 3:16: `PASSAGE_TEXT` = KJV verbatim do STORE; `INTERPRETATION` = análise acadêmica genuína do Gemini (7 seções, lente Baptist/Academic/Exegetical) com âncoras `[V:Strong]`/`[W:n]`; **26 citações TODAS de fontes locais/verificadas** — léxico Strong STEP CC-BY (TBESG: θεός/ἀγαπάω/μονογενής…) + **pesquisa web Wikipedia** (`[W:1]`..`[W:4]`) — **nunca do modelo**; **`WARNINGS: []`** (o core validou todas as âncoras — zero Strong/fonte inventado, prova central do anti-alucinação); `ACADEMIC_MARKDOWN` = 16654 chars (SBL do core). Chave nunca exibida/logada. **Cadeia de estudo profundo + pesquisa web provada ponta a ponta.** Rede só nesta validação opt-in; the-light `c8ecb2f`. | _(sign-off)_ |
| F3.9a | 2026-07-02 | **Pesquisa web Wikipedia (keyless, OPT-IN) no `deep_study` (prova MOCK, ADR-0028)**: `deep_study` ganhou `research_backend: Option<String>` (último param); corpo `cfg(not wasm32)` — `Some(backend)` **delega** a `ai::build_research_provider(&backend, None, lang.code())` + `provider.search(&reference_label, DEFAULT_RESEARCH_LIMIT=4)` → `Vec<WebSource>` injetado em `StudyRequest.web_sources` (antes `vec![]`); `None` → `vec![]` (offline, **padrão desligado**); stub web ignora (pesquisa web real = F3.12). Glue `reading.ts`/`.web.ts` repassa `researchBackend?`. Teste host **63/63** (3 novos): com `"mock"` (MockResearchProvider canned, SEM rede) o `citations` tem entrada `Web` das URLs buscadas (example.org) — **nunca do modelo** (interpretation invariante à web); com `None` sem citação Web (F3.3 preservado); backend inválido → CoreError sem rede; Devotional não emite aparato. Delegação real (from_web_results/[W:n]/cited_web_indices só no core); grafo wasm puro; `gen-bindings-web`/`tsc`/`expo export web` verdes; the-light/`Cargo.toml` intactos (`c8ecb2f`); sem PR. | `57ade6b` |
| F3.9 | 2026-07-02 | **🚦 GATE estratégico — pesquisa web + paridade web do estudo (DECIDIDO por sign-off humano)**: **D1** = pesquisa web assistida no estudo via **Wikipedia keyless, OPT-IN** (padrão desligado + aviso de privacidade; fontes citadas [W:n] com anti-alucinação do core; Tavily BYOK futuro) — **ADR-0028**. **D2** = paridade WEB do estudo/léxico/conversa via **PR `ai-pure` COMPLETO ao core** (fonte única, zero drift; study/verified_lexicon são embedded-only + user_prompt privado → precisa do PR, molde F2.7; conversa já é ai-pure) — **ADR-0029**. **D3** = **validação real (F3.10) com a chave do usuário** confirmada como gate (molde F2.6). Re-escopa F3.10 (validação real, bloqueante) · F3.11 (PR ai-pure ao the-light, bloqueante) · F3.12 (paridade web) · F3.13 (Marco 3). `loop/HALT` removido. | _(sign-off)_ |
| F3.8 | 2026-07-02 | **EXPORT acadêmico SBL (Markdown + sidecar) + Share + fix ADR-0027**: via (C) — Record `StudyResultOut` ganha `academic_markdown: String` populado no `From<StudyResult>` (`cfg(not wasm32)`) via `result.to_academic_markdown(result.language)` — **o Markdown SBL vem INTEIRO do core** (`pub`, embedded-only); o app NÃO reimplementa SBL/proveniência. Sidecar app-side `buildStudyExport` (PURA, molde F1.11) agrega `StudyCitation`/`sources` (atribuições) num JSON. Botão "Exportar (acadêmico)" no `ReaderStudyPanel` → **Share** do react-native com `{markdown, sidecar}`; web = stub (F3.12). Teste host **60/60** (`academic_markdown_from_core_cites_store_and_step_attribution`): o Markdown contém passage_text verbatim do store (João 3:16 KJV) + atribuição **STEP CC-BY** ("STEP Bible"/"CC BY 4.0") no rodapé de procedência + rótulo de interpretação IA ("confira sempre as fontes primárias") + citações do banco. Prova HEADLESS iPhone 17: `TLA_EXPORT ref="John 3:16" provider="mock" md_len=980 has_passage=true has_attribution=true`, sem regressão dos 10 demais TLA_*. **FOLLOW-UP ADR-0027 RESOLVIDO na raiz:** 9 doc-comments `**puro**/`→`**puro** /` em `core/src/lib.rs` (só comentário) → saneamento `**/` dos geradores agora redundante; `tsc` verde. Grafo wasm puro; the-light/`Cargo.toml` intactos (`c8ecb2f`); sem PR. | `c0a36e3` |
| F3.7 | 2026-07-02 | **UI nativa de COMPARAÇÃO MULTI-IA (prova MOCK no device)**: `ReaderComparePanel` — 1 pergunta ancorada + seletor MULTI de provedores (SUPPORTED_PROVIDERS + mock) → **N chamadas independentes** a `askAnchored` (`Promise.all`) sobre a MESMA `reference`; layout: `cited_text` (âncora, verbatim do store) exibido UMA vez ("Passagem (texto bíblico) — âncora comum") SEPARADO das N `interpretation` rotuladas por `provider · model` (anti-alucinação visível). Reusa `ask_anchored` (F2.1/F2.3a) — **sem função de fronteira/glue nova**; chave real viria de `keystore.getKey` só p/ provedores reais (coluna sem chave = "no-key", não chama a fronteira); prova usa "mock". Ação "Comparar (IA)" no `ReaderVersePanel`. Prova HEADLESS iPhone 17: `TLA_COMPARE ref="John 3:16" providers=2 cited_match=true first_provider="mock" cited_prefix="For God so loved the world..."` (N respostas, TODAS com o mesmo cited_text do store = anti-alucinação + wiring N-provedores), sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY/CHAT; `tsc`+`expo export web` verdes; the-light/`core/src/lib.rs`/`Cargo.toml` intactos (`c8ecb2f`). **Comparação de respostas DIFERENTES (provedores reais) = F3.10** (mock×mock é só wiring). | `ca7ad3f` |
| F3.6 | 2026-07-02 | **UI nativa de CONVERSA/follow-up ancorada (prova MOCK no device)**: `ReaderChatPanel` (molde ReaderAskPanel/ReaderStudyPanel) — conversa multi-turno sobre a passagem: mantém histórico `ChatTurn[]` (User/Assistant), cada follow-up chama `ask_session_anchored` (glue `reading.ts`→JSI; prova `"mock"`) com o MESMO book/chapter/verse; exibe o `cited_text` (âncora, verbatim do store, testID chat-cited-text, "Passagem (texto bíblico)") SEPARADO de cada `interpretation` (LLM, "IA — confira nas Escrituras"). Glue estende `askSessionAnchored` respeitando a ordem real dos args (lang antes de turns; studyMode/studyLens depois); web = stub (F3.12). Ação "Conversa (IA)" no `ReaderVersePanel`; bindings nativos regenerados. Prova HEADLESS iPhone 17: `TLA_CHAT ref="John 3:16" provider="mock" turns=3 cited_prefix="For God so loved the world..." interp_len=94`, sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY; `tsc`+`expo export web` verdes; the-light/`core/src/lib.rs`/`Cargo.toml` intactos (`c8ecb2f`). | `e39dad5` |
| F3.5 | 2026-07-02 | **UI nativa de ESTUDO PROFUNDO + léxico inline + atribuição STEP CC-BY (prova MOCK no device)**: `ReaderStudyPanel` (molde ReaderAskPanel) com seletores **modo × lente × profundidade** (StudyMode/StudyLens/StudyDepth) → `deep_study` (glue `reading.ts`→JSI; prova `"mock"`) exibindo `passage_text` ("Passagem (texto bíblico)", do STORE, testID study-passage-text) SEPARADO de `interpretation` ("Interpretação (IA) — confira nas Escrituras", testID study-interpretation) + `sections`/`citations`/`warnings`, mais **léxico Strong inline** (`lexical_entries`: strongs/lemma/translit/gloss) e a **atribuição STEP CC-BY VISÍVEL** (de `VerifiedLexiconOut.sources`, ADR-0026). Ação "Estudo (IA)" no `ReaderVersePanel`; web = stub (F3.12). **Léxico propagado ao subset** `reading-sample.sqlite` (gerador estendido via ATTACH, molde F1.9): `original_tokens=56268`/`lexicon=4534`/`scholarly_sources=4` (João 3:16=26 tokens), ~14,4 MB (ADR-0027). Bindings nativos regenerados. Prova HEADLESS iPhone 17: `TLA_STUDY ref="John 3:16" provider="mock" passage_prefix="For God so loved the world..." lexicon=21 attribution_ok=true`, sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK; `tsc`+`expo export web` verdes; the-light/`core/src/lib.rs`/`Cargo.toml` intactos (`c8ecb2f`). **Saneamento de artefato:** doc-comments `**puro**/` do core fechavam o JSDoc dos bindings gerados (562 erros tsc) → corrigido SÓ nos geradores (`**/`→`** /`) + bindings gerados-ignorados; core intocado; follow-up (corrigir doc-comments) em ADR-0027. | `04a7a61` |
| F3.4 | 2026-07-01 | **Fronteira de CONVERSA/REFINAMENTO (nativo) — prova MOCK**: 3 funções delegando ao core: `ask_session_anchored(db,translation,reference,turns,mode?,lens?,lang) -> Result<AiAnswer,CoreError>` (monta o `context` ancorado do STORE — capítulo numerado + rótulos de xref — e chama `ai::ask_session(&provider,lang,&context,&turns,study?)`; corpo `cfg(not wasm32)` + stub web); `refine_scope(...) -> Result<RefinementOut,CoreError>` (delega a `ai::refine_scope`; caminho **mock cfg-free** via `MockLlmProvider::default()` DIRETO — não `build_provider`; provider real sob `cfg(not wasm32)`); `parse_refinement(raw) -> RefinementOut` (**cfg-free**, puro/determinístico). Records `ChatTurn{role,content}`/`ChatRole`/`RefinementOut{question,options}`/`RefinementRound` (ai-pure, From cfg-free). **Correção:** `refine_scope` RECEBE provider — só `parse_refinement` é puro. `parse_refinement`+`refine_scope`-mock **rodam no wasm** (antecipam parte da paridade web F3.12). Anti-alucinação: `AiAnswer.cited_text` = passagem do store (âncora) × `interpretation` (LLM). Teste host **59/59** (7 `session_tests`): conversa mock (cited_text João 3:16 do store + interpretation==mock, multi-turn), anti-fake 3:16 vs 3:17, refine_scope mock, parse_refinement determinístico + vazio sem panic, error paths→CoreError. Delegação real; grafo wasm puro; the-light/`Cargo.toml` intactos (`c8ecb2f`); sem PR. | `851e97c` |
| F3.3 | 2026-07-01 | **Fronteira de ESTUDO PROFUNDO (nativo) → `ai::study::study` (prova MOCK)**: `deep_study(db,translation,reference,mode,lens,depth,provider,key?,model?,lang) -> Result<StudyResultOut,CoreError>` **delegando** a `ai::study::study(provider.as_ref(), &request)` — a fronteira MONTA o `StudyRequest` com fatos do STORE (`EmbeddedSource::passage` + `verified_lexicon` da F3.2 + `xref::passage_labels` + `format_reference`; `web_sources=[]`, `brief=None`) e converte `StudyResult`→`StudyResultOut`; provider via `build_provider` (na prova `"mock"`, sem chave/rede). Enums `StudyMode`/`StudyLens`(→Denomination)/`StudyDepth` (ai-pure, `From` cfg-free); Records `StudySection`/`StudyCitation` puros; `From<StudyResult>` gated `cfg(not wasm32)` (embedded-only). Corpo gated + stub web (estudo web=F3.12). **Anti-alucinação:** `StudyResultOut` separa `passage_text` (store, verbatim) da `interpretation` (LLM); `citations`/léxico de fontes locais; `warnings` do core. Teste host **52/52** (4 `study_tests`): passage_text=João 3:16 KJV do store + `interpretation`==resposta fixa do mock (não hardcoded); anti-fake 3:16 vs 3:17 (passage_text muda / interpretation invariante); error paths→CoreError sem panic. Delegação real (sem prompt/RAG/estudo reimplementado); grafo wasm puro; the-light/`Cargo.toml` intactos (`c8ecb2f`); sem PR. | `49919c3` |
| F3.2 | 2026-07-01 | **Fronteira de LÉXICO (nativo) → `verified_lexicon`**: `lexical_entries(db,book,chapter,verse?,lang,limit?) -> Result<VerifiedLexiconOut,CoreError>` **delegando** a `the_light_core::ai::lexicon::verified_lexicon(store.conn(), &reference, &[], lang, limit)` (via `Store::open`; `verse_numbers=&[]` deixa o core derivar do `Reference`; léxico é por book/chapter/verse, SEM `translation`). Records `LexEntry{strongs,lemma,translit,gloss,occurrences,testament}`/`VerifiedLexiconOut{entries,sources}` puros + `From` **cfg-free** (tipos-fonte são `ai-pure`, ao contrário de CrossRef/Note embedded-only). Corpo `cfg(not wasm32)` + stub web (léxico web = F3.12). Teste host **48/48** (5 novos `lexicon_tests`, fixture à mão): Gn 1:1 → Strong base `H0430` (de `H0430G`), `gloss="God"`, lemma אֱלֹהІם, `sources` com STEP Bible; verso sem cobertura → vazio sem panic; db inválido → CoreError; `limit` respeitado; bônus `bible.sqlite` real. Delegação real (sem SQL de léxico na fronteira); anti-alucinação (léxico do store verificado STEP CC-BY, nunca do LLM). Grafo wasm puro; the-light/`Cargo.toml` intactos (`c8ecb2f`). | `7f469d4` |
| F3.1 | 2026-07-01 | **Dados de LÉXICO STEP Bible (CC-BY 4.0) no `bible.sqlite` (Fase 3 abre)**: `scripts/gen-bible-db.sh` estendido roda, após `import`+`import-xref`, a 3ª etapa **`xtask import-scholarly`** (STEP Bible / STEPBible-Data) do rev pinado `c8ecb2f` SEM tocar o `the-light` (checkout do cargo + `CARGO_TARGET_DIR` fora + `--locked` + `--seed-dir` dedicado); REV do script alinhada `8f66004`→`c8ecb2f` (verificado `import.rs`/`xref_import.rs` byte-idênticos entre revs). Popula no MESMO `bible.sqlite` as tabelas `original_tokens`/`lexicon`/`scholarly_sources` (o `ai::lexicon::verified_lexicon` lê daí — **NÃO** é DB separado). Contagens LIDAS do banco: `original_tokens`=**447673** (tahot 305577 + tagnt 142096), `lexicon`=**22717** (tbesh 11682 + tbesg 11035), `scholarly_sources`=**4** (todas `cc-by`); Gênesis 1:1 = 7 tokens com Strong; `H0430G`→אֱלֹהִים "God", `G2316`→θεός "God". Atribuição CC-BY gravada em `scholarly_sources`: "Credit it to 'STEP Bible' linked to www.STEPBible.org (…; CC BY 4.0)" (exibição visível = F3.5). Idempotente (verses 62203 / xrefs 344799 intactos). Só dados livres (denylist do core recusa sblgnt/bdag/halot). `bible.sqlite` gerar-ignorado (~121 MB, ADR-0013); the-light intacto (`c8ecb2f`). ADR-0026. | `1e3892e` |
| F2.8 | 2026-07-01 | **🎯 MARCO 2 APROVADO (sign-off humano)** — IA BYOK ancorada, multiplataforma. Fase 2 (F2.1–F2.7b) aceita e verde: `ask` ancorado + streaming (nativo), GeminiProvider + `complete_stream` no core (2 PRs sancionados mergeados: `133077a`, `c8ecb2f`, cada um re-pinado), chave BYOK (Keychain nativo / session-only web), UI de `ask` (citado/interpretação), **validação REAL** (Gemini `gemini-2.5-flash`, `cited_text` = João 3:16 do store + interpretação real), IA no web (`ai-pure` wasm + `fetch`, **zero drift**). Regras não negociáveis mantidas: anti-alucinação (texto do store; LLM só interpreta; `citation` mesma impl Rust); BYOK (chave nunca em git/log; **OAuth de conta rejeitado** por pesquisa); offline-first (IA opt-in); `the-light` só via PR+ADR (wasm puro). ADRs 0023–0025. **Fase 3 (estudo profundo: léxico + pesquisa) LIBERADA.** `loop/HALT` removido. | _(sign-off)_ |
| F2.7b | 2026-07-01 | **Paridade WEB de IA (ai-pure wasm + fetch; zero drift)**: `ask` ancorado no browser — fronteira web nova em `core/src/lib.rs` (só `ai-pure`, corpo cfg-free, NÃO toca the-light/Cargo.toml): `ai_web_prepare(reference,question,provider,model?,lang,verses) -> AiWebRequest{cited_text,system,user,provider,model}` (cited_text via `ai::numbered_verses` sobre os versos do **store web** F1.13; system/user com **zero drift** via `CaptureProvider` + `ai::ask`, pois `ask_user_prompt` é privado) + `ai_web_finalize(...) -> AiAnswer` (`citation::rewrite_anchors`). Glue `app/web/ai-anchored.web.ts` faz o **transporte em TS** (`fetch` Gemini `generateContent`, chave só no header `x-goog-api-key`, injetável) — os `*_body`/`*_extract` do core são privados sob `ai-pure` (ADR-0025). `reading.web.ts` destuba `askAnchored` (web); `ReaderAskPanel` funciona no web. **Chave web session-only/in-memory** (`keystore.web.ts` = Map de módulo; nunca localStorage/sessionStorage/IndexedDB/git/log; ADR-0025). Prova HEADLESS node (fetch MOCK, sem rede/chave): `cited_text` = João 3:16 KJV verbatim do store, `interpretation` = resposta mock (âncora espúria removida por `rewrite_anchors`); **teste de host `ai_web_prepare_matches_native_anchored_cited_text_zero_drift`** prova paridade Rust nativo↔web. 43 testes; grafo wasm puro; tsc+expo export web+gen-bindings-web verdes; sem regressão nativa/web; the-light/`Cargo.toml` intactos (`c8ecb2f`). ADR-0025. | `744868d` |
| F2.7 | 2026-07-01 | **PR ao `the-light-core` — feature `ai-pure` (IA pura no wasm, D2) + fix `default_model` gemini** *(PR sancionado, ADR-0023/0024)*: implementado no branch `feat/ai-pure-wasm`, **revisado** (portão D2 verde; não-quebrante; workspace core 184 testes) e **mergeado pelo humano** na `main` do `the-light` → rev **`c8ecb2f`**. Feature fina `ai-pure` (opt-in; `embedded ⊃ ai-pure` → default byte-a-byte intacto) faz as partes PURAS do `ai` (prompt/RAG + `ask`/`ask_context`/`numbered_passage` + `citation::rewrite_anchors` anti-alucinação + `default_model`/`estimate_cost_usd` + `*_body`/`*_extract`) compilarem em `wasm32`; `research`/`keys`/reqwest/rusqlite/deep-study gated por `embedded`. Fix `default_model` gemini `2.0-flash`(retirado 3/mar)→`2.5-flash`. Driver **re-pinou** `core/Cargo.toml`+`Cargo.lock` (133077a→`c8ecb2f`) e **ligou `features=["ai-pure"]` na linha WEB** (default-features=false): 41 testes fronteira verdes, **grafo wasm ainda puro** (só entrou `serde_json`; sem rusqlite/reqwest), `gen-bindings-web` exit 0, fmt/clippy limpos. Habilita F2.7b (UI web de IA via `fetch`, prompt/citação puros em Rust — zero drift nativo↔web). | `c8ecb2f`† |
| F2.6 | 2026-07-01 | **🎯 Validação REAL do `ask` com a chave BYOK (VALIDADA por sign-off humano)**: o humano rodou `core/examples/ask_real.rs` (harness que lê a chave de **env var**, **nunca a imprime/commita**) → resposta **REAL** do provedor (`provider=gemini`, `model=gemini-2.5-flash`), **`cited_text` = João 3:16 KJV verbatim do STORE** (anti-alucinação provada na prática: texto bíblico do banco local, LLM só interpreta) + interpretação genuína do Gemini. Chave nunca exibida/logada. **Cadeia BYOK provada ponta a ponta.** Achado (1ª tentativa 429): o default `gemini-2.0-flash` da F2.3 foi **retirado (3/mar/2026)** → TODO: PR ao core mudando `default_model`→`gemini-2.5-flash` (batch com F2.7/D2). Rede só nesta validação opt-in; the-light `133077a`. | _(sign-off)_ |
| F2.5 | 2026-07-01 | **UI nativa de `ask` ancorado + streaming + anti-alucinação VISÍVEL (prova MOCK)**: ação "Perguntar (IA)" no `ReaderVersePanel` abre `ReaderAskPanel` (tokens de tema) com seletor de provedor/modelo (`SUPPORTED_PROVIDERS`/`listProviders` do keystore + `"mock"`), campo de pergunta, **streaming token-a-token** e **separação clara**: bloco "Passagem (texto bíblico)" (`citedText` verbatim do store) × "Interpretação (IA) — confira nas Escrituras" (`interpretation`/tokens do LLM). Glue `app/web/reading.ts` estende `askAnchored`/`askAnchoredStream` (+ wiring `AiTokenCallback`→callback JS) → JSI → fronteira F2.1/F2.3a; `reading.web.ts`=stub (IA web = F2.7). Chave via `keystore.getKey(provider)` só p/ provedor real; **prova usa "mock" (sem chave/rede)**; chave nunca logada. Bindings nativos regenerados. Prova HEADLESS iPhone 17: `TLA_ASK ref="John 3:16" provider="mock" streamed=true cited_prefix="For God so loved the world..." interp_len=94` (streamed=true de ≥1 token real; cited_prefix bate João 3:16 KJV do store), sem regressão READ/PARALLEL/SEARCH/XREF/NOTES; `tsc`+`expo export web` verdes; the-light/core intactos (`133077a`). Custo simplificado (fronteira não expõe `estimate_cost_usd`). Validação real com a chave = F2.6. | `31d191f` |
| F2.3a | 2026-07-01 | **Fronteira consome o re-pin: gemini + streaming (callback UniFFI)**: callback `#[uniffi::export(callback_interface)] AiTokenCallback{on_token(String)}` + `ask_anchored_stream(db,translation,reference,question,provider_name,key:Option,model:Option,lang, on_token: Box<dyn AiTokenCallback>) -> Result<AiAnswer,CoreError>` — monta o contexto ancorado pela MESMA rota da `ask_anchored` (store→`numbered_passage`→`ask_context`) e chama `provider.complete_stream(context, question, &mut sink)` (default não-quebrante do core emite a resposta 1×); corpo `cfg(not wasm32)` + stub web (streaming web = F2.7). Roteamento **gemini** via `build_provider` (core `133077a`). Anti-alucinação: `cited_text` do store, tokens da interpretação (mock). Teste host **41/41** (4 novos): streaming (tokens==resposta mock, `cited_text` KJV verbatim), anti-fake 3:16 vs 3:17 (cited_text muda/tokens invariantes), `gemini` sem chave → `CoreError` (contém "gemini", **sem rede**), provider desconhecido → `CoreError`. Grafo wasm puro (sem rusqlite/reqwest); `gen-bindings-web` 0 (`askAnchoredStream`/`AiTokenCallback` stub web); the-light intacto (`133077a`), `core/Cargo.toml` não tocado. Nota: core não expõe `ask_stream` público → fronteira compõe system=`ask_context`/user=question (paridade fina de prompt real = F2.6). | `b375f47` |
| F2.3 | 2026-07-01 | **PR ao `the-light-core` — Gemini (D1) + streaming no trait (D4)** *(PR sancionado, ADR-0023)*: implementado no branch `feat/ai-gemini-streaming`, **revisado** (workspace 184 testes, 7 novos; fmt/clippy limpos; não-quebrante) e **mergeado pelo humano** na `main` do `the-light` → rev **`133077a`**. `GeminiProvider: LlmProvider` registrado (`PROVIDERS`/`default_model=gemini-2.0-flash`/`build_provider` exige key/`estimate_cost_usd`); `gemini_body`/`gemini_extract` puros e testados; POST a `generativelanguage.googleapis.com`, key em header `x-goog-api-key` (nunca logada/na URL). `LlmProvider::complete_stream` com **default não-quebrante** (SSE real por provedor = evolução). Driver **re-pinou** `core/Cargo.toml`+`Cargo.lock` (8f66004→`133077a`) nos 2 blocos (web puro + nativo embedded): 37 testes da fronteira verdes, **grafo wasm ainda puro** (sem rusqlite/reqwest do novo `ai`), fmt/clippy limpos. D2 (wasm-decouple p/ IA web) = PR focado separado antes da F2.7. | `133077a`† |
| F2.4 | 2026-07-01 | **BYOK — chave nativa segura (`expo-secure-store`) + stub web**: serviço `app/lib/keystore.ts` (nativo) grava/lê/apaga a **API key por provedor** (anthropic/openai/gemini/ollama) no **Keychain/Keystore** (ids `tla.apikey.<provider>`, valor nunca impresso); `createKeystore(backend)` com backend injetável; `listProviders()` devolve só NOMES; import lazy; trim/rejeita vazio. `keystore.web.ts` = **stub sem chave no web** (D3/ADR-0023; molde `db.web.ts`). Implementa **D3** (BYOK API key indolor; login-de-conta OAuth rejeitado). UI de colar/deep-link fica na F2.5. Prova HEADLESS (backend fake em memória, **sem chave real**): round-trip por provedor, provedores isolados, validação; **chave NUNCA logada** (espião de console = 0; fonte sem `console.*`) **nem versionada** (`git grep` do placeholder só no teste). `tsc`+`expo export web` verdes (stub não puxa secure-store ao bundle); the-light/core intactos (`8f66004`, diff vazio; commit só toca `app/**`). App-side puro, não-bloqueante. A chave só é USADA com rede na F2.6. | `34c831f` |
| F2.2 | 2026-07-01 | **🚦 GATE estratégico — arquitetura da IA (DECIDIDO por sign-off humano)**: **D1** Gemini via **PR ao `the-light-core`** (`GeminiProvider: LlmProvider`; fonte única, precedente ADR-0005) · **D2** IA no web via **PR ao core** desacoplando `ai::study`/`citation` de `research::WebSource`/`reqwest` p/ compilar em wasm (anti-alucinação numa só impl Rust; chamada HTTP via `fetch`, ADR-0011) · **D3** **BYOK API key indolor** (Keychain via `expo-secure-store`, 1×, nunca em git/log) — **login-de-conta OAuth REJEITADO** após pesquisa (2026: OpenAI "Sign in" só identidade; Anthropic e Google **baniram** tokens OAuth de consumidor em apps de terceiros, com risco de banir a conta do usuário) · **D4** **streaming** (tokens incrementais). Re-escopa F2.3 (Gemini no core), F2.4 (chave nativa), F2.7 (IA web). **ADR-0023.** `loop/HALT` removido. | _(sign-off)_ |
| F2.1 | 2026-07-01 | **IA — pergunta ancorada (`ask`) na FRONTEIRA + MOCK (Fase 2 abre)**: Record `AiAnswer{reference,cited_text,interpretation,provider,model}` (puro, todos os alvos) + `ask_anchored(db,translation,reference,question,provider_name,key:Option<String>,model:Option<String>,lang) -> Result<AiAnswer,CoreError>` **delegando** a `the_light_core::ai::{numbered_passage,ask_context,build_provider,ask}` (o core já tem o módulo `ai`, `embedded`-only) + leitura da passagem via `Store::open`/`EmbeddedSource::passage` (rota F1.2); corpo `cfg(not wasm32)` + stub web (IA/rede fora do grafo wasm). Provider `"mock"` (`key=None`, **sem rede/sem chave**). **Anti-alucinação (contrato):** `AiAnswer` separa `cited_text` (verbatim do store) da `interpretation` (LLM). Teste host **37/37** (3 novos `ai_tests`): `cited_text` = KJV verbatim do store; prova anti-fake — 3:16 vs 3:17 o `cited_text` MUDA (segue o store) mas a `interpretation` do mock é INVARIANTE; error paths (ref inválida/tradução/provider) → `CoreError` sem panic. Grafo wasm puro (`cargo tree wasm` sem rusqlite/reqwest); `gen-bindings-web` 0 (`askAnchored`/`AiAnswer` stub web); the-light/core intactos (`8f66004`), `Cargo.toml` inalterado. Gemini/IA-web/chave = gate F2.2. | `357abca` |
| F1.17 | 2026-07-01 | **🎯 MARCO 1 APROVADO (sign-off humano)** — leitura offline completa, multiplataforma. Todas as 16 tarefas da Fase 1 (F1.1–F1.16) aceitas e verdes nos **3 alvos** (iOS + Android + Web): leitura (nav + versões + lado a lado + tema), busca FTS5, referências cruzadas (CC-BY OpenBible visível) e notas/highlights + export — com provas determinísticas (`TLA_*` no device / headless node no web) e review independente por tarefa. Regras não negociáveis mantidas: `the-light` intocado (`8f66004`); offline-first (assets locais, zero rede, sem SharedArrayBuffer); anti-alucinação (texto domínio público do store; xref CC-BY com atribuição); BYOK (sem segredos). ADRs 0013–0022. **Fase 2 (IA BYOK + Gemini) LIBERADA**; corpus completo (~59 MB) permanece backlog transversal. `loop/HALT` removido. | _(sign-off)_ |
| F1.16 | 2026-07-01 | **Paridade WEB de notas/highlights + export (Opção A/A1)**: as 7 funções de userdata (`putNote`/`getNote`/`deleteNote`/`listNotes` + `addHighlight`/`removeHighlight`/`listHighlights`) DESTUBADAS em `app/web/reading.web.ts`, persistindo em **OPFS** num `data_dir` web separado do conteúdo só-leitura. Como o módulo `userdata` do core é `embedded`-only (não está no wasm), o web **reimplementa o I/O como INFRA** (precedente ADR-0011), **espelhando o formato do core** byte-a-byte: slug `notes/<slug>.md` (`John 3:16`→`John_3.16.md`, Range→`Genesis_1.1-3.md`, WholeChapter→`Psalms_23.md`), `.md` só-corpo, `highlights.json` array `{ref,color,tag?}` pretty 2sp (tag omitido), `list_notes` ordenado por ref canônica / `list_highlights` por inserção (add substitui mesma ref). Referência canonicalizada pelo **wasm** (`parseReference` + `listBooks().nameEn`) nas duas direções. Novos glues `app/web/userdata-fs.web.ts` (VFS-agnóstico) + `userdata-opfs.web.ts` (backend OPFS); `userdata.web.ts` retorna sentinela `'web:userdata'` (UI `ReaderVersePanel` funciona no web). EXPORT reusa `buildNotesExport` (idêntico ao nativo). Prova HEADLESS node (OPFS-like em memória): `WEB_NOTES note_ref="John 3:16" note_len=36 highlights=1 persisted=true export_ok=true` (persistência relida de novo handle; slug == nativo F1.11). Leitura/busca/xref web sem regressão; `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). ADR-0022. **Última paridade web — Fase 1 funcionalmente completa nos 3 alvos.** | `7557f0e` |
| F1.15 | 2026-07-01 | **Paridade WEB de xref + atribuição CC-BY (Opção A/A1)**: `crossRefs(book,chapter,verse,minVotes?,limit?)` em `app/web/reading.web.ts` DESTUBADO espelhando o SELECT de `xref::for_verse` (`SELECT to_book,to_chapter,to_verse_start,to_verse_end,votes FROM cross_references WHERE from_*=? AND votes>=? ORDER BY votes DESC, to_book, to_chapter, to_verse_start LIMIT ?`; clamp≥1; Single se `to_verse_start>=to_verse_end` senão Range; xref independe de tradução), sobre o subset, **reusando** `openReadingDbWeb` das F1.13/F1.14 (sem recarregar). Novo glue `app/web/sqlite-xref.web.ts`. Defaults minVotes=1/limit=20 (= core/F1.8); `votes` i64→bigint. Telas `ReaderXrefPanel`/`ReaderVersePanel` funcionam no web; **atribuição CC-BY** EXATA "Cross references courtesy of OpenBible.info (CC-BY)" (ADR-0016) confirmada no bundle. Prova HEADLESS node: `crossRefs(43,3,16)` → 9 xrefs, **João 3:15 1º por votos (439, Single)** = paridade c/ `TLA_XREF` nativo; Range provado (João 11:25-26, 400); verso sem xref→[]; minVotes/limit respeitados. Sem ordenação/filtro reimplementado em TS (só espelho do SQL); leitura/busca web sem regressão; `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). ADR-0021. | `1fe14e6` |
| F1.14 | 2026-07-01 | **Paridade WEB de busca FTS5 (Opção A/A1)**: `search` em `app/web/reading.web.ts` DESTUBADO espelhando o SQL FTS5 do core (`SELECT … highlight(verses_fts,0,?,?) AS hl, bm25(verses_fts) AS score FROM verses_fts JOIN verses … WHERE verses_fts MATCH ? AND translation_id=? [AND book_number=?] ORDER BY score LIMIT ?`; `has_translation` antes → UnknownTranslation; `build_match_query` split→aspas→AND), sobre o subset, **reusando** o store da F1.13 (sem recarregar). Novo glue `app/web/sqlite-search.web.ts`. **RISCO FTS5 resolvido (ADR-0020):** o `wa-sqlite@1.0.0` do npm NÃO traz FTS5 (`no such module: fts5`, 0 strings) → rebuild SYNC com `-DSQLITE_ENABLE_FTS5` da fonte (docker/emscripten, pinado no gitHead `5147454` = release npm 1.0.0) → vendorizado em `app/web/vendor/wa-sqlite-fts5/wa-sqlite.{mjs,wasm}` (asset local VERSIONADO, offline-first, sem SharedArrayBuffer); rebuild reprodutível via `scripts/build-wa-sqlite-fts5.sh`. Prova HEADLESS node: `search("God","kjv",-,1000)` → 646 hits, **João 3:16 localizado** (43/3/16, bm25 score −2.389, KJV verbatim, markers U+0002/U+0003 só em `highlighted`, não em `text`); acento-insensível `ceus`/`alm1911` → 20 hits c/ "céus"; vazio→[]; limit/book/tradução-inexistente→erro. Telas de busca (F1.6) funcionam no web; ref clicável → Reader. Leitura web sem regressão; sem ranqueamento reimplementado (bm25/highlight do FTS5); `tsc`+`expo export web` verdes; the-light/core/nativo intactos (`8f66004`). | `b2f1d8f` |
| F1.13 | 2026-07-01 | **Paridade WEB de leitura (Opção A/A1)**: `app/web/reading.web.ts` DESTUBADO p/ leitura REAL sobre o subset `reading-sample.sqlite` (~4,4 MB, o MESMO do nativo) via `wa-sqlite` (build SYNC) OPFS no browser + MemoryVFS (molde F0.10). Novos `sqlite-reading.web.ts` (queries VFS-agnósticas) + `sqlite-reading-opfs.web.ts` (backend OPFS). Funções espelham os SELECTs do core (`getChapter`→`SELECT verse,text FROM verses … ORDER BY verse` + `has_translation`→UnknownTranslation; `chapterCount`→`max(chapter)`; `listTranslations`→`FROM translations ORDER BY language,id`); `listBooks` vem do **wasm/Rust** (`reference::BOOKS`, 66), não reescrito. As mesmas telas `read/**` (incl. lado a lado da F1.4) funcionam no browser via gate `useWasmReady` (no-op no nativo). Prova HEADLESS node (MemoryVFS): João 3:16 KJV+Almeida **verbatim** do store, `chapterCount=21`, João 3 KJV=36 vv, traduções [kjv,alm1911], 66 livros — **paridade** com `TLA_READ`/`TLA_PARALLEL`. `tsc`+`expo export web` verdes (subset 4.567.040 B + `wa-sqlite.wasm` 558.343 B no bundle). Sem domínio reimplementado; sem regressão nativa (diff nativo vazio); the-light/core intactos (`8f66004`). Busca/xref/notas web = F1.14–F1.16. ADR-0019. | `0479b5b` |
| F1.12 | 2026-06-30 | **🚦 GATE estratégico — store WEB do corpus (DECIDIDO por sign-off humano)**: escolhida a **Opção A** (`wa-sqlite` + OPFS espelhando os SELECTs do core em TS; reaproveita F0.10 + semântica FTS5/xref do SQLite; zero PR ao the-light), começando por **A1 = paridade com o subset `reading-sample.sqlite` (~4,4 MB)** via o MemoryVFS já provado. Corpus completo (~59 MB; 62.203 versículos, FTS5, 344.799 xrefs) + VFS-live em Worker/cota OPFS = decisão transversal separada (não bloqueia a paridade). Habilita F1.13–F1.16. **ADR-0018.** `loop/HALT` removido (motivo resolvido). | _(sign-off)_ |
| F1.11 | 2026-06-30 | **UI de notas/highlights NATIVA + export + persistência**: painel combinado `ReaderVersePanel` (nota markdown + highlight cor/tag + export + xref) aberto pelo gesto de seleção de versículo da F1.9; criar/editar/remover nota e marcar/desmarcar highlight com indicadores no Reader. Tudo lê/grava **pela fronteira F1.10** (glue `app/web/reading.ts` estendido: `putNote`/`getNote`/`deleteNote`/`listNotes`/`addHighlight`/`removeHighlight`/`listHighlights` → JSI → Rust); SEM reimplementar I/O/serialização/ordenação de userdata em TS. **Persistência** em `${documentDirectory}userdata/` (`app/lib/userdata.ts`), separado do `reading-sample.sqlite` só-leitura; sobrevive a restart (provado por 2ª leitura). **Export** = apresentação pura dos Records de `list_notes`/`list_highlights` (`app/lib/notesExport.ts`) via `Share` do react-native (sem `expo-sharing`). Atribuição CC-BY (ADR-0016) preservada no painel. Bindings nativos regenerados; web = stub (notas web = F1.16). Prova HEADLESS iPhone 17: `TLA_NOTES note_ref="John 3:16" note_len=55 highlights=1 persisted=true export_ok=true` (do retorno real), sem regressão READ/PARALLEL/SEARCH/XREF/parse; `tsc`+`expo export web` verdes; the-light intacto (`8f66004`), `core/src/lib.rs`+`Cargo.toml` não tocados. ADR-0017. | `deb3b0c` |
| F1.10 | 2026-06-30 | **Notas/marcações na FRONTEIRA (userdata file-based)**: 7 funções (`put_note`/`get_note`/`delete_note`/`list_notes` + `add_highlight`/`remove_highlight`/`list_highlights`) **delegando** a `the_light_core::userdata::{NoteStore,HighlightStore}` (situação A — o core já entrega a camada file-based); recebem um `data_dir` gravável (deriva `notes/*.md` + `highlights.json`; **NUNCA** o `bible.sqlite` só-leitura) e a referência como String canonicalizada por `parse_reference`. Records `Note{reference,body}`/`Highlight{reference,color,tag}` puros + `From<userdata::*>` gated `cfg(not wasm32)` (tipos-fonte `embedded`-only, molde F1.8); corpo gated + stub web. Teste host **34/34** (8 novos `userdata_tests` num diretório temporário, **relendo de OUTRO handle** = persistência em disco): round-trip (`John_3.16.md`), PT==EN mesma nota, lista ordenada por ref canônica, delete/remove idempotentes, vazio sem panic, ref inválida → `CoreError` antes do I/O. Web puro (`cargo tree wasm` sem rusqlite); `core/Cargo.toml` inalterado. UI/persistência device/export = F1.11; web = F1.16. | `b6381d0` |
| F1.9 | 2026-06-30 | **UI de xref NATIVA + atribuição CC-BY**: ao selecionar um versículo no Reader (`read/[book]/[chapter]`), `ReaderXrefPanel` lista as referências cruzadas vindas da fronteira `crossRefs` (F1.8→glue `app/web/reading.ts`→JSI→`xref::for_verse`, já ordenado por votos DESC) — cada item = referência+votos, clicável → `router.push` p/ `read/[book]/[chapter]` (molde F1.6); SEM SQL/ordenação/filtro de xref em TS (só apresenta o `Vec<CrossRef>`). Atribuição CC-BY EXATA "Cross references courtesy of OpenBible.info (CC-BY)" visível (ADR-0016). Subset bundled: gerador `gen_reading_sample_db.rs` propaga `cross_references` do `bible.sqlite` (ambos os lados em Gn/Sl/Jo) → `reading-sample.sqlite` com **22413** xrefs (João 3:16 → 9). Bindings nativos regenerados (`crossRefs`/`CrossRef`; `votes: i64`→`bigint` formatado via `String()`). Web = stub (xref web = F1.15). Prova HEADLESS iPhone 17: `TLA_XREF verse="John 3:16" count=9 first_ref="John 3:15" first_votes=439` (do retorno real de `cross_refs`), sem regressão READ/PARALLEL/SEARCH/parse; `tsc`+`expo export web` verdes; the-light intacto (`8f66004`), `core/src/lib.rs` não tocado. | `2ff73c5` |
| F1.8 | 2026-06-30 | **Xref na fronteira**: `cross_refs(db,book,chapter,verse,min_votes:Option<i64>,limit:Option<u32>) -> Vec<CrossRef>` delegando a `the_light_core::xref::for_verse(store.conn(),…)`; Record `CrossRef{reference,votes:i64}` via `From<xref::CrossRef>` sob `cfg(not wasm32)` (tipo-fonte `embedded`-only); corpo gated nativo + stub web. Teste host 26/26 (5 novos `xref_tests`, fixture `cross_references` à mão): Rm 5:8 (book 45/cap 5/`Single 8`, 50 votos) em [0] por votos DESC; `Range 8-9`; `min_votes=None` oculta disputada (-5) e `Some(-100)` inclui; `limit`; verso sem xref → vazio sem panic. Web puro (`cargo tree wasm` sem rusqlite); sem SQL/ranking reimplementado. Atribuição CC-BY exibida na F1.9. | `36b2cb0` |
| F1.7 | 2026-06-30 | **Referências cruzadas (DADOS)**: `scripts/gen-bible-db.sh` estendido roda, APÓS o `import` (verses+FTS), o subcomando DEDICADO `xtask import-xref` (≠ `import --xref`) canônico do the-light (rev pinado `8f66004`) SEM tocá-lo (checkout do cargo + `CARGO_TARGET_DIR` fora + `--locked`) → popula `cross_references` no MESMO `assets/data/bible.sqlite` com OpenBible.info/TSK (CC-BY; XREF_URL mirror raw scrollmapper, baixado 8.293.834 bytes). Contagem validada LENDO do banco: **344799** (guarda do xtask aborta <300k); João 3:16 (`from_book=43,ch=3,v=16`)→**23** xrefs; votos negativos preservados (1166); verses intactos (62203). Idempotente (2× estável; DELETE+reinsert). Banco ~51 MB (gerar-ignorado, ADR-0013 mantém-se; `.gitignore` inalterado). Atribuição CC-BY obrigatória: "Cross references courtesy of OpenBible.info (CC-BY)" (exibição visível = F1.9). ADR-0016. | `4d2d3bf` |

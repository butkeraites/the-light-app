---
id: F5.3
title: Tracer: web-bundle baseline metric + defer the 4MB frontier wasm off first paint
phase: 5
status: ready
gate: false
depends_on: []
attempts: 0
created_by: planner
created_at: 2026-07-02
---

## Objetivo

Establish the objective, repeatable performance metric for the web target AND land the smallest end-to-end deferral: stop blocking the whole-app render on the ~4.02MB frontier wasm. Deliver a measurement script that exports the web bundle and emits a machine-readable budget (per-asset + eager-JS + total + gzip sizes) with the recorded baseline, then change app/app/_layout.tsx so the app shell renders immediately and the frontier wasm warms in the background, gating ONLY the reading routes that call the synchronous listBooks().

## Contexto / arquivos

_Workstream:_ Performance web

- `/Users/butkeraites/Documents/the-light-app/app/app/_layout.tsx`
- `/Users/butkeraites/Documents/the-light-app/app/web/wasm.web.ts`
- `/Users/butkeraites/Documents/the-light-app/app/web/wasm.ts`
- `/Users/butkeraites/Documents/the-light-app/app/web/generated/index.web.ts`
- `/Users/butkeraites/Documents/the-light-app/app/app/index.tsx`
- `/Users/butkeraites/Documents/the-light-app/app/app/read`
- `/Users/butkeraites/Documents/the-light-app/scripts/gen-bindings-web.sh`
- `/Users/butkeraites/Documents/the-light-app/app/package.json`

## Passos sugeridos

1. Add scripts/measure-web-bundle.sh: runs `cd app && npx expo export --platform web`, then parses app/dist into loop/perf/web-bundle-baseline.json — fields: entryJsBytes (sum of _expo/static/js/web/*.js), frontierWasmBytes (assets/web/generated/wasm-bindgen/index_bg.*.wasm), readingDbBytes (assets/_assets/data/reading-sample.*.sqlite), waSqliteBytes (per file), totalBytes, and gzipBytes per asset (gzip -9 -c | wc -c). Deterministic ordering; no network.
2. Run it once and commit loop/perf/web-bundle-baseline.json with the measured numbers (entry ~1,425,848; frontier wasm ~4,212,193; reading DB ~14,409,728; FTS5 wasm ~666,267; npm wa-sqlite ~558,343; sample ~131,072).
3. Refactor the render gate: in app/app/_layout.tsx, remove the top-level `if (!wasmReady) return <ActivityIndicator/>` so RootNavigator renders the Stack immediately; keep ensureWasmReady() warming in the background (call it once on mount, non-blocking).
4. Move the wasm-ready gate down to the reading routes that call synchronous listBooks() (e.g. read/index and read/[book]) — each shows a lightweight spinner via useWasmReady() until the frontier wasm is initialized, so those screens still call listBooks() safely.
5. Ensure the home index.tsx demo (parseReference/getPassage) tolerates wasm-not-ready (its calls are already async/effect-driven; add a ready guard if needed so first paint never throws).
6. Add a deterministic proof that the shell no longer awaits wasm: a react-test-renderer unit (add react-test-renderer as a devDep) that mounts RootLayout with ensureWasmReady stubbed to a never-resolving promise and asserts the Stack/children are present synchronously; wire it as app/package.json script test:web:firstpaint.

## Critério de aceite

- [ ] scripts/measure-web-bundle.sh runs headlessly and writes loop/perf/web-bundle-baseline.json with all fields populated (raw + gzip), reproducibly.
- [ ] app/app/_layout.tsx renders the app shell without awaiting ensureWasmReady() (no global blocking spinner); the frontier wasm warms in the background.
- [ ] Reading routes still call listBooks() only after the frontier wasm is ready (per-route gate), and reading works end-to-end.
- [ ] The new first-paint test proves children mount while ensureWasmReady() is unresolved.
- [ ] All 15 existing web self-tests + tsc --noEmit + expo export --platform web stay green (no regression); no the-light change (2fc2dab intact).
- [ ] Baseline JSON is checked into loop/perf/ as the metric of record for subsequent tasks.

## Comando(s) de verificação

```sh
cd /Users/butkeraites/Documents/the-light-app && ./scripts/measure-web-bundle.sh && cat loop/perf/web-bundle-baseline.json
cd /Users/butkeraites/Documents/the-light-app/app && npx tsc --noEmit
cd /Users/butkeraites/Documents/the-light-app/app && npx expo export --platform web
cd /Users/butkeraites/Documents/the-light-app/app && npm run test:web:firstpaint && for t in reading search xref notes ai ai-stream ai-multiprovider study lexicon export session research research-tavily compare; do npm run test:web:$t || exit 1; done
```

## Restrições

TRACER BULLET — must be first. Metric = the bundle-budget JSON (fully deterministic/headless) + a structural deferral proof. The frontier wasm is the single biggest first-paint cost (4.02MB) and it is on the render-blocking path today; deferring it is the highest-value smallest slice. Offline-first preserved (wasm is a local asset, just no longer render-blocking). Do NOT regress reading (listBooks is synchronous and needs wasm — keep the per-route gate). No eslint (not configured).

- **Offline-first:** capacidade essencial nunca exige rede/conta; recurso novo liga OFF/local por padrão.
- **BYOK/segredos:** chaves/tokens nunca em git/log; secure-store nativo / session-only web.
- **Anti-alucinação:** texto de versículo sempre do store local (Rust); i18n NUNCA traduz versículo; atribuições CC-BY (OpenBible/STEP) VISÍVEIS e verbatim.
- **the-light só via PR+ADR:** consumir `the-light-core` pinado (`2fc2dab`); nenhuma mudança no core nesta tarefa (senão vira PR+ADR gate à parte).
- **Qualidade:** Rust fmt/clippy -D warnings/test; TS `tsc --noEmit`; grafo wasm puro; prova determinística/headless (sem chave/rede real no loop).

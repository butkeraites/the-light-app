---
id: F5.8
title: i18n da busca + componentes de navegação (search screen, SearchResultItem, BookList, ChapterGrid, ChapterView)
phase: 5
status: ready
gate: false
depends_on: [F5.2]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-03
---

## Objetivo

Migrar para t() as strings da tela de busca e dos componentes de navegação de leitura, com labels de acessibilidade nos itens clicáveis, sem tocar a lógica de busca/store.

> **Herda F5.2/F5.5 (aceitas):** camada i18n (`useI18n`/`t()`, catálogos PT/EN) + o padrão de
> migração de tela (F5.5 migrou o fluxo de leitura). Esta tarefa migra a BUSCA + componentes de
> navegação (search, SearchResultItem, BookList, ChapterGrid, ChapterView). REUSA a infra; adiciona
> chaves aos dois catálogos. NÃO traduzir texto de resultado/versículo/nome de livro (vem do store;
> `locale` só seleciona campo). Escopo: busca + nav; painéis de IA = F5.11.

## Contexto / arquivos

_Workstream:_ Acessibilidade + i18n + temas · _Depende de:_ F5.2 (aceita)

- `app/lib/i18n.ts`
- `app/app/search/index.tsx`
- `app/components/ReaderSearchResultItem.tsx`
- `app/components/ReaderBookList.tsx`
- `app/components/ReaderChapterGrid.tsx`
- `app/components/ReaderChapterView.tsx`

## Passos sugeridos

1. app/app/search/index.tsx: migrar placeholder ('Buscar na Bíblia (ex.: ...)'), hints ('Digite um termo...','Nenhum resultado para “…”'), mensagens de erro → t(); accessibilityLabel no TextInput e nos itens.
2. ReaderSearchResultItem.tsx, ReaderBookList.tsx, ReaderChapterGrid.tsx, ReaderChapterView.tsx: migrar rótulos visíveis (ex.: 'Book N' fallback, rótulos de capítulo) e adicionar accessibilityRole='button'/accessibilityLabel (com a referência) nos elementos tocáveis.
3. Manter a referência/snippet verbatim do store (a busca não é reimplementada; i18n só no cromo).
4. Gates: tsc/eslint/expo export web verdes.

## Critério de aceite

- [ ] Strings da tela de busca e dos componentes de navegação via t(); alternam PT⇄EN.
- [ ] Itens de resultado e células de navegação têm accessibilityRole+Label (com a referência) e alvo de toque adequado.
- [ ] Nenhuma string PT hardcoded remanescente nesses arquivos; snippet/referência do store inalterados.
- [ ] tsc 0, eslint 0, expo export web exit 0; core intacto.

## Comando(s) de verificação

```sh
set -e; cd /Users/butkeraites/Documents/the-light-app/app
npx tsc --noEmit && npx eslint . && npx expo export --platform web
```

## Restrições

Pode rodar em paralelo com i18n-2/i18n-4 (arquivos disjuntos). Anti-alucinação: texto de resultado do store, nunca traduzido.

- **Offline-first:** i18n/prefs 100% local (reusa F5.2); nada exige rede.
- **Anti-alucinação:** i18n só CHROME — resultados de busca mostram texto/refs do STORE verbatim; nomes de livro do store (`locale` só seleciona campo); CC-BY intacto; NUNCA traduzir versículo/ref via `t()`.
- **the-light só via PR+ADR:** `the-light-core` pinado (`2fc2dab`); app-side.
- **Qualidade:** `tsc --noEmit`; `test:web:search` + demais sem regressão; prova headless da troca de idioma nos componentes migrados; paridade de catálogo pt/en em compile-time.

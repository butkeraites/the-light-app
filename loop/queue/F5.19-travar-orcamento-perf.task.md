---
id: F5.19
title: Lock the web performance budget as a regression guard (workstream exit)
phase: 5
status: ready
gate: false
depends_on: [F5.6, F5.9, F5.12, F5.15, F5.17]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-03
---

## Objetivo

Turn the metric into a gate that keeps future work honest: derive concrete thresholds from the improved numbers (first-paint transfer bytes, entry JS, frontier wasm, reading-path DB, total) and add a checker that fails when any budget is exceeded, wired so it runs alongside the web self-tests. This is the objective 'green' definition for the whole workstream.

> **SAÍDA do workstream perf** (após F5.3 métrica, F5.6 release, F5.9 code-split, F5.12 dead
> assets, F5.15 léxico on-demand, F5.17 precompress). `measure-web-bundle.sh` já falha em
> BUDGET breach. Esta tarefa TRAVA o orçamento como GUARDA OFICIAL de regressão: consolida os
> limites finais (entry moduleCount/bytes, first-paint transfer, assets estáveis) num gate wired
> (ex.: `test:web:perf-budget` ou doc de CI), prova que a guarda FALHA se o bundle inchar, e
> documenta o orçamento. _Blocking:_ **sim** (SAÍDA/def. de 'verde' do workstream perf). NÃO tocar o app (só formalizar a guarda).

## Contexto / arquivos

_Workstream:_ Performance web · _Depende de:_ F5.6, F5.9, F5.12, F5.15, F5.17 (aceitas) · _Blocking:_ **sim** (SAÍDA/def. de 'verde' do workstream perf)

- `/Users/butkeraites/Documents/the-light-app/scripts/measure-web-bundle.sh`
- `/Users/butkeraites/Documents/the-light-app/loop/perf/web-bundle-baseline.json`
- `/Users/butkeraites/Documents/the-light-app/app/package.json`
- `scripts/measure-web-bundle.sh`, `loop/perf/web-bundle-baseline.json`, `loop/perf/SERVING.md`

## Passos sugeridos

1. Create loop/perf/web-bundle-budget.json with thresholds set just above the post-optimization measured values (frontier wasm, entry JS, reading-path DB, firstPaintTransferBytes, total).
2. Add scripts/check-web-bundle-budget.sh that runs measure-web-bundle.sh (or reads the latest JSON) and exits non-zero if any measured value exceeds its budget, printing the offending asset + delta.
3. Add app/package.json script test:web:perf-budget that invokes the checker; document in loop/perf/README how to re-baseline intentionally (with justification).
4. Run the full suite and confirm the budget passes at the optimized numbers.

## Critério de aceite

- [ ] check-web-bundle-budget.sh fails loudly on any over-budget asset and passes at the current optimized numbers.
- [ ] Budget thresholds recorded in loop/perf/web-bundle-budget.json and referenced by the checker.
- [ ] test:web:perf-budget is part of the web verification suite.
- [ ] All 15 web self-tests + first-paint test + budget check + tsc --noEmit + expo export --platform web green; no the-light change.
- [ ] A short summary of before/after numbers (baseline vs optimized) is written to loop/perf/README for the Marco 5 reviewer.
- [ ] Orçamento perf TRAVADO como guarda wired: `measure-web-bundle.sh` (ou um `test:*` que o chama) é a guarda oficial; PROVAR que FALHA se o entry/assets regredirem além do orçamento; limites finais documentados.

## Comando(s) de verificação

```sh
cd /Users/butkeraites/Documents/the-light-app && ./scripts/check-web-bundle-budget.sh
cd /Users/butkeraites/Documents/the-light-app/app && npm run test:web:perf-budget
cd /Users/butkeraites/Documents/the-light-app/app && npx tsc --noEmit && npx expo export --platform web && for t in reading search xref notes study lexicon; do npm run test:web:$t || exit 1; done
```

## Restrições

Workstream EXIT and regression guard — blocking but NOT a milestone gate (the Marco 5 human sign-off is cross-workstream, owned by the orchestrator). This is what makes 'performance green' objective and durable. Keep budgets tied to measured reality, not aspiration; provide a documented, justified re-baseline path.

- **Offline-first:** guarda local; nada exige rede.
- **Zero-drift:** travar o orçamento NÃO muda o app; só formaliza a guarda de regressão. Self-tests idênticos.
- **the-light só via PR+ADR:** `the-light-core` pinado (`2fc2dab`); app-side.
- **Qualidade:** `tsc --noEmit`; o `measure-web-bundle.sh` (F5.3/6/9/12/15/17) já faz BUDGET OK/FAIL — esta tarefa TRAVA o orçamento como guarda oficial (wire num script `test:*` e/ou doc), define os limites finais (entry moduleCount/bytes, transfer, assets) e prova que a guarda FALHA se o bundle regredir; self-tests sem regressão.

---
id: F5.5
title: i18n do fluxo de leitura: títulos de header reativos + telas read/* + fio locale→`lang` da IA
phase: 5
status: ready
gate: false
depends_on: [F5.2]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-02
---

## Objetivo

Migrar as strings do fluxo de leitura para t() e tornar os títulos de header do expo-router REATIVOS ao locale, e ELIMINAR o `lang="pt"` HARDCODED (4 pontos em [chapter].tsx) passando o locale ativo do useI18n() como `lang` aos painéis de IA — de modo que trocar o idioma da UI também troque o idioma das respostas da IA (reúso do fio de `lang` já existente).

> **Herda F5.2 (aceita):** a camada i18n (`app/lib/i18n.ts`: catálogos PT/EN tipados,
> `I18nProvider`/`useI18n`/`t()`) + o KV de prefs offline (`app/lib/prefs.ts`) já existem.
> Esta tarefa ESTENDE o i18n ao fluxo de LEITURA (headers reativos + telas `read/*`) e liga
> o `locale` da UI ao `lang` da IA quando aplicável. REUSA a infra da F5.2 — NÃO criar
> segunda camada i18n/KV. NÃO traduzir texto bíblico.

## Contexto / arquivos

_Workstream:_ Acessibilidade + i18n + temas · _Depende de:_ F5.2 (aceita)

- `app/lib/i18n.ts`
- `app/app/_layout.tsx`
- `app/app/read/index.tsx`
- `app/app/read/[book]/index.tsx`
- `app/app/read/[book]/[chapter].tsx`
- `app/components/ThemeToggleButton.tsx`

## Passos sugeridos

1. Em app/app/_layout.tsx: consumir useI18n() no RootNavigator (já consome useTheme) e definir os títulos das Stack.Screen ('The Light','Ler a Bíblia','Capítulos','Leitura','Buscar') via t(); garantir re-render ao trocar de idioma.
2. Migrar strings visíveis de app/app/read/index.tsx, app/app/read/[book]/index.tsx e app/app/read/[book]/[chapter].tsx para t() (títulos dinâmicos, estados vazios, rótulos, mensagens de erro); adicionar accessibilityRole/Label onde faltar em elementos interativos.
3. Substituir os 4 `lang="pt"` de [chapter].tsx pelo `locale` de useI18n() repassado aos painéis (ReaderAskPanel/ReaderChatPanel/ReaderComparePanel/ReaderStudyPanel) — manter o tipo `lang: string` desses painéis; o core faz o default sensato. `translation` (versão) permanece separado de `locale`.
4. Confirmar que o texto do capítulo continua vindo do store (get_chapter) — i18n só toca cromo de UI.
5. Gates: tsc/eslint/expo export web verdes; sem regressão.

## Critério de aceite

- [ ] Títulos de header e strings das telas read/* trocam PT⇄EN ao alternar o idioma (sem reiniciar).
- [ ] Nenhum `lang="pt"` hardcoded em [chapter].tsx: os painéis de IA recebem o `locale` ativo (a resposta da IA segue o idioma da UI).
- [ ] Elementos interativos das telas de leitura têm accessibilityRole+Label; nenhuma string PT hardcoded remanescente nesses arquivos (checável pela guarda i18n-5).
- [ ] Texto do capítulo inalterado (do store); `translation` e `locale` distintos.
- [ ] tsc 0, eslint 0, expo export web exit 0; core intacto; the-light 2fc2dab.

## Comando(s) de verificação

```sh
set -e; cd /Users/butkeraites/Documents/the-light-app
grep -nE 'lang="pt"' app/app/read/'[book]'/'[chapter]'.tsx && { echo 'FAIL: lang=pt hardcoded'; exit 1; } || echo 'OK: locale fluindo'
cd app && npx tsc --noEmit && npx eslint . && npx expo export --platform web
```

## Restrições

Reúso-chave: o prop `lang` já flui até a IA — aqui ele passa a vir do locale, sem novo tipo. Anti-alucinação intacta (texto do store). Offline-first: nada novo exige rede.

- **Offline-first:** i18n/prefs 100% local (reusa o KV de prefs da F5.2); nada exige rede/conta.
- **Anti-alucinação:** i18n só traduz CHROME da UI — texto de versículo/refs/citações verbatim do store/core; atribuições CC-BY VISÍVEIS e verbatim; `locale`(UI) ≠ `translation`(versão bíblica).
- **the-light só via PR+ADR:** consumir `the-light-core` pinado (`2fc2dab`); nenhuma mudança no core (app-side).
- **Qualidade:** TS `tsc --noEmit`; eslint só se configurado (não está → tsc é o gate); prova headless/self-test; sem regressão nos `test:web:*`.

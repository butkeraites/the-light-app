---
id: F6.9
title: Léxico — popular morph_legend (0 linhas) + expandir LEXICON_BOOKS além dos 3 de amostra
status: queue
gate: false
depends_on: []
phase: 6
trilha: Dados/perf
---

## Objetivo

O interlinear de estudo profundo hoje é uma AMOSTRA de 3 livros (`LEXICON_BOOKS=[1,19,43]`
Gênesis/Salmos/João) e a tabela `morph_legend` está **VAZIA (0 linhas)** — então códigos de
morfologia não resolvem para nada, e o estudo interlinear degrada para vazio fora desses 3 livros.
A leitura/busca/xref já cobrem a Bíblia inteira (F5.36); falta o LÉXICO acompanhar (parcialmente).

## Escopo (incremental — NÃO o léxico completo de ~90MB)

1. **Popular `morph_legend`** (ganho barato/alto): parar de dropá-la em
   `scripts/gen-reading-sample-db.sh` (~L75 `DROP TABLE morph_legend`) e copiá-la do `bible.sqlite`
   no `core/examples/gen_reading_sample_db.rs` (mesmo molde de `scholarly_sources`/`lexicon`). Assim os
   códigos de morfologia passam a resolver para descrições.
2. **Expandir `LEXICON_BOOKS`** (`gen_reading_sample_db.rs:48`) de `[1,19,43]` para um conjunto MAIOR
   e útil — preferir **o Novo Testamento inteiro** (40..66) OU, se o tamanho ficar proibitivo, um
   subconjunto documentado (evangelhos + Atos + epístolas principais). É asset ON-DEMAND (não entra no
   first-paint), mas cresce o download único do estudo — reportar o tamanho resultante e manter o
   **léxico completo de 90MB (todo o AT+NT) DEFERIDO** (decisão de produto).
3. **Regenerar os assets** via `./scripts/gen-reading-sample-db.sh` (roda o cargo example + o split
   sqlite3 — pesado, determinístico). `lexicon-sample.sqlite` cresce (mais `original_tokens`/`lexicon`
   + `morph_legend`). `reading-lite.sqlite` (leitura, SEM léxico) NÃO deve mudar.
4. **Re-baseline do budget** (`scripts/measure-web-bundle.sh` + `loop/perf/web-bundle-budget.json`):
   só o `lexiconDbBytes` (asset ON-DEMAND) muda — `moduleCount`/entry NÃO mudam. Nota justificando
   (nota `noteF69`), tolerâncias inalteradas. Anti-alucinação: léxico VERBATIM do `bible.sqlite`/STEP.
5. **Atualizar as asserções de sanidade** do gerador se necessário (ele já assere tokens de João 3:16 +
   fonte STEP) e verificar que `deepStudy`/`lexicalEntries` retornam léxico para um livro NOVO do NT
   (ex.: Romanos) além de João.

## Aceite

- [ ] `morph_legend` populada (>0 linhas) do `bible.sqlite`; códigos de morfologia resolvem.
- [ ] `LEXICON_BOOKS` expandido (NT inteiro ou subconjunto documentado); estudo interlinear funciona
      para um livro NOVO do NT (não só João) — provado por `deepStudy`/`lexicalEntries`.
- [ ] Assets regenerados; `reading-lite.sqlite` inalterado; `lexicon-sample.sqlite` maior (tamanho reportado).
- [ ] `test:web:perf-budget` LOCKED OK após re-baseline SÓ do `lexiconDbBytes` (moduleCount inalterado); nota + (se quiser) ADR.
- [ ] `tsc` 0; `test:web:study`/`test:web:lexicon` verdes; asserções do gerador passam; the-light `225b8c9`; sem segredo.
- [ ] Léxico completo 90MB (AT+NT integral) segue DEFERIDO como decisão de produto.

## Verificação

```sh
cd /Users/butkeraites/Documents/the-light-app && ./scripts/gen-reading-sample-db.sh
sqlite3 assets/data/lexicon-sample.sqlite "SELECT COUNT(*) FROM morph_legend; SELECT COUNT(DISTINCT book_number) FROM original_tokens;"
cd app && npx tsc --noEmit && npm run test:web:study && npm run test:web:lexicon && npm run test:web:perf-budget
```

## Notas

- `bible.sqlite` (fonte, todos os livros + léxico completo) já existe. O gerador é app-side (`core/examples`),
  NÃO toca o `the-light`. Regen roda cargo (feature `embedded`, alguns min) + split sqlite3 — pesado.
- Se o NT inteiro deixar o `lexicon-sample.sqlite` grande demais (ex.: >40MB), escopar a um subconjunto
  do NT e DOCUMENTAR o corte + deixar o "resto do NT/OT" como follow-up. É on-demand (não first-paint).
- Reusar o molde de cópia (INSERT..SELECT FROM src) já usado p/ `original_tokens`/`lexicon` (verbatim, anti-alucinação).

---
id: F5.31
title: Seletor de tradução na Busca (hoje fixo em KJV; store também tem Almeida 1911)
status: queue
gate: false
depends_on: []
phase: 5
trilha: Refinamento (varredura F5.28+)
---

## Objetivo

`app/app/search/index.tsx` fixa `const TRANSLATION = 'kjv'` (~linha 27, usado ~linha 98),
então a busca só consulta KJV mesmo com o store trazendo KJV + Almeida 1911, e o Reader já
tem um seletor de versão (`components/ReaderVersionPicker.tsx`). A glue `search()` já aceita
um argumento de tradução (`web/reading.web.ts` + `web/reading.ts`) — então: adicionar um
seletor que lista as traduções presentes no store e re-executa a query contra a escolhida.
100% offline, sem tocar o `the-light`, sem segredo.

## Aceite

- [ ] A tela de Busca expõe um seletor de tradução listando as traduções presentes no store
      (via `listTranslations`, ex.: KJV + Almeida 1911), modelado no seletor de versão do Reader.
- [ ] Selecionar uma tradução re-executa a query atual contra ela via o parâmetro existente
      `search(dbPath, term, translation)` e atualiza os resultados; **KJV permanece o padrão**.
- [ ] Todo o cromo novo passa por `t()` com paridade pt/en (`npm run test:i18n` +
      `npm run test:i18n-coverage` verdes); `npm run test:web:search` verde; `npx tsc --noEmit` 0.
- [ ] Anti-alucinação: snippet/referência dos resultados vêm do store, nunca traduzidos por `t()`;
      nomes de tradução vêm do store (`listTranslations`), não hardcoded. the-light intocado (`225b8c9`); sem segredo.

## Verificação

```sh
cd app && npx tsc --noEmit && npm run test:web:search && npm run test:i18n && npm run test:i18n-coverage
```

## Origem

`app/app/search/index.tsx:25-27,98`; `app/components/ReaderVersionPicker.tsx`;
`app/web/reading.web.ts:112` (listTranslations). Varredura de refinamento (sweep code-gaps).
Reusar o padrão do ReaderVersionPicker (não criar um segundo componente se o existente servir).

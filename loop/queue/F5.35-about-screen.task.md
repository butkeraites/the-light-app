---
id: F5.35
title: Tela in-app Sobre / Créditos / Licenças (KJV, Almeida 1911, OpenBible CC-BY, STEP CC BY 4.0)
status: queue
gate: false
depends_on: []
phase: 5
trilha: Refinamento (varredura F5.28+) — ÚLTIMA da varredura
---

## Objetivo

Não há tela central de "Sobre" (grep por about/onboarding/welcome/credits vazio). As
atribuições aparecem só contextualmente (painel de xref = OpenBible CC-BY; painel de estudo =
STEP CC-BY) e os textos bíblicos embarcados (KJV, Almeida 1911) não têm atribuição exibida em
lugar nenhum. Adicionar uma rota "Sobre" consolidando créditos/licenças, declarando
offline-first + BYOK + anti-alucinação, servindo também como explicador de primeiro uso, e
com link para backup/export (a entrada existente open-sync / `SyncSettings`). 100% offline,
sem tocar o `the-light`, sem segredo. **Última tarefa da varredura de refinamento.**

## Aceite

- [ ] Nova rota (ex.: `app/app/about.tsx`) alcançável a partir da Home e/ou do header; lista as
      quatro fontes de dados com sua licença (KJV domínio público, Almeida 1911 domínio público,
      cross-references OpenBible.info CC-BY, léxico STEP/Tyndale CC BY 4.0), declara offline-first
      + BYOK + anti-alucinação, e tem link para backup/export (open-sync / `SyncSettings`).
- [ ] Todo cromo via `t()` com paridade pt/en; as strings CC-BY VERBATIM (XREF_ATTRIBUTION /
      STEP_ATTRIBUTION) passam pelo allowlist do guarda i18n-coverage inalteradas.
- [ ] `npm run test:i18n-coverage` + `npm run test:a11y-scan` + `npm run test:web:contrast` verdes;
      `npx tsc --noEmit` 0; `npx expo export --platform web` sucede.
- [ ] the-light intocado (`225b8c9`); sem segredo/rede.

## Verificação

```sh
cd app && npx tsc --noEmit && npm run test:i18n-coverage && npm run test:a11y-scan && npm run test:web:contrast && npx expo export --platform web >/dev/null && echo EXPO_OK
```

## Origem

grep(about|onboarding|welcome|credits) vazio; `app/app/_layout.tsx` (stack); atribuições in-app
`app/components/ReaderXrefPanel.tsx:30` (OpenBible CC-BY) e `app/components/ReaderStudyPanel.tsx:76`
(STEP CC-BY). Varredura de refinamento (sweep polish-openness). Notas: reusar tokens de tema (sem
hex), roles/labels de a11y (padrão F5.20/21), e o `SnapshotStore`/entrada de sync existente para o
link de backup. **Providers de IA:** se listar provedores BYOK, incluir os REAIS — Claude, GPT,
Gemini **e Ollama (modelos locais)** (o README omitiu Ollama; aqui seja preciso). Reusar as
constantes de atribuição in-app (fonte da verdade) — não retipar de memória; podem ser expostas
via `t()` como valores fixos (não traduzidos) mantendo o identificador de licença verbatim.

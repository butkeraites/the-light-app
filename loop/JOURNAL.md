# Loop — Journal (append-only)

> Uma linha por ciclo do Driver. Formato:
> `<timestamp> · ciclo <n> · <ação> · tarefa <ID> · <desfecho>`
> O Guia (auditor) lê este journal para aferir cadência e saúde do loop.

2026-06-29 20:57 UTC · ciclo 0 · loop inicializado pelo Guia · tarefa F0.0 pronta na queue · aguardando primeiro ciclo do Driver
2026-06-29 22:24 UTC · ciclo 1 · executor+reviewer em F0.0 (descoberta de toolchain) · tarefa F0.0 · passed (efe334f), arquivada · lacunas futuras: targets Rust/ubrn (instaláveis), Xcode→F0.7 e Android NDK→F0.8 (setup humano)
2026-06-29 22:26 UTC · ciclo 2 · queue vazia → planner semeou F0.1 (bootstrap do repo) em queue/ e marcou ready no STATUS · sem execução neste ciclo (próxima invocação executa F0.1)
2026-06-29 22:36 UTC · ciclo 3 · executor+reviewer em F0.1 (bootstrap do repo) · tarefa F0.1 · passed (595c70c), arquivada · estrutura+docs criados; ADR-0002 consumir the-light como git dep pinada; pendência: resolução real do core na F0.2
2026-06-29 22:38 UTC · ciclo 4 · queue vazia → planner semeou F0.2 (crate core/ com UniFFI trivial, sem lógica/sem dep do core) · resolução real do the-light-core transferida p/ F0.3 · sem execução neste ciclo
2026-06-29 22:49 UTC · ciclo 5 · executor+reviewer em F0.2 (crate core UniFFI trivial) · tarefa F0.2 · passed (7b922eb), arquivada · the-light-app-core compila com uniffi 0.31.2 (modo library); ADR-0003 registrado
2026-06-29 22:55 UTC · ciclo 6 · queue vazia → planner semeou F0.3 (parse_reference delegando ao core) · Driver apurou the-light público, HEAD 0888ac0 (pin) · risco: resolução/build/API do the-light-core · sem execução neste ciclo
2026-06-29 23:03 UTC · ciclo 7 · executor+reviewer em F0.3 (parse_reference) · tarefa F0.3 · passed (9881c72), arquivada · PONTE REAL ao core provada: git dep pinada (the-light-core v1.2.0), delegação genuína, PT==EN, sem fork/cópia
2026-06-29 23:12 UTC · ciclo 8 · queue vazia → planner semeou F0.4 (script gen-bindings.sh: instala/fixa ubrn e gera bindings TS) · risco: maturidade do ubrn / incompat. com uniffi 0.31.2 · sem execução neste ciclo
2026-06-29 23:25 UTC · ciclo 9 · executor+reviewer em F0.4 (gen-bindings via ubrn) · tarefa F0.4 · passed (e19064a), arquivada · RISCO #1 VENCIDO: ubrn 0.31.0-3 gera bindings/*.ts reprodutíveis, compat. uniffi 0.31.2 sem tocar fronteira; ADR-0004
2026-06-29 23:40 UTC · ciclo 10 · queue vazia → planner semeou F0.5 (app Expo mínimo SDK 56 + 1 tela, sem core; verif. não-interativa web+tsc) · run nativo adiado p/ F0.7/F0.8 · risco: Node v25.8.1 vs Expo SDK 56 · sem execução neste ciclo
2026-06-29 23:41 UTC · ciclo 11 · executor+reviewer em F0.5 (app Expo mínimo) · tarefa F0.5 · passed (3262f56), arquivada · app Expo SDK 56 (expo-router) compila p/ web (export estático), tsc limpo; Node 25 compat. SDK 56 (risco descartado)
2026-06-29 23:55 UTC · ciclo 12 · queue vazia → planner semeou F0.6 (ligar core no web/WASM) · planner apurou na fonte do core (rev 0888ac0): SEM [features], rusqlite{bundled}+reqwest incondicionais → wasm sem rusqlite exige modificar o core (PR+ADR) · risco alto de blocked · sem execução neste ciclo
2026-06-29 23:54 UTC · ciclo 13 · executor+reviewer em F0.6 (web/WASM) · tarefa F0.6 · **BLOCKED** (confirmado pelo reviewer) · cargo build --target wasm32 falha (exit 101) em sqlite-wasm-rs/SQLite-C; core sem features → excluir rusqlite exige PR ao the-light · ⛔ HALT escrito (loop/HALT): gate estratégico SQLite-no-WASM, decisão humana/Guia · core/fronteira intactos · LOOP PARADO
2026-06-30 01:04 UTC · (resolução de direção, fora de ciclo — humano aprovou) · ADR-0005 escrito (feature-gating store/net no core + matriz por alvo) · spec do PR em loop/proposals/the-light-PR-feature-gating.md · F0.6 arquivada (blocked) e re-escopada em F0.6a/F0.6b (backlog) · HALT AJUSTADO (permanece): retomar exige PR mesclado + re-pin do rev · the-light segue intocado
2026-06-30 01:30 UTC · (implementação no the-light, autorizada pelo humano) · feature-gating IMPLEMENTADO e VERIFICADO no the-light (branch feat/core-wasm-feature-gating, commit 8f66004): feature única `embedded` (default-on) — store/net entrelaçados demais p/ separar · the-light-core --no-default-features compila p/ wasm32; defaults intactos (177 testes + workspace OK); clippy/fmt limpos; não-quebrante · BLOQUEIO SQLite-no-WASM RESOLVIDO no core · achado downstream: uniffi_core 0.31.2 não compila cru p/ wasm (Send) → F0.6b via caminho web do ubrn · HALT permanece: falta push+merge no GitHub + re-pin do rev (ação outward, a confirmar)
2026-06-30 01:45 UTC · (humano fez push+merge na main do the-light) · HALT REMOVIDO (motivo resolvido) · F0.6a executada pelo Driver: re-pin do core/Cargo.toml p/ rev 8f66004 (do GitHub) + matriz de features por alvo (web: default-features=false → puro; nativo: features=["embedded"]) · verificado: build nativo + 5 testes OK, clippy/fmt limpos, cargo tree confirma 0 deps pesadas no grafo wasm e rusqlite/reqwest no nativo · F0.6a ACEITA · próxima: F0.6b (caminho web do ubrn; resolver uniffi-wasm)
2026-06-30 02:45 UTC · ciclo (retomada) · queue vazia → planner semeou F0.6b (caminho web do ubrn: `ubrn web build` → wasm; glue app/web/; prova headless) · confirmado que o caminho web existe no pacote ubrn; risco moderado-alto (uniffi-wasm Send / wasm-pack / prova headless) · sem execução neste ciclo
2026-06-30 03:10 UTC · ciclo · executor+reviewer em F0.6b (caminho web do ubrn) · tarefa F0.6b · **FAILED** (attempt 1; reviewer confirmou) · caminho web do ubrn existe e gera bindings/wasm-crate, mas cargo nao resolve: fronteira uniffi=0.31.2 (uniffi_core =0.31.2) vs runtime web do ubrn uniffi-runtime-javascript (uniffi_core =0.31.0) · +Send NAO era o muro (feature wasm-unstable-single-threaded) · correcao EM ESCOPO (nossa fronteira, nao the-light) → gerada precursora F0.6c (alinhar uniffi a =0.31.0, revalidar F0.3/F0.4, ADR-0006) → depois retry F0.6b · repo limpo, core/fronteira intactos
2026-06-30 03:19 UTC · ciclo · executor+reviewer em F0.6c (alinhar uniffi a =0.31.0) · tarefa F0.6c · passed (7b98644), arquivada · fronteira verde sob uniffi 0.31.0 (5 testes; "Jo 3.16"=="John 3:16"); JSI (F0.4) regenera; ADR-0006; forma da fronteira intacta; the-light nao tocado · conflito de pin eliminado → F0.6b liberada p/ retry (attempt 2)
2026-06-30 03:55 UTC · ciclo · executor+reviewer em F0.6b (retry, attempt 2) · tarefa F0.6b · passed (1cdde6c), arquivada · ALVO WEB FECHADO: ubrn build web + gen-bindings-web.sh → wasm-bindgen; glue app/web/; tela ligada; prova headless parseReference PT==EN (43/3/Single 16) pelo Rust/wasm; expo export web ok (.wasm asset local, offline-first); 3 muros resolvidos sem hack (opt-level, resolver=2, wasm-bindgen lock-step); core/the-light intactos; ADR-0007 · próxima: F0.7 (iOS) — deve bloquear por Xcode ausente
2026-06-30 03:58 UTC · ciclo · Driver re-verificou ambiente p/ F0.7/F0.8 · tarefas F0.7 (iOS) e F0.8 (Android) · **BLOCKED** · Xcode completo ausente (só CLT), Android SDK/NDK + cargo-ndk ausentes, targets Rust iOS/Android não instalados — setup humano de máquina · ⛔ HALT escrito: alvos nativos exigem provisionamento; web já provado (F0.6b) · LOOP PARADO aguardando humano
2026-06-30 04:30 UTC · (provisionamento humano+Driver, fora de ciclo) · toolchains nativos INSTALADOS: targets Rust iOS/Android, cargo-ndk 4.1.2, JDK 17, Android SDK/NDK 27.1 + emulador headless (boot_completed=1), Xcode 26.6 + runtime iOS 26.5 (humano instalou Xcode/sudo) · env persistido em ~/.zshrc · HALT REMOVIDO (motivo resolvido) · F0.7/F0.8 destravadas · loop RETOMADO
2026-06-30 06:00 UTC · ciclo · queue vazia → planner semeou F0.7 (ligar core no iOS via Turbo Module nativo do ubrn + Expo prebuild; prova headless via self-test no launch capturado por simctl) · risco moderado-alto: integração Turbo Module do ubrn ↔ Expo prebuild · sem execução neste ciclo
2026-06-30 04:30 UTC · ciclo · executor+reviewer em F0.7 (iOS) · tarefa F0.7 · passed (a6f6797), arquivada · ALVO iOS FECHADO: ubrn build ios → Turbo Module JSI; integração Expo (prebuild+New Arch codegen+CocoaPods+autolinking); RUN REAL no simulador iPhone 17/iOS 26.5; prova headless TLA_SELFTEST PT==EN (43/3/16) pelo Rust nativo via JSI (anti-fake confirmado pelo reviewer); core/the-light intactos; sem regressão web; ADR-0008 · próxima: F0.8 (Android)
2026-06-30 05:10 UTC · ciclo · queue vazia → planner semeou F0.8 (ligar core no Android via Turbo Module nativo; espelho da F0.7) · reaproveita Turbo Module/autolink/glue/selftest; adiciona android: no config + gen-bindings-android.sh + run-android-selftest.sh (adb logcat) · risco MENOR que F0.7 · sem execução neste ciclo
2026-06-30 05:40 UTC · ciclo · executor+reviewer em F0.8 (Android) · tarefa F0.8 · passed (36af016), arquivada · ALVO ANDROID FECHADO: ubrn build android (cargo-ndk→jniLibs staticlib) → Turbo Module JNI/JSI; integração Expo (prebuild+autolink mesmo react-native.config.js+New Arch codegen); 2 patches de template em gerados (AGP8 manifest; CMakeLists C++ Node25); RUN REAL no emulador headless; prova TLA_SELFTEST PT==EN (43/3/16) via adb logcat (anti-fake confirmado); core/the-light intactos; web+iOS sem regressão; ADR-0009 · **OS 3 ALVOS PROVADOS** · próxima: F0.9 (store nativo)
2026-06-30 06:10 UTC · ciclo · queue vazia → planner semeou F0.9 (store nativo: get_passage delegando ao store do the-light-core via rusqlite/embedded, cfg(not(wasm32))) · API/schema descobertos na fonte do core (Store::open + EmbeddedSource::passage; migrations auto-criam schema) · prova = teste Rust lendo João 3:16 do sample.sqlite · risco: get_passage precisa ser cfg(not wasm32) p/ não regredir web · sem execução neste ciclo
2026-06-30 06:00 UTC · ciclo · executor+reviewer em F0.9 (store nativo) · tarefa F0.9 · passed (1d16897), arquivada · get_passage delega ao the-light-core (Store::open + EmbeddedSource::passage); lê João 3:16 KJV verbatim do assets/data/sample.sqlite (versionado, domínio público, gerado via Store::open); teste Rust host verde; anti-fake confirmado (texto do banco, não hardcoded); achado: gatear corpo (não exportação) por alvo p/ não quebrar ubrn build web; grafo wasm puro (sem rusqlite); the-light intacto; ADR-0010 · próxima: F0.10 (store web wa-sqlite+OPFS)
2026-06-30 06:05 UTC · ciclo · Driver investigou o store do the-light-core p/ F0.10 · tarefa F0.10 (store web) · **BLOCKED (decisão de arquitetura)** · store/source do core são CONCRETOS sobre rusqlite::Connection (sem trait injetável); web sem rusqlite (embedded off) → get_passage web é stub · escolher estratégia de SQLite no web é decisão humana (VISION §4 fricção #1, análogo ADR-0005) · opções: A wa-sqlite TS+OPFS (recomendada Fase 0) / B PR ao core abstraindo store / C pré-indexado → ADR-0011 · ⛔ HALT escrito · LOOP PARADO p/ sign-off · 3 alvos+store nativo já provados
2026-06-30 06:40 UTC · (decisão humana, fora de ciclo) · F0.10 store web: humano escolheu Opção A (wa-sqlite TS+OPFS) · ADR-0011 escrito (query de passagem em TS no glue web; parse_reference do Rust; texto do store local wa-sqlite/OPFS; sem mudar o core) · HALT REMOVIDO · loop RETOMADO; F0.10 a re-escopar/semear
2026-06-30 09:10 UTC · ciclo · queue vazia → planner semeou F0.10 re-escopada (ADR-0011 Opção A: wa-sqlite+OPFS) · getPassage web em TS = parseReference(wasm/Rust) + SELECT no wa-sqlite sobre o sample.sqlite; prova headless node (VFS memória) · risco: wa-sqlite no Metro/Expo web (.wasm asset, COOP/COEP p/ OPFS) · sem execução neste ciclo
2026-06-30 13:45 UTC · ciclo · executor+reviewer em F0.10 (store web) · tarefa F0.10 · passed (d6e968d), arquivada · STORE WEB PROVADO: getPassage web em TS = parseReference(Rust/wasm) + queryPassage espelhando EmbeddedSource::passage num wa-sqlite@1.0.0 (build sync, sem SharedArrayBuffer); OPFS no browser + VFS memória; tela web exibe texto do store; prova headless node verde (KJV verbatim + 43/3/16); expo export web 0; sem regressão; the-light/core intactos; ADR-0011/0012 (incl. symlink fonte única) · próxima: F0.11 (GATE Marco 0 → HALT p/ sign-off)
2026-06-30 13:50 UTC · ciclo · queue vazia → Driver semeou F0.11 (gate Marco 0, gate:true) e aplicou a regra do gate · tarefa F0.11 · **GATE → HALT** (sign-off humano) · Fase 0 técnica COMPLETA e verificada (F0.6b web, F0.7 iOS, F0.8 Android, F0.9 store nativo, F0.10 store web); ponte UniFFI→Expo provada nos 3 alvos com run real; fricção SQLite-WASM e risco ubrn eliminados; the-light sem fork (só PR sancionado da feature embedded) · ⛔ loop/HALT escrito · LOOP PARADO aguardando aprovação do Marco 0 p/ liberar a Fase 1
2026-06-30 14:00 UTC · ciclo · **MARCO 0 APROVADO pelo humano (Renan)** · tarefa F0.11 (gate) · passed/aceito (aed825d), arquivada · Fase 0 COMPLETA (F0.0–F0.11): ponte UniFFI→Expo provada nos 3 alvos + store nativo/web · HALT REMOVIDO · **Fase 1 LIBERADA** · próximo ciclo: planner decompõe a Fase 1 (leitura offline)
2026-06-30 15:30 UTC · ciclo · Marco 0 aprovado → planner decompôs a FASE 1 em backlog/PHASE-1.md (F1.1–F1.17, 2 gates: F1.12 store-web estratégico, F1.17 Marco 1) e semeou F1.1 (pipeline de dados + banco embarcado completo KJV+ALM1911 via xtask do the-light) · padrão: núcleo→UI nativa→paridade web pós-gate · risco F1.1: xtask baixa datasets por rede em build (~12MB; data/seed vazio) → blocked se offline · sem execução neste ciclo
2026-06-30 15:10 UTC · ciclo · executor+reviewer em F1.1 (banco bíblico) · tarefa F1.1 · passed (b1a9be4), arquivada · scripts/gen-bible-db.sh roda o xtask import canônico do the-light (rev pinado 8f66004, checkout do cargo + CARGO_TARGET_DIR fora — the-light intocado) → assets/data/bible.sqlite ~27MB; contagens lidas do banco kjv=31102/alm1911=31101, 66 livros/versão, verses_fts 62203 (acento-insensível); idempotente; gerar-ignorado (ADR-0013); sample.sqlite segue versionado; sem texto hardcoded · próxima: F1.2 (expor leitura no core)
2026-06-30 15:24 UTC · ciclo · queue vazia → planner semeou F1.2 (expor leitura no core: list_translations/list_books/get_chapter/chapter_count delegando ao the-light-core) · descoberta: core não lista books por tradução → list_books() puro via reference::BOOKS; corpo cfg(not wasm32)+stub web (molde F0.9); prova = teste Rust host sobre fixture determinístico KJV · sem execução neste ciclo
2026-06-30 15:40 UTC · ciclo · executor+reviewer em F1.2 (leitura no core) · tarefa F1.2 · passed (e06b0c6), arquivada · fronteira expõe list_translations/list_books/get_chapter/chapter_count delegando ao the-light-core (gating de corpo cfg(not wasm32)+stub web; list_books puro via reference::BOOKS); teste host 14/14 (66 livros; chapter_count(kjv,43)==21; João 3 numerado v16 KJV verbatim); web puro (sem rusqlite); the-light intacto · próxima: F1.3 (UI de leitura nativa)
2026-06-30 15:46 UTC · ciclo · queue vazia → planner semeou F1.3 (UI de leitura nativa: livro→cap→texto + seletor de versão, lendo do store no device via fronteira F1.2) · subproblema: bundling do bible.sqlite no app nativo (asset+cópia p/ path gravável, ADR-0014) · prova = self-test de leitura no device (TLA_READ books=66/john3_v16/john_chapters=21) via simctl/logcat · risco: 47MB no bundle (possível blocked) · sem execução neste ciclo
2026-06-30 16:20 UTC · ciclo · executor+reviewer em F1.3 (UI leitura nativa) · tarefa F1.3 · passed (9cf4a9e), arquivada · 1ª UI de leitura nativa (app/app/read/** + Reader*) lê do store no device via fronteira F1.2; bundling ADR-0014 (subset reading-sample.sqlite ~1.8MB com João KJV completo, asset+copy p/ documentDirectory via expo-asset/file-system); prova iOS sim: TLA_READ books=66/João 3:16 verbatim/john_chapters=21 (texto do get_chapter, anti-fake ok); bindings nativos regenerados (ubrn build ios) sem tocar core/the-light; sem regressão (parse PT/EN, web tsc/export) · próxima: F1.4
2026-06-30 17:05 UTC · ciclo · queue vazia → planner semeou F1.4 (UI versões lado a lado + tema claro/escuro) · 2 chamadas get_chapter (kjv|alm) alinhadas por versículo (tolera lacuna; alm tem 1 verso a menos); tema useColorScheme+toggle sessão; prova TLA_PARALLEL lê 2 versões do store (João 3:16 KJV + Almeida verbatim) · sem execução neste ciclo
2026-06-30 17:40 UTC · ciclo · executor+reviewer em F1.4 (lado a lado + tema) · tarefa F1.4 · passed (24cd3b8), arquivada · ReaderParallelView (2x get_chapter, alinha por versículo) + tema claro/escuro (tokens+useColorScheme+sessão); prova iOS TLA_PARALLEL (KJV+Almeida João 3:16 do store, não hardcoded); sem dep nova; sem regressão (TLA_READ/parse/web); core/the-light intactos; ADR-0015 · próxima: F1.5 (busca FTS5)
2026-06-30 18:10 UTC · ciclo · queue vazia → planner semeou F1.5 (busca FTS5 na fronteira, depende F1.2) · search(db,query,translation,book?,limit?)->Vec<SearchHit> delegando a BibleSource::search; Record SearchHit via From; corpo gated cfg(not wasm32)+stub web · fixture deve popular verses_fts à mão (sem trigger no schema); acento-insensível via Gn 1:1 Almeida ceus→céus · sem execução neste ciclo
2026-06-30 18:30 UTC · ciclo · executor+reviewer em F1.5 (busca FTS5) · tarefa F1.5 · passed (f006a31), arquivada · search(db,query,translation,book?,limit?) delega a BibleSource::search; Record SearchHit; corpo gated+stub web; teste host 20/20 (João 3:16, acento-insensível ceus→céus, vazio/limit/erro); web puro (sem rusqlite); sem FTS reimplementado; the-light intacto · próxima: F1.6 (UI de busca)
2026-06-30 18:40 UTC · ciclo · queue vazia → planner semeou F1.6 (UI busca nativa; depende F1.5+F1.3) · campo+debounce + lista de hits (ref clicável→read/[book]/[chapter]) via fronteira search; HL markers→estilo; prova TLA_SEARCH no device · riscos: reading-sample sem verses_fts povoado (gerador deve popular o FTS) + bindings nativos defasados (regenerar gen-bindings-ios.sh) · sem execução neste ciclo
2026-06-30 18:55 UTC · ciclo · executor+reviewer em F1.6 (UI busca nativa) · tarefa F1.6 · passed (ab75071), arquivada · tela app/app/search (TextInput+debounce→search F1.5→FlatList), ReaderSearchResultItem (HL markers→estilo), ref clicável→read/[book]/[chapter]; verses_fts populado no gerador de dados; prova iOS TLA_SEARCH hits=646/John 3:16/texto do store; sem FTS/SQL em TS; sem regressão; core/the-light intactos · próxima: F1.7 (dados de xref)
2026-06-30 19:05 UTC · ciclo · queue vazia → planner semeou F1.7 (dados de xref; depende F1.1) · xtask import-xref (subcomando dedicado, rev 8f66004, sem tocar the-light) popula cross_references do bible.sqlite com ~344799 OpenBible/TSK (CC-BY); baixa ~8.3MB; idempotente; ADR-0016 (atribuição CC-BY + armazenamento); recomenda estender gen-bible-db.sh num pipeline único · sem execução neste ciclo
2026-06-30 19:20 UTC · ciclo · executor+reviewer em F1.7 (dados de xref) · tarefa F1.7 · passed (4d2d3bf), arquivada · gen-bible-db.sh estendido roda xtask import-xref (rev 8f66004, sem tocar the-light) → cross_references=344799 (OpenBible CC-BY); João 3:16=23 xrefs; idempotente; bible.sqlite gerar-ignorado; ADR-0016 (atribuição) · próxima: F1.8 (xref na fronteira)
2026-06-30 19:30 UTC · ciclo · queue vazia → planner semeou F1.8 (xref na fronteira; depende F1.7+F1.2) · cross_refs(db,book,ch,v,min_votes?,limit?) delega a xref::for_verse(store.conn()) — função livre, não método de BibleSource; CrossRef é embedded-gated → From cfg(not wasm32); corpo gated+stub web; fixture popula cross_references à mão; DEFAULT_MIN_VOTES=1 · sem execução neste ciclo
2026-06-30 20:40 UTC · ciclo · executor+reviewer em F1.8 (xref na fronteira) · tarefa F1.8 · passed (36b2cb0), arquivada · cross_refs(db,book,ch,v,min_votes?,limit?) delega a the_light_core::xref::for_verse(store.conn(),…) (função livre, não método); Record CrossRef{reference,votes:i64} via From<xref::CrossRef> sob cfg(not wasm32) (tipo-fonte embedded-only); corpo gated nativo + stub web; 26 testes host (5 xref_tests, fixture cross_references à mão): Rm 5:8 (book 45/cap 5/Single 8, 50 votos) em [0] por votos DESC, Range 8-9, min_votes None oculta disputada (-5)/Some(-100) inclui, limit, verso sem xref vazio; web puro (cargo tree wasm sem rusqlite); sem SQL/ranking reimplementado; the-light intacto (8f66004); ADR-0016 (atribuição CC-BY) exibida na F1.9 · próxima: F1.9 (UI xref nativa + atribuição CC-BY visível)
2026-06-30 21:05 UTC · ciclo · queue vazia → planner semeou F1.9 (UI de xref nativa + atribuição CC-BY visível; depende de F1.8+F1.3, ambas aceitas) · ao selecionar um versículo no Reader (read/[book]/[chapter]), painel ReaderXrefPanel lista as xrefs via a fronteira crossRefs (F1.8 → JSI → xref::for_verse, votos DESC) — cada item = referência+votos, clicável → router.push (molde F1.6); SEM SQL/ordenação/filtro em TS; atribuição CC-BY EXATA "Cross references courtesy of OpenBible.info (CC-BY)" visível (ADR-0016, obrigatória). Riscos sinalizados: (1) cross_references NÃO populada no subset reading-sample (sem trigger no schema) → gerador gen_reading_sample_db.rs deve copiar do bible.sqlite as xrefs com AMBOS os lados no subset (from/to ∈ {Gn1,Sl19,Jo43}; João 3:15≈439 votos sobrevive → count≥1); (2) bindings nativos defasados (pré-F1.8) → regenerar gen-bindings-ios.sh p/ crossRefs/CrossRef; (3) votes i64 pode vir como bigint no binding → formatar via String(). Prova determinística: xref-selftest.ts chama crossRefs(db,43,3,16) e emite TLA_XREF verse="John 3:16" count=<N≥1> first_ref/first_votes do retorno real; run-ios-selftest.sh asserta count≥1 + first_ref de livro do subset (Genesis/Psalm/John) + sem regressão TLA_READ/TLA_PARALLEL/TLA_SEARCH/parse. Só nativo (xref web = F1.15 pós-gate F1.12). Restrições: não tocar the-light/fronteira (F1.8 já entregou cross_refs); não reimplementar SQL/xref no app; anti-alucinação; CC-BY obrigatória; offline-first; gerados/bundled ignorados; blocked+HALT com erro EXATO se exigir mudar o core. · sem execução neste ciclo
2026-06-30 21:40 UTC · ciclo · executor+reviewer em F1.9 (UI de xref nativa) · tarefa F1.9 · passed (2ff73c5), arquivada · ReaderXrefPanel lista crossRefs (F1.8→glue reading.ts→JSI→xref::for_verse, votos DESC) com referência+votos clicáveis (router.push, molde F1.6); SEM SQL/ordenação de xref em TS; atribuição CC-BY EXATA "Cross references courtesy of OpenBible.info (CC-BY)" visível (ADR-0016); gerador gen_reading_sample_db.rs propaga cross_references ao subset (ambos os lados em Gn/Sl/Jo) → reading-sample.sqlite com 22413 xrefs (João 3:16→9); bindings nativos regenerados (votes i64→bigint via String()); web=stub (xref web=F1.15); prova HEADLESS iPhone 17 TLA_XREF verse="John 3:16" count=9 first_ref="John 3:15" first_votes=439 (do retorno real), sem regressão READ/PARALLEL/SEARCH/parse; tsc+expo export web verdes; the-light intacto (8f66004), core/src/lib.rs não tocado · próxima: F1.10 (notas/marcações na fronteira do core, file-based; depende só de F1.2)
2026-06-30 22:05 UTC · ciclo · queue vazia → planner semeou F1.10 (notas/marcações na fronteira; depende só de F1.2, aceita) · INVESTIGAÇÃO na fonte pinada (8f66004) determinou SITUAÇÃO (A): o the-light-core JÁ TEM camada userdata file-based (módulo `userdata` embedded-only) — `notes::{Note,NoteStore}` (Markdown 1 arquivo/.md por ref: get/put/delete/list/covering, delete idempotente) e `highlights::{Highlight,HighlightStore}` (highlights.json: load/save/list/add(replace)/remove/covering). F1.10 DELEGA a ela (NÃO reimplementa store, NÃO é (B)/(C), NÃO toca o the-light). Fronteira expõe 7 funções (put_note/get_note/delete_note/list_notes + add_highlight/remove_highlight/list_highlights) recebendo UM data_dir gravável (deriva notes/ + highlights.json do mesmo layout default do core; NUNCA o bible.sqlite só-leitura) e a referência como String canonicalizada pelo core (parse_reference). Records Note{reference,body}/Highlight{reference,color,tag:Option<String>} em todos os alvos (tipos puros); From<userdata::*> gated cfg(not wasm32) (tipos-fonte embedded-only, molde F1.8); corpo gated + stub web; sem Cargo.toml change. Teste de host = DIRETÓRIO temporário, relê de OUTRO handle (persistência em disco): notas round-trip + PT==EN mesma nota + list ordenada + delete idempotente + vazio sem panic + highlights add/replace/remove idempotente + ref inválida→CoreError. Anti-alucinação NÃO se aplica ao corpo (dado do usuário), mas a ref é canônica; offline-first. UI/persistência device/EXPORT = F1.11; paridade web = F1.16 pós-gate F1.12 · sem execução neste ciclo
2026-06-30 22:35 UTC · ciclo · executor+reviewer em F1.10 (notas/marcações na fronteira) · tarefa F1.10 · passed (b6381d0), arquivada · 7 funções (put_note/get_note/delete_note/list_notes + add_highlight/remove_highlight/list_highlights) DELEGAM a the_light_core::userdata::{NoteStore,HighlightStore} (situação A — core já entrega a camada file-based); recebem data_dir gravável (deriva notes/*.md + highlights.json; NUNCA o bible.sqlite só-leitura) + ref canonicalizada por parse_reference; Records Note{reference,body}/Highlight{reference,color,tag} puros + From<userdata::*> gated cfg(not wasm32) (molde F1.8); corpo gated + stub web; 34 testes host (8 novos userdata_tests num dir temporário, relendo de OUTRO handle = persistência em disco): round-trip John_3.16.md, PT==EN mesma nota, lista ordenada, delete/remove idempotentes, vazio sem panic, ref inválida→CoreError antes do I/O; web puro (cargo tree wasm sem rusqlite); core/Cargo.toml inalterado; the-light intacto (8f66004) · próxima: F1.11 (UI de notas/highlights nativa + export + persistência no device) — ÚLTIMA antes do gate F1.12
2026-06-30 23:05 UTC · ciclo · queue vazia → planner semeou F1.11 (UI de notas/highlights nativa + export + persistência; depende de F1.10+F1.3, ambas aceitas) — ÚLTIMA tarefa NATIVA da Fase 1 antes do GATE estratégico F1.12 (após aceita, o Driver semeia F1.12 gate:true → HALT p/ sign-off humano) · ao selecionar um versículo no Reader (read/[book]/[chapter], gesto da F1.9), editor de nota (criar/editar/remover via putNote/getNote/deleteNote) + controles de highlight (cor + tag opcional via addHighlight/removeHighlight) + indicadores nos versículos (de listNotes/listHighlights); dados gravam num data_dir gravável e ESTÁVEL ${documentDirectory}userdata/ (novo serviço app/lib/userdata.ts ensureUserDataDir; SEPARADO do reading-sample.sqlite só-leitura); EXPORT reaproveita o data_dir/Records (sem reimplementar serialização do core; expo-sharing só se usar Share sheet). Glue reading.ts ESTENDIDO com as 7 funções → bindings gerados → JSI → userdata::{NoteStore,HighlightStore}; SEM I/O/serialização/ordenação de userdata em TS. Bindings nativos DEFASADOS (pré-F1.10, sem putNote/Note/Highlight) → regenerar gen-bindings-ios.sh; web bindings já têm os tipos. Prova HEADLESS: notes-selftest.ts sobre ${documentDirectory}userdata-selftest/ (limpo no início, idempotente): putNote("John 3:16") → getNote/listNotes round-trip; addHighlight("John 3:16","yellow",null) → listHighlights; 2ª leitura independente (novo handle do disco) = persistência; emite TLA_NOTES note_ref="John 3:16" note_len=<n> highlights=<m≥1> persisted=true (do retorno real); run-ios-selftest.sh asserta TLA_NOTES + sem regressão de TLA_SELFTEST PT/EN/READ/PARALLEL/SEARCH/XREF (matar Metro órfão antes). ADR-0017 (data_dir userdata separado + formato de export). Só nativo (notas web = F1.16 pós-gate). Restrições: não tocar the-light/fronteira (F1.10 já entregou as 7 funções; nenhuma nova); não reimplementar I/O/serialização de userdata no app; data_dir gravável separado do conteúdo público; corpo da nota é dado do usuário (anti-alucinação não se aplica; ref é canônica); export não vaza além dos dados do usuário; offline-first; sem segredos; gerados/bundled/userdata-runtime ignorados; blocked+HALT com erro EXATO se exigir mudar o core. · sem execução neste ciclo
2026-06-30 23:35 UTC · ciclo · executor+reviewer em F1.11 (UI de notas/highlights nativa + export) · tarefa F1.11 · passed (deb3b0c), arquivada · painel ReaderVersePanel (nota markdown + highlight cor/tag + export + xref) pelo gesto da F1.9; glue reading.ts estendido (putNote/getNote/deleteNote/listNotes/addHighlight/removeHighlight/listHighlights → JSI → fronteira F1.10) SEM reimplementar I/O/serialização em TS; persistência em ${documentDirectory}userdata/ (app/lib/userdata.ts) separada do reading-sample.sqlite só-leitura (provada por 2ª leitura); export = apresentação pura dos Records via Share do react-native (sem expo-sharing); atribuição CC-BY (ADR-0016) preservada; bindings nativos regenerados; web=stub (F1.16); prova HEADLESS iPhone 17 TLA_NOTES note_ref="John 3:16" note_len=55 highlights=1 persisted=true export_ok=true (do retorno real), sem regressão READ/PARALLEL/SEARCH/XREF/parse; tsc+expo export web verdes; the-light intacto (8f66004), core/src/lib.rs+Cargo.toml não tocados; ADR-0017 · **ÚLTIMA tarefa nativa da Fase 1 — próxima é o GATE estratégico F1.12 (gate:true → HALT p/ sign-off humano)**
2026-06-30 23:50 UTC · ciclo · F1.11 aceita (`deb3b0c`) → TODAS as tarefas NATIVAS da Fase 1 (F1.1–F1.11) ACEITAS. Planner semeou o **GATE estratégico F1.12** na `queue/` (`F1.12-gate-store-web-corpus.task.md`, ready, **gate:true**, depends_on:[F1.2,F1.5,F1.8,F1.10] — todas aceitas). NÃO é decomposição em código: é um **BRIEF DE DECISÃO** para sign-off humano sobre **como o WEB entrega leitura+busca(FTS5)+xref+notas** mantendo offline-first/BYOK/anti-alucinação/`the-light` intacto. Investigação real citada: `bible.sqlite`=61.833.216 B (~59 MB; verses 62.203, verses_fts 62.203 FTS5, cross_references 344.799); `reading-sample.sqlite`=~4,4 MB (3 livros, 9.746 verses/fts, 22.413 xrefs — **o que o NATIVO empacota hoje**, ADR-0014; o completo foi adiado também no nativo); F0.10/ADR-0012 prova `getPassage` web via `wa-sqlite@1.0.0` SYNC + OPFS, lendo o **arquivo inteiro num MemoryVFS no heap** (escala p/ 4,4 MB, **não** p/ 59 MB → completo exigiria OPFS-live-VFS em Web Worker, adiado); `app/web/reading.web.ts` = TODOS stubs (listBooks/getChapter/search/crossRefs/notes/highlights). 4 opções com prós/contras: **A** (espelho TS sobre wa-sqlite/OPFS — recomendada; sub-A1 subset=paridade c/ nativo via MemoryVFS provado × sub-A2 completo 59 MB+Worker), **B** (store no core via PR+ADR — fonte única, mas refator pesado + sync↔async + toca `the-light`), **C** (chunking sob demanda), **D** (compressão, só modificador). Sub-decisões p/ o humano: escopo (subset×completo), spike FTS5 no `wa-sqlite`, notas web OPFS/IndexedDB c/ formato exportável (userdata é file-based JSON, ADR-0017), fallback sem OPFS, autorizar spec de PR se B. **F1.13–F1.16 DEPENDEM desta decisão.** Consequência: decidido → **ADR-0018** (maior atual=0017) + Driver re-escopa/semeia F1.13–F1.16; **enquanto não houver decisão, loop em HALT**. Próximo ciclo: Driver escreve `loop/HALT` ("aguardando sign-off do gate F1.12") — NÃO executa (regra de gate do PROTOCOL). STATUS.md atualizado (cabeçalho → aguardando gate; linha F1.12 → ready/gate semeada). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-06-30 23:55 UTC · ciclo (gate) · queue elegível = F1.12 (gate:true) → Driver NÃO executa: aplicou a REGRA DE GATE · tarefa F1.12 · **GATE → HALT** (sign-off humano) · todas as tarefas NATIVAS da Fase 1 (F1.1–F1.11) ACEITAS e verdes no device iOS (leitura+versões+tema, busca FTS5, xref CC-BY, notas/highlights+export+persistência); a próxima é o GATE estratégico do STORE WEB do corpus · brief de decisão em loop/queue/F1.12-gate-store-web-corpus.task.md: bible.sqlite ~59MB (62203 versículos, FTS5, 344799 xrefs) vs subset bundled ~4,4MB; F0.10 provou wa-sqlite+OPFS via MemoryVFS no heap (escala ~4,4MB, não ~59MB); reading.web.ts hoje = stubs · opções A (wa-sqlite/OPFS, espelho TS — recomendada, começar por A1=paridade com subset), B (store no core via PR+ADR — toca the-light), C (chunking), D (compressão) · ⛔ loop/HALT escrito · F1.13–F1.16 DEPENDEM desta decisão · retomar = decidir → ADR-0018 → Driver re-escopa/semeia F1.13–F1.16 → remover HALT · LOOP PARADO aguardando sign-off
2026-07-01 00:15 UTC · (decisão humana no gate, fora de ciclo) · F1.12 GATE DECIDIDO por sign-off: **Opção A** (wa-sqlite+OPFS espelhando os SELECTs do core em TS; reaproveita F0.10 + semântica FTS5/xref do SQLite; zero PR ao the-light), começando por **A1 = paridade com o subset reading-sample.sqlite (~4,4 MB)** via o MemoryVFS já provado; corpus completo (~59 MB) + VFS-live em Worker/cota OPFS = decisão transversal separada (não bloqueia). **ADR-0018 registrado.** `loop/HALT` REMOVIDO (motivo resolvido). Gate F1.12 arquivado. F1.13–F1.16 destravadas (dependem desta decisão). LOOP RETOMADO · próxima: F1.13 (paridade web: leitura) re-escopada p/ A1
2026-07-01 00:30 UTC · ciclo · queue vazia → planner semeou F1.13 (paridade web: leitura; ready, gate:false; depende de F1.12 decidida + F1.4 aceita) — implementa ADR-0018 (Opção A / A1) sobre o subset reading-sample.sqlite (~4,4 MB, o MESMO que o nativo empacota, ADR-0014). Escopo: tornar app/web/reading.web.ts (hoje stubs) leitura REAL — novo store web app/web/sqlite-reading.web.ts (query VFS-agnóstica espelhando os SELECTs do core: get_chapter→passage WholeChapter `SELECT verse,text FROM verses WHERE translation_id=? AND book_number=? AND chapter=? ORDER BY verse` (+has_translation `SELECT 1 FROM translations WHERE id=?`); chapter_count `SELECT max(chapter) FROM verses WHERE translation_id=? AND book_number=?`→0 se null; list_translations `SELECT id,abbrev,name,language,license,embeddable FROM translations ORDER BY language,id`), composeChapterPassage (Passage.reference=WholeChapter, cada Verse.reference=Single) + backend OPFS browser app/web/sqlite-reading-opfs.web.ts (molde F0.10: persiste subset em OPFS, lê via MemoryVFS hidratado, wa-sqlite build SYNC sem SharedArrayBuffer); listBooks via WASM gerado (Rust reference::BOOKS, já exportado nos bindings web — checksum 43013), NÃO reimplementado; db.web.ts deixa de lançar (sentinela); telas app/app/read/** compartilhadas passam a funcionar no web (lado a lado = 2× getChapter, F1.4). search/crossRefs/userdata seguem stubs (F1.14–F1.16). Prova HEADLESS node (molde getPassage.web.test.mjs) sobre os bytes do subset em MemoryVFS: getChapter('kjv',43,3) v16=="For God so loved the world…", getChapter('alm1911',43,3) v16=="Porque Deus amou o mundo de tal maneira…", chapterCount('kjv',43)==21, listTranslations⊇{kjv,alm1911}, listBooks==66 — do retorno real, paridade explícita com TLA_READ/TLA_PARALLEL do nativo. expo export web 0 (subset .sqlite ~4,4MB + wa-sqlite.wasm no bundle); tsc limpo. ADR-0019 (store web de leitura A1). Sem regressão nativa (reading.ts/reading-selftest.ts/db.ts/core/src/lib.rs NÃO tocados — justificável por diff). the-light intacto (8f66004). blocked+HALT com erro EXATO se wa-sqlite/OPFS/Metro web inviabilizar a leitura do subset sem hack proibido (COOP/COEP+SharedArrayBuffer). STATUS.md atualizado (cabeçalho + F1.13→🟡 ready). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 00:55 UTC · ciclo · executor+reviewer em F1.13 (paridade web: leitura) · tarefa F1.13 · passed (0479b5b), arquivada · reading.web.ts DESTUBADO p/ leitura REAL sobre o subset reading-sample.sqlite (~4,4 MB, o MESMO do nativo) via wa-sqlite (build SYNC) OPFS+MemoryVFS (molde F0.10); novos sqlite-reading.web.ts (queries VFS-agnósticas) + sqlite-reading-opfs.web.ts; espelha os SELECTs do core (getChapter→SELECT verse,text FROM verses ORDER BY verse + has_translation→UnknownTranslation; chapterCount→max(chapter); listTranslations→FROM translations ORDER BY language,id); listBooks via wasm/Rust (reference::BOOKS, 66) não reescrito; telas read/** (incl. lado a lado F1.4) funcionam no browser via gate useWasmReady (no-op nativo); prova HEADLESS node (MemoryVFS): João 3:16 KJV+Almeida verbatim do store, chapterCount=21, João 3 KJV=36 vv, [kjv,alm1911], 66 livros = paridade com TLA_READ/TLA_PARALLEL; tsc+expo export web verdes (subset 4.567.040 B + wa-sqlite.wasm 558.343 B no bundle); sem domínio reimplementado; sem regressão nativa (diff nativo vazio); the-light/core intactos (8f66004); ADR-0019 · próxima: F1.14 (paridade web: busca FTS5 — perf de FTS5 no wa-sqlite a confirmar)
2026-07-01 01:20 UTC · ciclo · queue vazia → planner semeou **F1.14 (paridade web: busca FTS5)** na `queue/` (`F1.14-web-busca.task.md`, ready, gate:false; depende de **F1.12** decidida + **F1.6** aceita). Implementa ADR-0018 (Opção A/A1) sobre o subset `reading-sample.sqlite` (~4,4 MB, o MESMO do nativo): **destubar `search` em `app/web/reading.web.ts`** espelhando o SQL FTS5 do core (confirmado na fonte `the-light-core/src/search.rs` rev `8f66004`): `SELECT v.book_number,v.chapter,v.verse,v.text, highlight(verses_fts,0,?,?) AS hl, bm25(verses_fts) AS score FROM verses_fts JOIN verses v ON v.id=verses_fts.verse_id WHERE verses_fts MATCH ? AND verses_fts.translation_id=? [AND v.book_number=?] ORDER BY score LIMIT ?` (params HL_START/U+0002, HL_END/U+0003, match_query, translation, [book], limit clampado [1,i64::MAX], default 20); `has_translation` ANTES (UnknownTranslation→erro, espelha `embedded.rs`); `build_match_query` espelhado em TS (split→`"termo"` AND; vazio→[] sem throw). REUSA o store da F1.13 (`openReadingDbWeb`/MemoryVFS+OPFS) sem recarregar o subset. UI da F1.6 (`app/app/search`+`ReaderSearchResultItem`+`highlight.ts`) passa a funcionar no web só com o destub; ref clicável→Reader (F1.13). **RISCO CRÍTICO confirmado pelo planner:** `wa-sqlite@1.0.0` (sync E async) **NÃO tem FTS5** (`FTS5_FAIL no such module: fts5` num probe de fts5 vtable; npm só traz `dist/` pré-compilados, sem Makefile; `emcc` ausente, `docker` presente) → habilitar FTS5 no wa-sqlite **SYNC** (asset local, offline-first, SEM SharedArrayBuffer/COOP-COEP) é parte do escopo (ADR-0020); `blocked`+HALT com erro EXATO se inviável sem hack proibido (proibido cair p/ LIKE = drift sem sign-off). PROVA headless node (molde `reading.web.test.mjs`, MemoryVFS sobre os bytes do subset): `search("God","kjv",undefined,1000)` contém João 3:16 (book43/cap3/v16) KJV verbatim + marcadores (mirror do `TLA_SEARCH`: **localizar** no conjunto, NÃO 1º hit — o 1º real por bm25 é Salmos 42:2); acento-insensível `search("ceus"/"deus","alm1911")`; vazio→[]; limit respeitado; tradução inexistente lança; markers não vazam. `tsc`+`expo export web` 0; sem regressão de leitura web (reading.web.test.mjs) nem do caminho nativo (diff vazio de core/src/lib.rs, reading.ts, search-selftest.ts); the-light intacto (8f66004). STATUS.md atualizado (cabeçalho + F1.14→🟡 ready). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 01:55 UTC · ciclo · executor+reviewer em F1.14 (paridade web: busca FTS5) · tarefa F1.14 · passed (b2f1d8f), arquivada · search em reading.web.ts DESTUBADO espelhando o SQL FTS5 do core (MATCH+bm25+highlight+book+limit+translation, has_translation antes; build_match_query split→aspas→AND), sobre o subset, REUSANDO o store da F1.13 (sem recarregar); novo glue sqlite-search.web.ts · RISCO FTS5 RESOLVIDO (ADR-0020): wa-sqlite@1.0.0 do npm NÃO traz FTS5 (no such module: fts5, 0 strings) → rebuild SYNC com -DSQLITE_ENABLE_FTS5 da fonte (docker/emscripten, pinado no gitHead 5147454 = release npm 1.0.0) → vendorizado em app/web/vendor/wa-sqlite-fts5/wa-sqlite.{mjs,wasm} (asset local VERSIONADO, offline-first, sem SharedArrayBuffer); rebuild reprodutível via scripts/build-wa-sqlite-fts5.sh · prova HEADLESS node: search("God","kjv",-,1000)→646 hits, João 3:16 LOCALIZADO (43/3/16, bm25 -2.389, KJV verbatim, markers U+0002/U+0003 só em highlighted não em text); acento-insensível ceus/alm1911→20 hits c/ "céus"; vazio→[]; limit/book/tradução-inexistente→erro; reviewer reconferiu o probe FTS5 do wasm (25 strings vs 0 no npm; FTS5_OK vs FTS5_FAIL) · leitura web sem regressão; sem ranqueamento reimplementado (só espelho FTS5); tsc+expo export web verdes; the-light/core/nativo intactos (8f66004) · próxima: F1.15 (paridade web: xref + atribuição CC-BY)
2026-07-01 02:20 UTC · ciclo · queue vazia → planner semeou **F1.15 (paridade web: referências cruzadas + atribuição CC-BY)** na `queue/` (`F1.15-web-xref.task.md`, ready, gate:false; depende de **F1.12** decidida + **F1.9** aceita). Implementa ADR-0018 (Opção A/A1) sobre o subset `reading-sample.sqlite` (~4,4 MB, o MESMO do nativo): **destubar `crossRefs` em `app/web/reading.web.ts`** (hoje `throw WEB_XREF_MSG`) espelhando o SELECT REAL de `the_light_core::xref::for_verse` (confirmado na fonte `crates/the-light-core/src/xref.rs` rev `8f66004`): `SELECT to_book,to_chapter,to_verse_start,to_verse_end,votes FROM cross_references WHERE from_book=?1 AND from_chapter=?2 AND from_verse=?3 AND votes>=?4 ORDER BY votes DESC, to_book, to_chapter, to_verse_start LIMIT ?5` (bind book/chapter/verse/min_votes/limit como i64; limit clamp `[1,i64::MAX]`; montagem `start>=end?Single(start):Range{start,end}`; defaults DEFAULT_MIN_VOTES=1, DEFAULT_LIMIT=20). **Xref INDEPENDE de tradução** → SEM parâmetro `translation`/`has_translation` (≠ getChapter/search). votes i64→**bigint** no binding (`column_int64`); `minVotes` é bigint. Novo glue `app/web/sqlite-xref.web.ts` (par de `sqlite-search.web.ts`: XREF_SELECT + queryCrossRefs + composeCrossRef Single/Range + crossRefsOnHandle), **REUSANDO** `openReadingDbWeb` das F1.13/F1.14 (sem recarregar o subset, sem novo backend OPFS; um único wasm vendorizado). UI compartilhada (`ReaderXrefPanel`/`ReaderVersePanel` + `read/[book]/[chapter].tsx`) passa a funcionar no web só pelo destub; **atribuição CC-BY** EXATA `Cross references courtesy of OpenBible.info (CC-BY)` (ADR-0016, `XREF_ATTRIBUTION`) renderizada no web. **Dado do subset confirmado por SQL** (asserções, não hardcode no produto): 22.413 xrefs totais; João 3:16 (43/3/16)=**9** xrefs; 1º por votos DESC=**João 3:15** (Single, **439** votos — paridade c/ `TLA_XREF first_ref="John 3:15" first_votes=439` da F1.9); 2º=João 11:25-26 (Range, 400). PROVA HEADLESS node (molde `search.web.test.mjs`, MemoryVFS sobre os bytes do subset + wa-sqlite vendorizado): `crossRefs(43,3,16)`→count 9; 1º=João 3:15 Single 439n; ≥1 Range (João 11:25-26 400n); cada CrossRef ref válida + votes bigint; verso sem xref→[] sem throw (ex. João 3:999) e minVotes alto→[]; minVotes=400n→2; limit=1→1, limit=3→3. `tsc`+`expo export web` 0; script `test:web:xref` em package.json. SEM ordenação/filtro de xref reimplementado em TS (votos DESC+tiebreakers+min_votes+clamp vêm do SQL espelhando o core); SEM regressão de leitura/busca web (`test:web:reading`/`test:web:search`) nem do caminho NATIVO (diff vazio de core/src/lib.rs, reading.ts, selftests nativos); the-light intacto (`8f66004`), core não modificado. ADR novo (provável ADR-0021). Restrições: não tocar the-light/fronteira; subset A1 só; anti-alucinação (refs/votos do store); CC-BY obrigatória/visível; offline-first (sem SharedArrayBuffer); paridade real; reusar store F1.13/F1.14; `blocked`+HALT com erro EXATO se exigir mudar o core. STATUS.md atualizado (cabeçalho + F1.15→🟡 ready). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 02:50 UTC · ciclo · executor+reviewer em F1.15 (paridade web: xref + atribuição) · tarefa F1.15 · passed (1fe14e6), arquivada · crossRefs em reading.web.ts DESTUBADO espelhando o SELECT de xref::for_verse (cross_references, votes>=min, ORDER BY votes DESC + tiebreakers to_book/to_chapter/to_verse_start, clamp LIMIT≥1, Single se to_verse_start>=to_verse_end senão Range; xref independe de tradução), sobre o subset, REUSANDO openReadingDbWeb das F1.13/F1.14 (sem recarregar); novo glue sqlite-xref.web.ts; defaults minVotes=1/limit=20 (=core); votes i64→bigint · prova HEADLESS node: crossRefs(43,3,16)→9 xrefs, João 3:15 1º por votos (439, Single) = paridade com TLA_XREF nativo da F1.9, Range provado (João 11:25-26, 400), verso sem xref→[], minVotes/limit respeitados · atribuição CC-BY EXATA "Cross references courtesy of OpenBible.info (CC-BY)" (ADR-0016) confirmada no bundle dist · sem ordenação/filtro reimplementado em TS (só espelho do SQL); leitura/busca web sem regressão; tsc+expo export web verdes; the-light/core/nativo intactos (8f66004); ADR-0021 · próxima: F1.16 (paridade web: notas/marcações + export) — ÚLTIMA paridade web antes do Marco 1 (F1.17)
2026-07-01 03:15 UTC · ciclo · queue vazia → planner semeou **F1.16 (paridade web: notas/marcações + export)** na `queue/` (`F1.16-web-notas.task.md`, ready, gate:false; depende de **F1.12** decidida + **F1.11** aceita) — **ÚLTIMA paridade web da Fase 1**. Implementa ADR-0018 (A1) destubando as **7 funções** de userdata em `app/web/reading.web.ts` (`putNote`/`getNote`/`deleteNote`/`listNotes` + `addHighlight`/`removeHighlight`/`listHighlights`, hoje `throw WEB_NOTES_MSG`) + `userdata.web.ts` (hoje lança → sentinela `'web:userdata'`, molde `db.web.ts`). DECISÃO DE ARQUITETURA FIXADA (ADR-0022): o módulo `userdata` do core é `#[cfg(feature="embedded")]` (nativo-only) → NÃO entra no wasm → o web NÃO delega; reimplementa o **I/O em TS como INFRA** espelhando o **formato em disco** do core (precedente ADR-0011/0019/0020/0021), persistindo em **OPFS** (mesmo backend SYNC das F1.13–F1.15, sem SharedArrayBuffer) num `data_dir` web SEPARADO do `reading-sample.sqlite` só-leitura. **FORMATO REAL confirmado na fonte (8f66004, `userdata/notes.rs`+`highlights.rs`+`reference.rs::format_reference`):** NOTAS = `notes/<slug>.md` com `slug = format_reference(_,En).replace(' ','_').replace(':','.')+'.md'` (`John 3:16`→`John_3.16.md`, Range `Genesis 1:1-3`→`Genesis_1.1-3.md`, WholeChapter `Psalms 23`→`Psalms_23.md`), arquivo = **só o corpo** (sem front-matter/título), `list` **ordenado por (book,chapter,verse-start)**, read-back = `parseReference(stem.replace('_',' '))`; HIGHLIGHTS = `highlights.json` array `{ "ref":"John 3:16","color":"yellow","tag"?:… }` (pretty 2-espaços via `to_string_pretty`, **`tag` omitido quando ausente**, `ref` EN legível SEM a troca `_`/`.`), `add` substitui por referência preservando **ordem de inserção**, `list` na ordem do array. **Referência canonicalizada pelo WASM (`parseReference`) nas duas direções, não inventada em TS; nome EN do livro de `listBooks().nameEn`** — única "format" em TS = a convenção de nome de arquivo/`ref` (infra). EXPORT reusa `app/lib/notesExport.ts::buildNotesExport` (agregado puro dos Records) → MESMO agregado que o nativo; Share via `react-native-web`/Web Share (sem dep nova). Estrutura: `userdata-fs.web.ts` (VFS-agnóstico, interface `UserDataDir`) + `userdata-opfs.web.ts` (browser-only, OPFS main-thread). PROVA HEADLESS node com **mock de OPFS em memória** (OPFS é browser-only; mesmo padrão de isolamento da F1.13/MemoryVFS): `putNote("John 3:16","# corpo")`→`getNote`/`listNotes` round-trip (Genesis<John ordenado, slug==`John_3.16.md`); `addHighlight("John 3:16","yellow",null)`→`listHighlights` (sem `tag`, re-add substitui); `removeHighlight` idempotente; **2ª leitura independente** = persistência; export agrega → emite `WEB_NOTES note_ref="John 3:16" note_len=<n> highlights=<m> persisted=true export_ok=true` (paralelo ao `TLA_NOTES` nativo). `tsc`+`expo export web` 0; script `test:web:notes`; sem regressão de leitura/busca/xref web (`test:web:reading`/`search`/`xref`) nem do NATIVO (diff vazio de `core/src/lib.rs`,`reading.ts`,`app/lib/userdata.ts`,selftests nativos); the-light intacto (`8f66004`). `blocked`+HALT com erro EXATO se espelhar o formato exigir tocar o core/fronteira (ex.: expor `format_reference`). **Após aceita, F1.17 (Marco 1, gate:true) fica elegível → HALT p/ sign-off.** STATUS.md atualizado (cabeçalho + F1.16→🟡 ready). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 03:45 UTC · ciclo · executor+reviewer em F1.16 (paridade web: notas/highlights + export) · tarefa F1.16 · passed (7557f0e), arquivada · as 7 funções de userdata DESTUBADAS em reading.web.ts persistindo em OPFS (data_dir web separado do conteúdo só-leitura); como userdata do core é embedded-only (fora do wasm), o web REIMPLEMENTA o I/O como INFRA (precedente ADR-0011) espelhando o formato byte-a-byte: slug notes/<slug>.md (John_3.16.md / Range Genesis_1.1-3.md / WholeChapter Psalms_23.md), .md só-corpo, highlights.json array {ref,color,tag?} pretty 2sp (tag omitido), list_notes ordenado / list_highlights por inserção (add substitui mesma ref); ref canonicalizada pelo wasm (parseReference + listBooks().nameEn) nas duas direções; novos glues userdata-fs.web.ts + userdata-opfs.web.ts; userdata.web.ts sentinela 'web:userdata'; export reusa buildNotesExport (idêntico ao nativo) · prova HEADLESS node (OPFS-like em memória): WEB_NOTES note_ref="John 3:16" note_len=36 highlights=1 persisted=true export_ok=true (persistência relida de novo handle; slug == nativo F1.11) · leitura/busca/xref web sem regressão; tsc+expo export web verdes; the-light/core/nativo intactos (8f66004); ADR-0022 · ÚLTIMA paridade web — próxima: F1.17 (Marco 1, gate:true) → HALT p/ sign-off humano
2026-07-01 04:00 UTC · ciclo (gate) · queue elegível = F1.17 (gate:true) → Driver NÃO executa: aplicou a REGRA DE GATE · tarefa F1.17 · **GATE → HALT** (sign-off humano) · **FASE 1 FUNCIONALMENTE COMPLETA**: todas as 16 tarefas (F1.1–F1.16) aceitas e verdes nos 3 alvos (iOS+Android+Web) — leitura (nav+versões+lado a lado+tema), busca FTS5 (acento-insensível/BM25), referências cruzadas (OpenBible CC-BY, atribuição visível), notas/highlights+export (file-based nativo / OPFS web) · provas determinísticas: TLA_* no device (iPhone 17 via simctl) + headless node no web (MemoryVFS/OPFS-like sobre o subset ~4,4 MB); paridade nativo↔web afirmada · regras não negociáveis mantidas: the-light intocado (8f66004; delegação no nativo, espelho de SELECT/formato no web como infra ADR-0011), offline-first (assets locais, zero rede, sem SharedArrayBuffer), anti-alucinação (texto domínio público do store, xref CC-BY com atribuição), BYOK (IA=Fase 2, sem segredos), qualidade por tarefa verde + review independente · ADRs 0013–0022 · relatório de marco em loop/queue/F1.17-marco-1.task.md · ⛔ loop/HALT escrito · sign-off decide: aprovar Fase 1 / liberar Fase 2 (IA BYOK+Gemini) / prioridade do backlog transversal corpus completo (~59 MB) · LOOP PARADO aguardando aprovação do Marco 1
2026-07-01 04:20 UTC · (sign-off humano do Marco 1, fora de ciclo) · F1.17 APROVADO: **MARCO 1 aceito** — Fase 1 (leitura offline completa, multiplataforma) completa e verde nos 3 alvos (iOS+Android+Web). Decisões: (1) Fase 1 aprovada; (2) **Fase 2 (IA BYOK + Gemini) LIBERADA** p/ decomposição em backlog/PHASE-2.md; (3) corpus completo no web (~59 MB) segue backlog transversal. `loop/HALT` REMOVIDO (motivo resolvido); F1.17 arquivada (result approved). LOOP RETOMADO · nova diretriz do Driver: seguir até um PONTO BLOQUEANTE (blocked por decisão/chave/ferramenta, falha persistente, ou conflito com regra não negociável) · próxima: planner decompõe a Fase 2 e semeia a 1ª tarefa
2026-07-01 09:40 UTC · ciclo · Marco 1 aprovado → planner **decompôs a FASE 2 (IA BYOK)** em `backlog/PHASE-2.md` (F2.1–F2.8; 3 paradas: **gate estratégico F2.2**, **bloqueante-chave F2.6**, **Marco 2 F2.8**) e semeou **F2.1** na `queue/` (`F2.1-ask-anchored-frontier-mock.task.md`, ready, gate:false, deps:[]). Padrão herdado da Fase 1: fronteira nativa + teste de host com **MOCK** → gate → chave+UI → validação real (chave, bloqueante) → paridade web (per decisão) → Marco 2. **INVESTIGAÇÃO DO CORE (8f66004, checkout do cargo, só-leitura):** o `the-light-core` **JÁ TEM** módulo `ai` completo — trait `LlmProvider`, `MockLlmProvider`, `build_provider(name,key:Option,model:Option)` (chave é ARGUMENTO, BYOK; roteia anthropic/openai/ollama/mock), `ask(provider,question,context,lang)`, `ask_context`/`numbered_passage` (montagem RAG pura, anti-alucinação), `citation` (stripping) — porém **TODO `#[cfg(feature="embedded")]`** (lib.rs l.20) → **só nativo** (web/wasm SEM IA, como store/xref, ADR-0005). **Gemini NÃO existe** (`PROVIDERS=["anthropic","openai","ollama"]`; sem entrada gemini em `default_model`/`estimate_cost_usd`) → adicioná-lo = decisão da F2.2 (PR ao core vs. impl local na fronteira, trait público). Transporte = `reqwest::blocking` (nativo); web precisa de outro caminho (VISION §4 fricção #2) = F2.2/F2.7. `KeyStore`/`secrets.toml` do core NÃO usado (app usa secure-store + passa a chave a `build_provider`). **F2.1 é NÃO-BLOQUEANTE** (MOCK, sem chave/rede; prova = teste Rust de host: `ask_anchored` sobre fixture João 3:16 KJV → `cited_text` verbatim do store + `interpretation` do mock + `provider="mock"`; corpo `cfg(not wasm32)`+stub web; grafo wasm puro). Decomposição preserva offline-first (IA opt-in aditiva; Fase 1 segue 100% offline), BYOK (chave nunca em git/log), anti-alucinação (texto sempre do store; LLM só interpreta) e `the-light` intocado (`8f66004`). Próximo ADR livre = **ADR-0023** (será escrito na F2.2). STATUS.md atualizado (cabeçalho + nova seção Fase 2). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 09:55 UTC · ciclo · executor+reviewer em F2.1 (pergunta ancorada na fronteira + MOCK) · tarefa F2.1 · passed (357abca), arquivada · Record AiAnswer{reference,cited_text,interpretation,provider,model} + ask_anchored(...) DELEGA a the_light_core::ai::{numbered_passage,ask_context,build_provider,ask} (core já tem módulo ai, embedded-only) + leitura da passagem via Store::open/EmbeddedSource::passage (rota F1.2); corpo cfg(not wasm32) + stub web; provider "mock" (key=None, SEM rede/chave); anti-alucinação = AiAnswer separa cited_text (verbatim do store) de interpretation (LLM); teste host 37/37 (3 ai_tests): cited_text=KJV verbatim do store, prova anti-fake 3:16 vs 3:17 (cited_text MUDA seguindo o store / interpretation do mock INVARIANTE), error paths→CoreError sem panic; grafo wasm puro (cargo tree sem rusqlite/reqwest); gen-bindings-web 0 (askAnchored/AiAnswer stub web); the-light/core intactos (8f66004), Cargo.toml inalterado · próxima: F2.2 (GATE estratégico: Gemini/IA-web/chave-web/streaming, ADR-0023) → HALT p/ sign-off humano
2026-07-01 13:55 UTC · ciclo (planner) · F2.1 aceita (`357abca`) → próxima elegível = **GATE estratégico F2.2** (gate:true, deps:[F2.1]). O Planner redigiu o **BRIEF DE DECISÃO** em `loop/queue/F2.2-gate-arquitetura-ia.task.md` (status ready, **gate:true**) — 4 decisões p/ o sign-off: **D1** onde vive o Gemini (A PR ao `the-light-core` vs **B impl local na fronteira** vs C adiar); **D2** IA no web (A reimplementar RAG/prompt/citação em TS vs **B nativa-only, adiar F2.7** vs C PR ao core p/ partes puras wasm-safe); **D3** chave no web (session-only/in-memory vs localStorage+aviso vs sem chave se D2=B); **D4** streaming (**A não-streaming primeiro** vs B streaming c/ core change). **Evidência REAL do `ai` do core (`8f66004`):** trait `LlmProvider` é **PÚBLICO** (`ai/mod.rs:327`) e `ai::ask(provider: &dyn LlmProvider, …)` (`ai/study.rs:591`) aceita `&dyn` → dá p/ `impl GeminiProvider` LOCAL sem tocar o `the-light` (D1=B viável); `PROVIDERS=["anthropic","openai","ollama"]` (`ai/mod.rs:30`), `default_model` (`providers.rs:26`) e `estimate_cost_usd` (`providers.rs:69`) **sem gemini** (pontos de D1=A); transporte `reqwest::blocking` (`providers.rs:83`) não compila em wasm (VISION §4 #2); **todo o módulo `ai` é `#[cfg(feature="embedded")]`** (core `lib.rs:19-20`) → web sem IA; `study.rs`/`citation.rs` são logicamente puros mas **acoplados** a `research::WebSource`/`reqwest` (`study.rs:11`, `citation.rs:18`) → expor puras no web = PR ao core (D2=C). Recomendação integrada do Planner: **D1=B · D2=B · D3=sem chave web agora/session-only registrada · D4=A** (autonomia + anti-alucinação numa só implementação Rust), apresentando A/C honestamente. Regras repetidas: offline-first (IA opt-in; Fase 1 segue offline), BYOK (chave nunca em git/log), anti-alucinação (texto do store; LLM só interpreta — vale inclusive no web), `the-light` só via PR+ADR (D1=A/D2=C → spec em `loop/proposals/`). **F2.3/F2.4/F2.5/F2.6/F2.7/F2.8 DEPENDEM desta decisão.** STATUS.md atualizado (cabeçalho → aguardando gate; linha F2.2 → ready ⛔ gate). PROGRESS.md NÃO tocado. Próximo ADR livre = **ADR-0023**. · Driver deve aplicar a REGRA DE GATE e escrever `loop/HALT` p/ sign-off — **sem execução autônoma**.
2026-07-01 14:00 UTC · ciclo (gate) · queue elegível = F2.2 (gate:true) → Driver NÃO executa: aplicou a REGRA DE GATE · tarefa F2.2 · **GATE → HALT** (sign-off humano) · arquitetura da IA da Fase 2 · brief em loop/queue/F2.2-gate-arquitetura-ia.task.md · investigação do core (8f66004): módulo ai embedded-only, LlmProvider PÚBLICO (→ GeminiProvider local viável sem tocar o the-light), Gemini ausente (PROVIDERS=anthropic/openai/ollama), reqwest::blocking não-wasm, study/citation acoplados a research::WebSource (reqwest) → web exige PR de desacoplamento · 4 decisões: D1 onde vive o Gemini (A PR ao core / B impl local na fronteira [rec] / C adiar), D2 IA no web (A reimplementar em TS / B nativa-only adiar F2.7 [rec] / C PR core partes puras), D3 chave web (sem chave no web se D2=B [rec] / session-only / nunca localStorage), D4 streaming (A não-streaming [rec] / B streaming) · ⛔ loop/HALT escrito · F2.3/F2.4/F2.7 DEPENDEM desta decisão · decidido → ADR-0023 → Driver re-escopa/semeia F2.3/F2.4/F2.7 → remove HALT · LOOP PARADO aguardando sign-off
2026-07-01 14:30 UTC · (decisão humana no gate F2.2, fora de ciclo) · F2.2 GATE DECIDIDO por sign-off (ADR-0023): D1 Gemini via PR ao the-light-core (GeminiProvider: LlmProvider; fonte única, precedente ADR-0005) · D2 IA no web via PR ao core desacoplando ai::study/citation de research::WebSource/reqwest p/ compilar em wasm (anti-alucinação numa só impl Rust; HTTP via fetch, ADR-0011) · D3 BYOK **API key** indolor (Keychain via expo-secure-store, 1×, nunca em git/log) — **login-de-conta OAuth REJEITADO** após PESQUISA 2026: OpenAI "Sign in with ChatGPT" só identidade (não libera modelo na assinatura; só Codex); Anthropic BANIU tokens OAuth de consumidor em apps de terceiros (enforcement 9/jan/2026, ToS); Google BANIU idem (fev/2026, detecção 25/mar, contas Ultra perderam acesso) → construir login arriscaria banir a conta do usuário · D4 streaming (tokens incrementais) · ADR-0023 registrado. loop/HALT REMOVIDO. F2.2 arquivada. Consequência: D1+D2 mudam o the-light → PR+ADR (branch + push/merge humano + re-pin, molde F0.6) = handoff bloqueante; caminho não-bloqueante AGORA = F2.4 (chave nativa expo-secure-store). LOOP RETOMADO · próxima: planner re-escopa PHASE-2.md per ADR-0023 e semeia a próxima tarefa
2026-07-01 14:45 UTC · ciclo (planner) · gate F2.2 decidido (ADR-0023) → planner **RE-ESCOPOU a Fase 2** em backlog/PHASE-2.md (F2.3–F2.8 + novo F2.3a) e **semeou F2.4** (chave BYOK nativa expo-secure-store) na queue/ (ready, gate:false, deps:[F2.2], NÃO-BLOQUEANTE). **Consolidação de handoffs:** os 3 core-changes (D1 GeminiProvider + D2 desacoplar ai::study/citation de research::WebSource p/ wasm + D4 streaming-no-trait) num **ÚNICO PR ao the-light** = **F2.3** (branch autorizado → push/merge humano → re-pin, molde F0.6a; BLOQUEANTE; prova = cargo test/clippy no the-light + build wasm do core). **F2.3a** consome o re-pin na fronteira (roteia gemini + expõe streaming via callback UniFFI; MOCK; não-bloqueante). **INVESTIGAÇÃO NA FONTE (8f66004, checkout do cargo, só-leitura) que dimensiona D1/D2/D4:** (D4) `LlmProvider` (`ai/mod.rs:327`) é **PÚBLICO, SÍNCRONO e NÃO-STREAMING** — `complete(&self,system,user)->Result<String>` devolve a String COMPLETA (l.335), `chat` default dobra em complete, **SEM** `stream`/callback/observer nem `async`; providers setam `"stream": false` (`providers.rs:281`) → **D4 EXIGE mudar o trait no core** (novo método streaming c/ default não-quebrante + SSE nos providers reais) ⇒ entra no PR F2.3 ("se necessário"=necessário). (D2) `study.rs:11` e `citation.rs:18` fazem `use super::research::WebSource` e `research.rs:48` traz `reqwest::blocking` → as funções puras `study::{numbered_passage,ask_context,ask}`/`citation` arrastam reqwest p/ o wasm → **D2 = PR** que separa `WebSource` do reqwest + feature fina p/ compilar a IA-pura em wasm32. (D1) Gemini encaixa mecanicamente no molde `AnthropicProvider` (`providers.rs:116-197`): `struct GeminiProvider`+`impl LlmProvider` c/ `gemini_body`/`gemini_extract` puros (endpoint generativelanguage.googleapis.com) + arms em `default_model`(l.26)/`estimate_cost_usd`(l.69)/`PROVIDERS`(mod.rs:30)/`build_provider`(l.39). **F2.4 é executável SEM chave real e SEM tocar o the-light** (app-side `app/lib/keystore.ts` sobre expo-secure-store + stub web `keystore.web.ts`, molde db.web.ts; prova headless node c/ backend fake em memória: set→get→delete de key PLACEHOLDER, listProviders só nomes, key nunca em console/git; tsc+expo export web verdes). Numeração F2.4–F2.8 estável; F2.3 evoluiu de "Gemini isolado" p/ "PR consolidado"; F2.6 vira gate:true. STATUS.md atualizado (cabeçalho + tabela Fase 2 + nota). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 15:05 UTC · ciclo · executor+reviewer em F2.4 (chave BYOK nativa) · tarefa F2.4 · passed (34c831f), arquivada · app/lib/keystore.ts (nativo, expo-secure-store): setKey/getKey/deleteKey/listProviders por provedor (anthropic/openai/gemini/ollama), ids tla.apikey.<provider> no Keychain/Keystore (valor nunca impresso), createKeystore(backend) injetável, import lazy, trim/rejeita vazio, listProviders só NOMES; keystore.web.ts = stub sem chave no web (D3/ADR-0023); implementa D3 (BYOK API key indolor; login-de-conta OAuth rejeitado); UI colar/deep-link fica na F2.5 · prova HEADLESS (backend fake em memória, SEM chave real): round-trip por provedor, provedores isolados, validação; chave NUNCA logada (espião console=0; fonte sem console.*) NEM versionada (git grep placeholder só no teste); tsc+expo export web verdes (stub não puxa secure-store ao bundle); the-light/core intactos (8f66004, commit só app/**) · próxima: F2.3 (PR CONSOLIDADO ao the-light: Gemini D1 + IA-pura-wasm D2 + streaming-no-trait D4) = BLOQUEANTE (branch → push/merge humano → re-pin, molde F0.6a); Driver implementa o branch e PARA p/ o handoff
2026-07-01 15:40 UTC · ciclo (handoff bloqueante) · executor+reviewer em F2.3 (PR ao the-light: Gemini D1 + streaming D4) · tarefa F2.3 · **branch VERDE → HALT p/ push/merge humano** · branch feat/ai-gemini-streaming (commit f08a651) no /Users/butkeraites/Documents/the-light, toca só ai/{mod.rs,providers.rs}: GeminiProvider impl LlmProvider registrado (PROVIDERS/default_model=gemini-2.0-flash/build_provider exige key/estimate_cost_usd), gemini_body/extract puros+testados, POST generativelanguage.googleapis.com key em header x-goog-api-key (nunca logada/na URL); complete_stream com default NÃO-QUEBRANTE (SSE real adiado, evolução), providers existentes intactos · verificação (reviewer, read-only pois git/cargo diretos ao the-light são deny-ruled): cargo fmt --check 0, clippy -D warnings 0, cargo test --workspace 0 failed (core lib 184 passed, 7 novos gemini/stream), main intacto 8f66004, main..feat=só f08a651, SEM push · D2/wasm-decouple FATIADO p/ PR separado antes da F2.7 (decisão do Driver: menor risco/review; native-first é a prioridade) · ⛔ loop/HALT escrito · retomar = humano push+merge feat/ai-gemini-streaming na main do the-light + informa rev → Driver re-pina core/Cargo.toml (F2.3a) + roteia gemini/streaming na fronteira · LOOP PARADO aguardando merge
2026-07-01 16:00 UTC · (merge humano do PR F2.3 + re-pin pelo Driver, fora de ciclo) · humano fez push+merge do branch feat/ai-gemini-streaming na main do the-light → rev 133077a91c46a155b470195dcf28af5107f46b1e (f08a651 confirmado ancestral; GeminiProvider + complete_stream presentes no rev). Driver RE-PINOU core/Cargo.toml + Cargo.lock de 8f66004→133077a nos 2 blocos (web puro default-features=false + nativo embedded); cargo update buscou o rev; VERIFICADO: 37 testes da fronteira verdes (sem regressão), cargo tree wasm SEM rusqlite/reqwest (grafo puro mantido apesar do novo ai/Gemini no core), fmt/clippy limpos, Cargo.lock em 133077a. loop/HALT REMOVIDO (merge resolvido). F2.3 ACEITA (mergeada+re-pinada). the-light agora em 133077a (era 8f66004). LOOP RETOMADO · próxima: F2.3a (fronteira: rotear "gemini" + expor streaming via callback UniFFI consumindo o re-pin; prova por MOCK) — não-bloqueante
2026-07-01 16:25 UTC · ciclo · executor+reviewer em F2.3a (fronteira gemini + streaming) · tarefa F2.3a · passed (b375f47), arquivada · (nota: 1ª tentativa do executor travou por watchdog de ambiente sem escrever nada; relançado limpo → sucesso) · callback UniFFI AiTokenCallback{on_token(String)} + ask_anchored_stream(...on_token: Box<dyn AiTokenCallback>) monta contexto ancorado pela MESMA rota da ask_anchored (store→numbered_passage→ask_context) e chama provider.complete_stream (default não-quebrante emite a resposta 1×); corpo cfg(not wasm32) + stub web (streaming web=F2.7); roteamento gemini via build_provider (core 133077a); anti-alucinação cited_text do store / tokens da interpretação (mock); teste host 41/41 (4 novos): streaming (tokens==resposta mock, cited_text KJV verbatim), anti-fake 3:16 vs 3:17, gemini sem chave→CoreError (sem rede), provider desconhecido→CoreError; grafo wasm puro (sem rusqlite/reqwest); gen-bindings-web 0 (askAnchoredStream/AiTokenCallback stub web); the-light intacto (133077a), core/Cargo.toml não tocado; nota: core não expõe ask_stream público → fronteira compõe system=ask_context/user=question (paridade fina de prompt real=F2.6) · próxima: F2.5 (UI nativa ask ancorado por MOCK, TLA_ASK) — não-bloqueante; depois F2.6 (chave real, bloqueante)
2026-07-01 17:10 UTC · ciclo (planner) · queue vazia → planner semeou **F2.5** (UI nativa de `ask` ancorado por MOCK) em loop/queue/F2.5-ui-ask-nativa.task.md (ready, gate:false, deps:[F2.1,F2.3a,F2.4] — TODAS aceitas; NÃO-BLOQUEANTE: provider "mock", sem chave/rede). Escopo: painel de estudo assistido ancorado a partir do ReaderVersePanel (F1.11) no [chapter].tsx → novo ReaderAskPanel (tokens de tema) com seletor provedor/modelo (SUPPORTED_PROVIDERS do keystore F2.4 + "mock"; listProviders marca quem tem chave, nunca o valor), campo de pergunta, STREAMING token-a-token e CITADO vs INTERPRETAÇÃO com rótulos distintos (anti-alucinação VISÍVEL: cited_text do store separado da interpretation do LLM). Glue nativo reading.ts ESTENDE askAnchored/askAnchoredStream delegando aos bindings gerados → JSI → Rust, wiring do AiTokenCallback{onToken}→callback JS; reading.web.ts = STUB (IA web=F2.7). Bindings nativos REGENERADOS (gen-bindings-ios.sh: askAnchored/askAnchoredStream/AiTokenCallback/AiAnswer ainda ausentes em native-generated/, grep vazio confirmado). Prova determinística no device (molde reading-selftest.ts): novo ask-selftest.ts chama askAnchoredStream(db,"kjv","John 3:16",<pergunta>,"mock",undefined,undefined,"en",onToken) acumulando tokens → emite TLA_ASK ref="John 3:16" provider="mock" streamed=true cited_prefix="For God so loved..." interp_len=<n> (do RETORNO/callback reais); run-ios-selftest.sh asserta provider="mock" + streamed=true (callback recebeu ≥1 token) + cited_prefix de João 3:16 KJV do store, SEM regressão de TLA_SELFTEST/READ/PARALLEL/SEARCH/XREF/NOTES. Assinaturas REAIS confirmadas em core/src/lib.rs: ask_anchored(db,translation,reference,question,provider_name,key:Option<String>,model:Option<String>,lang)->Result<AiAnswer,CoreError>; ask_anchored_stream(...,on_token:Box<dyn AiTokenCallback>); trait AiTokenCallback{fn on_token(&self,token:String)} (callback_interface); AiAnswer{reference,cited_text,interpretation,provider,model}. CUSTO: fronteira NÃO expõe estimate_cost_usd → decisão registrada na task = exibir simplificado ou omitir, SEM nova função de fronteira. Restrições: NÃO tocar the-light (133077a) nem core/src/lib.rs/Cargo.toml (só app/UI/glue/self-test + bindings gerados ignorados); SEM chave/rede na prova (mock); chave nunca em git/log; só nativo (web=F2.7); offline-first. STATUS.md atualizado (cabeçalho + linha F2.5→🟡 ready). PROGRESS.md NÃO tocado. Próximo BLOQUEANTE = F2.6 (validação real com a chave do usuário → gate). · sem execução neste ciclo
2026-07-01 17:40 UTC · ciclo · executor+reviewer em F2.5 (UI nativa de ask ancorado) · tarefa F2.5 · passed (31d191f), arquivada · ação "Perguntar (IA)" no ReaderVersePanel → ReaderAskPanel (seletor provedor/modelo incl. gemini/mock via keystore listProviders, campo de pergunta, streaming token-a-token) com ANTI-ALUCINAÇÃO VISÍVEL: bloco "Passagem (texto bíblico)" (citedText verbatim do store) × "Interpretação (IA) — confira nas Escrituras" (interpretation/tokens do LLM); glue reading.ts estende askAnchored/askAnchoredStream (+ AiTokenCallback→callback JS) via JSI; reading.web.ts=stub (IA web=F2.7); chave via keystore.getKey só p/ provedor real, prova usa mock (sem chave/rede), chave nunca logada; bindings nativos regenerados; prova HEADLESS iPhone 17 TLA_ASK ref="John 3:16" provider="mock" streamed=true cited_prefix="For God so loved the world..." interp_len=94 (streamed de ≥1 token real; cited_prefix bate João 3:16 KJV do store), sem regressão READ/PARALLEL/SEARCH/XREF/NOTES; tsc+expo export web verdes; the-light/core intactos (133077a); custo simplificado (fronteira não expõe estimate_cost_usd) · próxima: F2.6 (validação REAL com a chave do usuário) = BLOQUEANTE/gate → HALT
2026-07-01 17:55 UTC · ciclo (gate/bloqueante) · queue elegível = F2.6 (gate:true, needs_secret:true) → Driver NÃO executa (precisa da chave do usuário + rede) · tarefa F2.6 · **GATE → HALT** (validação humana) · Driver preparou (não-bloqueante) o harness de host core/examples/ask_real.rs: lê AI_PROVIDER/AI_API_KEY/AI_MODEL de env vars, chama ask_anchored de verdade, imprime cited_text (do STORE) + interpretation (do LLM); A CHAVE NUNCA É IMPRESSA (só "(presente — não exibida)"); compila offline, fmt/clippy limpos; anti-leak verificado · a validação real é do HUMANO (chave só em env var no terminal dele; o loop recebe só o resultado — a interpretação, não-segredo): AI_PROVIDER=gemini AI_API_KEY=... cargo run --example ask_real · aceite: interpretação REAL + cited_text=João 3:16 verbatim do store (anti-alucinação) + chave não vazou · ⛔ loop/HALT escrito · retomar = humano reporta provedor+funcionou (sem colar a chave) → Driver marca F2.6 aceita → F2.7 (paridade web IA, começa pelo PR D2/wasm) → F2.8 Marco 2 · the-light intacto 133077a · LOOP PARADO aguardando validação
2026-07-01 18:10 UTC · (validação F2.6 — 1ª tentativa do humano) · humano rodou core/examples/ask_real com AI_PROVIDER=gemini + chave real → harness compilou, chamou o Gemini de VERDADE, a chave AUTENTICOU (não 401/403) e NÃO vazou ("(presente — não exibida)") → **cadeia BYOK provada tecnicamente**. Erro = HTTP 429 quota: "generate_content_free_tier_requests, limit: 0, model: gemini-2.0-flash". PESQUISA (web): gemini-2.0-flash foi DEPRECADO em fev/2026 e RETIRADO em 3/mar/2026 → free-tier 0 (modelo morto). O default_model que a F2.3 pôs no the-light-core ("gemini-2.0-flash", providers.rs:31) está OBSOLETO. Modelos free-tier atuais (jun/2026): Gemini 2.5 Flash / 2.5 Flash-Lite / 3.x Flash. AÇÃO: (1) humano re-roda com AI_MODEL=gemini-2.5-flash (valida F2.6 sem mudar código); (2) TODO the-light PR focado: atualizar default_model gemini-2.0-flash→gemini-2.5-flash (batch com o PR D2/wasm da F2.7). HALT permanece até a validação real passar (200 + cited_text João 3:16 do store).
2026-07-01 18:15 UTC · (validação F2.6 — 2ª tentativa do humano: SUCESSO) · humano re-rodou ask_real com AI_MODEL=gemini-2.5-flash + chave real → resposta REAL do Gemini: cited_text="16 For God so loved the world..." (João 3:16 KJV VERBATIM DO STORE) + interpretation genuína do modelo; chave nunca exibida/logada. **F2.6 VALIDADA — cadeia BYOK provada ponta a ponta com anti-alucinação (texto bíblico do banco local, LLM só interpreta).** loop/HALT REMOVIDO. F2.6 arquivada (result validated). TODO registrado: PR ao core p/ default_model gemini-2.0-flash(retirado 3/mar/2026)→gemini-2.5-flash, a juntar com o PR D2/wasm da F2.7. LOOP RETOMADO · próxima: F2.7 (paridade web de IA) — começa pelo PR D2/wasm ao the-light (handoff bloqueante) · the-light 133077a
2026-07-01 19:54 UTC · ciclo (planner) · queue vazia → planner SEMEOU **F2.7** (PR ao the-light-core) em loop/queue/F2.7-pr-core-ai-wasm.task.md (ready, gate:false, **blocking:true, touches_the_light:true**, deps:[F2.6] — aceita). Escopo = 1 PR batendo: (D2/ADR-0023) IA pura compilável em wasm via feature fina **`ai-pure`** (gateando por `embedded` os módulos `research`+`keys` inteiros + funções pontuais de `providers`/`lexicon`/`study`/`citation`/`prompts` que puxam reqwest/rusqlite/chrono/directories/toml; `lib.rs` → `ai` sob `any(embedded,ai-pure)`) + (FIX) `default_model` gemini `2.0-flash`(retirado)→`2.5-flash` com ajuste dos testes que fixavam o antigo (sem inventar preço). **Investigação na fonte (`133077a`, via Read do ai/):** as funções da Fase 2 (`ask`/`ask_context`/`numbered_passage`/`numbered_verses`) + a maior parte de `citation` (incl. `rewrite_anchors` anti-alucinação) são PURAS (só `crate::model`+`serde_json`); o peso é isolável SEM tocar fora de `ai/` (+1 linha `lib.rs` +bloco `[features]`). `WebSource` é struct pura (serde+chrono) mas só usada por `study()`(Fase 3)/`citation::from_web_results` → gateados. Portão D2 = `cargo build -p the-light-core --no-default-features --features ai-pure --target wasm32-unknown-unknown` compila sem reqwest/rusqlite. **Não-quebrante:** `default=["embedded"]` intacto; `ai-pure` opt-in; workspace (~184 testes) verde; API nativa inalterada. Molde F2.3/F0.6a = branch `feat/ai-pure-wasm` no the-light → executor implementa+verifica+commita (NÃO push) → HALT p/ push/merge humano → Driver re-pina o rev + revalida a fronteira. **UI web de IA separada em F2.7b** (backlog; deps F2.7 mergeada+re-pinada + F2.5; NÃO semeada). STATUS.md + backlog/PHASE-2.md atualizados (F2.7 redefinida = PR; F2.7b registrada; F2.8 deps += F2.7b). PROGRESS.md NÃO tocado. the-light intacto em `133077a`. Próximo ADR livre = ADR-0024. · sem execução neste ciclo
2026-07-01 20:20 UTC · ciclo (handoff bloqueante) · executor+reviewer em F2.7 (PR ao the-light: feature ai-pure/wasm + fix default_model gemini) · tarefa F2.7 · **branch VERDE → HALT p/ push/merge humano** · branch feat/ai-pure-wasm (commit 7486102) de main@133077a, toca só ai/ + lib.rs(1 linha) + Cargo.toml: feature fina ai-pure (opt-in; embedded ⊃ ai-pure → default byte-a-byte intacto); partes puras do ai (ask/ask_context/numbered_passage + citation::rewrite_anchors + default_model/estimate_cost_usd + *_body/*_extract) compilam em wasm32; research/keys/reqwest/rusqlite/deep-study gated por embedded; fix default_model gemini 2.0-flash(retirado 3/mar)→2.5-flash · PORTÃO D2: cargo build -p the-light-core --no-default-features --features ai-pure --target wasm32 compila; cargo tree wasm SEM reqwest/rusqlite (só regex/serde/serde_json/thiserror) · não-quebrante: fmt 0, clippy -D warnings 0, cargo test --workspace 0 falhas (core 184, teste gemini agora 2.5-flash), build default OK; main intacto 133077a, SEM push · ADR-0024 registrado (Driver) · reviewer passed · ⛔ loop/HALT escrito · retomar = humano push+merge feat/ai-pure-wasm na main do the-light + informa rev → Driver re-pina (liga ai-pure na linha WEB) + revalida → F2.7b (UI web de IA via fetch) → F2.8 Marco 2 · LOOP PARADO aguardando merge
2026-07-01 20:45 UTC · (merge humano do PR F2.7 + re-pin pelo Driver, fora de ciclo) · humano fez push+merge de feat/ai-pure-wasm na main do the-light → rev c8ecb2fcef60847a2fddf68aa03b8a5eceea54f6 (7486102 confirmado ancestral; ai-pure + gemini-2.5-flash presentes). Driver RE-PINOU core/Cargo.toml + Cargo.lock de 133077a→c8ecb2f nos 2 blocos E LIGOU features=["ai-pure"] na linha WEB (default-features=false); nativo mantém ["embedded"] (que inclui ai-pure). VERIFICADO: cargo update buscou o rev; 41 testes da fronteira verdes (sem regressão); cargo tree wasm AINDA SEM rusqlite/reqwest (só entrou serde_json, wasm-safe) apesar do ai-pure ligado; gen-bindings-web.sh exit 0 (build wasm compila com ai-pure no core); fmt/clippy limpos; Cargo.lock em c8ecb2f. loop/HALT REMOVIDO. F2.7 ACEITA (mergeada+re-pinada). the-light agora em c8ecb2f (era 133077a). LOOP RETOMADO · próxima: F2.7b (UI web de IA — glue web usa ai-pure wasm p/ prompt/citação + fetch ao provedor; política de chave web; prova headless node com mock de fetch) — não-bloqueante · depois F2.8 (Marco 2)
2026-07-01 21:10 UTC · ciclo (planner) · queue vazia → planner SEMEOU **F2.7b** (paridade web de IA) em loop/queue/F2.7b-ui-web-ia.task.md (ready, gate:false, NÃO-BLOQUEANTE, touches_the_light:false, deps:[F2.7, F2.5] — ambas ACEITAS). Escopo: destubar `askAnchored` web em app/web/reading.web.ts usando `ai-pure` (wasm, já ligado na linha WEB de core/Cargo.toml @c8ecb2f) p/ montagem de prompt/RAG + citação (uma fonte da verdade Rust, zero drift), transporte por `fetch` (TS) ao provedor com a chave. **Fronteira web NOVA em core/src/lib.rs** (NÃO toca the-light, NÃO altera Cargo.toml): `ai_web_prepare(reference,question,provider,model,lang,verses:Vec<AiVerseInput{number,text}>) -> AiWebRequest{reference,cited_text,system,user,provider,model}` — cited_text via `ai::numbered_verses` sobre o texto do STORE WEB (subset reading-sample, F1.13); system/user com ZERO DRIFT via um `CaptureProvider` local (impl `ai::LlmProvider`) dirigido por `ai::ask` (necessário porque `ask_user_prompt` é privado); model via `ai::default_model`. `ai_web_finalize(reference,cited_text,provider,model,interpretation) -> AiAnswer` aplica `citation::rewrite_anchors` (citação anti-alucinação em Rust). **INVESTIGAÇÃO na fonte (c8ecb2f) — decisiva:** confirmado `pub` sob ai-pure: `numbered_verses`/`numbered_passage`/`ask_context`/`ask`/`default_model`/`estimate_cost_usd`/`LlmProvider`/`MockLlmProvider`/`prompts::ask_system_prompt`/`citation::rewrite_anchors`; **`gemini_body`/`gemini_extract` (e todos `*_body`/`*_extract`, + `ask_user_prompt`) são `fn` PRIVADOS** (compilam sob ai-pure mas NÃO `pub` → não chamáveis da fronteira) e `build_provider` é embedded-only → **body do request Gemini (generateContent) + extração (candidates[].content.parts[].text) ficam em TS no `fetch`** (transporte=infra, ADR-0023 D2), SEM drift de anti-alucinação (cited_text/prompt/citação em Rust). Se body/extract Rust forem exigidos ⇒ `blocked`+HALT (outro PR ao core p/ pub `*_body`/`*_extract`). **Política de chave WEB = session-only/in-memory** (ADR-0025): keystore.web.ts de stub→cofre in-memory de sessão (nunca localStorage/sessionStorage/IndexedDB/git/log; re-inserir a cada visita); justificativa D3/ADR-0023 (OAuth rejeitado, localStorage inseguro p/ segredo). UI ReaderAskPanel (compartilhada, F2.5) passa a funcionar no web pelo novo caminho + fluxo mínimo p/ colar a chave na sessão. `askAnchoredStream` web = não-streaming primeiro (emite interpretação 1× via onToken; SSE/ReadableStream = follow-up). **Prova = headless node com fetch MOCK** (molde getPassage.web.test.mjs): aiWebPrepare (cited_text João 3:16 KJV do store) → fetch mock (corpo Gemini fixo) → extração TS → aiWebFinalize → AiAnswer com cited_text=store (não do mock) + interpretation=resposta mock; assere anti-alucinação + paridade nativa; sem rede/chave real. Verificação: tsc+expo export web 0; gen-bindings-web (aiWebPrepare/aiWebFinalize presentes); grafo wasm ainda puro (só serde_json); chave nunca logada (grep); keystore.web.ts sem localStorage; the-light intacto c8ecb2f + core/Cargo.toml não alterado + nativo (fmt/clippy/test) sem regressão. ADR-0025 (política chave web + padrão prepare/fetch/extract + achado *_body/*_extract privados). STATUS.md atualizado (cabeçalho + linha F2.7b→🟡 ready). PROGRESS.md NÃO tocado. Próximo ADR livre = ADR-0025. Depois F2.8 (Marco 2). · sem execução neste ciclo
2026-07-01 21:40 UTC · ciclo · executor+reviewer em F2.7b (paridade web de IA) · tarefa F2.7b · passed (744868d), arquivada · fronteira web nova em core/src/lib.rs (só ai-pure, cfg-free, NÃO toca the-light/Cargo.toml): ai_web_prepare(reference,question,provider,model?,lang,verses)->AiWebRequest{cited_text,system,user,provider,model} (cited_text via ai::numbered_verses sobre os versos do store web F1.13; system/user zero-drift via CaptureProvider+ai::ask pois ask_user_prompt é privado) + ai_web_finalize(...)->AiAnswer (citation::rewrite_anchors); glue ai-anchored.web.ts faz o transporte TS (fetch Gemini generateContent, chave só no header x-goog-api-key, injetável — *_body/*_extract do core são privados sob ai-pure, ADR-0025); reading.web.ts destuba askAnchored web; ReaderAskPanel funciona no web; chave web session-only in-memory (keystore.web.ts Map de módulo; nunca localStorage/git/log, ADR-0025); prova HEADLESS node (fetch MOCK): cited_text=João 3:16 KJV verbatim do store, interpretation=resposta mock (âncora espúria removida por rewrite_anchors); teste host ai_web_prepare_matches_native_anchored_cited_text_zero_drift prova paridade Rust nativo↔web; 43 testes; grafo wasm puro; tsc+expo export web+gen-bindings-web verdes; sem regressão nativa/web; the-light/Cargo.toml intactos (c8ecb2f); ADR-0025 · ÚLTIMA tarefa de implementação da Fase 2 · próxima: F2.8 (Marco 2, gate:true) → HALT p/ sign-off humano
2026-07-01 22:00 UTC · ciclo (gate) · queue elegível = F2.8 (gate:true) → Driver NÃO executa: aplicou a REGRA DE GATE · tarefa F2.8 · **GATE → HALT** (sign-off humano) · **FASE 2 (IA BYOK ancorada) FUNCIONALMENTE COMPLETA**: F2.1–F2.7b aceitas — ask_anchored + ask_anchored_stream (callback UniFFI); GeminiProvider + complete_stream no core (2 PRs sancionados mergeados por sign-off: F2.3→133077a, F2.7 ai-pure→c8ecb2f, cada um re-pinado); chave BYOK nativa (Keychain via expo-secure-store) + web session-only in-memory; UI nativa de ask (streaming + citado/interpretação); VALIDAÇÃO REAL (Gemini gemini-2.5-flash, cited_text=João 3:16 do store + interpretação real, chave nunca no loop); IA no web (ai-pure wasm p/ prompt/citação + fetch, zero drift nativo↔web provado por teste host) · regras não negociáveis mantidas: BYOK/chave nunca em git/log (login-de-conta OAuth REJEITADO por pesquisa — banido/indisponível nos 3, ADR-0023), anti-alucinação (cited_text do store, citation mesma impl Rust, zero drift), offline-first (IA opt-in), the-light só via PR+ADR (2 PRs, wasm puro), qualidade+review por tarefa · ADRs 0023–0025 · relatório em loop/queue/F2.8-marco-2.task.md · ⛔ loop/HALT escrito · sign-off decide: aprovar Fase 2 / próximo rumo (Fase 3 estudo profundo · streaming web real · corpus completo web · outra) · LOOP PARADO aguardando aprovação do Marco 2
2026-07-01 22:20 UTC · (sign-off humano do Marco 2, fora de ciclo) · F2.8 APROVADO: **MARCO 2 aceito** — Fase 2 (IA BYOK ancorada, multiplataforma) completa e verde. Decisões: (1) Fase 2 aprovada; (2) **próxima direção = Fase 3 (estudo profundo: léxico Strong + pesquisa assistida)** — o core já tem ai::study/ai::lexicon/ai::research (embedded); a decompor em backlog/PHASE-3.md no molde das fases anteriores. loop/HALT REMOVIDO; F2.8 arquivada (result approved). the-light c8ecb2f. LOOP RETOMADO · próxima: planner decompõe a Fase 3 e semeia a 1ª tarefa · nota: léxico Strong provavelmente precisa de pipeline de dados (molde F1.1); pesquisa web = rede opt-in (anti-alucinação: citações do store/fontes, LLM só interpreta)
2026-07-01 22:45 UTC · ciclo (planner) · Marco 2 aprovado + Fase 3 LIBERADA → planner DECOMPÔS a **Fase 3 (estudo profundo)** em `loop/backlog/PHASE-3.md` (F3.1–F3.13: dados de léxico → fronteira de léxico → fronteira de estudo profundo (MOCK) → conversa/refinamento (pura) → UI nativa (estudo + léxico inline + citações + comparação multi-IA + export SBL) → **gate estratégico F3.9** (pesquisa web opt-in + via de paridade web) → **validação real F3.10** (gate) → **PR ai-pure F3.11** (condicional) → paridade web F3.12 → **Marco 3 F3.13** (gate)) e SEMEOU **F3.1** em `loop/queue/F3.1-dados-lexico.task.md` (ready, gate:false, deps:[]). **Investigação na fonte do core (`c8ecb2f`, via Read do `ai/`):** `ai::study::study`/`StudyRequest`/`StudyResult`/`to_markdown`/`to_academic_markdown` + `ai::lexicon::verified_lexicon` + todo `ai::research` (reqwest) + `build_provider` são **`#[cfg(feature="embedded")]` (nativo)** → o **estudo profundo NATIVO consome o core pinado SEM PR**; `ask_session`(conversa)/`refine_scope`/`numbered_passage`/`ask_context`/`format_verified_block`/`verify` + os tipos `LexicalEntry`/`VerifiedLexicon` são **PUROS (ai-pure → web já disponível)**; o **léxico usa o MESMO `bible.sqlite`** (tabelas `original_tokens`/`lexicon`/`scholarly_sources`, criadas por `Store::open`, populadas por **`xtask import-scholarly`** — confirmado `xtask/src/main.rs:29` → `scholarly_import.rs` → `the_light_core::scholarly`). **Fonte do léxico = STEP Bible / STEPBible-Data (TAHOT/TAGNT tokens + TBESH/TBESG glosas), CC BY 4.0 (Tyndale House) — NÃO domínio público puro** (a numeração de Strong é PD, mas os tokens/glosas amalgamados são CC-BY) → **atribuição obrigatória** (string verbatim `scholarly::ATTRIBUTION`, gravada em `scholarly_sources`; molde ADR-0016). **Pesquisa web (`ai::research`) = rede OPT-IN**: backends wikipedia (keyless) / tavily (BYOK chave) / mock (sem rede) → **gate estratégico F3.9**. A **paridade web do estudo** (hoje embedded-only, `user_prompt` privado) exige decisão em F3.9: app-side ai-pure (molde F2.7b, risco de drift) **ou** PR ao core p/ `study` ai-pure (molde F2.7, F3.11 condicional = handoff bloqueante). **F3.1 = molde F1.1/F1.7:** rodar `import-scholarly` do rev pinado `c8ecb2f` (CARGO_TARGET_DIR fora + --locked, SEM tocar the-light), contagens validadas lendo do banco (original_tokens ≳400k, lexicon ≳20k, scholarly_sources ≥4), idempotente, gerar-ignorado (ADR-0026). **Risco/blocked legítimo:** seed STEP vazio (`data/seed/scholarly`) → rede em build (dezenas de MB); offline sem seed → `blocked` (decisão: origem/seed). Regras preservadas: offline-first (estudo/pesquisa opt-in), anti-alucinação (texto/glosas/citações do store; LLM sinalizado se inventar Strong/fonte), BYOK (chave nunca em git/log), só dados livres (CC-BY com atribuição), the-light intocado (`c8ecb2f`; nativo sem PR). STATUS.md atualizado (cabeçalho + seção Fase 3 F3.1–F3.13). PROGRESS.md NÃO tocado. Próximo ADR livre = ADR-0026. · sem execução neste ciclo
2026-07-01 22:50 UTC · ciclo · executor+reviewer em F3.1 (dados de léxico STEP) · tarefa F3.1 · passed (1e3892e), arquivada · gen-bible-db.sh estendido roda a 3ª etapa xtask import-scholarly (STEP Bible/STEPBible-Data CC-BY 4.0) do rev pinado c8ecb2f SEM tocar the-light (checkout cargo + CARGO_TARGET_DIR fora + --locked + --seed-dir dedicado); REV do script alinhada 8f66004→c8ecb2f (import.rs/xref_import.rs byte-idênticos verificados); popula no MESMO bible.sqlite as tabelas original_tokens/lexicon/scholarly_sources (ai::lexicon lê daí, NÃO DB separado); contagens LIDAS do banco: original_tokens=447673 (tahot 305577+tagnt 142096), lexicon=22717 (tbesh 11682+tbesg 11035), scholarly_sources=4 (todas cc-by); Gênesis 1:1=7 tokens Strong; H0430G→אֱלֹהִים/God, G2316→θεός/God; atribuição CC-BY gravada em scholarly_sources verbatim de scholarly::ATTRIBUTION (exibição visível=F3.5); idempotente (verses 62203/xrefs 344799 intactos); só dados livres (denylist do core recusa sblgnt/bdag/halot); bible.sqlite gerar-ignorado ~121MB (ADR-0013); the-light intacto c8ecb2f; ADR-0026 · próxima: F3.2 (fronteira de léxico nativa → ai::lexicon::verified_lexicon, teste host MOCK-free/determinístico) — não-bloqueante
2026-07-01 23:05 UTC · ciclo (planner) · queue vazia + F3.1 aceita → planner SEMEOU **F3.2** em `loop/queue/F3.2-fronteira-lexico.task.md` (ready, gate:false, deps:[F3.1]). **NÚCLEO/fronteira** (molde F1.5/F1.8): expor `lexical_entries(db_path, book:u8, chapter:u16, verse:Option<u16>, lang:String, limit:Option<u32>) -> Result<VerifiedLexiconOut, CoreError>` em `core/src/lib.rs`, **delegando** a `the_light_core::ai::lexicon::verified_lexicon(store.conn(), &Reference, &[], lang, limit)` via `Store::open`. **Investigação só-leitura da fonte (`c8ecb2f` `ai/lexicon.rs`+`ai/mod.rs`+`model.rs`):** assinatura REAL `verified_lexicon(conn, &Reference, verse_numbers:&[u16], _lang:Lang, limit:usize) -> VerifiedLexicon` é **`#[cfg(feature="embedded")]`** e **INFALÍVEL** (não Result — engole erro SQLite → `default()` vazio; único erro da fronteira = `Store::open`); **SEM `translation`** (léxico independente de tradução, chaveado por book/chapter/verse como a xref F1.8) → fronteira recebe book/chapter/verse **numéricos**, não string de ref. `LexicalEntry{strongs(base, ex.: "H7225"),lemma:Option,translit:Option,gloss:Option,occurrences:u32,testament:String}` e `VerifiedLexicon{entries:Vec<LexicalEntry>,sources:Vec<String>}` são **PUROS (ai-pure**, re-exportados `the_light_core::ai::{LexicalEntry,VerifiedLexicon}`, alcançáveis no wasm) → os 2 `From` na fronteira são **cfg-free** (DIVERGÊNCIA vs. F1.8: lá o `xref::CrossRef` é embedded-only → `From` gated); **só o corpo** que abre o store é `cfg(not(wasm32))` + stub web (`léxico indisponível no alvo web (F3.12)`). Schema real (colunas `strongs`/`source_id`, confirmando a nota do reviewer da F3.1): `original_tokens(testament,book_number,chapter,verse,word_index,surface,lemma,strongs,strongs_raw,translit,gloss,source_id)`, `lexicon(strongs,lemma,gloss,gloss_pt,source_id)`, `scholarly_sources(id,name,license,embeddable,attribution,url,version)`. Teste de host **determinístico/offline** (fixture arquivo temp: molde `read_tests`+`ai/lexicon.rs::seeded()`, Gn 1:1 H0430G→אֱלֹהִים/gloss "God", ≥2 base-Strongs p/ o limit; asserções: strongs BASE "H0430"+gloss "God"+sources "STEP Bible"; passagem sem cobertura→vazio sem panic; `Store::open` inválido (diretório)→CoreError; limit Some(1)→1) + bônus `if assets/data/bible.sqlite exists` (F3.1, ~132 MB gerar-ignorado). Records `LexEntry`/`VerifiedLexiconOut{entries,sources}` (sources=atribuição STEP CC-BY p/ a F3.5). **Não-bloqueante · NÃO toca the-light** (superfície já `pub` sob `embedded` em `c8ecb2f`; nenhuma fn nova → nenhum PR; `blocked`+HALT com erro EXATO só se algo não-`pub` for exigido) · NÃO altera `core/Cargo.toml`. Regras: anti-alucinação (glosas/lemas do store verificado, nunca do LLM), offline-first, grafo wasm puro, léxico web = F3.12. **lang:String** parseado via `Lang::from_str` (default Pt, como `ask_anchored`) — aceito por fidelidade à assinatura real (o core `_lang` ATUALMENTE ignora). Sem ADR novo (consome o core pinado; próximo ADR livre = ADR-0027). STATUS.md atualizado (linha F3.2 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 23:15 UTC · ciclo · executor+reviewer em F3.2 (fronteira de léxico) · tarefa F3.2 · passed (7f469d4), arquivada · lexical_entries(db,book,chapter,verse?,lang,limit?)->Result<VerifiedLexiconOut,CoreError> DELEGA a ai::lexicon::verified_lexicon(store.conn(),&reference,&[],lang,limit) via Store::open (verse_numbers=&[] deixa o core derivar do Reference; léxico por book/chapter/verse, sem translation; verified_lexicon é infalível → único map_err é Store::open); Records LexEntry{strongs,lemma,translit,gloss,occurrences,testament}/VerifiedLexiconOut{entries,sources} puros + From cfg-free (tipos-fonte ai-pure, divergência vs CrossRef/Note embedded-only); corpo cfg(not wasm32) + stub web (léxico web=F3.12); teste host 48/48 (5 lexicon_tests, fixture à mão): Gn 1:1→Strong base H0430 (de H0430G), gloss="God", lemma אֱלֹהІם, testament OT, sources STEP Bible; verso sem cobertura vazio sem panic; db inválido→CoreError; limit respeitado; bônus bible.sqlite real; delegação real (sem SQL de léxico na fronteira); anti-alucinação (léxico do store verificado STEP CC-BY, nunca do LLM); grafo wasm puro; gen-bindings-web 0; the-light/Cargo.toml intactos (c8ecb2f) · próxima: F3.3 (fronteira de estudo profundo → ai::study::study, prova por MOCK) — não-bloqueante
2026-07-01 23:55 UTC · ciclo (planner) · queue vazia + F3.2 aceita → planner SEMEOU **F3.3** em `loop/queue/F3.3-fronteira-estudo.task.md` (ready, gate:false, deps:[F3.2]). **NÚCLEO/fronteira + prova por MOCK** (molde F2.1 `ask_anchored` + F3.2 `lexical_entries`): expor `deep_study(db_path, translation, book:u8, chapter:u16, verse:Option<u16>, mode:StudyMode, lens:StudyLens, depth:StudyDepth, lang:String, provider_name, key:Option<String>, model:Option<String>) -> Result<StudyResultOut, CoreError>` em `core/src/lib.rs`, **delegando** a `the_light_core::ai::study::study(&provider, &StudyRequest)`. **Investigação só-leitura da fonte (`c8ecb2f` `ai/study.rs`+`ai/mod.rs`+`ai/citation.rs`+`ai/providers.rs`+`reference.rs`+`xref.rs`):** assinatura REAL `#[cfg(feature="embedded")] pub fn study(provider:&dyn LlmProvider, req:&StudyRequest) -> ai::Result<StudyResult>` (`study.rs:294`) — **NÃO recebe conn/Store** (o CHAMADOR monta o `StudyRequest`); chama `provider.complete(system,user)` **síncrono/não-streaming**; separa `passage_text`(banco,numerado por `numbered_passage`) de `interpretation`(modelo); anti-alucinação embutida (`lexicon::verify` sinaliza Strong `[V:...]` fora do acervo; `cited_web_indices` sinaliza `[W:n]` fora do intervalo; `citations` do banco/URLs, NUNCA do modelo). **`StudyRequest<'a>`** (embedded-only, lifetime por `passage:Option<&'a Passage>`): reference/reference_label/mode:StudyMode/lens:Denomination/depth:StudyDepth/language:Lang/passage/cross_references:Vec<String>/verified_lexicon:VerifiedLexicon/web_sources:Vec<WebSource>/brief:Option<String>. **`StudyResult`** (embedded-only): reference/reference_label/mode/lens/depth/language/passage_text:String/interpretation:String/sections:Vec<StudySection>/warnings:Vec<String>/citations:Vec<Citation>/provider/model. **Enums variantes REAIS:** StudyMode{Academic,Devotional,Introductory,Sermon} (wants_lexical=Academic|Sermon; emits_apparatus=Academic); Denomination(lente){Baptist,Presbyterian,Lutheran,Pentecostal,Catholic,Orthodox}; StudyDepth{Overview,Exegetical,WordStudy} — **todos PUROS/ai-pure** (como StudySection{heading,body}/Citation{kind,key,author?…quote?}) → `From` cfg-free; só `study`/`StudyRequest`/`StudyResult` são **embedded-only** → o `From<StudyResult>`+o corpo são `cfg(not wasm32)` (molde `From<xref::CrossRef>`) + stub web (estudo web=F3.12). A fronteira MONTA o StudyRequest com fatos do store: `passage` via `EmbeddedSource::passage(&ref,&TranslationId)` (**translation-specific** — translation É necessário, ≠ léxico/xref), `verified_lexicon` (rota F3.2), `cross_references` via `xref::passage_labels(conn,&ref,&[],lang,limit)`, `reference_label` via `reference::format_reference(&ref,lang)` (puro), `web_sources:vec![]` (offline; pesquisa=F3.9 gate), `brief:None`. MOCK via `build_provider("mock",None,None)`→`MockLlmProvider::default()` (name="mock", model="mock-1", resposta FIXA quando o system não contém "PERGUNTA:"; provider desconhecido→`AiError::UnknownProvider`); passar `&*provider` a `study`. Records/enums UniFFI: StudyMode/StudyLens/StudyDepth (uniffi::Enum + From p/ os enums puros do core), StudySection, StudyCitation, StudyResultOut (espelha os campos REAIS de StudyResult). Teste de host determinístico (molde F2.1, reusa `build_kjv_fixture`/`mock_fixed_response`/`JOHN_3_16_KJV`): passage_text verbatim ("16 For God so loved…") ≠ interpretation==mock_fixed_response (LLM nunca gera texto bíblico); anti-fake (3:16 vs 3:17 → passage_text muda, interpretation invariante); provider desconhecido/db-diretório→CoreError sem panic; sections/warnings/citations tipados presentes (com o mock canned: vazios — o sinalizar-Strong e o aparato são cobertos pelos testes do PRÓPRIO core). **F3.3 depende SÓ de F3.2 (aceita), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (`study`/`StudyRequest`/`StudyResult`/`build_provider`/`passage_labels`/`verified_lexicon`/`format_reference` já `pub` sob embedded em `c8ecb2f`; `blocked`+HALT só se algo não-`pub`/wasm-safe for exigido, molde F2.7/F3.11). NÃO altera `core/Cargo.toml`; anti-alucinação (passage_text/citações do store; LLM só interpreta; warnings do core), offline-first, grafo wasm puro, uniffi =0.31.0 intacto. Sem ADR novo (consome o core pinado; próximo ADR livre = ADR-0027). STATUS.md atualizado (linha F3.3 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-01 23:45 UTC · ciclo · executor+reviewer em F3.3 (fronteira de estudo profundo) · tarefa F3.3 · passed (49919c3), arquivada · deep_study(...) DELEGA a ai::study::study(provider.as_ref(),&request) — a fronteira monta o StudyRequest com fatos do STORE (EmbeddedSource::passage + verified_lexicon F3.2 + xref::passage_labels + format_reference; web_sources=[], brief=None) e converte StudyResult→StudyResultOut; provider via build_provider (prova "mock", sem chave/rede); enums StudyMode/StudyLens(→Denomination)/StudyDepth ai-pure (From cfg-free), Records StudySection/StudyCitation puros, From<StudyResult> gated cfg(not wasm32) (embedded-only); corpo gated + stub web; anti-alucinação: StudyResultOut separa passage_text (store verbatim) de interpretation (LLM), citations/léxico de fontes locais, warnings do core; teste host 52/52 (4 study_tests): passage_text=João 3:16 KJV do store + interpretation==mock_fixed_response() (do core, não hardcoded), anti-fake 3:16 vs 3:17 (passage_text muda/interpretation invariante), error paths→CoreError sem panic; delegação real (sem prompt/RAG reimplementado); grafo wasm puro; gen-bindings-web 0; the-light/Cargo.toml intactos (c8ecb2f); sem PR · próxima: F3.4 (fronteira conversa/refinamento ask_session/refine_scope, PURAS ai-pure) — não-bloqueante; gate F3.9 (pesquisa web) = próximo bloqueante previsto
2026-07-02 00:10 UTC · ciclo (planner) · queue vazia + F3.3 aceita → planner SEMEOU **F3.4** em `loop/queue/F3.4-fronteira-conversa.task.md` (ready, gate:false, deps:[F3.3]). **NÚCLEO/fronteira + prova por MOCK** (molde F2.1 `ask_anchored` + F3.3 `deep_study`): expor 3 funções em `core/src/lib.rs` delegando a `the_light_core::ai::{ask_session,refine_scope,parse_refinement}` — (1) `ask_session_anchored(db_path,translation,book,chapter,verse?,lang,turns:Vec<ChatTurn>,study_mode?,study_lens?,provider_name,key?,model?)->Result<AiAnswer,CoreError>` (conversa multi-turno ancorada, contexto montado do store), (2) `refine_scope(mode:StudyMode,lang,brief,prior:Vec<RefinementRound>,round:u8,provider_name,key?,model?)->Result<RefinementOut,CoreError>`, (3) `parse_refinement(raw:String)->RefinementOut` (parser puro sem provider). **Investigação só-leitura da fonte (`c8ecb2f` `ai/study.rs`+`ai/mod.rs`):** `ask_session`/`refine_scope`/`parse_refinement`/`Refinement`/`ChatMessage`/`ChatRole`/`MockLlmProvider` são **TODOS ai-pure** (re-export puro `mod.rs:24-30`; `MockLlmProvider` pub ungated `mod.rs:399`); `build_provider` é **embedded-only** (`mod.rs:38`). **`ask_session`(`study.rs:626`)**: `pub fn ask_session(provider:&dyn LlmProvider, lang:Lang, context:&str, turns:&[ChatMessage], study:Option<(StudyMode,Denomination)>)->Result<String>` — chama `provider.chat`; contexto embutido **só no 1º turno de usuário** (invariante do core); devolve **só a String** da interpretação (âncora = `context`, montado do store pela fronteira). **`refine_scope`(`study.rs:411`)**: `pub fn refine_scope(provider:&dyn LlmProvider, mode:StudyMode, lang:Lang, brief:&str, prior:&[(String,String)], round:u8)->Result<Refinement>` — **RECEBE provider** (`provider.complete`), **SEM `lens`** — CORREÇÃO do rascunho do backlog, que supôs "refine_scope pura sem provider" (FALSO). Quem é **determinística SEM provider** é **`parse_refinement`(`study.rs:346`)** `pub fn parse_refinement(raw:&str)->Refinement` (parser `PERGUNTA:`/`- opção`, dedup). `Refinement{question,options}` (`study.rs:70` puro); `ChatMessage{role:ChatRole,content}`/`ChatRole{User,Assistant}` (`mod.rs:339`/`320` puros); `LlmProvider::chat` (default `mod.rs:380`) dobra o histórico→`complete` (o Mock não sobrescreve → resposta FIXA). **`MockLlmProvider` CRÍTICO** (`mod.rs:399`): `complete` devolve rodada canônica quando `system.contains("PERGUNTA:")` (o `refine_system_prompt` contém) → `refine_scope`+mock é **determinístico** (`PERGUNTA: Qual recorte você prefere?\n- Efésios 2.8-9\n- Panorama temático da graça\n- Aplicação pastoral`). **DECISÃO DE GATING (byte-verificada, justificada na task):** `parse_refinement`→**CFG-FREE** (roda no wasm, ANTECIPA a paridade web do parser); `refine_scope`→mock **CFG-FREE** (`MockLlmProvider::default()` **direto**, NÃO `build_provider("mock")` que é embedded — ANTECIPA o refinamento web), provider real sob `cfg(not wasm32)` (`build_provider`) + stub web (transporte `fetch`=F3.12, molde F2.7b); `ask_session_anchored`→corpo `cfg(not wasm32)` + stub web (molde F3.3), porque a **âncora** `context` é montada do **store nativo** (`Store::open`+`EmbeddedSource::passage`+`ai::numbered_passage`+`ai::ask_context`+`xref::passage_labels` — rusqlite/embedded); embora `ask_session` **em si** seja pura, o store nativo não existe no wasm → a paridade web (F3.12) montará o context do store web e chamará a `ask_session` pura (como a F2.7b passou versículos verbatim). **NÃO force cfg-free onde a realidade exige embedded.** Retorno da conversa **REUSA `AiAnswer`** (F2.1: reference/cited_text(=passagem numerada do store)/interpretation/provider/model — separa citado de interpretação, anti-alucinação). Records novos UniFFI: `ChatTurn{role:ChatRole,content}` + `ChatRole{User,Assistant}` + `RefinementOut{question,options}` + `RefinementRound{question,answer}` (prior) — `From` **cfg-free** (tipos-fonte ai-pure). Teste de host determinístico `session_tests` (molde F2.1/F3.3, reusa `build_kjv_fixture`/`mock_fixed_response`/`JOHN_3_16_KJV`/`JOHN_3_17_KJV`): conversa mock → `cited_text` "16 For God so loved…" do store ≠ `interpretation`==mock_fixed_response() (do core, não hardcoded); multi-turno (3 turnos) sem panic; anti-fake 3:16 vs 3:17 (cited_text muda/interpretation invariante); `refine_scope`+mock → question não-vazia + 3 opções incl. "Efésios 2.8-9"; `parse_refinement` 3 casos (PERGUNTA:+opções, 1ª-linha-vira-pergunta, dedup) + vazio/só-espaço sem panic; error paths (provider "nope", db-diretório)→CoreError sem panic. **F3.4 depende SÓ de F3.3 (aceita `49919c3`), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (toda a superfície já `pub` e ai-pure/embedded em `c8ecb2f`; usar o mock direto evita `build_provider` embedded no caminho puro; `blocked`+HALT só se algo não-`pub`/wasm-safe for exigido, molde F2.7/F3.11). NÃO altera `core/Cargo.toml`; anti-alucinação (cited_text/âncora do store; LLM só conversa), offline-first, grafo wasm puro (só símbolos ai-pure), uniffi `=0.31.0` intacto. Sem ADR novo (consome o core pinado; próximo ADR livre = ADR-0027). STATUS.md atualizado (linha F3.4 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-02 00:10 UTC · ciclo · executor+reviewer em F3.4 (fronteira conversa/refinamento) · tarefa F3.4 · passed (851e97c), arquivada · 3 funções delegando ao core: ask_session_anchored (monta context ancorado do STORE — numbered_passage + xref::passage_labels — e chama ai::ask_session; corpo cfg(not wasm32)+stub web), refine_scope (delega ai::refine_scope; caminho mock cfg-free via MockLlmProvider::default() DIRETO não build_provider; real sob cfg(not wasm32)), parse_refinement (cfg-free, puro/determinístico); Records ChatTurn/ChatRole/RefinementOut/RefinementRound ai-pure (From cfg-free); correção: refine_scope RECEBE provider (só parse_refinement é puro); parse_refinement+refine_scope-mock rodam no wasm (antecipam paridade web F3.12); anti-alucinação AiAnswer.cited_text (passagem do store/âncora) × interpretation (LLM); teste host 59/59 (7 session_tests): conversa mock (cited_text João 3:16 do store + interpretation==mock, multi-turn), anti-fake 3:16 vs 3:17, refine_scope mock, parse_refinement determinístico + vazio sem panic, error paths→CoreError; delegação real; grafo wasm puro; gen-bindings-web 0; the-light/Cargo.toml intactos (c8ecb2f); sem PR · próxima: F3.5 (UI nativa de estudo — léxico inline + citações + atribuição STEP CC-BY visível, prova MOCK no device) — não-bloqueante; gate F3.9 (pesquisa web) = próximo bloqueante previsto
2026-07-02 00:40 UTC · ciclo (planner) · queue vazia + F3.4 aceita → planner SEMEOU **F3.5** em `loop/queue/F3.5-ui-estudo-nativa.task.md` (ready, gate:false, deps:[F3.2,F3.3] aceitas). **UI NATIVA (iOS/Android) + prova por MOCK** (molde F2.5 `ReaderAskPanel`/`ask-selftest` + F1.9 atribuição CC-BY): novo `app/components/ReaderStudyPanel.tsx` (bottom sheet) com seletores **modo×lente×profundidade** → `deep_study` (provider "mock") exibindo `passage_text`(store, rotulado "Passagem (texto bíblico)") SEPARADO de `interpretation`(IA, "Interpretação (IA) — confira nas Escrituras") + `sections`/`citations`/`warnings` + **léxico Strong inline** (`lexical_entries`) + **atribuição STEP CC-BY VISÍVEL** (ADR-0026, de `VerifiedLexiconOut.sources` verbatim). Estender glue `app/web/reading.ts` (`deepStudy`/`lexicalEntries` → JSI; web=stub/F3.12); fiar ação "Estudo (IA)" no `ReaderVersePanel` + `[book]/[chapter].tsx`. **Assinaturas REAIS confirmadas em `core/src/lib.rs` (NÃO modificar):** `deep_study(db,translation,book:u8,chapter:u16,verse:Option,mode:StudyMode,lens:StudyLens,depth:StudyDepth,lang,provider_name,key:Option,model:Option)->StudyResultOut` (l.1311 — **lang ANTES de provider; passagem é book/chapter/verse numéricos, NÃO string**); `lexical_entries(db,book:u8,chapter:u16,verse:Option,lang,limit:Option)->VerifiedLexiconOut` (l.948 — **SEM translation**). Enums StudyMode{Academic,Devotional,Introductory,Sermon}/StudyLens{Baptist,Presbyterian,Lutheran,Pentecostal,Catholic,Orthodox}/StudyDepth{Overview,Exegetical,WordStudy}; Records StudyResultOut{...,passage_text,interpretation,sections[],warnings[],citations[],provider,model}/VerifiedLexiconOut{entries:LexEntry[],sources[]}/LexEntry{strongs,lemma?,translit?,gloss?,occurrences,testament}. `deepStudy`/`lexicalEntries` JÁ nos bindings gerados (F3.2/F3.3); só o glue `reading.ts` não os reexporta. **DECISÃO — léxico no subset bundled:** o `reading-sample.sqlite` NÃO tem léxico (F3.1 populou o `bible.sqlite`; o `gen_reading_sample_db.rs` propaga só verses/xref/FTS) → **adotado (a) PROPAGAR** `original_tokens`/`lexicon`/`scholarly_sources` ao subset (molde F1.9 que propagou xref), registrar em **ADR-0027** (tamanho, molde ADR-0014) → self-test no device asserta `lexicon>=1`/`attribution_ok=true`. Prova headless `study-selftest.ts` chama `deepStudy(db,"kjv",43,3,16,Academic,Presbyterian,Exegetical,"en","mock",…)`+`lexicalEntries(db,43,3,16,"en",…)` e emite `TLA_STUDY ref="John 3:16" provider="mock" passage_prefix="For God so loved..." lexicon=<n> attribution_ok=true` (do RETORNO real); `run-ios-selftest.sh` asserta provider="mock" + passage_prefix João 3:16 KJV do store + lexicon>=1 + attribution_ok, SEM regressão de TLA_SELFTEST/READ/PARALLEL/SEARCH/XREF/NOTES/ASK. **F3.5 depende SÓ de F3.2+F3.3 (aceitas), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (superfície já entregue em `c8ecb2f`; F3.5 é só app/UI/glue/self-test + dados do subset; bindings gerados ignorados; `blocked`+HALT com erro EXATO só se exigir mudar o core). Regras: atribuição STEP CC-BY VISÍVEL obrigatória, anti-alucinação visível (passage_text/léxico do store × interpretation), offline-first, só nativo (estudo web=F3.12), chave nunca logada. Próximo ADR livre = **ADR-0027**. STATUS.md atualizado (linha F3.5 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-02 01:00 UTC · ciclo · executor+reviewer em F3.5 (UI nativa de estudo) · tarefa F3.5 · passed (04a7a61), arquivada · ReaderStudyPanel (seletores modo×lente×profundidade → deep_study mock via glue reading.ts/JSI) exibe passage_text (store, "Passagem (texto bíblico)") SEPARADO de interpretation ("Interpretação (IA) — confira nas Escrituras") + sections/citations/warnings + léxico Strong inline (lexical_entries) + atribuição STEP CC-BY VISÍVEL (VerifiedLexiconOut.sources, ADR-0026); ação "Estudo (IA)" no ReaderVersePanel; web=stub F3.12; léxico PROPAGADO ao subset reading-sample.sqlite (gerador estendido via ATTACH, molde F1.9): original_tokens=56268/lexicon=4534/scholarly_sources=4 (João 3:16=26 tokens), ~14,4MB (ADR-0027); bindings nativos regenerados; prova HEADLESS iPhone 17 TLA_STUDY ref="John 3:16" provider="mock" passage_prefix="For God so loved the world..." lexicon=21 attribution_ok=true, sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK; tsc+expo export web verdes; the-light/core/src/lib.rs/Cargo.toml intactos (c8ecb2f) · SANEAMENTO de artefato: doc-comments **puro**/ do core fechavam o JSDoc dos bindings gerados (562 erros tsc) → corrigido SÓ nos geradores (**/→** /) + bindings gerados-ignorados; core intocado; FOLLOW-UP registrado (ADR-0027): corrigir os doc-comments **puro** em core/src/lib.rs p/ tornar o saneamento redundante · próxima: F3.6 (UI conversa/follow-up, prova MOCK no device) — não-bloqueante; gate F3.9 (pesquisa web) = próximo bloqueante previsto
2026-07-02 02:00 UTC · ciclo (planner) · queue vazia + F3.5 aceita → planner SEMEOU **F3.6** em `loop/queue/F3.6-ui-conversa-nativa.task.md` (ready, gate:false, deps:[F3.4,F3.5] aceitas). **UI NATIVA (iOS/Android) de CONVERSA/FOLLOW-UP + prova por MOCK** (molde F2.5 `ReaderAskPanel`/`ask-selftest` + F3.5 `ReaderStudyPanel`/`study-selftest`): novo `app/components/ReaderChatPanel.tsx` (bottom sheet ancorado) que mantém histórico multi-turno `ChatTurn[]` e a cada follow-up chama `ask_session_anchored` (glue → JSI, provider "mock") com o MESMO book/chapter/verse — âncora preservada; exibe `cited_text` (âncora, verbatim do store) SEPARADO/rotulado de cada `interpretation` (LLM), anti-alucinação visível. Estender glue `app/web/reading.ts` (wrapper `askSessionAnchored` + reexport ChatTurn/ChatRole → JSI; `reading.web.ts`=stub `= F3.12`); fiar ação de conversa no `ReaderVersePanel` + `[book]/[chapter].tsx`; novo `chat-selftest.ts` (+ `.web.ts` SKIP) encadeado em `selftest.ts`; regenerar bindings; estender `run-ios-selftest.sh`. **Assinatura REAL confirmada em `core/src/lib.rs` (NÃO modificar):** `ask_session_anchored(db_path,translation,book:u8,chapter:u16,verse:Option<u16>,lang,turns:Vec<ChatTurn>,study_mode:Option<StudyMode>,study_lens:Option<StudyLens>,provider_name,key:Option,model:Option)->Result<AiAnswer,CoreError>` (l.1656 — **ATENÇÃO: `lang` ANTES de `turns`; `study_mode`/`study_lens` DEPOIS de `turns` e ANTES de `provider_name`; passagem NUMÉRICA book/chapter/verse, NÃO string "John 3:16"**). Retorno REUSA `AiAnswer{reference,cited_text(=passagem numerada verbatim do store),interpretation(=saída só do modelo/mock),provider,model}` (l.2121) — SEM campo `turns` no retorno → o marcador `turns=<n>` vem do tamanho do histórico de ENTRADA. `ChatTurn{role:ChatRole,content}`+`ChatRole{User,Assistant}` (l.1463/l.1438), puros/ai-pure; invariante do core: `context`(âncora) embutido só no 1º turno de usuário → a UI passa sempre o mesmo book/chapter/verse. Bindings F3.4 JÁ gerados (`native-generated`/`generated`, l.276/l.285 `askSessionAnchored`); o glue ainda não reexporta o wrapper. Opcionais (NÃO ao aceite): `refine_scope`/`parse_refinement`. Prova headless `chat-selftest.ts`: conversa de 1–2 turnos via `askSessionAnchored(db,"kjv",43,3,16,"en",turns,undefined,undefined,"mock",undefined,undefined)` (2ª chamada com histórico User→Assistant→User, provando o follow-up) → emite `TLA_CHAT ref="John 3:16" provider="mock" turns=<n> cited_prefix="For God so loved..." interp_len=<n>` (do RETORNO real: cited_text do store, interpretation do mock; turns do histórico enviado); `run-ios-selftest.sh` asserta provider="mock" + cited_prefix João 3:16 KJV do store + turns>=1 + interp_len>=1, SEM regressão de TLA_SELFTEST/READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY. **F3.6 depende SÓ de F3.4+F3.5 (aceitas), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (superfície `ask_session_anchored`/`ChatTurn`/`ChatRole` já `pub`/`ai-pure` em `c8ecb2f`; F3.6 é só app/UI/glue/self-test + bindings gerados ignorados; `blocked`+HALT com erro EXATO só se exigir mudar o core). Regras: anti-alucinação visível (cited_text/âncora do store × interpretation), offline-first, só nativo (conversa web=F3.12), chave nunca logada. FOLLOW-UP do ADR-0027 (doc-comments `**puro**` em lib.rs) NÃO é escopo da F3.6 (que não toca lib.rs) — pendente p/ quando alguma tarefa TOCAR lib.rs (ex.: F3.12). Sem ADR novo (consome o core pinado; próximo ADR livre = ADR-0028). STATUS.md atualizado (linha F3.6 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-02 01:35 UTC · ciclo · executor+reviewer em F3.6 (UI conversa/follow-up nativa) · tarefa F3.6 · passed (e39dad5), arquivada · ReaderChatPanel conversa multi-turno: histórico ChatTurn[] (User/Assistant), cada follow-up chama ask_session_anchored (glue reading.ts→JSI, mock) com o MESMO book/chapter/verse; cited_text (âncora verbatim do store, testID chat-cited-text, "Passagem (texto bíblico)") SEPARADO de cada interpretation (LLM, "IA — confira nas Escrituras"); glue respeita ordem real (lang antes de turns; studyMode/studyLens depois); web=stub F3.12; ação "Conversa (IA)" no ReaderVersePanel; bindings regenerados; prova HEADLESS iPhone 17 TLA_CHAT ref="John 3:16" provider="mock" turns=3 cited_prefix="For God so loved the world..." interp_len=94, sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY; tsc+expo export web verdes; the-light/core/src/lib.rs/Cargo.toml intactos (c8ecb2f) · próxima: F3.7 (UI comparação multi-IA, prova MOCK) — não-bloqueante; gate F3.9 (pesquisa web) = próximo bloqueante previsto
2026-07-02 03:10 UTC · ciclo (planner) · queue vazia + F3.6 aceita → planner SEMEOU **F3.7** em `loop/queue/F3.7-ui-comparacao-multi-ia.task.md` (ready, gate:false, deps:[F3.5] aceita). **UI NATIVA (iOS/Android) de COMPARAÇÃO MULTI-IA + prova por MOCK** (molde F2.5 `ReaderAskPanel`/`ask-selftest` + chips do F3.5 `ReaderStudyPanel`): novo `app/components/ReaderComparePanel.tsx` (bottom sheet ancorado por `reference` string) com seletor MULTI de provedores (`SUPPORTED_PROVIDERS`=anthropic/openai/gemini/ollama + `mock`) + uma pergunta; ao comparar faz N chamadas INDEPENDENTES a `askAnchored` (uma por provedor) sobre a MESMA âncora e renderiza N respostas lado a lado, cada coluna rotulada `provider · model`. **DESENHO adotado (registrado/justificado na task):** o `cited_text` (âncora, verbatim do store) é IDÊNTICO em todas as colunas → exibido UMA vez como bloco de âncora comum, SEPARADO das N `interpretation` (anti-alucinação MAIS visível; é o invariante `cited_match`). **Custo OMITIDO** (a fronteira NÃO expõe `estimate_cost_usd`, molde F2.5). **DECISÃO CENTRAL sobre a prova mock:** o `MockLlmProvider` devolve resposta FIXA → mock×mock é degenerado em conteúdo; a prova por MOCK cobre o **WIRING de N provedores** (≥2 colunas "mock"): N chamadas retornam N `AiAnswer`, todos com o MESMO `cited_text` do store (`cited_match=true` = anti-alucinação + mesma âncora lida N×) e cada coluna com seu provider. **A comparação de respostas DIFERENTES (provedores reais Claude/GPT/Gemini) é a F3.10** (chave real, gate) — a task deixa EXPLÍCITO que NÃO se finge comparação real com mock. **REUSO confirmado (NÃO cria função/glue novo):** `ask_anchored` (F2.1/F2.3a, aceitas) já exposta em `askAnchored(dbPath,translation,reference:string,question,provider,key?,model?,lang)->AiAnswer` (`reading.ts` l.224; `reference` é STRING "John 3:16", NÃO numérico; `lang` por último), **já exportada nos DOIS glues** (`reading.ts` nativo E `reading.web.ts` destubada F2.7b) → nenhum stub web novo, nenhuma fronteira nova; regen de bindings idempotente. Chave real (não-mock) via `getKey(provider)` (keystore F2.4); prova usa só `'mock'`. Fiar `onCompare`+botão "Comparar (IA)" (`verse-compare`) no `ReaderVersePanel` + estado `compareVerse` + `<ReaderComparePanel>` em `[book]/[chapter].tsx` (molde EXATO do `ReaderAskPanel`). Novo `compare-selftest.ts` (2 chamadas `askAnchored(...,'mock',...)` p/ João 3:16 → `TLA_COMPARE ref="John 3:16" providers=2 cited_match=true first_provider="mock" cited_prefix="For God so loved..."`, do RETORNO real) + `compare-selftest.web.ts` SKIP, encadeado em `selftest.ts`; `run-ios-selftest.sh` asserta providers>=2 + cited_match=true + first_provider="mock" + cited_prefix, SEM regressão de TLA_SELFTEST/READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY/CHAT. **F3.7 depende SÓ de F3.5 (aceita `04a7a61`), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (reusa `ask_anchored` já `pub`/`ai-pure` em `c8ecb2f`; só app/UI/self-test + bindings gerados ignorados; `blocked`+HALT com erro EXATO só se exigir mudar o core). Regras: anti-alucinação visível (cited_text/âncora do store comum × N interpretation), offline-first, só nativo (comparação web=F3.12), chave nunca logada, custo não inventado. FOLLOW-UP do ADR-0027 (doc-comments `**puro**` em lib.rs) NÃO é escopo da F3.7 (que não toca lib.rs) — pendente p/ quando alguma tarefa TOCAR lib.rs. Sem ADR novo (consome o core pinado; próximo ADR livre = ADR-0028). STATUS.md atualizado (linha F3.7 → 🟡 ready + cabeçalho). PROGRESS.md NÃO tocado. · sem execução neste ciclo
2026-07-02 02:30 UTC · ciclo · executor+reviewer em F3.7 (UI comparação multi-IA) · tarefa F3.7 · passed (ca7ad3f), arquivada · NOTA: 1ª execução implementou mas caiu por 529 (server overloaded) antes do commit; 2ª execução verificou o código já na árvore (nada a corrigir) e commitou · ReaderComparePanel: 1 pergunta ancorada + seletor MULTI de provedores (SUPPORTED_PROVIDERS+mock) → N chamadas independentes a askAnchored (Promise.all) sobre a MESMA reference; cited_text (âncora verbatim do store) exibido UMA vez SEPARADO das N interpretation rotuladas por provider·model (anti-alucinação); reusa ask_anchored (sem função/glue nova); chave real de keystore.getKey só p/ provedores reais (coluna no-key não chama a fronteira); prova "mock"; ação "Comparar (IA)" no ReaderVersePanel; prova HEADLESS iPhone 17 TLA_COMPARE ref="John 3:16" providers=2 cited_match=true first_provider="mock" cited_prefix="For God so loved the world...", sem regressão READ/PARALLEL/SEARCH/XREF/NOTES/ASK/STUDY/CHAT; tsc+expo export web verdes; the-light/core/src/lib.rs/Cargo.toml intactos (c8ecb2f); mock×mock=wiring N-provedores, comparação real DIFERENTE=F3.10 · próxima: F3.8 (export acadêmico SBL → Markdown + sidecar) — ÚLTIMA antes do gate F3.9 (pesquisa web)

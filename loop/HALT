# ⛔ HALT — aguardando PUSH+MERGE humano do PR ao the-light (F3.11 / ai-pure study) + re-pin

Escrito pelo Driver em 2026-07-02. Motivo: **ação outward / mudança no the-light** — o
branch está pronto e verde, mas **push + merge na `main` do `the-light` é ação do humano**
(git push é negado ao loop; molde F2.7/F0.6a). Depois do merge, o Driver re-pina o core
(liga `ai-pure` na linha WEB) e o loop segue para a F3.12 (paridade web do estudo).

## O que está pronto (verificado pelo reviewer — `loop/done/F3.11.result.md`)
Branch **`feat/ai-pure-study`** no repo `/Users/butkeraites/Documents/the-light`, commit
**`e0edda5`**, tocando só `crates/the-light-core/Cargo.toml` + `ai/{mod,study,research,citation}.rs`:
- **Estende `ai-pure`** (feature) p/ a **superfície PURA do estudo** compilar em `wasm32`:
  `StudyRequest`/`StudyResult`/`to_academic_markdown`/`to_markdown`/`cited_web_indices`/
  `user_prompt` + `WebSource`/`from_web_results` (research pura). Widening de `#[cfg]`
  (`embedded` → `any(embedded, ai-pure)`); **1 `pub` novo** (`user_prompt`) + `cited_web_indices` pub.
- **`embedded ⊃ ai-pure`** + chrono **clock-free** na ai-pure → **default byte-a-byte
  intacto** (nativo mantém o clock via `chrono/clock`); API pública nativa preservada.
- **PORTÃO D2:** `cargo build -p the-light-core --no-default-features --features ai-pure
  --target wasm32-unknown-unknown` compila COM as peças de estudo; `cargo tree` wasm **sem**
  reqwest/rusqlite/wasm-bindgen (grafo puro: chrono/regex/serde/serde_json/thiserror).
- **Verde e não-quebrante:** `cargo fmt --check` 0, `clippy -D warnings` 0, `cargo test
  --workspace` 0 falhas (core **184**, sem regressão). `main` intacto em `c8ecb2f`; **sem push**.
  Detalhes em **ADR-0030** + `loop/proposals/the-light-PR-ai-pure-study.md`.
- `study()` em si fica `embedded`; o web (F3.12) usa **prepare→fetch→finalize** com as peças
  puras (molde F2.7b): prepare = `system_prompt_in` + `user_prompt`; fetch = TS; finalize =
  `split_sections`/`verify`/`cited_web_indices`/citações/`to_academic_markdown`/`rewrite_anchors`
  — a MESMA impl Rust do nativo (zero drift).

## O que você precisa fazer (push + merge)
No repo `/Users/butkeraites/Documents/the-light` (rode com `! <cmd>` ou no seu terminal):
```
cd /Users/butkeraites/Documents/the-light
git switch main && git merge --no-ff feat/ai-pure-study
git push origin main
git rev-parse main        # <-- me passe este rev (o merge)
```

## Como retomar
Você mergeia e me passa o **rev mergeado da `main`** → o Driver:
1. **re-pina** `core/Cargo.toml` nesse rev e confirma `features=["ai-pure"]` na linha WEB
   (já ligada desde a F2.7; só troca o rev nos 2 blocos),
2. revalida a fronteira (build nativo + grafo wasm ainda puro + gen-bindings-web),
3. remove este `loop/HALT` e segue para **F3.12** (paridade web do estudo/léxico/conversa/
   pesquisa via ai-pure + fetch) → depois **F3.13 (Marco 3)**, que encerra a Fase 3.

Regras intactas: chave nunca em git/log; anti-alucinação (mesma impl Rust nativo↔web — zero
drift); offline-first (IA/pesquisa opt-in); the-light só por este PR sancionado
(ADR-0029/0030). Nada foi improvisado; nenhum push feito.

# ⛔ HALT — aguardando PUSH+MERGE humano do PR ao the-light (F2.3) + re-pin

Escrito pelo Driver em 2026-07-01. Motivo: **ação outward / mudança no the-light** —
o branch está pronto e verde, mas o **push + merge na `main` do `the-light` é ação do
humano** (git push é negado ao loop; molde F0.6a). Depois do merge, o Driver re-pina o
core na fronteira (F2.3a) e o loop continua.

## O que está pronto (verificado pelo reviewer — `loop/done/F2.3.result.md`)
Branch **`feat/ai-gemini-streaming`** no repo `/Users/butkeraites/Documents/the-light`,
commit **`f08a651`**, tocando **só** `crates/the-light-core/src/ai/{mod.rs,providers.rs}`:
- **D1 — GeminiProvider:** `impl LlmProvider`, registrado em `PROVIDERS` /
  `default_model` (`gemini-2.0-flash`) / `build_provider` (exige key) /
  `estimate_cost_usd`; `gemini_body`/`gemini_extract` puros e testados; POST a
  `generativelanguage.googleapis.com`, key em header `x-goog-api-key` (nunca logada/na URL).
- **D4 — streaming:** `LlmProvider::complete_stream` com **default não-quebrante**
  (emite a resposta inteira 1×); SSE real por provedor adiado (evolução). Providers
  existentes intactos.
- **Verde e não-quebrante:** `cargo fmt --check` 0, `clippy -D warnings` 0,
  `cargo test --workspace` 0 failed (core lib **184 passed**, 7 novos). `main` intacto
  em `8f66004`; **sem push**.
- **D2 (wasm-decouple) NÃO está aqui** — é um PR focado separado antes da F2.7 (web AI).

## O que você precisa fazer (push + merge)
No repo `/Users/butkeraites/Documents/the-light` (rode com `! <cmd>` na sessão, ou no seu terminal):

Opção A — via GitHub PR:
```
cd /Users/butkeraites/Documents/the-light
git push -u origin feat/ai-gemini-streaming
# abrir o PR no GitHub e mergear na main
git switch main && git pull
git rev-parse main        # <-- me passe este rev (o merge)
```
Opção B — merge local + push:
```
cd /Users/butkeraites/Documents/the-light
git switch main && git merge --no-ff feat/ai-gemini-streaming
git push origin main
git rev-parse main        # <-- me passe este rev
```

## Como retomar
Você mergeia e me passa o **rev mergeado da `main`** → o Driver:
1. **re-pina** `core/Cargo.toml` da fronteira nesse rev (F2.3a),
2. roteia `"gemini"` + expõe streaming (callback UniFFI) na fronteira consumindo o re-pin,
3. remove este `loop/HALT` e segue (F2.5 UI nativa `ask` por MOCK → F2.6 validação real).

Regras intactas: chave nunca em git/log; anti-alucinação; offline-first (IA opt-in);
o the-light só muda por este PR sancionado (ADR-0023). Nada foi improvisado; nenhum push feito.

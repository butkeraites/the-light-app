# F0.0 — Notas do Executor (relatório bruto de ambiente)

> Insumo para o Reviewer. NÃO é o resultado final (`done/F0.0.result.md` é do Reviewer).
> Tarefa de descoberta de ambiente: nenhum código de produto foi escrito, nenhuma
> árvore de projeto criada (isso é F0.1). Apenas inspeção + relatório.

- Data da coleta: 2026-06-29
- Host: macOS (Darwin 25.5.0), arch host = aarch64-apple-darwin
- Restrições respeitadas: não toquei em `../the-light`; nenhum segredo lido; nada
  instalado (apenas inspeção). A resolução de versão do Expo via `npx` é só
  descoberta de versão, não embarca nada no produto.

## Tabela: ferramenta → versão → status

| Ferramenta | Comando | Versão detectada | Status |
|---|---|---|---|
| rustc | `rustc --version` | 1.96.0 (ac68faa20 2026-05-25) | ok |
| cargo | `cargo --version` | 1.96.0 (30a34c682 2026-05-25) | ok |
| rustup | `rustup --version` | 1.29.0 (28d1352db 2026-03-05) | ok |
| target `aarch64-apple-darwin` (host) | `rustup target list --installed` | instalado | ok |
| target `wasm32-unknown-unknown` (web/WASM) | `rustup target list --installed` | ausente | pendência |
| target `aarch64-apple-ios` (iOS device) | idem | ausente | pendência |
| target `aarch64-apple-ios-sim` (iOS sim) | idem | ausente | pendência |
| target `aarch64-linux-android` (Android arm64) | idem | ausente | pendência |
| target `armv7-linux-androideabi` (Android armv7) | idem | ausente | pendência |
| Node.js | `node --version` | v25.8.1 | ok |
| npm | `npm --version` | 11.11.0 | ok |
| pnpm | `pnpm --version` | 11.3.0 | ok |
| yarn | `which yarn` | não encontrado | faltando (não requerido) |
| bun | `which bun` | não encontrado | faltando (não requerido) |
| Expo SDK/CLI | `npx --no-install expo --version` | não instalado localmente; npm resolve `expo@56.0.12` como o que seria instalado (linha SDK 56) | pendência (nasce na F0.1 com o scaffold) |
| `ubrn` (uniffi-bindgen-react-native) | `which ubrn` / `ubrn --version` | não encontrado no PATH | pendência |
| `uniffi-bindgen` | `which uniffi-bindgen` | não encontrado no PATH | pendência |
| `cargo-ndk` | `which cargo-ndk` | não encontrado | faltando (bloqueia build Android) |
| Xcode (full) | `xcodebuild -version` | indisponível — só Command Line Tools em `/Library/Developer/CommandLineTools` | faltando (bloqueia build iOS) |
| Android SDK | `echo $ANDROID_HOME` | vazio (ANDROID_HOME e ANDROID_SDK_ROOT não definidos) | faltando (bloqueia build Android) |
| adb | `which adb` | não encontrado | faltando (bloqueia build Android) |
| Android NDK | listagem sob `$ANDROID_HOME/ndk` | inexistente (sem ANDROID_HOME) | faltando (bloqueia build Android) |

Legenda de status:
- **ok** — presente e pronto para uso.
- **pendência** — ausente, mas instalável dentro do escopo do loop antes da tarefa
  que o exige (rust targets via `rustup target add`; `ubrn`/`uniffi-bindgen` via
  npm/cargo; `cargo-ndk` via `cargo install`; Expo via scaffold F0.1). Não bloqueia
  F0.0.
- **faltando** — gap externo real que exige ação humana (download grande / config
  de sistema). Vira bloqueio explícito de tarefas futuras específicas (ver abaixo).

## Saída bruta dos comandos

### Rust
```
rustc 1.96.0 (ac68faa20 2026-05-25)
cargo 1.96.0 (30a34c682 2026-05-25)
rustup 1.29.0 (28d1352db 2026-03-05)
targets instalados:
  aarch64-apple-darwin
```
Nenhum dos 5 targets de build alvo (wasm32, iOS x2, Android x2) está instalado.

### Node / JS
```
node v25.8.1
npm 11.11.0
pnpm 11.3.0
yarn: not found
bun: not found
```

### Expo
```
which expo            -> não encontrado globalmente
which eas             -> não encontrado globalmente
npx --no-install expo --version
  -> npm error: npx canceled due to missing packages and no YES option: ["expo@56.0.12"]
```
Interpretação: Expo não está instalado localmente (esperado — não há projeto;
o app nasce na F0.1). O resolvedor do npm aponta `expo@56.0.12` como o pacote que
seria instalado, indicando a linha atual = Expo SDK 56. Não rodei a instalação
(seria fora de escopo da F0.0 e disparia download).

### UniFFI / bindgen
```
which ubrn            -> ubrn not found
ubrn --version        -> não disponível
which uniffi-bindgen  -> não encontrado
which cargo-ndk       -> cargo-ndk not found
```

### Mobile (Xcode / Android)
```
xcodebuild -version
  -> erro: requires Xcode, but active developer directory
     '/Library/Developer/CommandLineTools' is a command line tools instance
xcode-select -p       -> /Library/Developer/CommandLineTools
ANDROID_HOME=''  (vazio)
ANDROID_SDK_ROOT='' (vazio)
which adb             -> não encontrado
$ANDROID_HOME/ndk     -> inexistente
```

## Fluxo esperado do gerador de bindings (VISION §4)

Arquitetura escolhida: `uniffi-bindgen-react-native` (Mozilla/Filament), CLI
`ubrn`, que a partir de uma interface Rust anotada com UniFFI gera bindings
TypeScript que funcionam como Turbo Modules nativos (Swift/iOS, Kotlin/Android) e
como WASM para web (`wasm32-unknown-unknown`). O projeto está em transição de nome
para `uniffi-bindgen-javascript`. Nome/versão exatos a fixar na tarefa que
instalar a ferramenta (provável F0.2). Pendência: confirmar nome de pacote e
versão no momento da instalação.

## Bloqueios futuros explícitos (NÃO bloqueiam a F0.0)

A F0.0 apenas documenta estas lacunas; elas bloquearão tarefas específicas:

1. **Targets Rust ausentes** (wasm32 + iOS x2 + Android x2): resolvíveis via
   `rustup target add ...` na tarefa que compilar para cada alvo. Pendência leve.
2. **`ubrn` / `uniffi-bindgen` ausentes**: instalar na tarefa da fronteira UniFFI
   (provável F0.2). Pendência.
3. **`cargo-ndk` ausente + Android SDK/NDK não configurados** (ANDROID_HOME vazio,
   sem adb, sem NDK): BLOQUEIO real para a tarefa de build Android (ex.: F0.8).
   Exige instalar Android SDK + NDK e definir ANDROID_HOME — ação humana/setup.
4. **Xcode completo ausente** (só Command Line Tools): BLOQUEIO real para a tarefa
   de build iOS (ex.: F0.7). Exige instalar o Xcode (App Store) e
   `xcode-select`/`xcodebuild`. Ação humana.

## Conclusão do Executor (recomendação, não decisão)

Relatório produzido com sucesso. Nenhum bloqueio para a própria F0.0 — todos os
critérios de aceite têm dado coletado:
- [x] Tabela ferramenta → versão → status preenchida.
- [x] `wasm32-unknown-unknown` ausente, marcado como pendência a instalar.
- [x] Gerador de bindings UniFFI: nome confirmado (`uniffi-bindgen-react-native`,
      CLI `ubrn`, em transição p/ `uniffi-bindgen-javascript`); versão a fixar na
      instalação — ferramenta ainda não presente no ambiente.
- [x] Lacunas (Xcode, Android SDK/NDK, cargo-ndk, targets) listadas como bloqueios
      explícitos de tarefas futuras.
</content>
</invoke>

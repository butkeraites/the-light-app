# Loop — Board de status

> **Modo autônomo:** o Claude Code (Driver + planner/executor/reviewer) mantém
> esta tabela a cada ciclo. O Guia só audita. Legenda: ⬜ backlog · 🟡 ready ·
> 🔵 in_progress · 🔴 blocked/failed · ✅ aceito · ⛔ gate (HALT p/ sign-off)

Última atualização: 2026-07-03 00:45 UTC · Estado do loop: **✅ LIVE — Fase 5 em execução; F5.1–F5.6 ACEITAS (6/27); próxima F5.7.** F5.6 (build wasm em release + wasm-opt) aceita (`3e7e5e8`, reviewer independente): frontier wasm **4.24MB→1.14MB (−71.8%)** via `cargo build --release` + `wasm-opt -Oz`; **zero-drift** (15 self-tests, wasm-bindgen glue byte-idêntico, sem mudança de API); reviewer rebuildou → mesmo sha; `panic="abort"` evitado (preserva `catch_unwind`/UniFFI); baseline da F5.3 atualizada. the-light `2fc2dab`. Próximo ADR livre = **ADR-0039**. **[F5.1–F5.5 abaixo]** F5.5 (i18n do fluxo de leitura) aceita (`694ca88`, reviewer independente): títulos de header reativos (troca PT⇄EN sem restart via `_layout` consumindo `useI18n`), telas `read/*` migradas, `lang={locale}` nos 4 painéis de IA (resposta segue o idioma da UI); **anti-alucinação confirmada** (versículo/refs nunca via `t()`; nomes de livro do STORE, `locale` só seleciona o campo; `reference` canônica EN; CC-BY intacto). tsc/i18n(25 chaves)/reading/search/expo-web verdes; the-light `2fc2dab`. Próximo ADR livre = **ADR-0039**. **[F5.1–F5.4 abaixo]** F5.4 (persistência nativa do progresso do plano) aceita (`f52e16e`, reviewer independente): 4 fns de fronteira (`reading_plan_progress`/`start_reading_plan`/`set_reading_plan_completed`/`clear_reading_plan`) delegando ao `PlanStore` do core (`<data_dir>/reading-plans/active.json`), impl nativa + stub web; 5 host tests (incl. round-trip start→marca→reload de handle novo); validação anti-alucinação contra o CATALOG antes de I/O; 80 testes; the-light `2fc2dab` intacto. Reading-plans nativo: F5.1 (geração) + F5.4 (progresso) prontos → **F5.7 (UI nativa) desbloqueada**. Próximo ADR livre = **ADR-0039**. **[F5.1–F5.3 abaixo]** F5.3 (tracer perf web) aceita em **2 tentativas**: attempt 1 reprovada pelo reviewer (métrica de bundle não byte-reproduzível + header falso) → retry focado (sem HALT) → attempt 2 aceita (`675ca64` defer do wasm de 4MB do first-paint via `WasmGate` por-rota + `26b29ec` métrica honesta/reproduzível: assets estáveis exatos + `moduleCount=854` + nominal±tolerância, `loop/perf/web-bundle-baseline.json` byte-idêntico em 5 runs). Auto-correção do loop funcionando (reviewer independente barrou trabalho falho). Próximo ADR livre = **ADR-0039**. **[F5.1/F5.2 abaixo]** F5.2 (tracer i18n) aceita (`05f8102`, reviewer independente): camada i18n PT/EN dependency-free (catálogos com paridade em compile-time) + KV de prefs offline (backend injetável: arquivo nativo / localStorage web, reusável p/ theme-persist) + seletor de idioma + home migrada (100% via `t()`, a11y, zero hex); i18n só toca CHROME (versículo/CC-BY verbatim, nunca traduzidos); ADR-0038; tsc/i18n-test/expo-web/self-tests verdes; the-light `2fc2dab` intacto. Próximo ADR livre = **ADR-0039** (0036=F5.22 sync, 0037=F5.10 plans PR, 0038=F5.2 i18n). **[histórico F5.1 abaixo]** Marco 4 aprovado; F5.1 (fronteira NATIVA de planos, `eb81a72`) aceita. Marco 4 aprovado (Fase 4 completa). **F5.1 (tracer de planos de leitura) aceita** (`eb81a72`, reviewer independente): expôs a geração de planos JÁ no core (`userdata::plans`) pela **fronteira NATIVA** (impl `cfg(not wasm32)` + stub web) + 6 host tests; 75 testes; fmt/clippy/tsc/wasm-puro verdes; zero drift (TS só reexporta; nomes/refs do core); the-light `2fc2dab` intacto. **RE-ESCOPO registrado:** a F5.1 cfg-free original era impossível (`userdata` é `#[cfg(feature="embedded")]` → ausente sob `ai-pure`/wasm); seguido o padrão native-first → **paridade web (F5.10) passou a exigir PR `ai-pure` ao core** (gate estratégico futuro, ADR-0037). **Queue:** F5.2 (i18n PT/EN + prefs KV — **próxima**, menor ID), F5.3 (perf web), F5.22 (⛔ gate de sync). **Próximo ciclo:** Driver seleciona **F5.2** (gate:false) → executor+reviewer. Próximo ADR livre = **ADR-0036** (F5.22 sync) · **ADR-0037** reservado à PR `ai-pure` de planos (F5.10). F4.6 (streaming NATIVO real por provedor) é a última tarefa técnica antes do Marco 4 e **toca o the-light** → PR + ADR + merge humano + re-pin (regra não-negociável). O executor preparou (`269b8fe`), SEM tocar o the-light, a **proposta completa** em `loop/proposals/the-light-PR-native-sse-streaming.md` (645 linhas: parsers de delta PUROS testáveis por fixture + `stream_reader` genérico sobre `BufRead` + override de `complete_stream` sob `#[cfg(embedded)]` nos 4 provedores — Anthropic/OpenAI/Gemini SSE, Ollama NDJSON) + **ADR-0035**. **Não-quebrante** (default `complete_stream` preservado; `complete` intacto), **zero-drift** (concat dos deltas == `complete`; `cited_text`/citações do store/`ai-pure`), **embedded-only** (nada no grafo wasm), **assinatura da fronteira/bindings INALTERADA** (`ask_anchored_stream`/`AiTokenCallback` só passam a receber N tokens reais). Ver `loop/HALT`. **Retomada:** humano cria o branch `feat/native-sse-streaming` (de `04b9b24`) no the-light, aplica a proposta, roda `cargo fmt/clippy -D warnings/test`, mergeia e fornece o novo rev → Driver re-pina `core/Cargo.toml` (2 linhas) + revalida (`ask_anchored_stream` emite tokens reais; tsc/expo/self-tests verdes) → **F4.7 ⛔ Marco 4**. the-light `04b9b24`. Próximo ADR livre = **ADR-0036**.
**F4.4 — dimensionamento (fonte `04b9b24`, só-leitura; 100% app-side):** paridade WEB do Tavily (F4.3 fez o nativo). Como `TavilyProvider`/`build_research_provider` são `embedded`-only (reqwest → não compilam em wasm), no web a busca é `fetch` TS — molde EXATO de `research.web.ts::wikipediaSearch`, **mas com chave**. **Forma REAL da request (research.rs:240-287, TavilyProvider::search):** `POST https://api.tavily.com/search`, header `content-type: application/json`, corpo JSON `{ "api_key": <chave BYOK>, "query": <reference_label>, "max_results": <limit clamp 1..10>, "search_depth": "basic" }` — a **chave vai NO CORPO** (`api_key`), NÃO em header/URL. **Parse:** `v["results"][]` → por item (pular `url` vazio) `url=it["url"]`, `title=it["title"]`(default "(sem título)"), `snippet=it["content"]`(default ""), `site`=host da url → `StudyWebSourceInput{title,url,snippet,site,fetchedAt}` (mesmo tipo que `wikipediaSearch` já produz). **Mudanças (só glue TS + UI + self-test):** (1) `research.web.ts::tavilySearch(fetch,key,query,lang,limit)` novo (POST corpo `{api_key,query,max_results,search_depth:"basic"}`; erro cita só status; `DEFAULT_TAVILY_LIMIT=4`); (2) `study.web.ts::resolveWebSources` ganha `researchKey`, ramo `"tavily"` exige a chave (senão erro citando só "tavily", 0 fetch), e `deepStudyOnHandle` **para de ignorar** `researchKey` (remove `void researchKey`) e o repassa; wikipedia/off inalterados; (3) `ReaderStudyPanel.tsx` seletor 3-vias off/Wikipedia/Tavily + campo de chave session-only + aviso; (4) chave session-only (ADR-0025) — **fricção real:** `keystore.web.ts::assertProvider` só aceita provedores de LLM → usar `useState` do componente OU `sessionSecureBackend` sob `tla.research.tavily` (não o `setKey` de LLM); nunca persiste/loga/git. **Prova MOCK** (headless node, sem chave/rede): `deepStudyOnHandle` + `researchBackend="tavily"` + `researchKey="dummy"` + `fetch` MOCK de `api.tavily.com/search` → assert chave **no CORPO** (não na URL/log), ≥1 citação `kind="Web"` da URL Tavily montada pelo Rust `study_web_finalize`, `academic_markdown` cita `[W`, `passage_text` João 3:16 do store ≠ interpretação; tavily **sem chave**→erro só "tavily" 0 fetch; wikipedia preservado (keyless); off→0 pesquisa. `tsc`+`expo export web` 0; self-tests web sem regressão; grafo wasm PURO (nenhum Rust tocado); the-light `04b9b24`/`core/Cargo.toml` intactos. Regras: anti-alucinação ZERO-DRIFT (citações Web do Rust das URLs, NUNCA do TS — ADR-0029), BYOK (chave session-only só no corpo, nunca git/log — ADR-0025), Tavily=rede opt-in padrão OFF (validação real=F4.5 ⛔), Wikipedia keyless preservado (ADR-0028), the-light só via PR+ADR (aqui NENHUM Rust muda). **NÃO-BLOQUEANTE, NÃO toca the-light.** Possível **ADR-0035** (Tavily web: transporte `fetch` + chave session-only + toggle UI). Chave real Tavily = **F4.5** (gate). Próximo ADR livre = **ADR-0035**.
Heartbeat: ver `HEARTBEAT` · HALT: **ausente** · **F4.4 elegível** (ready, gate:false, deps:[F4.3] aceita). **F4.3 — dimensionamento (fonte `04b9b24`, só-leitura):** `deep_study` (`lib.rs:1355`, corpo `cfg(not wasm32)` + stub web) hoje passa `build_research_provider(&backend, None, lang.code())` (`lib.rs:1438`, chave `None` HARD-CODED) → basta p/ `"wikipedia"` (keyless) e `"mock"` (sem rede), mas o Tavily **exige a chave**. **REAIS:** `#[cfg(feature="embedded")] pub fn build_research_provider(backend:&str, key:Option<String>, lang:&str) -> Result<Box<dyn ResearchProvider>>` (`ai/research.rs:296`): `"tavily"`→`key.ok_or(AiError::NoKey("research.tavily"))?` + `TavilyProvider::new(key)` (`research.rs:305`); `"wikipedia"|"wiki"`→chave IGNORADA; `"mock"`→`MockResearchProvider::canned()` (2 WebSource fixos, sem rede). `#[cfg(feature="embedded")] pub struct TavilyProvider{key:String}` + `pub fn new(key: impl Into<String>)` (`research.rs:222/229`); `search` faz `POST https://api.tavily.com/search` (`api_key` no corpo, `reqwest::blocking`, BYOK) → embedded-only. **Mudança de assinatura:** `deep_study` ganha `research_key: Option<String>` (último param, depois de `research_backend`) → passa a `build_research_provider(&backend, research_key, lang.code())`; stub web `let _ = (…)` inclui `research_key`; grafo wasm puro (`research`/Tavily embedded-only, no bloco `cfg(not wasm32)`). Higiene TS: `deepStudy` (`reading.ts:301`, `reading.web.ts:382`) + `deepStudyOnHandle` (`study.web.ts:129`, aceito e IGNORADO — Tavily web = F4.4) ganham `researchKey?: string` opcional final; 9 call sites de teste em `lib.rs` ganham o arg; bindings regenerados. **MOCK (sem rede/chave):** `tavily+research_key=None`→CoreError (NoKey, antes de qualquer HTTP); `mock` preservado (F3.9a, com/sem chave); wikipedia ignora a chave; `None`→offline. Chave real Tavily = **F4.5** (gate). Regras: BYOK (chave nunca em git/log; só repassada ao core), Tavily=rede opt-in (F4.5), Wikipedia keyless preservado (ADR-0028), anti-alucinação (citações das URLs, nunca do modelo), offline-first (padrão desligado), the-light só via PR+ADR. **NÃO-BLOQUEANTE, NÃO toca the-light** (`04b9b24`; `core/Cargo.toml` intacto). Possível **ADR-0035** (Tavily BYOK threading da chave de pesquisa). **Fase 4 = Polimento da IA** (decomposta): F4.1 streaming web real (token-a-token via fetch SSE, NÃO-BLOQUEANTE, não toca the-light) → F4.2 transporte web multi-provedor (anthropic/openai/ollama, NÃO-BLOQ) → F4.3 fronteira Tavily BYOK nativo `research_key` (NÃO-BLOQ) → F4.4 Tavily web fetch+chave session-only+UI (NÃO-BLOQ) → **F4.5 ⛔ validação real** (streaming/multi-IA/Tavily reais = chaves, BLOQUEANTE) → **F4.6 PR streaming NATIVO SSE** (BLOQUEANTE, toca the-light) → **F4.7 ⛔ Marco 4**. **Achados do core (`04b9b24`, só-leitura):** `complete_stream` tem SÓ o default não-quebrante (`mod.rs:378`, emite 1×); NENHUM provedor sobrescreve com SSE (todos `"stream": false`) → streaming nativo real = PR (F4.6); `TavilyProvider`/`build_research_provider` são `pub` mas `embedded`-only (BYOK `research.tavily`) → nativo já expõe (só threading de `research_key`, hoje `None` em `lib.rs:1438`), web = fetch TS; `PROVIDERS=[anthropic,openai,ollama,gemini]` → transporte web multi-provedor é app-side (o `webLlmTransport` só trata gemini+mock hoje). Regras: anti-alucinação ZERO-DRIFT (streaming muda só o TRANSPORTE; texto/glosas/citações do store/`ai-pure`; tokens só da interpretação), BYOK (chave só no header; Tavily session-only web/Keychain nativo), offline-first (IA/pesquisa opt-in), the-light só via PR+ADR. `the-light` `04b9b24`. Próximo ADR livre = **ADR-0033**.
**Superfície ai-pure do ESTUDO confirmada no wasm (fonte `04b9b24`, só-leitura — dimensiona F3.12a):** disponíveis sob `ai-pure` (compilam em `wasm32`, chamáveis do corpo web da fronteira): `study::StudyRequest`/`StudyResult` (`any(embedded,ai-pure)`, construíveis), `study::user_prompt(&StudyRequest,passage_text)->String` (**`pub` novo da F3.11**), `study::split_sections` (pura), `study::cited_web_indices` (`pub`), `numbered_verses`/`numbered_passage`/`ask_context` (puras), `StudyResult::to_academic_markdown(lang)`/`to_markdown` (`any(embedded,ai-pure)`, aplicam `rewrite_anchors`+aparato), `prompts::system_prompt_in(mode,lens,depth,lang,None)->String` (**pura cfg-free**, `None`=prompt embutido; o `system_prompt` de disco é `embedded`-only), `lexicon::{VerifiedLexicon,LexicalEntry,format_verified_block,verify,strong_set,EMPTY_SENTINEL}` (puros; **MAS `verified_lexicon(conn,…)` é `embedded`-only/rusqlite → não existe no wasm**), `citation::{CitationCollector::{from_verified_lexicon,from_web_results,into_vec},rewrite_anchors,to_json,sbl_*,provenance_footer,Citation}`, `research::WebSource` (**puro sob `ai-pure`**, chrono clock-free; **`build_research_provider`/`WikipediaProvider` são `embedded`-only/reqwest → pesquisa web = `fetch` TS, F3.12b**), `model::Passage`/`Verse` (módulo sempre disponível → construível p/ `passage: Some`). **Desenho da fronteira web (só `ai-pure`, cfg-free, molde `ai_web_prepare`):** `study_web_prepare(reference,mode,lens,depth,lang,provider,model?,verses:Vec<AiVerseInput>,lexicon_entries:Vec<StudyLexEntryInput>,lexicon_sources:Vec<String>,web_sources:Vec<StudyWebSourceInput>)->StudyWebRequest{reference,reference_label,passage_text,system,user,provider,model}` = `passage_text=numbered_verses(verses)` (verbatim store) + `Passage` dos verses + `VerifiedLexicon` dos inputs + `system=system_prompt_in(…,None)` + `user=user_prompt(&req,&passage_text)`; `study_web_finalize(…,passage_text,provider,model,raw_llm_response,lexicon_entries,lexicon_sources,web_sources)->StudyResultOut` = `split_sections` + `verify(&raw,&vl).warnings` + `cited_web_indices` + `CitationCollector{from_verified_lexicon+from_web_results}` + `to_academic_markdown(lang)` → `StudyResultOut` (Record já tem `academic_markdown`, F3.8). `web_sources` **vazio na F3.12a** (populado por Wikipedia fetch na F3.12b, sem re-tocar Rust). Léxico web = recuperação TS do subset (`sqlite-lexicon.web.ts`, SELECT `original_tokens`+`lexicon`+`scholarly_sources`, agrega por Strong base — infra ADR-0011, NÃO anti-alucinação). **Risco/decisão:** se a fronteira exigir símbolo do core ausente em `ai-pure` (aggregador de léxico só-`embedded` cuja replicação seria drift do anti-alucinação) → `blocked`+HALT (outro PR). Prova: headless node `deepStudy` web (fetch MOCK) — João 3:16 do store + citações do léxico STEP CC-BY + `academic_markdown>0` — + paridade nativo↔web (host). Grafo wasm puro; `tsc`/`expo export web` 0; chave session-only nunca logada. **NÃO** espelhar prompt/citação/aparato em TS (ADR-0029). Próximo ADR livre = **ADR-0031**.
**F3.9a — dimensionamento (fonte `c8ecb2f`, só-leitura):** `deep_study` (`core/src/lib.rs:1330`, corpo já `cfg(not wasm32)` + stub web) hoje passa `web_sources: vec![]` ao `study()`. D1 = novo param `research_backend: Option<String>` → quando `Some`, `the_light_core::ai::build_research_provider(backend, None, lang.code())` (`ai/research.rs:268`, **`pub` re-export sob `#[cfg(feature="embedded")]`** `ai/mod.rs:40`; `"mock"`→`MockResearchProvider::canned()` 2 fontes fixas SEM rede, `"wikipedia"`→`WikipediaProvider` KEYLESS rede real, `"tavily"`→exige chave) + `ResearchProvider::search(&reference_label, limit)` → `web_sources`. `study()` (`study.rs:294`, embedded) em modo **Academic** (`emits_apparatus`) chama `from_web_results` → **1 citação `Web` por fonte** (`kind="Web"`, `url`/`quote`/`attribution`), independe do modelo citar; `cited_web_indices` sinaliza `[W:n]` fora do intervalo. **`research` é embedded-only (reqwest) → a busca fica DENTRO do bloco `cfg(not wasm32)`** (grafo wasm puro); `WebSource` NÃO atravessa a UniFFI (sem novo Record). Prova MOCK: `deep_study(...,Academic,...,"mock",None,None,Some("mock"))` → `citations` com ≥1 `kind="Web"` url `example.org`, sem rede; `None`→sem `Web`; `passage_text`≠`interpretation`. Higiene `tsc`: wrapper `deepStudy` (`app/web/reading.ts:296` + stub `reading.web.ts:365`) ganha `researchBackend?: string` opcional (callers/self-tests intactos). **F3.9a NÃO-BLOQUEANTE, NÃO exige PR** (`research` já `pub`/embedded em `c8ecb2f`), **não toca the-light**. Rede real (`"wikipedia"`) = opt-in do usuário, validado na F3.10.
**Símbolos REAIS de `ai::research` (fonte `c8ecb2f`, só-leitura — dimensionam D1) + fricção study-embedded (D2):** todo `ai::research` é `#[cfg(feature="embedded")]` (`ai/mod.rs:19-20`, re-exports :39-40) → usa `reqwest::blocking` (`research.rs:48`) → **nativo-only, NÃO compila em wasm/`ai-pure`**. `RESEARCH_BACKENDS=&["wikipedia","tavily","mock"]` (`research.rs:23`); `WebSource{title,url,snippet,site,fetched_at:DateTime<Utc>}` (:27); `trait ResearchProvider{name();search(query,limit)->Result<Vec<WebSource>>}` (:41); **`WikipediaProvider::new(lang)` = KEYLESS rede real** (API pública `/w/api.php`, :138), **`TavilyProvider::new(key)` = BYOK** (chave `research.tavily`, :200), **`MockResearchProvider::canned()` = SEM rede** (2 fontes fixas, :101); **`build_research_provider(backend, key:Option<String>, lang:&str) -> Result<Box<dyn ResearchProvider>>`** (:268): `"mock"`→mock; `"wikipedia"|"wiki"`→Wikipedia (chave ignorada); `"tavily"`→exige chave (`Err(NoKey("research.tavily"))` se None); `other`→`UnknownProvider`. **Como `study()` usa a pesquisa (anti-alucinação embutida):** `StudyRequest.web_sources:Vec<WebSource>` (`study.rs:53`; **hoje a fronteira F3.3 passa `vec![]`**) — habilitar = a fronteira BUSCAR (`build_research_provider(...).search(...)`) e passar as fontes; `study()` (`study.rs:294`, embedded) injeta bloco "FONTES SECUNDÁRIAS … cite por [W:n], NÃO invente URLs" (:200-217), `cited_web_indices` sinaliza `[W:n]` fora do intervalo (:308-317, warning) e `CitationCollector::from_web_results` monta citações das URLs, NUNCA do modelo (:322). **Fricção study-embedded (D2):** `study`/`StudyRequest`/`StudyResult` são `embedded`-only e `user_prompt` é privado (`study.rs:181`); `verified_lexicon` embedded (rusqlite); no web NADA disso existe. Contraste: `ask`/`ask_context`/`numbered_passage`/`rewrite_anchors` já `ai-pure` (F2.7/ADR-0024, web via prepare→fetch→finalize F2.7b/ADR-0025) e **conversa `ask_session`/`refine_scope`/`parse_refinement` JÁ `ai-pure`** (F3.4 → roda no web SEM PR). **A paridade web do ESTUDO exige um PR `ai-pure`** (molde ADR-0024 → F3.11 condicional) para zero drift — espelhar a montagem do estudo em TS app-side é PROIBIDO (drift do anti-alucinação, sem entrada pública pura). **F3.9 NÃO toca código** (brief de decisão); the-light `c8ecb2f` intacto. Próximo ADR livre = **ADR-0028**.
**Investigação da fonte (`c8ecb2f`, só-leitura) + design REAIS de F3.8:** `StudyResult::to_academic_markdown(&self, lang: Lang) -> String` (`ai/study.rs:508`) é **`pub`** dentro de `impl StudyResult` marcado `#[cfg(feature="embedded")]` (`ai/study.rs:425`) → **`pub` porém `embedded`-only** = callável da fronteira NATIVA (que usa `embedded`, `core/Cargo.toml:42`), **SEM PR ao the-light**. Produz o paper SBL: YAML (`title/author/lang`), `## Texto (acervo local)`(=`passage_text` verbatim do store), `## Análise`(=`interpretation`/`sections` com âncoras `[V:Strong]`/`[W:n]` reescritas p/ `[^chave]` deterministicamente pelo core — inválidas DESCARTADAS via `citation::rewrite_anchors`), `## Notas`(`citation::sbl_footnote`), `## Bibliografia`(`sbl_bibliography_entry`), `citation::provenance_footer` (**separa verificável/acervo local COM atribuição STEP CC-BY das citações `Source` do gerado por IA**: "…geradas por {provider}/{model} … confira sempre as fontes primárias"). `to_markdown()`(`study.rs:432`)=Markdown simples, NÃO é o alvo. Toda a lógica SBL (`sbl_footnote`/`sbl_bibliography_entry`/`provenance_footer`/`rewrite_anchors`/`to_json`) vive em `ai/citation.rs` → **reimplementar no app = drift PROIBIDO**. A atribuição STEP aparece pq `study()` constrói citações `Source` do `verified_lexicon` (`CitationCollector::from_verified_lexicon`); João 3:16 tem léxico no subset (F3.5/ADR-0027 `lexicon=21`) → prova robusta. **DECISÃO (fonte única, zero drift) — PREFERIDA (C):** estender `StudyResultOut` (`core/src/lib.rs:1219`) com `academic_markdown: String` populado no `From<StudyResult>` existente (l.1256, já `cfg(not wasm32)`) via `result.to_academic_markdown(result.language)` → **UMA chamada** (reusa o estudo que a UI já rodou; **não** re-roda o LLM na F3.10 real), **sem reconstrução reversa**. Alternativa (A): fn `deep_study_academic_markdown(...)` compartilhando helper com `deep_study` — **caveat: re-roda `study()` = 2ª chamada paga com provedor real**. **NÃO** a via (b) app-side (drift). O corpo/`From` fica `cfg(not(target_arch="wasm32"))` + stub web (export web = F3.12); grafo wasm puro. **Sidecar = app-side (molde F1.11 `notesExport.buildNotesExport`):** JSON `{reference,provider,model,disclaimer IA,attributions:[STEP CC-BY,…],citations:StudyCitation[]}` a partir de `StudyResultOut.citations` JÁ retornado (agrega Records, NÃO reimplementa serialização; `Citation` do core é "round-trippável p/ `.citations.json`"). **Share** = botão "Exportar (acadêmico)" (`study-export-academic`) no `ReaderStudyPanel` → Share do `react-native` (molde F1.11). Prova `export-selftest.ts` (molde `study-selftest.ts`): `deepStudy(...,"mock",…)` → Markdown acadêmico + sidecar → `TLA_EXPORT ref="John 3:16" provider="mock" md_len=<n> has_passage=true has_attribution=true` (has_passage = md contém `passagePrefix(passageText)` do store; has_attribution = md contém token STEP das `sources` — NÃO hardcode); `run-ios-selftest.sh` asserta sem regressão dos demais `TLA_*`. **ESTA tarefa TOCA `core/src/lib.rs` → resolve o FOLLOW-UP ADR-0027** (9 doc-comments `**puro**/` → `**puro** /` nas linhas 846/874/993/1036/1085/1122/1146/1433 etc., tornando o saneamento `**/` dos geradores redundante). **F3.8 depende SÓ de F3.3 (`49919c3`)+F3.5 (`04a7a61`), ambas aceitas; é NÃO-BLOQUEANTE (só mock, sem chave/rede); NÃO exige PR** (`to_academic_markdown` já `pub`/`embedded` em `c8ecb2f`). `core/Cargo.toml` inalterado; the-light `c8ecb2f`. Próximo ADR livre = **ADR-0028** (só se decidir forma do sidecar). **É a ÚLTIMA tarefa antes do gate F3.9 (⛔) → após ela o loop PARA (HALT).**
**Assinatura/tipos REAIS de F3.7 (reuso confirmado — NÃO criar função nova):** a comparação **reusa** `ask_anchored` (F2.1/F2.3a, aceitas) já exposta no glue `askAnchored(dbPath, translation, reference:string("John 3:16"), question, provider, key:string|undefined, model:string|undefined, lang) -> AiAnswer` (`app/web/reading.ts` l.224; **`reference` é STRING canônica, NÃO book/chapter/verse numéricos; `key`/`model` opcionais; `lang` por último**). Retorno **REUSA** `AiAnswer{reference,cited_text(=passagem numerada verbatim do store),interpretation(=saída só do modelo/mock),provider(=LlmProvider::name),model}`. **`askAnchored` JÁ exportada nos DOIS glues** (`reading.ts` nativo E `reading.web.ts` destubada F2.7b) → **NENHUM glue novo, NENHUM stub web novo, NENHUMA função de fronteira nova**; regen de bindings **idempotente** (binding `ask_anchored` já existe desde F2.1/F2.3a). Provedores do seletor = `SUPPORTED_PROVIDERS` (`anthropic/openai/gemini/ollama`, `app/lib/keystore.ts` l.20) + `'mock'`; chave real (não-mock) via `getKey(provider)` (keystore F2.4); prova usa só `'mock'` (sem chave/rede). **DESENHO adotado:** `cited_text` (âncora, verbatim do store) é IDÊNTICO em todas as colunas → exibido UMA vez como bloco de âncora comum, SEPARADO das N `interpretation` (anti-alucinação MAIS visível; é o invariante `cited_match` que o self-test prova). **Custo OMITIDO** (a fronteira NÃO expõe `estimate_cost_usd`, simplificação da F2.5). Self-test `compare-selftest.ts` (molde `chat-selftest.ts`): 2 chamadas `askAnchored(...,'mock',...)` p/ João 3:16 → `TLA_COMPARE ref="John 3:16" providers=2 cited_match=true first_provider="mock" cited_prefix="For God so loved..."`; `run-ios-selftest.sh` asserta providers>=2 + cited_match=true + first_provider="mock" + cited_prefix, sem regressão dos demais TLA_*. `compare-selftest.web.ts` = SKIP (comparação web = F3.12). **F3.7 depende SÓ de F3.5 (aceita `04a7a61`), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR** (só app/UI/self-test; the-light `c8ecb2f`/`core` intactos).
**Assinatura/tipos REAIS de F3.6 (confirmados em `core/src/lib.rs` — NÃO modificar):** `ask_session_anchored(db_path, translation, book:u8, chapter:u16, verse:Option<u16>, lang, turns:Vec<ChatTurn>, study_mode:Option<StudyMode>, study_lens:Option<StudyLens>, provider_name, key:Option, model:Option) -> AiAnswer` (l.1656; **ATENÇÃO à ordem: `lang` ANTES de `turns`; `study_mode`/`study_lens` DEPOIS de `turns` e ANTES de `provider_name`; passagem NUMÉRICA book/chapter/verse, NÃO string "John 3:16"**). Retorno **REUSA** `AiAnswer{reference,cited_text(=passagem numerada verbatim do store),interpretation(=saída só do modelo/mock),provider,model}` (l.2121) — **SEM campo `turns` no retorno** → o marcador `turns=<n>` do self-test vem do tamanho do histórico de ENTRADA. `ChatTurn{role:ChatRole,content}` + `ChatRole{User,Assistant}` (l.1463/l.1438), puros/`ai-pure`. Invariante do core: o `context` (âncora) é embutido só no 1º turno de usuário → a UI passa SEMPRE o mesmo book/chapter/verse (âncora preservada). **Bindings F3.4 JÁ gerados** (`native-generated`/`generated`, l.276/l.285 `askSessionAnchored`); o glue `reading.ts` (+ stub `reading.web.ts`=F3.12) ainda NÃO reexporta o wrapper de conversa. Opcionais (NÃO ao aceite): `refine_scope`/`parse_refinement` (`RefinementOut`/`RefinementRound`). **F3.6 depende SÓ de F3.4+F3.5 (aceitas), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR** (só app/UI/glue/self-test + bindings gerados ignorados; the-light `c8ecb2f`/`core` intactos).
**Assinaturas/tipos REAIS de F3.5 (confirmados em `core/src/lib.rs` — NÃO modificar):** `deep_study(db_path, translation, book:u8, chapter:u16, verse:Option<u16>, mode:StudyMode, lens:StudyLens, depth:StudyDepth, lang, provider_name, key:Option, model:Option) -> StudyResultOut` (l.1311; **atenção: `lang` ANTES de `provider_name`; passagem é book/chapter/verse numéricos, NÃO string "John 3:16"**); `lexical_entries(db_path, book:u8, chapter:u16, verse:Option<u16>, lang, limit:Option) -> VerifiedLexiconOut` (l.948; **SEM `translation`** — léxico independe de tradução). Enums: `StudyMode{Academic,Devotional,Introductory,Sermon}`, `StudyLens{Baptist,Presbyterian,Lutheran,Pentecostal,Catholic,Orthodox}`, `StudyDepth{Overview,Exegetical,WordStudy}`. Records: `StudyResultOut{reference,reference_label,mode,lens,depth,language,passage_text,interpretation,sections[],warnings[],citations[],provider,model}`; `VerifiedLexiconOut{entries:LexEntry[],sources:string[]}`; `LexEntry{strongs,lemma?,translit?,gloss?,occurrences,testament}`; `StudyCitation{kind,key,author?,title?,locus?,publisher?,year?,url?,license?,attribution?,accessed?,quote?}`. **Atribuição STEP CC-BY** = `VerifiedLexiconOut.sources` verbatim (`Credit it to 'STEP Bible' linked to www.STEPBible.org … CC BY 4.0`). **DECISÃO — léxico no subset bundled:** o `reading-sample.sqlite` NÃO tem léxico (F3.1 populou o `bible.sqlite`; o `gen_reading_sample_db.rs` propaga só verses/xref/FTS) → **adotado (a) PROPAGAR** `original_tokens`/`lexicon`/`scholarly_sources` ao subset (molde F1.9 que propagou xref), registrar em **ADR-0027** (tamanho, molde ADR-0014) → self-test asserta `lexicon>=1`/`attribution_ok=true` no device. `deepStudy`/`lexicalEntries` JÁ estão nos bindings gerados (F3.2/F3.3); o glue `reading.ts` ainda não os reexporta. **F3.5 depende SÓ de F3.2+F3.3 (aceitas), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (superfície `deep_study`/`lexical_entries` já `pub`/entregue em `c8ecb2f`; UI/glue/self-test é app-side). Próximo ADR livre = **ADR-0027**.
**Investigação da fonte (`c8ecb2f`, só-leitura) — assinaturas/tipos REAIS de F3.4:** `ask_session`, `refine_scope`, `parse_refinement`, `Refinement`, `ChatMessage`/`ChatRole`, `MockLlmProvider` são **TODOS ai-pure** (re-export puro `ai/mod.rs:24-30`; `MockLlmProvider` `pub` ungated `ai/mod.rs:399`); `build_provider` é **embedded-only** (`ai/mod.rs:38`). Assinaturas: `pub fn ask_session(provider:&dyn LlmProvider, lang:Lang, context:&str, turns:&[ChatMessage], study:Option<(StudyMode,Denomination)>) -> Result<String>` (`ai/study.rs:626`) — chama `provider.chat(system,messages)`; contexto embutido **só no 1º turno de usuário**; devolve **só a String** da interpretação (a âncora = `context` que a **fronteira monta do store**). `pub fn refine_scope(provider:&dyn LlmProvider, mode:StudyMode, lang:Lang, brief:&str, prior:&[(String,String)], round:u8) -> Result<Refinement>` (`ai/study.rs:411`) — **RECEBE provider** (`provider.complete`), **SEM `lens`** (CORREÇÃO do backlog, que supôs "pura sem provider" = FALSO). `pub fn parse_refinement(raw:&str) -> Refinement` (`ai/study.rs:346`) — **PURA, determinística, SEM provider** (o parser `PERGUNTA:`/`- opção`, dedup). `Refinement{question:String,options:Vec<String>}` (`ai/study.rs:70`, puro); `ChatMessage{role:ChatRole,content:String}`/`ChatRole{User,Assistant}` (`ai/mod.rs:339`/`320`, puros); `LlmProvider::chat` (default `ai/mod.rs:380`) dobra histórico→`complete` (Mock não sobrescreve → resposta FIXA). **`MockLlmProvider` CRÍTICO:** `complete` devolve rodada canônica se `system.contains("PERGUNTA:")` (o `refine_system_prompt` contém) → `refine_scope`+mock determinístico (`PERGUNTA: Qual recorte você prefere?\n- Efésios 2.8-9\n- ...`). **DECISÃO DE GATING (byte-verificada):** `parse_refinement` → **CFG-FREE** (roda no wasm, ANTECIPA parser web); `refine_scope` → mock **CFG-FREE** (`MockLlmProvider::default()` direto, ANTECIPA refinamento web), provider real `cfg(not wasm32)` (`build_provider`) + stub web (transporte=F3.12/F2.7b); `ask_session_anchored` → corpo `cfg(not wasm32)` + stub web (a **âncora** `context` vem do **store nativo** `Store::open`+`EmbeddedSource::passage`+`numbered_passage`+`xref::passage_labels` — embedded/rusqlite; F3.12 monta do store web e chama a `ask_session` pura, molde F3.3/F2.7b). **NÃO force cfg-free onde exige embedded.** Retorno da conversa **REUSA** `AiAnswer{reference,cited_text(=passagem numerada do store),interpretation,provider,model}` (F2.1). Records novos: `ChatTurn{role:ChatRole,content}` + `ChatRole` enum + `RefinementOut{question,options}` + `RefinementRound{question,answer}` (prior) — todos `From` **cfg-free**. Teste host (molde F2.1/F3.3, reusa `build_kjv_fixture`/`mock_fixed_response`): conversa mock → `cited_text` João 3:16 store ≠ `interpretation`==mock_fixed_response; multi-turno sem panic; anti-fake 3:16 vs 3:17; `refine_scope`+mock → question+3 opções; `parse_refinement` 3 casos + vazio/inválido sem panic; error paths→CoreError. **F3.4 depende SÓ de F3.3 (aceita `49919c3`), é NÃO-BLOQUEANTE (só mock, sem chave/rede), NÃO exige PR ao the-light** (toda a superfície já `pub` em `c8ecb2f`; mock direto evita `build_provider` embedded no caminho puro). Grafo wasm puro; uniffi `=0.31.0` e pin `c8ecb2f` intactos; `core/Cargo.toml` inalterado.
Heartbeat: ver `HEARTBEAT` · HALT: **ausente** · **F3.3 elegível** (ready, gate:false, deps:[F3.2] aceita). **Investigação da fonte (`c8ecb2f`, só-leitura) — assinatura/tipos REAIS de F3.3:** `#[cfg(feature="embedded")] pub fn study(provider:&dyn LlmProvider, req:&StudyRequest) -> ai::Result<StudyResult>` (`ai/study.rs:294`) — **NÃO recebe conn/Store** (o chamador monta o `StudyRequest`); chama `provider.complete(system,user)` **síncrono** (system=`prompts::system_prompt(mode,lens,depth,lang)`, user=numbered_passage+xrefs+bloco léxico verificado+fontes `[W:n]`); separa `passage_text`(banco) de `interpretation`(modelo); anti-alucinação embutida (`lexicon::verify` sinaliza Strong fora do acervo; `cited_web_indices` sinaliza `[W:n]` fora do intervalo; `citations` do banco/URLs, NUNCA do modelo). **`StudyRequest<'a>`** (embedded-only, lifetime por `passage:Option<&'a Passage>`): `reference`, `reference_label`, `mode:StudyMode`, `lens:Denomination`, `depth:StudyDepth`, `language:Lang`, `passage:Option<&Passage>`, `cross_references:Vec<String>`, `verified_lexicon:VerifiedLexicon`, `web_sources:Vec<WebSource>`, `brief:Option<String>`. **`StudyResult`** (embedded-only): `reference`, `reference_label`, `mode`, `lens`, `depth`, `language`, `passage_text:String`(banco,numerado), `interpretation:String`(modelo), `sections:Vec<StudySection>`, `warnings:Vec<String>`, `citations:Vec<Citation>`, `provider`, `model`. **Enums (variantes REAIS):** `StudyMode`{Academic,Devotional,Introductory,Sermon}; `Denomination`(lente){Baptist,Presbyterian,Lutheran,Pentecostal,Catholic,Orthodox}; `StudyDepth`{Overview,Exegetical,WordStudy} — **todos PUROS/ai-pure** (como `StudySection`/`Citation`) → `From` cfg-free; só `study`/`StudyRequest`/`StudyResult` são embedded-only → o `From<StudyResult>`+o corpo são `cfg(not wasm32)` (molde `From<xref::CrossRef>`). Monta o `StudyRequest` com fatos do store: `passage` via `EmbeddedSource::passage` (translation-specific), `verified_lexicon` (rota F3.2), `cross_references` via `xref::passage_labels(conn,&ref,&[],lang,limit)`, `reference_label` via `reference::format_reference`, `web_sources:vec![]`; MOCK via `build_provider("mock",None,None)`→`MockLlmProvider::default()` (`model()=="mock-1"`, resposta fixa; provider desconhecido→`AiError::UnknownProvider`). Teste de host determinístico (molde F2.1: `build_kjv_fixture`/`mock_fixed_response`): passage_text verbatim ≠ interpretation(mock invariante); anti-fake 2 refs; provider/ref-inválida→CoreError sem panic. **F3.3 depende só de F3.2 (aceita), é NÃO-BLOQUEANTE (só mock, sem chave/rede) e NÃO exige PR ao the-light** (superfície `pub` sob embedded em `c8ecb2f`). **Assinatura/tipos REAIS de F3.2:** `verified_lexicon(conn, &Reference, verse_numbers:&[u16], _lang:Lang, limit:usize) -> VerifiedLexicon` é **`embedded`-only** e **infalível** (engole erro SQLite → default vazio; **sem `translation`** — léxico é independente de tradução, chaveado por book/chapter/verse, como a xref F1.8). `LexicalEntry{strongs(base),lemma?,translit?,gloss?,occurrences,testament}` e `VerifiedLexicon{entries,sources}` são **PUROS (ai-pure)** → os `From` na fronteira são **cfg-free** (divergência vs. F1.8/xref, cujo tipo-fonte é embedded-only). Schema real: `original_tokens(testament,book_number,chapter,verse,word_index,surface,lemma,strongs,strongs_raw,translit,gloss,source_id)`, `lexicon(strongs,lemma,gloss,gloss_pt,source_id)`, `scholarly_sources(id,name,license,embeddable,attribution,url,version)` — colunas `strongs`/`source_id`. Teste de host **determinístico** (fixture temp: Gn 1:1 H0430G→אֱלֹהִים/God, molde `seeded()`+`read_tests`) + bônus `if bible.sqlite exists` (F3.1, ~132 MB gerar-ignorado). Anti-alucinação: glosas/lemas do store verificado (STEP CC-BY), nunca do LLM. **Investigação do core (`c8ecb2f`) — dimensiona a fase:** `ai::study`/`StudyResult`/`verified_lexicon`/`research`/`build_provider`/`to_academic_markdown` são **`embedded`-only (nativo)** → **estudo profundo nativo NÃO exige PR ao core**; `ask_session`/`refine_scope` são **puros (ai-pure → web já disponível)**; o **léxico usa o MESMO `bible.sqlite`** (tabelas `original_tokens`/`lexicon`/`scholarly_sources`, populadas por **`xtask import-scholarly`** — STEP Bible **CC-BY 4.0**, atribuição obrigatória, molde ADR-0016); **pesquisa web (`ai::research`) = rede opt-in** (wikipedia keyless / tavily BYOK) → **gate estratégico F3.9**; a **paridade web do estudo** pode exigir **PR ai-pure** (molde F2.7, F3.11 condicional). Regras: anti-alucinação (texto/glosas/citações do store; LLM só interpreta e é sinalizado se inventar Strong/fonte), BYOK, offline-first (estudo/pesquisa opt-in), the-light só via PR+ADR. `the-light` `c8ecb2f`. Próximo ADR livre = **ADR-0026**.

## Fase 0 — Prova da ponte Rust → Expo

| ID | Tarefa | Estado | Depende de | Resultado |
|----|--------|--------|------------|-----------|
| F0.0 | Confirmar toolchain e versões; registrar ADR | ✅ aceito | — | passed (efe334f) |
| F0.1 | Bootstrap do repo + docs de processo (DECISIONS/PROGRESS/.gitignore) | ✅ aceito | F0.0 | passed (595c70c) |
| F0.2 | Crate `core/` com UniFFI compilando (sem lógica) | ✅ aceito | F0.1 | passed (7b922eb) |
| F0.3 | `parse_reference` na fronteira + teste | ✅ aceito | F0.2 | passed (9881c72) |
| F0.4 | Script de geração de bindings TS | ✅ aceito | F0.3 | passed (e19064a) |
| F0.5 | App Expo mínimo (expo-router) + tela | ✅ aceito | F0.1 | passed (3262f56) |
| F0.6 | Ligar core no **web (WASM)** | 🔴 blocked → ♻️ re-escopada | F0.4, F0.5 | blocked (SQLite-no-WASM); ADR-0005 |
| F0.6a | Consumir core c/ features por alvo (re-pin `8f66004`) | ✅ aceito | F0.4, F0.5, core | matriz por alvo OK; SQLite-no-WASM resolvido |
| F0.6c | Alinhar `uniffi` da fronteira a `=0.31.0` (compat. ubrn web) | ✅ aceito | F0.6a | passed (7b98644) |
| F0.6b | Bindings web (ubrn) + glue + tela + prova headless | ✅ aceito | F0.6a, F0.6c | passed (1cdde6c) — web/WASM ponta a ponta |
| F0.7 | Ligar core no **iOS**: chamar parse_reference | ✅ aceito | F0.4, F0.5 | passed (a6f6797) — Turbo Module nativo, run real no sim |
| F0.8 | Ligar core no **Android**: chamar parse_reference | ✅ aceito | F0.4, F0.5 | passed (36af016) — Turbo Module nativo, run real no emulador |
| F0.9 | Store nativo (`rusqlite`): ler 1 passagem do sample.sqlite | ✅ aceito | F0.6b, F0.7, F0.8 | passed (1d16897) — get_passage lê João 3:16 do store |
| F0.10 | Store web (`wa-sqlite`+OPFS): ler 1 passagem | ✅ aceito | F0.9 | passed (d6e968d) — getPassage web lê João 3:16 do store |
| F0.11 | **Marco 0** (⛔ gate): revisão dos 3 alvos + store; ADR + PROGRESS | ✅ **APROVADO** | F0.6b/7/8/9/10 | sign-off humano (aed825d) — **Fase 1 liberada** |

## Fase 1 — Leitura offline multiplataforma (zero IA, zero rede)

> Decomposta em `loop/backlog/PHASE-1.md`. Padrão: capacidade nasce no núcleo
> (fronteira nativa, teste Rust de host) → UI nativa → paridade web após o gate
> estratégico F1.12. Só **F1.1** está semeada na `queue/`; as demais entram
> conforme as dependências forem aceitas.

| ID | Tarefa | Estado | Depende de | Resultado |
|----|--------|--------|------------|-----------|
| F1.1 | Pipeline de dados + banco embarcado completo (KJV+ALM1911 via xtask) | ✅ aceito | — | passed (b1a9be4) — kjv 31102 / alm1911 31101 |
| F1.2 | Expor leitura no core (fronteira nativo): translations/books/chapter/passage | ✅ aceito | F1.1 | passed (e06b0c6) — 4 funções delegam ao core; web puro |
| F1.3 | UI de leitura nativa: navegação livro→cap→texto + seletor de versão | ✅ aceito | F1.2 | passed (9cf4a9e) — leitura real no device (iOS); bundling ADR-0014 |
| F1.4 | UI de leitura nativa: múltiplas versões lado a lado + tema | ✅ aceito | F1.3 | passed (24cd3b8) — lado a lado KJV|Almeida + tema (iOS) |
| F1.5 | Busca FTS5 na fronteira (core, nativo) | ✅ aceito | F1.2 | passed (f006a31) — search delega ao core; 20/20 testes; web puro |
| F1.6 | UI de busca nativa (resultados com referência clicável) | ✅ aceito | F1.5, F1.3 | passed (ab75071) — busca real no device (iOS) |
| F1.7 | Referências cruzadas: dados (xtask import-xref, OpenBible CC-BY ~344.799) | ✅ aceito | F1.1 | passed (4d2d3bf) — 344799 xrefs; ADR-0016 |
| F1.8 | Xref na fronteira (core, nativo): cross_refs → xref::for_verse | ✅ aceito | F1.7, F1.2 | passed (36b2cb0) — Rm 5:8 em [0]; 26 testes; web puro |
| F1.9 | UI de xref nativa + atribuição CC-BY visível | ✅ aceito | F1.8, F1.3 | passed (2ff73c5) — TLA_XREF João 3:16 count=9; CC-BY visível |
| F1.10 | Notas/marcações na fronteira (core userdata, file-based) | ✅ aceito | F1.2 | passed (b6381d0) — 7 funções → userdata; 34 testes; web puro |
| F1.11 | UI de notas/highlights nativa + export + persistência | ✅ aceito | F1.10, F1.3 | passed (deb3b0c) — TLA_NOTES João 3:16 persisted=true export_ok=true |
| F1.12 | **GATE estratégico** (⛔): store web do corpus completo (FTS5/OPFS/Opção A vs B) | ✅ **DECIDIDO** | F1.2, F1.5, F1.8, F1.10 | sign-off humano — **Opção A / A1** (subset ~4,4 MB); ADR-0018; HALT removido |
| F1.13 | Paridade web: leitura (navegação + versões) | ✅ aceito | F1.12, F1.4 | passed (0479b5b) — leitura web real; João 3:16 verbatim; paridade c/ nativo; ADR-0019 |
| F1.14 | Paridade web: busca FTS5 | ✅ aceito | F1.12, F1.6 | passed (b2f1d8f) — busca web FTS5 real; João 3:16 localizado; FTS5 vendorizado; ADR-0020 |
| F1.15 | Paridade web: referências cruzadas + atribuição | ✅ aceito | F1.12, F1.9 | passed (1fe14e6) — xref web real; João 3:15 1º (439); CC-BY no bundle; ADR-0021 |
| F1.16 | Paridade web: notas/marcações + export | ✅ aceito | F1.12, F1.11 | passed (7557f0e) — notas/highlights web em OPFS; WEB_NOTES persisted=true; export idêntico; ADR-0022 |
| F1.17 | **Marco 1** (⛔ gate): leitura offline completa, multiplataforma | ✅ **APROVADO** | F1.4, F1.6, F1.9, F1.11, F1.13–F1.16 | sign-off humano — **Fase 1 completa nos 3 alvos**; Fase 2 liberada; HALT removido |

## Fase 2 — Camada de IA BYOK (Claude · GPT · Gemini · `ask` ancorado)

> Decomposta em `loop/backlog/PHASE-2.md` (**RE-ESCOPADA per ADR-0023**). **IA
> OPCIONAL e ADITIVA:** a Fase 1 segue 100% offline sem IA; a IA só liga com a
> **API key do usuário** (nunca em git/log). **Anti-alucinação:** o texto do
> versículo vem **sempre do store local** (verbatim); o LLM só interpreta. Padrão
> (herdado da Fase 1): fronteira nativa + teste de host com **MOCK** → gate F2.2
> [feito] → **PR consolidado ao core** (F2.3: Gemini+wasm+streaming, BLOQUEANTE) →
> fronteira que consome o re-pin (F2.3a) → chave BYOK nativa (F2.4, paralela e
> não-bloqueante) → UI nativa MOCK (F2.5) → validação real (F2.6, gate) → **PR
> IA-pura-wasm ao core (F2.7, BLOQUEANTE)** → paridade web de IA (F2.7b) → Marco 2
> (F2.8). **F2.7 está SEMEADA** (ready). O **D2/wasm foi fatiado da F2.3 para a F2.7**
> (PR próprio, menor review): a F2.3 mergeou só D1(Gemini)+D4(streaming); a F2.7
> desacopla as partes puras do `ai` do reqwest/rusqlite (feature `ai-pure`) e bate junto
> o fix do `default_model` gemini `2.0`→`2.5-flash`. **Investigação do core (`133077a`):** `ai` é
> `embedded`-only (só nativo); **Gemini não existe** e o `LlmProvider` é
> **síncrono/não-streaming** → Gemini+streaming+wasm = **um PR ao the-light** (F2.3).

| ID | Tarefa | Estado | Depende de | Resultado |
|----|--------|--------|------------|-----------|
| F2.1 | Pergunta ancorada (`ask`) na fronteira (nativo) + MOCK + anti-alucinação | ✅ aceito | — | passed (357abca) — ask_anchored→ai::ask; cited_text do store, interpretation do mock; 37 testes; web puro |
| F2.2 | **GATE estratégico** (⛔): arquitetura da IA (Gemini · web · chave · streaming) | ✅ **DECIDIDO** | F2.1 | sign-off — D1 PR core (Gemini) · D2 PR core (wasm) · D3 BYOK API key (OAuth banido) · D4 streaming; ADR-0023 |
| F2.3 | **PR ao `the-light-core`**: Gemini (D1) + streaming-no-trait (D4) — *(D2/wasm fatiado p/ antes da F2.7)* | ✅ aceito | F2.2 | **mergeado** (main→`133077a`); re-pinado; 37 testes fronteira verdes; wasm puro; chave nunca logada |
| F2.3a | Fronteira: rotear `"gemini"` + expor streaming (callback UniFFI), consumindo o re-pin; MOCK | ✅ aceito | F2.3 | passed (b375f47) — ask_anchored_stream via complete_stream; gemini no-key→CoreError; 41 testes; web puro |
| F2.4 | **Gestão segura de chaves (BYOK) nativa — `expo-secure-store`** | ✅ aceito | F2.2 | passed (34c831f) — keystore por provedor (Keychain), chave nunca logada/versionada; stub web; app-side |
| F2.5 | UI nativa: `ask` ancorado (provedor/modelo + custo + streaming + citado/interpretação) | ✅ aceito | F2.1, F2.3a, F2.4 | passed (31d191f) — TLA_ASK mock streamed; anti-alucinação visível; chave nunca logada |
| F2.6 | **Validação real** (⛔): `ask` real com a chave do usuário (Claude/GPT/Gemini) | ✅ **VALIDADA** | F2.5 | sign-off humano — Gemini `gemini-2.5-flash` real; `cited_text` João 3:16 do store; anti-alucinação provada; HALT removido |
| F2.7 | **PR ao `the-light-core`**: IA-pura-wasm (D2, feature `ai-pure`) + fix `default_model` gemini `2.0`→`2.5-flash` | ✅ aceito | F2.6 | **mergeado** (main→`c8ecb2f`); re-pinado (`ai-pure` na linha web); 41 testes fronteira; wasm puro; ADR-0024 |
| F2.7b | Paridade web de IA: `ai-pure` wasm (prompt/RAG/citação) + `fetch` ao provedor + keystore/política de chave web; prova headless MOCK | ✅ aceito | F2.7, F2.5 | passed (744868d) — IA web zero-drift; cited_text João 3:16 do store; chave session-only; ADR-0025 |
| F2.8 | **Marco 2** (⛔ gate): IA BYOK ancorada com Claude/GPT/Gemini | ✅ **APROVADO** | F2.5, F2.6, F2.7, F2.7b | sign-off humano — **Fase 2 completa**; Fase 3 (estudo profundo) liberada; HALT removido |

## Fase 3 — Estudo profundo (léxico Strong verificado · `study` ancorado · pesquisa opt-in)

> Decomposta em `loop/backlog/PHASE-3.md` (F3.1–F3.13). **Estudo profundo OPCIONAL
> e ADITIVO:** Fases 1–2 seguem intactas; o estudo só liga com a chave do usuário.
> **Léxico é DADO local** no MESMO `bible.sqlite` (STEP Bible CC-BY 4.0 via `xtask
> import-scholarly`, molde F1.1/F1.7). **Anti-alucinação:** texto/glosas/citações
> do store; o LLM só interpreta e é sinalizado se inventar Strong/fonte. Padrão:
> dados → fronteira nativa + teste host **MOCK** → UI nativa (MOCK) → **gate
> estratégico F3.9** (pesquisa web opt-in + via de paridade web) → validação real
> (F3.10, gate) → [PR ai-pure F3.11 condicional] → paridade web (F3.12) → **Marco 3
> F3.13**. **Estudo nativo NÃO toca o core** (superfície `embedded` já pinada em
> `c8ecb2f`); só a paridade web pode exigir PR. **F3.1 semeada**; as demais entram
> conforme as deps forem aceitas.

| ID | Tarefa | Estado | Depende de | Resultado |
|----|--------|--------|------------|-----------|
| F3.1 | Dados de léxico (xtask import-scholarly: TAHOT/TAGNT + TBESH/TBESG, STEP Bible CC-BY 4.0) no bible.sqlite | ✅ aceito | — | passed (1e3892e) — original_tokens 447673 / lexicon 22717 / 4 fontes cc-by; ADR-0026 |
| F3.2 | Fronteira de léxico (core, nativo): `lexical_entries` → `ai::lexicon::verified_lexicon` | ✅ aceito | F3.1 | passed (7f469d4) — Gn 1:1 H0430/God; 48 testes; web puro |
| F3.3 | Fronteira de estudo profundo (core, nativo): `deep_study` → `ai::study::study` → StudyResult; MOCK | ✅ aceito | F3.2 | passed (49919c3) — passage_text store × interpretation mock; 52 testes; web puro |
| F3.4 | Fronteira de conversa/refinamento (nativo+web): `ask_session`/`refine_scope`/`parse_refinement`; MOCK | ✅ aceito | F3.3 | passed (851e97c) — conversa/refinamento; cited_text store × interpretation mock; 59 testes; web puro |
| F3.5 | UI nativa: painel de estudo profundo (modos×lentes×profundidades + léxico inline + citações) + STEP CC-BY; MOCK | ✅ aceito | F3.2, F3.3 | passed (04a7a61) — TLA_STUDY mock lexicon=21 attribution_ok; léxico no subset; ADR-0027 |
| F3.6 | UI nativa: conversa com follow-up (`ask_session_anchored`); MOCK (TLA_CHAT) | ✅ aceito | F3.4, F3.5 | passed (e39dad5) — TLA_CHAT mock turns=3; cited_text store × interpretation; sem regressão |
| F3.7 | UI nativa: comparação multi-IA (N provedores lado a lado, mesma âncora); MOCK | ✅ aceito | F3.5 | passed (ca7ad3f) — TLA_COMPARE providers=2 cited_match=true; wiring N-IA; real=F3.10 |
| F3.8 | Exportação acadêmica (SBL → Markdown + sidecar de citações) + UI Share | ✅ aceito | F3.3, F3.5 | passed (c0a36e3) — TLA_EXPORT mock; Markdown SBL do core + STEP CC-BY; fix ADR-0027 |
| F3.9 | **GATE estratégico** (⛔): pesquisa web opt-in (backend/chave) + via de paridade web do estudo | ✅ **DECIDIDO** | F3.3 | sign-off — D1 Wikipedia keyless (ADR-0028) · D2 PR ai-pure (ADR-0029) · D3 F3.10 |
| F3.9a | Fronteira nativa: pesquisa web **Wikipedia (keyless, opt-in)** no `deep_study` (`research_backend`→`build_research_provider`→`web_sources`); MOCK, sem rede/PR | ✅ aceito | F3.3, F3.9 | passed (57ade6b) — web research opt-in mock; citações das URLs não do modelo; 63 testes; web puro |
| F3.10 | **Validação real** (⛔): estudo + conversa + comparação multi-IA + **pesquisa web Wikipedia** reais com a chave do usuário | ✅ **VALIDADA** | F3.5, F3.9, F3.9a | sign-off — Gemini real, João 3:16 do store, 26 citações léxico/web (warnings vazio); HALT removido |
| F3.11 | **PR ao `the-light-core`** (**INCONDICIONAL/BLOQUEANTE**, ADR-0029): `ai-pure` completo (study + verified_lexicon + pesquisa web pura) em wasm | ✅ aceito | F3.9, F3.10 | mergeado (main→04b9b24); re-pinado; 63 testes fronteira; wasm puro; ADR-0030 |
| F3.12a | Paridade web: **estudo profundo + léxico + export acadêmico** (ai-pure prepare→fetch→finalize; recuperação de léxico web ADR-0011) | ✅ aceito | F3.11, F3.5, F3.8 | passed (c340d7a) — estudo web zero-drift; João 3:16 do store + léxico STEP; academic_md; 65 testes |
| F3.12b | Paridade web: **conversa ancorada + pesquisa web Wikipedia (opt-in) + comparação multi-IA** | ✅ aceito | F3.12a, F3.6, F3.7 | passed (e4da90a) — conversa web zero-drift + Wikipedia [W:n] opt-in + comparação; 68 testes; wasm puro |
| F3.13 | **Marco 3** (⛔ gate): plataforma de estudo profundo completa | ✅ **APROVADO** | F3.5, F3.6, F3.7, F3.8, F3.10, F3.12a, F3.12b | sign-off humano — Fase 3 completa; Polimento da IA (Fase 4) liberado; HALT removido |

## Fase 4 — Polimento da IA (streaming web real · multi-provedor · Tavily BYOK · multi-IA de conteúdo real)

> Decomposta em `loop/backlog/PHASE-4.md` (F4.1–F4.7). Direção aprovada no sign-off do
> **Marco 3** (2026-07-02): refino da camada de IA já entregue (Fases 2–3), **sem**
> escopo novo de produto. **IA/pesquisa OPT-IN** (Fase 1 segue 100% offline). Padrão:
> capacidade testável por **MOCK** (app-side) → UI → **validação real (gate/chave)** →
> **[PR ao core] + re-pin** → **Marco 4**. Front-load das tarefas NÃO-BLOQUEANTES
> (F4.1–F4.4) até o 1º ponto bloqueante (F4.5, validação real). **F4.1 semeada**; as
> demais entram conforme as deps forem aceitas. Anti-alucinação ZERO-DRIFT: o streaming
> muda **só o transporte**; texto/glosas/citações do store/`ai-pure`.

| ID | Tarefa | Estado | Depende de | Resultado |
|----|--------|--------|------------|-----------|
| F4.1 | Streaming WEB real (token-a-token via fetch SSE/ReadableStream); Rust ai-pure inalterado; MOCK | ✅ aceito | — | passed (882bdb5) — streaming web token-a-token; zero-drift (âncora removida pelo Rust); mock offline; ADR-0033 |
| F4.2 | Transporte web multi-provedor (anthropic/openai/ollama) no `webLlmTransport` + streaming; MOCK | ✅ aceito | F4.1 | passed (9ba3d52) — anthropic/openai/ollama endpoint+header+streaming; zero-drift; ADR-0034 |
| F4.3 | Fronteira nativa: Tavily BYOK no `deep_study` (`research_key: Option<String>`); MOCK | ✅ aceito | F3.9a | passed (0141f0f) — `research_key` roteado a `build_research_provider` (`None` hard-coded removido); `tavily`+key=None→CoreError sem rede; `mock`/F3.9a preservado (chave ignorada); `wikipedia` ignora key; `None`→offline; 69 testes; wasm puro; bindings expõem `researchKey`; the-light `04b9b24` intacto; chave não-vazada |
| F4.4 | Tavily web (`fetch` a `api.tavily.com/search` + chave BYOK session-only só no corpo) + UI toggle opt-in off/Wikipedia/Tavily; MOCK | ✅ aceito | F4.3 | passed (69b369f) — `tavilySearch` POST com `api_key` NO CORPO (nunca URL/log); `deepStudyOnHandle` honra `researchBackend="tavily"`+`researchKey`; toggle UI 3-vias + chave session-only (`useState`); zero-drift (citação Web pelo Rust `study_web_finalize`); tavily-sem-chave→erro só "tavily"/0 fetch; Wikipedia keyless preservado; tsc/expo/wasm-puro verdes; the-light `04b9b24`/`core` intactos; chave não-vazada |
| F4.5 | **⛔ Validação real**: streaming real + multi-IA de conteúdo real + Tavily real (chaves) | ✅ **VALIDADO** | F4.1, F4.2, F4.3, F4.4 | sign-off humano — Gemini `gemini-2.5-flash` + Tavily REAIS; João 3:16 do store; 26 citações (léxico STEP + 4 web Tavily reais `[W:1..4]`); **WARNINGS=[]**; ACADEMIC_MARKDOWN 22982; streaming/multi-IA web zero-drift por mock; HALT removido |
| F4.6 | **PR ao `the-light-core`**: streaming NATIVO real por provedor (SSE/NDJSON, override `complete_stream`) | ✅ **aceito** | F4.5 | passed (`5cffd32`) — PR #2 MERGEADO no the-light → rev `2fc2dab`; override `complete_stream` nos 4 provedores (Anthropic/OpenAI/Gemini SSE, Ollama NDJSON), embedded-only, não-quebrante, zero-drift; assinatura/fronteira/bindings INALTERADOS; revisão adversarial 5-lentes (1 achado in-band CORRIGIDO no mesmo PR +4 testes); the-light fmt/clippy -D/196 testes/wasm puro; re-pin (2 linhas→2fc2dab) revalidado: fronteira 69 testes, tsc/expo web/15 self-tests web verdes, wasm puro; ADR-0035 |
| F4.7 | **⛔ Marco 4**: Polimento da IA completo | ✅ **APROVADO** | F4.1–F4.6 | sign-off humano (2026-07-02) — **Fase 4 (Polimento da IA) completa** nos 3 alvos; Fase 5 (Refinamento e abertura) liberada e semeada; HALT removido |

## Fase 5 — Refinamento e abertura (planos de leitura · i18n/a11y/temas · performance web · sync opcional)

> Mapeia o **§FASE 4 do `IMPLEMENTATION_PLAN.md`** (loop numera como **Fase 5** p/ não
> colidir com a Fase 4 "Polimento da IA"). Decomposta em `loop/backlog/PHASE-5.md`
> (workflow de 4 planners paralelos → síntese) após o **sign-off do Marco 4**. 27 tarefas,
> **NENHUMA toca o `the-light`** (`2fc2dab`) — reusa o core já pinado (`userdata::plans`,
> VFS OPFS de userdata, keystore). Padrão: tracer-bullet por workstream → largura →
> paridade web. **Semeadas na `queue/` (elegíveis):** F5.1/F5.2/F5.3 (tracers `ready`) +
> **F5.22** (⛔ gate de sync — HALT p/ ADR-0036, só selecionado após esgotar os IDs
> menores). Sync = aditivo/opt-in; offline-first permanece a base. Legenda: 🟡 queue ·
> ⬜ backlog · ⛔ gate.

| ID | Tarefa | Estado | Depende de | Workstream |
|----|--------|--------|------------|------------|
| F5.1 | Fronteira NATIVA: geração de planos de leitura (list/day/day_index) + host test — TRACER (re-escopo native-first) | ✅ **aceito** | — | Planos leitura |
| F5.2 | TRACER: camada i18n PT/EN + prefs offline (KV) + seletor de idioma persistido + home migrada ponta a ponta | ✅ **aceito** | — | i18n/a11y |
| F5.3 | Tracer: métrica de baseline do bundle web + adiar o wasm de ~4MB do first paint | ✅ **aceito** | — | Perf web |
| F5.4 | Fronteira: persistência nativa do progresso do plano (PlanStore fs) + host test round-trip | ✅ **aceito** | F5.1 | Planos leitura |
| F5.5 | i18n do fluxo de leitura: títulos de header reativos + telas read/* + fio locale→`lang` da IA | ✅ **aceito** | F5.2 | i18n/a11y |
| F5.6 | Build do frontier wasm em release + wasm-opt (eliminar o build DEBUG) | ✅ **aceito** | F5.3 | Perf web |
| F5.7 | UI nativa: rota de Planos de leitura (lista → iniciar → dia de hoje → marcar concluído) + self-test | ⬜ backlog | F5.4 | Planos leitura |
| F5.8 | i18n da busca + componentes de navegação (search, SearchResultItem, BookList, ChapterGrid, ChapterView) | ⬜ backlog | F5.2 | i18n/a11y |
| F5.9 | Code-split de AI/study/research/lexicon + wa-sqlite factory fora do entry JS | ⬜ backlog | F5.3 | Perf web |
| F5.10 | Paridade web de planos: **exige PR `ai-pure` ao core** (expor geração PURA de planos) + progresso OPFS + prova headless — ⛔ gate estratégico (ADR-0037) | ⬜ backlog ⛔ | F5.7 | Planos leitura |
| F5.11 | i18n dos painéis de IA (Ask, Chat, Compare, Study): strings + labels de acessibilidade | ⬜ backlog | F5.2 | i18n/a11y |
| F5.12 | Remover assets duplicados mortos (npm wa-sqlite async wasm + F0.10 sample.sqlite) do bundle web | ⬜ backlog | F5.3 | Perf web |
| F5.13 | Lembretes locais offline do plano ativo (expo-notifications, opt-in, sem servidor) — reusa o KV de prefs | ⬜ backlog | F5.7, F5.2 | Planos leitura |
| F5.14 | Persistir o modo de tema entre reinícios (fecha a lacuna do ADR-0015) reusando o KV de prefs | ⬜ backlog | F5.2 | i18n/a11y |
| F5.15 | Separar o léxico de ~9MB do caminho de leitura (carga opt-in on-demand) + UX de carregamento | ⬜ backlog ● | F5.3 | Perf web |
| F5.16 | i18n dos componentes restantes (VersePanel, Xref, Parallel, VersionPicker) + GUARDA de cobertura i18n/tema | ⬜ backlog | F5.5, F5.8, F5.11 | i18n/a11y |
| F5.17 | Precompress .wasm/.sqlite + estratégia de cache/serving + medir transfer size | ⬜ backlog | F5.6 | Perf web |
| F5.18 | Auditoria de contraste WCAG das paletas de tema (headless) + ajuste dos tokens reprovados | ⬜ backlog | F5.14 | i18n/a11y |
| F5.19 | Travar o orçamento de performance web como guarda de regressão (saída do workstream) | ⬜ backlog ● | F5.6, F5.9, F5.12, F5.15, F5.17 | Perf web |
| F5.20 | Varredura de a11y de elementos interativos (labels/roles/alvos de toque) + guarda headless | ⬜ backlog | F5.16 | i18n/a11y |
| F5.21 | Dynamic type / escala de fonte + ordem de foco + semântica de modais (painéis de leitura) — TLA_A11Y | ⬜ backlog | F5.20 | i18n/a11y |
| F5.22 | **GATE** — Brief de decisão + ADR-0036: sync opcional por conta (o QUE sincroniza · conflito · e2e/BYO-backend · offline default · privacidade) | 🟡 **queue** ⛔ | — | Sync opc. |
| F5.23 | Oplog local (change-log append-only) das mutações de userdata — tracer offline, prova por MOCK | ⬜ backlog | F5.22 | Sync opc. |
| F5.24 | Transporte de sync atrás de feature-flag (opt-in, OFF por padrão) — push/pull do oplog, token BYOK, MOCK | ⬜ backlog | F5.22, F5.23 | Sync opc. |
| F5.25 | Resolução de conflito / merge de oplogs — aplica a estratégia do ADR, prova determinística | ⬜ backlog | F5.22–F5.24 | Sync opc. |
| F5.26 | UI opt-in de sync (conta/ajustes OFF por padrão · aviso de privacidade · passphrase e2e · "funciona offline sem isto") | ⬜ backlog | F5.22, F5.24 | Sync opc. |
| F5.27 | **⛔ Validação real** do sync (backend/conta/rede/e2e reais) — gate; o loop nunca roda sozinho | ⛔ gate | F5.22–F5.26 | Sync opc. |

_● = blocking (define o "verde" do workstream / gargalo)._ Detalhe completo em `loop/backlog/PHASE-5.md`.

## Fases seguintes (após a Fase 5)

Aberturas futuras além do §FASE 4 do plano (planos de leitura avançados, i18n de conteúdo,
conectores de versões protegidas opt-in, etc.) — a decompor em `backlog/` quando/se retomado.

## Log de ciclos

| Data | Evento |
|------|--------|
| 2026-06-29 | Loop criado; PROTOCOL.md e backlog da Fase 0 escritos; F0.0 semeada na queue. |
| 2026-06-29 | Migrado p/ **modo autônomo**: subagentes, run-loop.sh, journal/heartbeat/HALT, gate no Marco 0. Guia vira auditor. |
| 2026-06-29 | Ciclo 1: F0.0 executada (executor) e verificada (reviewer) → **passed** (efe334f); arquivada. Lacunas registradas: targets Rust, ubrn, Xcode (F0.7), Android NDK (F0.8). |
| 2026-06-29 | Ciclo 2: queue vazia → planner semeou F0.1. |
| 2026-06-29 | Ciclo 3: F0.1 executada e verificada → **passed** (595c70c); arquivada. Repo estruturado; DECISIONS/PROGRESS/.gitignore criados. ADR-0002: consumir `the-light` como git dep pinada; resolução real pendente p/ F0.2. |
| 2026-06-29 | Planner semeou **F0.2** na `queue/` (crate `core/` com UniFFI trivial `ping`, SEM lógica e SEM dep do `the-light-core`). Resolução real do `the-light-core` transferida da F0.2 p/ a **F0.3** (onde `parse_reference` delega ao core). |
| 2026-06-29 | Ciclo 5: F0.2 executada e verificada → **passed** (7b922eb); arquivada. Crate `the-light-app-core` compila com uniffi 0.31.2 (modo library); ADR-0003. |
| 2026-06-29 | Planner semeou **F0.3** na `queue/` (`parse_reference` delegando ao `the-light-core::reference`). Git dep pinada no rev `0888ac0…` (the-light público); API descoberta via fonte clonada em `~/.cargo/git/checkouts/`. |
| 2026-06-29 | Ciclo 7: F0.3 executada e verificada → **passed** (9881c72); arquivada. **Ponte real ao core provada**: git dep pinada (the-light-core v1.2.0), `parse_reference` delega ao core, "Jo 3.16"=="John 3:16" → (43,3,Single 16). Sem fork/cópia. |
| 2026-06-29 | Planner semeou **F0.4** na `queue/` (status ready): script reproduzível `scripts/gen-bindings.sh` que instala+fixa o `ubrn`, configura e gera os bindings TS da fronteira UniFFI em `bindings/`. Escopo atômico = só os `.ts` (não liga app ainda; isso é F0.6/7/8). `.ts` gerados são ignorados por design; ADR-0004 fixará a versão do `ubrn`. Bloqueio legítimo se o `ubrn` não instalar/rodar/gerar (maturidade / incompat. com uniffi 0.31.2). |
| 2026-06-29 | Ciclo 9: F0.4 executada e verificada → **passed** (e19064a); arquivada. **Risco #1 (maturidade do ubrn) vencido**: ubrn 0.31.0-3 gera `bindings/*.ts` (parseReference/ping/tipos) de forma reprodutível; compat. com uniffi 0.31.2 sem tocar a fronteira; ADR-0004. |
| 2026-06-29 | Planner semeou **F0.5** na `queue/` (status ready): app Expo mínimo (SDK 56, expo-router) em `app/` (package.json próprio, independente da raiz/ubrn) com UMA tela `TextInput` + área de resultado (`testID="result"`) placeholder — **sem** chamar o core (guarda de escopo: ligar core é F0.6/web, F0.7/iOS, F0.8/Android). Verificação **não-interativa**: `tsc --noEmit` + `expo export --platform web` (gera `app/dist/`, sai 0). RUN nativo (`run:ios`/`run:android`) **adiado p/ F0.7/F0.8** (Xcode completo/Android NDK ausentes). `app/node_modules`,`.expo`,`dist` ignorados por design. |
| 2026-06-29 | Ciclo 11: F0.5 executada e verificada → **passed** (3262f56); arquivada. App Expo SDK 56 (expo-router) em `app/` com tela input+resultado; export web compila (769 módulos), `tsc` limpo. **Node 25 compatível com SDK 56** (risco descartado). |
| 2026-06-29 | Planner semeou **F0.6** na `queue/` (status ready, gate:false): ligar o core no **web/WASM** — `Jo 3.16` na tela web → referência resolvida pelo Rust (livro 43, cap. 3, v. 16) via wasm+bindings web, com **prova headless** (teste node). Portão decisivo = compilar a fronteira p/ `wasm32-unknown-unknown` **sem `rusqlite`**. **Risco alto de HALT (blocked):** o `the-light-core` (rev 0888ac0) **não tem `[features]` nem deps `optional`** — `rusqlite{bundled}`+`reqwest{blocking,default-tls}` são incondicionais; excluí-los exigiria **modificar o core (PR+ADR=HALT)**. Task instrui `blocked` com erro EXATO = gate estratégico de Fase 0 (Apêndice A #1 / VISION §4 fricção #1). |
| 2026-06-29 | Ciclo 13: executor tentou F0.6, reviewer confirmou de forma independente → **`blocked`**. `cargo build --target wasm32-unknown-unknown` falha (exit 101) no `sqlite-wasm-rs` (SQLite-C). Core sem `[features]` → impossível excluir `rusqlite` sem PR ao `the-light`. **⛔ `loop/HALT` escrito — loop PARADO p/ decisão estratégica (store-web: wa-sqlite/OPFS, PR ao core, ou web leitura+IA pré-indexada → ADR-0005).** Core/fronteira intactos. |
| 2026-06-30 | **Resolução de direção (humano aprovou):** ADR-0005 — feature-gating `store`/`net` no `the-light-core` (PR não-quebrante, spec em `loop/proposals/the-light-PR-feature-gating.md`) + matriz de features por alvo no app. Verificado que `reference`/`model` são puros/desacoplados. F0.6 arquivada (blocked) e **re-escopada em F0.6a/F0.6b** (backlog). HALT **ajustado**: retomar exige PR mesclado + re-pin do rev. Core ainda intocado (PR é ação humana). |
| 2026-06-30 | F0.6a aceita (re-pin `8f66004`, matriz por alvo: web puro / nativo embedded). Planner semeou **F0.6b** na `queue/` (status ready, gate:false, depende de F0.6a): caminho **web** do `ubrn 0.31.0-3` (subcomando `ubrn web build`, wasm-pack/wasm-bindgen) → bindings wasm + glue em `app/web/` + ligar `app/app/index.tsx` (a guarda "sem core" da F0.5 não vale mais) + **prova headless** (node: `parseReference` via wasm = book 43/cap 3/v Single 16 p/ "Jo 3.16" e "John 3:16") + `expo export --platform web` sai 0. **Achado embutido (ADR-0005):** build cru falha em `uniffi_core 0.31.2` (`UniffiCompatibleFuture: …+Send`) → DEVE usar caminho web do ubrn, não build cru. Risco de `blocked` por imaturidade do caminho web do ubrn / uniffi-wasm: instrui `blocked` com saída EXATA (decisão estratégica; ADR-0006 se decidido). |
| 2026-06-30 | F0.6b aceita (web ponta a ponta; ADR-0006/0007). Planner semeou **F0.7** na `queue/` (status ready, gate:false, depende de F0.4+F0.5, aceitas): ligar o core no **iOS** via **Turbo Module nativo** (`ubrn build ios --sim-only --and-generate`, schema `ios:` em `ubrn.config.yaml`) + integração Expo (`expo prebuild`/`pod install`/`run:ios`) + glue nativo (`app/web/reference.ts`) ligando a tela ao binding nativo + **run real no simulador iOS 26.5** com **prova headless** (self-test no launch que loga `TLA_SELFTEST PT/EN book=43 chapter=3 verse=16`, ou XCTest). Ambiente PRONTO (Xcode 26.6 + sim iPhone 17 + targets iOS). Gerados ignorados (app/ios, Pods, xcframework, codegen). **Risco de `blocked`** por imaturidade do caminho iOS do ubrn / integração Expo-prebuild-autolink: instrui `blocked` com erro EXATO (decisão estratégica = ADR-0008). |
| 2026-06-30 | F0.7 aceita (`a6f6797`; Turbo Module nativo iOS, run real no sim, prova headless PT==EN; ADR-0008). Planner semeou **F0.8** na `queue/` (status ready, gate:false, depende de F0.4+F0.5, aceitas): **ESPELHO da F0.7 no Android**. O Turbo Module (spec `NativeTheLightAppCore`, `cpp/`/`src/`/`bindings/`, autolink `react-native.config.js`, glue `app/web/reference.ts`/`selftest.ts`) **já existe e é compartilhado** — F0.8 só adiciona o **caminho de BUILD Android**: bloco `android:` em `ubrn.config.yaml`, `scripts/gen-bindings-android.sh` (`ubrn build android --and-generate --targets aarch64-linux-android` via `cargo-ndk` → `.so` em jniLibs + glue Gradle/CMake/Kotlin), `npx expo prebuild -p android` + Gradle, e `scripts/run-android-selftest.sh` (emulador **headless** `thelight_avd`, prova via `adb logcat` dos marcadores `TLA_SELFTEST PT/EN book=43 chapter=3 verse=16`). Ambiente PRONTO (NDK 27.1 + cargo-ndk 4.1.2 + JDK 17 + emulador headless; smoke test do core verde). Gerados ignorados (app/android, /android, jniLibs/.so, build, .gradle). **Risco MENOR que F0.7** (integração Turbo Module já feita); residual: caminho Android do ubrn + autolink Gradle/emulador → `blocked` com erro EXATO se hack proibido (decisão estratégica = ADR-0009). |
| 2026-06-30 | F0.8 aceita (`36af016`); **3 alvos provados** (web+iOS+Android). Planner semeou **F0.9** na `queue/` (status ready, gate:false, depende de F0.6b/F0.7/F0.8 — todas aceitas): **store nativo** — expor `get_passage`/`getPassage` na fronteira UniFFI que **lê 1 passagem** (`John 3:16`) de um `assets/data/sample.sqlite` (subset **KJV domínio público**) **delegando ao store do `the-light-core`** (`store::Store` + `source::EmbeddedSource::passage`, `rusqlite`, feature `embedded` ativa só no nativo). API/schema **descobertos na fonte** (`store.rs`/`source/embedded.rs`/`model.rs`/`migrations/v1_initial.sql`): `Store::open(path)` migra o schema; `passage()` lê `translations`+`verses`. Prova **determinística** = **teste Rust de host** (`embedded` on) abrindo o sample → `get_passage("John 3:16")` == texto KJV verbatim + `book=43/chapter=3/verse=16`; run nativo = adicional opcional. **`cfg(not(wasm32))` obrigatório** (web/F0.6b permanece puro; sem `rusqlite` no grafo wasm). Anti-alucinação: texto vem do store local; só domínio público. Decisão versionado-vs-gerado do `sample.sqlite` + origem do texto + API/schema → **ADR-0010**. `blocked` com erro EXATO se o store exigir mudar core/fronteira de forma proibida ou dados protegidos. |
| 2026-06-30 | F0.9 aceita (`1d16897`; store nativo — `get_passage` lê João 3:16 verbatim do `sample.sqlite` via `the-light-core`; ADR-0010). Humano decidiu a **estratégia de store web (ADR-0011: Opção A — wa-sqlite + OPFS, query de passagem em TS, sem mudar o core)**. Planner semeou **F0.10** na `queue/` (status ready, gate:false, depende de F0.9, aceita): **store web** — `getPassage` web em **TS** que resolve a referência **pelo Rust (wasm, F0.6b)** e lê o versículo de um **`wa-sqlite`+OPFS** com o **mesmo schema/sample** do nativo (`assets/data/sample.sqlite`), **espelhando o SELECT** de `EmbeddedSource::passage` (`SELECT verse,text FROM verses WHERE translation_id=? AND book_number=? AND chapter=? AND verse=?`). Prova **determinística** = **teste headless node** (OPFS é browser-only): parseReference via wasm (bytes do `index_bg.wasm`) + `wa-sqlite` em **VFS de memória** sobre os **bytes do sample** + a **mesma função de query** do produto → texto **KJV verbatim** + `book=43/chapter=3/verse=16`, **não hardcoded**. OPFS = VFS de runtime no browser (sob `typeof navigator`). `expo export web` 0 com wa-sqlite no bundle; sem regressão web parseReference (F0.6b) nem nativo (F0.9/F0.7/F0.8). **NÃO** reimplementar parsing (vem do Rust); só a query é TS. **NÃO** tocar `the-light` (ADR-0011 = Opção A não muda o core). `blocked` com erro EXATO se wa-sqlite/OPFS no Expo/Metro web for inviável sem hack proibido (COOP/COEP+SharedArrayBuffer, empacotar `.wasm`) → decisão/ADR-0012. |
| 2026-06-30 | **Marco 0 aprovado** (`aed825d`) — ponte + store provados nos 3 alvos. Planner **decompôs a Fase 1** em `backlog/PHASE-1.md` (F1.1–F1.17: pipeline+banco → leitura no core → UI nativa → busca FTS5 → xref CC-BY → notas/marcações → **gate estratégico de store web F1.12** → paridade web → **Marco 1 F1.17**). Padrão: núcleo (fronteira nativa + teste Rust de host) → UI nativa → paridade web pós-gate. Reaproveita Fase 0 (matriz por alvo `embedded`; Turbo Module compartilhado; wa-sqlite/OPFS). **F1.1 semeada** na `queue/` (ready, gate:false, depends:[]): gerar `assets/data/bible.sqlite` (KJV 31.102 + ALM1911 31.101) via o **xtask pinado** (`rev 8f66004`) **sem tocar `the-light`**, contagens validadas + idempotência; ADR-0013 decide origem/tamanho/**versionar-vs-gerar(LFS)**. **Risco/blocked legítimo:** seed dos datasets não está em cache → xtask baixa por rede em build; se o loop estiver offline → `blocked` (decisão: origem/seed dos dados). |
| 2026-06-30 | F1.2 aceita (`e06b0c6`; leitura no core: `list_translations`/`list_books`/`get_chapter`/`chapter_count` delegam ao `the-light-core`; web puro; testes de host determinísticos). Planner semeou **F1.3** na `queue/` (status ready, gate:false, depende de F1.2): **primeira UI de leitura NATIVA** (iOS/Android) — navegação **livro→capítulo→texto** + **seletor de versão** (KJV ⇄ Almeida 1911) lendo do **store no device** pela fronteira F1.2 (Turbo Module → JSI → core; texto sempre do store, anti-alucinação). **Subproblema central = bundling do banco no app nativo:** `get_chapter(db_path,…)` precisa de um **path real e gravável** — empacotar `bible.sqlite` (~47 MB, gerado-ignorado ADR-0013) como **asset** + **copiar p/ `FileSystem.documentDirectory`** no 1º boot (rusqlite precisa de write p/ as migrações; no Android o asset vive no APK), via `expo-asset`/`expo-file-system` (a adicionar) + `assetExts`. **ADR-0014** decide a estratégia e o **trade-off de tamanho** (completo vs subset de leitura com João KJV completo vs gzip). **Prova determinística (molde F0.7/F0.8):** self-test de leitura no device — `TLA_READ books=66 john3_v16="For God so loved..." john_chapters=21` capturado por `simctl log`/`adb logcat`; `run-ios-selftest.sh` OU `run-android-selftest.sh` (≥1 alvo) asserta os marcadores. Só nativo (paridade web = F1.13 pós-gate F1.12). **Restrições:** não tocar `the-light`/fronteira; não reimplementar SQL/leitura em TS; texto domínio público do store; gerados/bundled ignorados; offline-first; `blocked`+HALT com erro EXATO se o bundling no Expo nativo exigir hack proibido. |
| 2026-06-30 | F1.3 aceita (`9cf4a9e`; UI leitura nativa lê do store no device — books=66, João 3:16 KJV verbatim, john_chapters=21; bundling subset `reading-sample.sqlite` ADR-0014). Planner semeou **F1.4** na `queue/` (status ready, gate:false, depende de F1.3): **múltiplas versões LADO A LADO + tema claro/escuro** (nativo). (1) Lado-a-lado = mesmo capítulo em 2 traduções (KJV \| Almeida 1911) **alinhadas por número de versículo**, lendo do store via **2 chamadas de `get_chapter`** (uma por tradução) + alinhamento em TS (apresentação, **sem** SQL/leitura em TS); toggle + seletor da 2ª versão (`listTranslations`). (2) Tema = `useColorScheme` + toggle (persiste na sessão), `Reader*` migram hex→**tokens de tema**. **Prova determinística PRINCIPAL** (molde F1.3): self-test estendido lê `get_chapter` p/ **kjv E alm1911** de João 3 e emite `TLA_PARALLEL kjv_john3_16="For God so loved..." alm_john3_16="Porque Deus amou o mundo de tal maneira..."` **do retorno real** (não hardcoded); `run-ios-selftest.sh` asserta os 2 substrings (KJV + Almeida) + sem regressão de `TLA_READ`/parse. Almeida 1911 João 3:16 verbatim **lido do store pelo planner** (`reading-sample.sqlite`) → substring de asserção `Porque Deus amou o mundo de tal maneira`. Tema é UI = prova por `tsc`. **ADR-0015 só se houver decisão nova de tema/UI.** Restrições: não tocar `the-light`/fronteira (lado-a-lado não exige função nova); anti-alucinação (texto domínio público do store); offline-first; web = stub (F1.13); `blocked`+HALT com erro EXATO se exigir mudar o core. |
| 2026-06-30 | Planner semeou **F1.5** na `queue/` (status ready, gate:false, depende de **F1.2** — aceita; **não** da F1.3/F1.4: é tarefa de NÚCLEO, fronteira Rust + teste de host, análoga à F1.2 — a UI de busca é F1.6). **Busca FTS5 na fronteira** (`core/src/lib.rs`): expor `search(db_path, query, translation, book: Option<u8>, limit: Option<u32>) -> Result<Vec<SearchHit>, CoreError>` **delegando** a `the_light_core::source::BibleSource::search` (via `EmbeddedSource`, que valida `has_translation` → `UnknownTranslation`), montando `the_light_core::search::SearchOptions` (translation/book/limit; default 20). Novo Record `SearchHit { reference, translation, text, highlighted, score:f64 }` com `From<model::SearchHit>` (referencia só `model` puro → seguro no wasm). Molde F0.9/F1.2: export em todos os alvos, **corpo** que toca store/FTS sob `cfg(not(wasm32))` + stub web; **acento-insensível** (`verses_fts` usa `remove_diacritics 2`, `ceus`↔`céus`) vem de graça do core. **Risco CRÍTICO sinalizado:** o schema do core **NÃO tem trigger** que popule `verses_fts` a partir de `verses` (o próprio core insere no FTS à mão nos testes) → o fixture do teste **deve** popular `verses_fts` explicitamente (ids casados com `verses.id`); o fixture da F1.2 (`build_kjv_fixture`, só `verses`) **não** serve sem isso. **Teste de host determinístico/offline**: fixture KJV+Almeida (João 3:16/3:17 KJV + Gênesis 1:1 Almeida acentuado) povoando `verses` **e** `verses_fts`; asserções: `search("God","kjv")` → hit João 3:16 KJV verbatim (book 43/cap 3/v 16); acento-insensível `search("ceus","alm")` → Gênesis 1:1 com `céus`; query sem match e só-espaços → vazio sem panic; tradução inexistente → `CoreError`; `limit` respeitado. Web puro (`cargo tree wasm` sem `rusqlite`; gen-bindings-web verde). **NÃO** reimplementar SQL/FTS (`MATCH`/`bm25`/`highlight` proibidos); **NÃO** tocar `the-light`; anti-alucinação (texto do store); offline-first; `blocked`+HALT com erro EXATO se a busca exigir mudar o core. |
| 2026-06-30 | F1.5 aceita (`f006a31`; busca FTS5 na fronteira — `search` delega ao `the-light-core::search`, acento-insensível/BM25, 20/20 testes de host, web puro; `SearchHit{reference,translation,text,highlighted,score}` + `HL_START`/`HL_END`). Planner semeou **F1.6** na `queue/` (status ready, gate:false, depende de **F1.5** e **F1.3** — ambas aceitas): **UI de busca NATIVA** (iOS/Android) — campo (`TextInput`+debounce) + lista de resultados; cada hit mostra a **referência** (ex.: "John 3:16") + o **snippet** com o termo destacado, e a referência é **clicável** → `router.push` p/ `read/[book]/[chapter]` (rota F1.3; ancoragem do versículo best-effort). Busca **lê via a fronteira `search`** (F1.5) pelo glue `app/web/reading.ts` (a ESTENDER) → JSI → Rust — **sem SQL/FTS em TS**. Destaque: `SearchHit.highlighted` traz `HL_START`(`\u{2}`)/`HL_END`(`\u{3}`) → a UI converte em **estilo** (negrito/realce), nunca exibe os controles. Tema via `app/lib/theme.ts`. Só nativo (paridade web busca = F1.14 pós-gate F1.12). **RISCO CRÍTICO sinalizado:** o subset `reading-sample.sqlite` (gerador `core/examples/gen_reading_sample_db.rs`) **não popula `verses_fts`** e o schema do core **não tem trigger** → `search` retornaria 0 hits; a task **exige povoar `verses_fts`** no gerador de dados (`INSERT … SELECT text,translation_id,id FROM verses` — fixture/pipeline, NÃO busca). **Bindings nativos defasados** (pré-F1.5) → regenerar (`gen-bindings-ios.sh`) p/ `search`/`SearchHit` aparecerem. **Prova determinística:** self-test de busca no device chama `search(db,"God","kjv")`, localiza o hit de João 3:16 no retorno e emite `TLA_SEARCH query="God" hits=<N≥1> first_ref="John 3:16" first_text="For God so loved..."` (do retorno real, texto SEM os controles HL); `run-ios-selftest.sh` asserta hits≥1 + ref João 3:16 + substring do texto, sem regressão de `TLA_SELFTEST`/`TLA_READ`/`TLA_PARALLEL`. Restrições: não tocar `the-light`/fronteira; não reimplementar busca/FTS/SQL no app (usar a fronteira); anti-alucinação (texto do store); HL markers não vazam como texto cru; offline-first; gerados/bundled ignorados; `blocked`+HALT com erro EXATO se exigir mudar o core. |
| 2026-06-30 | F1.6 aceita (`ab75071`; UI de busca nativa — busca real no device iOS, ref clicável navega ao Reader). Planner semeou **F1.7** na `queue/` (status ready, gate:false, depende de **F1.1** — aceita; **não** da F1.6: é tarefa de DADOS/pipeline, análoga à F1.1, **sem UI** e **sem fronteira** — a fronteira de xref é F1.8, a UI+atribuição é F1.9). **Referências cruzadas (DADOS):** popular a tabela `cross_references` do `assets/data/bible.sqlite` (gerado pela F1.1) com as **~344.799** xrefs do **OpenBible.info / TSK**, rodando o **subcomando REAL `import-xref`** (descoberto na fonte: `xtask/src/main.rs` → `Some("import-xref")`; **não** `import --xref`; flags `--db/--seed-dir/--force/--offline`, **sem** `--version`) do **rev pinado `8f66004`** **sem tocar `the-light`** (mesmo padrão F1.1/ADR-0013: checkout do cargo + `CARGO_TARGET_DIR` fora + `--locked`). **Destino = o MESMO `bible.sqlite`** (a tabela `cross_references` já existe vazia, criada por `Store::open`/`v1_initial.sql`; o `import-xref` faz DELETE+reinsert → **idempotente**). Fonte CC-BY (`XREF_URL` = mirror scrollmapper dos dados OpenBible; atribuir **OpenBible.info**, não scrollmapper); rede em build (~8,3 MB) → `blocked` se offline sem cache do `cross_references.txt` (a F1.1 não o semeou). **Estender `scripts/gen-bible-db.sh`** com o passo de xref. Aceite: `count(*) FROM cross_references` ≈ 344.799 **lido do banco** (guarda de drift; o xtask aborta se < 300.000) + idempotência + **ADR-0016** (origem OpenBible CC-BY + **string de atribuição** `Cross references courtesy of OpenBible.info (CC-BY)` + como o xtask roda sem tocar the-light + armazenamento gerar-ignorado ADR-0013). **Escopo:** só os DADOS — fronteira é F1.8, UI+atribuição visível + propagação ao `reading-sample` são F1.9. |
| 2026-06-30 | F1.7 aceita (`4d2d3bf`; `cross_references` populada com 344799 xrefs OpenBible.info CC-BY no `bible.sqlite` via `xtask import-xref` pinado, idempotente; ADR-0016 + string de atribuição). Planner semeou **F1.8** na `queue/` (status ready, gate:false, depende de **F1.7** e **F1.2** — ambas aceitas; **não** da F1.6: é tarefa de NÚCLEO, fronteira Rust + teste de host, análoga à F1.2/F1.5 — a UI de xref + atribuição visível é F1.9). **Xref na fronteira** (`core/src/lib.rs`): expor `cross_refs(db_path, book, chapter, verse, min_votes: Option<i64>, limit: Option<u32>) -> Result<Vec<CrossRef>, CoreError>` **delegando** a `the_light_core::xref::for_verse(store.conn(), …)` (via `Store::open`), com defaults do core p/ `None` (`DEFAULT_MIN_VOTES=1` oculta disputadas/negativas; `DEFAULT_LIMIT=20`). Novo Record `CrossRef { reference, votes:i64 }`. Molde F1.2/F1.5: export em todos os alvos, **corpo** que toca store/xref sob `cfg(not(wasm32))` + stub web. **2 DIVERGÊNCIAS CRÍTICAS sinalizadas vs. F1.5:** (1) a delegação é por **função livre** `xref::for_verse(&Connection)` — **não** há método `cross_refs` em `BibleSource`/`EmbeddedSource` → usa `store.conn()` (público); (2) `xref::CrossRef` vive no módulo `xref` **`#[cfg(feature="embedded")]`** (só nativo, ≠ `model::SearchHit` puro) → o `From<xref::CrossRef>` **também** precisa ser gated `cfg(not(wasm32))` (o Record em si é puro: `Reference`+`i64`). **Teste de host determinístico/offline:** fixture com `cross_references` populada à mão (xrefs DE João 3:16: → Rm 5:8 votes 50, → Jo 3:15, → Rm 5:8-9 range, → alvo disputado votes -5); asserções: inclui Rm (book 45) 5:8 ordenado por votos; intervalo vira `VerseRange::Range`; `min_votes` default oculta a disputada e `Some(-100)` a inclui; `limit` respeitado; verso sem xref → vazio sem panic. Web puro (`cargo tree wasm` sem `rusqlite`; gen-bindings-web verde). Bônus `if bible.sqlite exists` (João 3:16 ~23 xrefs reais). **Atribuição CC-BY (ADR-0016) é exibida na F1.9** — a fronteira só entrega os dados (registrar). **NÃO** reimplementar SQL/xref; **NÃO** tocar `the-light`; offline-first; `blocked`+HALT com erro EXATO se a xref exigir mudar o core (ex.: `Store::conn()`/`xref` inacessível). |
| 2026-06-30 | F1.1 aceita (`b1a9be4`; `bible.sqlite` KJV 31.102 + ALM1911 31.101 via xtask pinado, gerar-ignorado ADR-0013). Planner semeou **F1.2** na `queue/` (status ready, gate:false, depende de F1.1): **expor leitura no core** na fronteira UniFFI (`core/src/lib.rs`), **delegando** ao `the-light-core` (rev `8f66004`), molde F0.9/ADR-0010 (export em todos os alvos; **corpo** que toca store sob `cfg(not(wasm32))` + stub web). 4 funções: `list_translations(db_path)`→`BibleSource::translations`; `get_chapter(db_path,translation,book,chapter)`→`Reference::whole_chapter`+`BibleSource::passage`; `chapter_count(db_path,translation,book)`→`EmbeddedSource::chapter_count`; **`list_books()` PURO** (sem `cfg`) via `reference::BOOKS`+`chapters_in_book`. **Divergência registrada do backlog** (`list_books(translation)`): o core **não** lista a tabela `books` por tradução sem reimplementar SQL/mudar o core → usar a tabela canônica pura (66 livros, version-independent; nome por idioma já em `Book.name_en/pt`). Novos Records/Enums `Translation`/`Book`/`Testament` com `From<the_light_core::…>`. **Teste de host determinístico/offline**: fixture pequeno-multi-capítulo (Store::open + DML KJV domínio público: João 3:16/3:17 + João **21:1** p/ `chapter_count`=21); `bible.sqlite` (47 MB, gerar-ignorado) só bônus opcional `if exists`. Asserções: kjv em `list_translations`; 66 livros; `get_chapter(kjv,43,3)` v16 KJV verbatim numerado; `chapter_count(kjv,43)`=21. Web puro (`cargo tree wasm` sem `rusqlite`; gen-bindings-web verde). **Riscos:** listagem de livros (resolvido via tabela pura `reference::BOOKS`) e banco de teste (resolvido via fixture determinístico, não dependente de rede/`bible.sqlite`). |

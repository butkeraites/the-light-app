# Auditoria do loop — 2026-07-02 (Guia)

**Estado geral:** ✅ **SAUDÁVEL — gate estratégico legítimo aguardando sign-off (GATE F3.9).**
Não há desvio nem travamento silencioso. O loop parou exatamente onde o PROTOCOL manda parar.
**Nenhuma intervenção feita** (não removi o `HALT`; a decisão D1/D2/D3 é do Renan).

---

## Checklist de saúde

**1. Vivacidade — OK.** `HEARTBEAT = 2026-07-02T02:07:44Z` (~10 h atrás). `loop/HALT` existe
e o motivo é **gate estratégico** (`F3.9`, `gate: true`) — legítimo, não travamento. Pela
regra do PROTOCOL (passo 1 do ciclo), com `HALT` presente o Driver não roda ciclos, então o
heartbeat não avança de propósito. Coerente. O último ciclo (JOURNAL #115) aplicou a regra de
gate e escreveu o `HALT` — comportamento correto.

**2. Progresso real — OK.** `JOURNAL.md` mostra cadência regular (ciclos a cada ~20–60 min)
com IDs avançando linearmente por dependências. Desde a última auditoria (01/07, parada no
Marco 1) o Marco 1 foi aprovado, o Marco 2 (IA BYOK ancorada) foi semeado, aprovado
(`f13a53f`) e a **Fase 3 nativa inteira (F3.1–F3.8) foi executada, aceita e arquivada**. Sem
ciclos girando à toa.

**3. Integridade da verificação — OK (amostrado).** `F3.8.result.md` (`status: passed`,
`c0a36e3`) cola saída **real**: teste host **60/60** (incl.
`academic_markdown_from_core_cites_store_and_step_attribution`), prova headless no device
`TLA_EXPORT ref="John 3:16" provider="mock" md_len=980 has_passage=true has_attribution=true`,
sem regressão dos 10 outros `TLA_*`, e **verificação independente reproduzida do zero**. Padrão
consistente em F3.1–F3.7 (marcadores determinísticos `TLA_STUDY/CHAT/COMPARE` do retorno real,
`tsc`+`expo export web` verdes, commits reais por tarefa). Não há "passou" sem evidência.

**4. Aderência aos princípios — OK.**
- **the-light intocado:** core consumido pinado em `c8ecb2f` em toda a Fase 3; nenhuma tarefa
  exigiu PR (superfície já `pub`/`embedded`/`ai-pure`). `git status` limpo (só
  `.claude/settings.local.json` não-versionado). ✓
- **BYOK / sem segredos:** varredura de padrões de chave nos diffs recentes → **0 achados**
  (só texto de política: "chave nunca em git/log", "keyless", BYOK). ✓
- **Offline-first:** toda a Fase 3 nativa roda com provider `"mock"`, sem rede/chave; léxico
  STEP CC-BY propagado ao subset local (`reading-sample.sqlite`, ADR-0027). Pesquisa/estudo web
  são explicitamente **diferidos** (opt-in aditivo) para o gate F3.9. ✓
- **Anti-alucinação:** `passage_text`/`cited_text` (verbatim do store) sempre SEPARADOS de
  `interpretation` (LLM) na UI; léxico e citações de fontes locais; Markdown acadêmico SBL vem
  **inteiro do core** (fonte única, zero drift — app não reimplementa serialização). ✓
- **Qualidade verde:** provas no device (simulador iPhone 17) + testes host + review
  independente por tarefa. ✓

**5. Fidelidade ao plano — OK.** O construído corresponde à Fase 3 do `IMPLEMENTATION_PLAN.md`
(léxico Strong, estudo profundo, conversa/refinamento, UI de estudo/conversa/comparação
multi-IA, export acadêmico). Sem escopo inventado. As fronteiras web e a validação com chave
real foram corretamente **diferidas** e dependem da decisão do gate.

**6. Gates respeitados — OK.** F3.9 (`gate: true`) parou de fato o loop (HALT, sem execução
autônoma). Os gates anteriores (Marco 1, Marco 2) também pararam e foram aprovados por sign-off.
✓

**7. Retentativas — OK.** Nenhuma tarefa em loop infinito. Histórico mostra apenas `F0.6b` com
`attempts: 2` (dentro de `MAX_RETRIES=2`, `failed` → resolvido via `F0.6c`) e `F0.6` `blocked`
(resolvido no re-escopo). Todo o resto da Fase 3 `attempts: 0`. ✓

---

## Gate aguardando sign-off — o que o F3.9 pede e recomendação ao Renan

O F3.9 **não entrega um marco**; é um **gate de decisão estratégica** que destrava F3.10–F3.13.
Ele te pede 3 decisões (recomendação do Planner em negrito):

**D1 — Pesquisa web assistida (opt-in).** Habilitar busca web para enriquecer o estudo?
- **(A) Wikipedia keyless** — rede opt-in, sem chave, fontes citadas; padrão DESLIGADO + UI de
  privacidade. **[recomendada]**
- (B) Tavily BYOK (melhor busca, exige chave) · (C) adiar · (D) Wikipedia default + Tavily opt-in.

**D2 — Paridade web do estudo/léxico.** Como levar estudo ao alvo web?
- (A) PR `ai-pure` ao core (zero drift; refactor pesado, molde F2.7) · (B) adiar (estudo web
  nativo-only) · **(C+A) subir a CONVERSA web já** (já `ai-pure` desde a F3.4, zero PR) **e**
  adotar o PR `ai-pure` para o estudo profundo quando quiser paridade total. **[recomendada]**
  Regra dura: **nunca** espelhar o anti-alucinação do estudo em TS.

**D3 — Validação real (F3.10).** Confirmar F3.10 como o **gate de chave real** (estudo +
conversa + comparação multi-IA reais, + pesquisa web se D1 aprovar), molde F2.6/`ask_real.rs`.
Recomendação: **confirmar**.

**Avaliação contra o critério.** O gate está bem instruído: a investigação da fonte (`c8ecb2f`)
é factual (o módulo `ai::research` é `embedded`-only via `reqwest`; `study()` já aceita
`web_sources` e cita `[W:n]` com anti-alucinação embutida; a conversa já é `ai-pure`). As
recomendações preservam offline-first (web = opt-in aditivo), BYOK e zero-drift. **Nada foi
improvisado; o brief não escondeu o custo** (D2=A é refactor pesado no core via PR+ADR).

**Recomendo:** aprovar **D1=A, D2=C+A, D3=confirmar**. É o caminho de menor risco: entrega valor
web imediato (conversa) sem PR, mantém o anti-alucinação do estudo numa fonte única (core), e
adia o refactor caro do core para quando você quiser paridade total do estudo.

> **Não removi o `loop/HALT`.** Conforme o AUDIT.md, a remoção do gate é sua. Ao decidir
> D1/D2/D3, o Driver registra ADR(s) (0028+), re-escopa F3.10–F3.13 e remove o `HALT`.

---

## Classificação das afirmações (confiança)

- **certeza:** loop parado num gate legítimo; F3.1–F3.8 aceitas com evidência real;
  the-light intocado (`c8ecb2f`); sem segredos nos diffs; retries dentro do limite.
- **provável:** D2=C+A é o melhor equilíbrio risco/valor (baseado no brief e no molde F2.7/F3.4).
- **chute:** nenhuma afirmação material deste relatório depende de chute.

---

## Resumo (1–3 linhas)

O loop está **saudável** e parado corretamente num **gate estratégico** (F3.9): a Fase 3 nativa
(F3.1–F3.8) está completa, verde e verificada, com the-light intocado (`c8ecb2f`) e princípios
mantidos. **Precisa da sua atenção:** três decisões — D1 (pesquisa web opt-in), D2 (paridade web
do estudo) e D3 (validação real) — que destravam F3.10–F3.13. Recomendo **D1=A, D2=C+A,
D3=confirmar**; nada travou e nenhuma correção foi necessária.

# Auditoria do loop — 2026-07-01 (Guia)

**Estado geral:** ✅ **SAUDÁVEL — gate de marco legítimo aguardando sign-off (Marco 1 / F1.17).**
Não há desvio nem travamento silencioso. O loop parou exatamente onde o PROTOCOL
manda parar. **Nenhuma intervenção feita** (não removi o `HALT`; a aprovação é do Renan).

---

## Checklist de saúde

**1. Vivacidade — OK.** `HEARTBEAT = 2026-06-30T23:18:54Z`. `loop/HALT` existe e o
motivo é **gate de marco** (`F1.17`, `gate: true`) — legítimo, não travamento. Pela
regra do PROTOCOL (passo 1 do ciclo), com `HALT` presente o Driver não roda ciclos,
então o heartbeat não avança de propósito. Coerente.

**2. Progresso real — OK.** `JOURNAL.md` mostra cadência regular (ciclos a cada
~20–40 min) com IDs avançando linearmente por dependências. Fase 0 (F0.x) concluída e
aprovada (Marco 0, `aed825d`); Fase 1 decomposta em F1.1–F1.17 e **as 16 tarefas de
trabalho (F1.1–F1.16) aceitas e arquivadas**. `archive/` tem 61 arquivos coerentes com
o plano. Sem ciclos girando à toa.

**3. Integridade da verificação — OK (amostrado).** `F1.16.result.md` cola saída real:
marcador determinístico `WEB_NOTES note_ref="John 3:16" … persisted=true export_ok=true`,
confronto byte-a-byte contra a fonte do core (`notes.rs`/`highlights.rs`/`reference.rs`
@ `8f66004`), `tsc`/`expo export web` exit 0, e checagem de não-regressão. Padrão
consistente nos demais (commits reais por tarefa: `0479b5b`, `b2f1d8f`, `1fe14e6`,
`7557f0e`…). Não há "passou" sem evidência.

**4. Aderência aos princípios — OK.**
- **the-light intocado:** `git -C the-light rev-parse HEAD == 8f6600460c3680a537d2f5df81b6980ed7e630d5`,
  working tree **limpo**. Capacidades **delegam** ao core no nativo e **espelham** os
  SELECTs/formato no web como infra (ADR-0011). ✓
- **BYOK / sem segredos:** varredura de padrões de chave (AIza…, sk-…, PRIVATE KEY,
  api_key=) em arquivos versionados → **0 achados**. ✓
- **Offline-first:** assets locais (subset ~4,4 MB, wasm FTS5 vendorizado, userdata em
  FS/OPFS); zero rede em runtime; sem SharedArrayBuffer/COOP-COEP. ✓
- **Anti-alucinação:** texto bíblico sempre do store (KJV + Almeida 1911, domínio
  público); xref OpenBible CC-BY com atribuição visível. ✓
- **Qualidade verde:** provas no device (TLA_* via simctl) + headless node no web,
  revisão independente por tarefa. ✓

**5. Fidelidade ao plano — OK.** O construído corresponde à Fase 1 do
`IMPLEMENTATION_PLAN.md` (leitura, busca FTS5, xref, notas/export, nos 3 alvos). Sem
escopo inventado; corpus completo (~59 MB) foi **explicitamente diferido** como backlog
transversal, com registro.

**6. Gates respeitados — OK.** Os dois gates da fase pararam de fato: F1.12 (store web,
decidido por sign-off → Opção A/A1, ADR-0018) e agora F1.17 (Marco 1). O Driver aplicou
a regra de gate (HALT, sem execução autônoma). ✓

**7. Retentativas — OK.** Nenhuma tarefa em loop infinito. Histórico mostra apenas
`F0.6b` com `attempts: 2` (dentro de `MAX_RETRIES=2`), resolvido; todo o resto
`attempts: 0`. ✓

---

## Gate aguardando sign-off — recomendação ao Renan

O Marco 1 (`loop/queue/F1.17-marco-1.task.md`) declara a **Fase 1 funcionalmente
completa**: leitura (nav + versões + lado a lado + tema), busca FTS5
(acento-insensível/BM25), referências cruzadas (OpenBible CC-BY, atribuição visível) e
notas/marcações + export — **aceitas e verdes nos 3 alvos (iOS + Android + Web)**, com
provas determinísticas e review independente. As regras não negociáveis foram todas
mantidas (verificadas acima de forma independente).

**Avaliação contra o critério: o marco satisfaz o que promete.** Recomendo **aprovar a
Fase 1**. As três decisões que o sign-off pede:

1. **Aceitar a Fase 1?** → Recomendo **sim** (evidência sólida, princípios intactos).
2. **Liberar a Fase 2** (IA BYOK + Gemini)? → Decisão sua. Se sim, o `planner`
   decompõe em `backlog/`. Atenção: a Fase 2 introduz rede/chave — reforçar BYOK
   (chaves nunca em git/log) e anti-alucinação (IA só interpreta, texto do store).
3. **Prioridade do "corpus completo no web (~59 MB)"** — antes ou depois da F2? Decisão
   sua; hoje nativo e web empacotam o mesmo subset ~4,4 MB, então não é regressão.

> **Não removi o `loop/HALT`.** Conforme o AUDIT.md, a remoção do gate é sua. Quando
> decidir, o Driver registra o sign-off, arquiva F1.17, remove o `HALT` e o `planner`
> decompõe a Fase 2 (ou o backlog do corpus) conforme sua priorização.

---

## Resumo (1–3 linhas)

O loop está **saudável** e parado corretamente num **gate de marco** (Fase 1 completa,
verde nos 3 alvos, princípios intactos, the-light em `8f66004`). **Precisa da sua
atenção:** um sign-off — aprovar a Fase 1 e decidir Fase 2 + prioridade do corpus
completo. Nada travou; nenhuma correção foi necessária.

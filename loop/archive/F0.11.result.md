---
id: F0.11
status: passed
commit: aed825d
reviewer: humano (Renan) — sign-off do Marco 0
finished_at: 2026-06-30
---

## Resumo

**Gate do Marco 0 APROVADO pelo humano (Renan).** A Fase 0 (prova de viabilidade da
ponte Rust→Expo) está concluída e verificada por reviewer independente em todas as
tarefas. O humano aprovou o Marco 0 e **liberou a Fase 1** (leitura offline
multiplataforma).

## Critério de aceite (Marco 0)

- [x] Ponte UniFFI→Expo provada com **run real** nos 3 alvos: Web/WASM (F0.6b
  `1cdde6c`), iOS (F0.7 `a6f6797`), Android (F0.8 `36af016`).
- [x] **Store** provado: nativo (F0.9 `1d16897`, rusqlite/core) e web (F0.10
  `d6e968d`, wa-sqlite+OPFS).
- [x] Fricção #1 (SQLite no WASM) e risco #1 (maturidade do ubrn) eliminados.
- [x] `the-light` sem fork — única mudança via PR sancionado (feature `embedded`,
  `8f66004`, ADR-0005); consumo por git dep pinada.
- [x] Anti-alucinação, offline-first, licenciamento (KJV domínio público), segredos
  fora do git — mantidos e verificados.
- [x] ADRs (0001–0012) e `PROGRESS.md` (F0.0–F0.10) atualizados.

## Saída da verificação

Sign-off humano via decisão registrada (2026-06-30): **"Aprovar e liberar a Fase 1"**.
Evidência por tarefa em `loop/archive/F0.*.result.md` (cada uma com reviewer
independente verde) e no `git log`.

## Decisões / ADRs

Marco 0 fecha a Fase 0. A Fase 1 (leitura offline: navegação livro/capítulo,
versões PT/EN, busca FTS5, xrefs, notas — `IMPLEMENTATION_PLAN.md` §F1) é liberada;
o planner a decompõe no `backlog/`/`queue/`.

## Bloqueios / perguntas

Nenhum. Follow-ups de Fase 0 registrados como dívida (não-bloqueante): bundling do
`sample.sqlite` no app nativo p/ exercitar `get_passage` nativo via Turbo Module;
store web completo (search/xref) via Opção B no futuro; CI das provas headless;
higiene de AVD/symlink.

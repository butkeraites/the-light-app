---
id: F5.37
status: passed
commit: 79fb114
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

Verificação independente da F5.37 (clareza de UX: deixar explícito o que precisa de
provedor de IA/BYOK vs. o que é 100% offline). Re-executei todos os gates a partir de
`app/` no commit `79fb114` — todos verdes. As 6 verificações adversariais passam. O
aviso "sem provedor de IA" é claro, acionável e HONESTO; os recursos offline NÃO foram
gated; anti-alucinação intacta; não-negociáveis respeitados. **PASSED.**

## Critério de aceite (marcado)

- [x] Sem provedor configurado, cada painel de IA (Ask/Study/Compare/Session) mostra CTA
      claro via `t()` com link p/ configuração — não erro cru/tela vazia.
      `AiProviderNotice` renderiza `t('ai.noProviderTitle')` + `t('ai.noProviderBody')`
      (nomeia Claude/GPT/Gemini/Ollama) + `t('ai.noProviderOffline')` + botão
      `t('ai.noProviderCta')`. Aparece nos 4 painéis quando `providersChecked &&
      providersWithKey.length === 0` (guardado por `checked` p/ não piscar). Card com
      borda/título/corpo — não é erro nem blank.
- [x] Recursos offline (leitura/busca/xref/notas/planos) NÃO exibem aviso nem pedem chave.
      O commit toca APENAS os 4 painéis de IA + `about.tsx` + `i18n.ts` (7 arquivos).
      `AiProviderNotice`/`useConfiguredAiProviders` são importados só pelos 4 painéis de
      IA (grep confirma zero uso fora deles). Nenhum key-check adicionado a reading/search.
- [x] Distinção offline-vs-IA visível: `about.tsx` ganhou seção `about.aiVsOfflineTitle`
      + `about.offlineFeatures` (lista os offline) + `about.aiFeatures` (lista os de IA).
- [x] `t()` paridade pt/en, a11y, contraste verdes; `tsc` 0; `expo export` 0 (saídas abaixo).
- [x] Anti-alucinação intacta: todas as chaves novas são CROMO (`ai.*`/`a11y.*`/`about.*`);
      nenhum texto bíblico via `t()`; `test:web:ai/study/compare` provam cited_text (store,
      verbatim) separado de interpretation (mock). the-light em `225b8c9`; sem segredo.

## Saída da verificação (real)

```
=== tsc --noEmit ===            TSC_EXIT=0
=== test:i18n ===              PASS — paridade pt↔en 233 chaves, sem órfã; todas CROMO  (EXIT 0)
=== test:i18n-coverage ===     PASS — 25 arquivos sem string PT fora de t(); sem hex     (EXIT 0)
=== test:a11y-scan ===         PASS — 74 interativos/25 arquivos (Pressable×59) c/ role+label+alvo (EXIT 0)
=== test:web:contrast ===      PASS — 18 pares WCAG AA; guarda não-vacuosa               (EXIT 0)
=== test:web:ai ===            PASS — cited_text=João3:16 KJV verbatim (store) vs interpretation (mock) (EXIT 0)
=== test:web:study ===         PASS — passageText do store vs interpretation mock; 22 citações STEP (EXIT 0)
=== test:web:compare ===       PASS — mesma âncora do store; provedor mock=0 fetch        (EXIT 0)
=== expo export --platform web ===  EXPO_EXIT=0  (Exported: dist)
```

## Verificações adversariais (as 6)

1. **Aviso claro + acionável — OK.** Sem provedor: card amigável via `t()` nomeando
   Claude/GPT/Gemini/Ollama local. CTA é um `Pressable` real com `accessibilityRole="button"`
   + `accessibilityLabel={t('a11y.aiConfigure')}`, `onPress` = `onClose()` seguido de
   `router.push('/about')`. `/about` existe (`app/app/about.tsx`). Não é erro/blank.
2. **Offline NÃO gated (crítico) — OK.** `git show --name-only` confirma que o commit não
   toca nada sob `core/`, `loop/` nem os componentes de reading/search/xref/notes/plans.
   `useConfiguredAiProviders` → `listProviders()` do keystore: itera `SUPPORTED_PROVIDERS`,
   lê o item só p/ checar presença (`value != null && value.length > 0`) e devolve APENAS
   os NOMES — nunca o valor; sem `fetch`/rede. Falha de leitura → lista vazia (best-effort).
3. **Mensageria honesta — OK.** O corpo diz "Este recurso usa IA. Para respostas de um
   provedor (Claude/GPT/Gemini/Ollama), configure uma chave" + reassegura offline. Não
   promete IA real onde só há mock. O aviso é ADITIVO (renderizado ANTES do seletor no
   ScrollView), não substitui o conteúdo: o provedor default `mock` (`useState(MOCK_PROVIDER)`)
   segue respondendo abaixo. Mock path intacto.
4. **Anti-alucinação intacta — OK.** As 8 chaves novas (4×`ai.*`, 1×`a11y.*`, 3×`about.*`)
   são cromo; `test:i18n` afirma que todas as 233 chaves são de namespace de cromo (nenhuma
   de versículo). cited_text vs interpretation seguem separados (web tests).
5. **Remoção de jargão segura — OK.** "F3.10" saiu de `ai.mockProviderNote` e
   `compare.columnNoKey` (pt+en). Nenhum teste asserta essas strings (grep em
   `web/__tests__/` por `columnNoKey`/`mockProviderNote`/`noProvider` = nada; a ocorrência de
   "F3.10" em `compare.web.test.mjs` é comentário, não asserção). Paridade pt/en preservada (233).
6. **Não-negociáveis — OK.** the-light limpo em `225b8c9`. `.claude/settings.json` NÃO está
   no commit `79fb114` (segue `M` só na árvore de trabalho local do auditor). Commit toca só
   produto/i18n. Sem literal de segredo/hex em `AiProviderNotice.tsx` (cores via tokens de tema).

## Decisões / ADRs

- **CTA → /about (adaptação):** não existe tela "Ajustes"/Settings dedicada; a configuração
  de chave é inline nos próprios painéis, e a About (F5.35) é o hub que EXPLICA o BYOK e
  agora carrega a distinção offline-vs-IA. Rotear o CTA para `/about` é uma adaptação
  razoável e fiel ao intento do aceite ("link para a tela de configuração de chave" →
  hub de explicação BYOK). Aceito.

## Bloqueios / perguntas

- Nenhum bloqueio. Observação menor (não-bloqueante): como a About explica mas não tem o
  campo de input de chave, o usuário fecha o painel, lê a About e precisa reabrir um painel
  para digitar a chave inline — uma leve indireção. Aceitável nesta entrega; um futuro
  refinamento poderia levar o CTA direto ao input inline. Não afeta o aceite.

---
id: F5.22
title: GATE — Brief de decisão + ADR: sincronização opcional por conta (o QUE sincroniza · conflito · e2e/BYO-backend vs provedor · offline permanece default · privacidade)
phase: 5
status: ready
gate: true
depends_on: []
attempts: 0
created_by: planner
created_at: 2026-07-02
---

## Objetivo

Tarefa de DECISÃO humana/arquitetural (gate:true). Por ser gate, o loop NÃO executa autonomamente: o Driver escreve loop/HALT e o humano/Guia decide antes de liberar QUALQUER código de sync. Não há código de produto a implementar — é um BRIEF DE DECISÃO que fixa, num ADR novo (ADR-0036), como (e SE) haverá sincronização por conta dos dados do usuário SEM quebrar o offline-first como base. Só após o ADR o Driver re-escopa/semeia os follow-ons (sync-1..sync-4).

## Contexto / arquivos

_Workstream:_ Sync opcional por conta  ·  _Blocking:_ **sim**  ·  _Gate:_ **sim (HALT p/ decisão humana)**

- `/Users/butkeraites/Documents/the-light-app/IMPLEMENTATION_PLAN.md`
- `/Users/butkeraites/Documents/the-light-app/VISION_AND_ARCHITECTURE.md`
- `/Users/butkeraites/Documents/the-light-app/DECISIONS.md`
- `/Users/butkeraites/Documents/the-light-app/loop/PROTOCOL.md`
- `/Users/butkeraites/Documents/the-light-app/loop/archive/F1.12-gate-store-web-corpus.task.md`
- `/Users/butkeraites/Documents/the-light-app/app/lib/userdata.ts`
- `/Users/butkeraites/Documents/the-light-app/app/web/userdata-fs.web.ts`
- `/Users/butkeraites/Documents/the-light-app/app/lib/notesExport.ts`
- `/Users/butkeraites/Documents/the-light-app/app/lib/keystore.ts`
- `/Users/butkeraites/Documents/the-light-app/app/lib/keystore.web.ts`

## Passos sugeridos

1. Escrever o BRIEF no corpo da tarefa e depois consolidar como ADR-0036 em DECISIONS.md, decidindo cada eixo: (A) ESCOPO — o que sincroniza. Dado REAL hoje: notas (`notes/<slug>.md`, só-corpo) + marcações (`highlights.json`, `[{ref,color,tag?}]`). Progresso de PLANO de leitura NÃO existe (é o §F4.1) → decidir se entra como alvo CONDICIONAL (só quando o §F4.1 aterrissar) ou fica fora. Tema/ajustes = provavelmente device-local (fora). Justificar o conjunto mínimo.
2. (B) MODELO DE CONFLITO — decidir op-based (oplog/replay) vs state-based LWW; granularidade (nota inteira LWW vs merge 3-vias do corpo; highlights como SET por `ref` com união + LWW da cor/tag); relógio (Lamport/vetor de versão/timestamp+deviceId); regra determinística de desempate. Tombstones para deleção. IDs de dispositivo (aleatório, local).
3. (C) BACKEND — decidir entre: (a) BYO-backend (o usuário informa URL+token de um serviço que ELE controla: WebDAV/S3/CouchDB/git remoto) — máximo alinhamento com offline-first/sem-lock-in/sem-conta-conosco; (b) provedor hospedado por nós (introduz conta, ToS, custo, superfície de privacidade); (c) arquivo de export/import cifrado que o usuário guarda no próprio nuvem (iCloud/Drive) — o mais leve. Registrar trade-offs e a escolha.
4. (D) CRIPTOGRAFIA E2E — decidir cifra/KDF (ex.: passphrase → Argon2id → chave; blobs do oplog cifrados no cliente; o backend só vê ciphertext) e ONDE mora a passphrase (secure-store nativo / session-only web; NUNCA sincronizada, NUNCA em git/log). Sem e2e, o backend leria as notas do usuário → justificar se e2e é obrigatório.
5. (E) OFFLINE PERMANECE O DEFAULT — declarar como invariante testável: sync é camada aditiva, DESLIGADA por padrão; com sync OFF todas as capacidades (leitura/versões/tema/busca/xref/notas/marcações/IA) funcionam com ZERO conta/rede; NENHUM caminho de código passa a EXIGIR conta/rede; desligar/apagar a conta deixa os dados LOCAIS intactos.
6. (F) PRIVACIDADE / SEM TELEMETRIA — sem analytics/beacons; só tráfego de sync iniciado pelo usuário; minimização de dados; aviso de privacidade na UI; direito de apagar. (G) ALVOS — decidir a ordem (molde: nativo com secure-store primeiro; web session-only), e SE algum campo/tombstone precisa nascer no core `userdata` (embedded-only) → nesse caso vira um PR+ADR SEPARADO (touches_the_light, gate) a ser semeado depois, NÃO projetado aqui.
7. Registrar ADR-0036 em DECISIONS.md (molde dos ADRs de gate: Contexto/Decisão/Alternativas rejeitadas/Consequências); escrever loop/HALT com o motivo (aguardando sign-off do gate de design de sync); atualizar loop/STATUS.md. NÃO tocar código de produto nem o the-light.

## Critério de aceite

- [ ] ADR-0036 escrito em DECISIONS.md respondendo, com decisão explícita e alternativas rejeitadas, aos 7 eixos: escopo (o que sincroniza + status condicional do progresso de plano), modelo de conflito, backend (BYO vs provedor vs export cifrado), criptografia e2e (cifra/KDF + onde mora a passphrase), offline-permanece-default (invariante testável), privacidade/sem-telemetria, alvos + se toca o core
- [ ] O ADR declara EXPLICITAMENTE os não-negociáveis herdados: offline-first (sync OFF por padrão; tudo funciona com zero conta/rede), BYOK/segredos (token/passphrase nunca em git/log; secure-store nativo / session-only web), sem telemetria, the-light só via PR+ADR
- [ ] NENHUM arquivo de produto alterado (só DECISIONS.md + arquivos do loop); the-light intacto (`2fc2dab`)
- [ ] loop/HALT escrito com o motivo do gate; loop/STATUS.md atualizado apontando o próximo ADR livre após ADR-0036

## Comando(s) de verificação

```sh
git status --porcelain | grep -vE '^\s*M?\s*(DECISIONS.md|loop/)' | wc -l  # deve ser 0: só DECISIONS.md + loop/ mudaram
grep -n 'ADR-0036' /Users/butkeraites/Documents/the-light-app/DECISIONS.md  # ADR presente
test -f /Users/butkeraites/Documents/the-light-app/loop/HALT && cat /Users/butkeraites/Documents/the-light-app/loop/HALT  # HALT escrito com motivo do gate
grep -n 'rev = "2fc2dab' /Users/butkeraites/Documents/the-light-app/core/Cargo.toml  # the-light NÃO re-pinado
```

## Restrições

ENTRY do workstream — é a PRIMEIRA tarefa e é o gate obrigatório do §F4.4 ("Registrar ADR antes de iniciar"). NÃO projetar a implementação completa além do gate: os follow-ons (sync-1..sync-4) são apenas esboços bloqueados neste ADR. RISCO ALTO: se o ADR decidir que algum campo/tombstone/merge precisa nascer no core `userdata` (embedded-only), isso é um PR+ADR SEPARADO ao the-light (touches_the_light:true, gate:true, blocking) semeado DEPOIS — não aqui. Anti-alucinação NÃO se aplica ao corpo da nota/tag (dado livre do usuário, ADR-0017), mas a REFERÊNCIA continua canônica via wasm.

- **Offline-first:** capacidade essencial nunca exige rede/conta; recurso novo liga OFF/local por padrão.
- **BYOK/segredos:** chaves/tokens nunca em git/log; secure-store nativo / session-only web.
- **Anti-alucinação:** texto de versículo sempre do store local (Rust); i18n NUNCA traduz versículo; atribuições CC-BY (OpenBible/STEP) VISÍVEIS e verbatim.
- **the-light só via PR+ADR:** consumir `the-light-core` pinado (`2fc2dab`); nenhuma mudança no core nesta tarefa (senão vira PR+ADR gate à parte).
- **Qualidade:** Rust fmt/clippy -D warnings/test; TS `tsc --noEmit`; grafo wasm puro; prova determinística/headless (sem chave/rede real no loop).

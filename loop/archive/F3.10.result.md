---
id: F3.10
status: validated
gate: true
reviewer: humano (validação com a própria chave, fora do loop)
finished_at: 2026-07-02
---

# F3.10 — Validação REAL do estudo profundo: **VALIDADA**

## Resumo
O humano rodou `core/examples/study_real.rs` com a **própria chave** (env var, nunca no
chat/git/log) e obteve um **estudo profundo REAL** de João 3:16, provando a cadeia ponta a
ponta com **anti-alucinação impecável** — inclusive a **pesquisa web opt-in** (Wikipedia).

## Resultado (reportado pelo humano; a chave nunca passou pelo loop)
- **provider = gemini**, **model = gemini-2.5-flash**, `research = wikipedia`.
- **PASSAGE_TEXT** (do STORE, verbatim): João 3:16 KJV ("For God so loved the world…").
- **INTERPRETATION** (IA real, não mock): análise acadêmica em **7 seções** (Text/Translation,
  Crítica textual, Análise lexical, Contexto histórico-literário, Estrutura/argumento,
  História da interpretação, Síntese teológica — lente Baptist/Academic/Exegetical), com
  âncoras `[V:G####]` (léxico) e `[W:n]` (web).
- **CITATIONS = 26**, TODAS de fontes locais/verificadas — **nunca do modelo**: léxico Strong
  **STEP CC-BY** (TBESG: G2316/θεός, G0025/ἀγαπάω, G3439/μονογενής, G2222/ζωή, …) + **web
  Wikipedia** (`[W:1]` John 3:16, `[W:2]` "3:16 game", `[W:3]`, `[W:4]`).
- **WARNINGS = []** — o core **validou todas as âncoras** (`cited_web_indices` + `lexicon::verify`);
  nenhum Strong/fonte inventado sobreviveu. **Prova central do anti-alucinação.**
- **ACADEMIC_MARKDOWN = 16654 chars** (SBL, `to_academic_markdown` do core).
- **Chave nunca exibida** ("(presente — não exibida)") nem logada/commitada.

## Critério de aceite
- [x] Interpretação REAL do provedor (Gemini), não o mock.
- [x] PASSAGE_TEXT = João 3:16 verbatim do STORE (anti-alucinação).
- [x] CITATIONS do léxico STEP CC-BY + web Wikipedia — nunca do modelo; ACADEMIC_MARKDOWN > 0.
- [x] Pesquisa web opt-in (`AI_RESEARCH=wikipedia`) funcionou (fontes `[W:n]` citadas/verificadas).
- [x] Sem erro; a chave não apareceu em nenhum output.
- [x] Humano reportou provedor/modelo + sucesso sem colar a chave.

## Consequência
F3.10 **validada** → o Driver arquiva, remove `loop/HALT`, e segue para **F3.11** (PR
`ai-pure` ao the-light p/ paridade web do estudo — ADR-0029; handoff de merge). Rede só nesta
validação opt-in; offline-first intacto; `the-light` em `c8ecb2f`.

---
id: F6.1
status: passed
commit: ab523d6
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

Harness de smoke em browser REAL (puppeteer-core sobre o Chrome do sistema)
verificado de forma independente, incluindo a **reprodução da auto-prova** — o
critério central. Dirige Chrome real contra o dev server (`expo start --web`
:8099) e o build de produção (`expo export` + static-server :8100), asserindo o
fluxo abrir-capítulo (Mateus 40/1 + João 43/3 verbatim, sem spinner infinito,
zero pageerror `SQLiteESMFactory|openReadingDbWeb|initAsync|Invalid base URL`).
Todos os gates verdes; nenhum órfão; the-light intocado; sem segredo. **passed.**

## Critério de aceite (verificado)

- [x] `app/web/__browser__/{chrome,server,static-server,smoke.browser,assertions}.mjs`
      criados (5 arquivos no commit ab523d6).
- [x] `test:web:smoke-dev` E `test:web:smoke-dist` verdes: Mateus 1 e João 3
      renderizam o texto verbatim em AMBOS os alvos, sem spinner infinito.
- [x] Chrome ausente ⇒ vermelho (banner + exit 1); só pula com
      `SMOKE_SKIP_IF_NO_CHROME=1` imprimindo `NOT VERIFIED` (exit 0).
- [x] **Auto-prova demonstrada por mim** (reverter locateFile → smoke RED com
      `Invalid base URL` no browser real; headless reading GREEN → restaurar → GREEN).
- [x] `puppeteer-core ^23.11.1` em devDeps; sem download de browser (core-only,
      sem postinstall); determinístico (userDataDir mkdtemp por run, waitForFunction).
- [x] `tsc --noEmit` 0; `test:web:reading` sem regressão; the-light `225b8c9`; sem segredo.

## Saída da verificação (re-rodada por mim, de `app/`)

```
$ npx tsc --noEmit            → TSC_EXIT=0
$ npm run test:web:reading    → PASS — leitura web (... João 3 KJV 36 versículos ...)   (READING_EXIT=0)
$ npm run test:web:smoke-dev  → TLA_WEB_open-chapter ok / ==> SMOKE OK (target=dev)
$ npm run test:web:smoke-dist → [rm -rf dist + expo export FRESCO: emite read/[book]/[chapter] (22KB)]
                                 static-server :8100 → TLA_WEB_open-chapter ok / ==> SMOKE OK (target=dist)
```

Órfãos após cada run: nada em :8099/:8100, sem `tla-smoke*`/`static-server.mjs`.

## Auto-prova reproduzida por mim (o ponto da tarefa)

Revertido `web/sqlite-reading-opfs.web.ts:109` de
`SQLiteESMFactory({ wasmBinary, locateFile: (path) => path })` para
`SQLiteESMFactory({ wasmBinary })` (diff de 1 linha):

- **Headless GREEN (cego ao bug):** `npm run test:web:reading` → `READING_EXIT=0`, `PASS`.
- **Smoke RED (pega o que headless perde):** `npm run test:web:smoke-dev` → exit 1:
  ```
  TLA_WEB_open-chapter FAIL
  Error: /read/40/1: em 60000ms o texto "The book of the generation of Jesus Christ" NÃO apareceu.
  body[0..400]="Matthew 1 PT ☀ ☾ ◐ Failed to construct 'URL': Invalid base URL"
  urlErrs(3): ... | Failed to construct 'URL': Invalid base URL | Failed to construct 'URL': Invalid base URL
  ==> SMOKE FAIL (target=dev, fluxos com falha=1)
  ```
- **Restaurado** via `git checkout -- web/sqlite-reading-opfs.web.ts` (linha 109 volta a
  `locateFile: (path: string) => path`, git status limpo p/ o arquivo); smoke-dev re-rodado
  **2x** (anti-flake) → ambos `TLA_WEB_open-chapter ok` / `SMOKE OK`, sem órfãos.
  A árvore de trabalho termina com o fix no lugar (nada commitado).

## Substância adversarial (5 checks)

1. **Sem falso verde.** `SMOKE_FORCE_NO_CHROME=1 node web/__browser__/chrome.mjs` →
   banner alto + exit 1. Com `SMOKE_SKIP_IF_NO_CHROME=1` → `SMOKE SKIPPED — NOT VERIFIED`
   + exit 0. Driver completo `SMOKE_FORCE_NO_CHROME=1 ... smoke.browser.mjs --target=dev`
   → exit 1 (falha-rápida antes de subir servidor; nada em :8099). O seam
   `SMOKE_FORCE_NO_CHROME` só produz RED/skip. O harness só **lê** `SMOKE_SKIP_IF_NO_CHROME`
   (comparação `=== '1'`); nunca o seta — os `test:web:smoke*` do package.json também não.
2. **dist real, não velho.** `startDist()` faz `fs.rmSync(dist)` + `expo export` por padrão;
   reuso só via `SMOKE_DIST_REUSE=1` (que o script smoke-dist não seta). Saída da minha run
   mostra export fresco emitindo o template `read/[book]/[chapter]`.
3. **Determinismo/anti-flake.** `userDataDir` novo por run (`mkdtempSync('tla-smoke-')`);
   asserção espera sinais estáveis de DOM (`page.waitForFunction`: texto presente E spinner
   ausente), sem `setTimeout` fixo p/ render; trap-teardown (`finalize`). smoke-dev rodado
   2x → GREEN estável, sem órfãos.
4. **Escopo.** Só o fluxo `open-chapter` é asserido (assertions.mjs `flows` = 1). Commit
   ab523d6 não toca `app/web/generated/*` nem `app/dist`.
5. **Não negociáveis.** `puppeteer-core ^23.11.1` core-only, sem postinstall/download
   (usa `executablePath` = Chrome do sistema, offline-first); the-light em `225b8c9`;
   `.claude/settings.json` NÃO commitado (M pré-existente na worktree); `app/dist` gitignored
   e não rastreado; sem segredo nos arquivos do harness; commit toca só os 5 `.mjs` +
   `package.json`/`package-lock.json`.

## Decisões / ADRs

Nenhuma. Round-trip de verificação não commitado; worktree termina com o fix no lugar.

## Bloqueios / perguntas

Nenhuma. Observação (não-bloqueante): existe `~/.cache/puppeteer` de 23/jun (anterior ao
commit de 03/jul) — resíduo de instalação prévia, não gerado por esta tarefa; `puppeteer-core`
não tem postinstall e o harness aponta para o Chrome do sistema, então não há download nesta
tarefa. Sem impacto no aceite.

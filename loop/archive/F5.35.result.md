---
id: F5.35
status: passed
commit: 89c34ad
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

Tela `/about` (Sobre / Créditos / Licenças) implementada, registrada na Stack e linkada
da Home. Consolida as 4 fontes embarcadas com licença, declara offline-first / BYOK /
anti-alucinação, lista os provedores de IA REAIS e oferece atalho de backup que REUSA o
`SyncSettings` existente (lazy). Todo cromo via `t()` (pt/en), tokens de tema (zero hex),
a11y (role/label/alvo). Novo guard `test:about-attr` trava o drift das 2 atribuições CC-BY.
Re-baseline de perf legítimo (+1 rota eager, 839→840). Verifiquei tudo de forma
independente re-rodando cada gate — VERDE. **PASSED.**

## Critério de aceite (verificado)

- [x] **Rota `/about` alcançável + 4 fontes com licença.** `app/app/about.tsx` registrada em
      `_layout.tsx:100` (`<Stack.Screen name="about">`) e linkada da Home em `index.tsx:231`
      (`<Link href="/about" testID="open-about">`). Lista KJV (domínio público), Almeida 1911
      (domínio público), OpenBible.info (CC-BY) e STEP/Tyndale (CC BY 4.0); declara offline-first,
      BYOK e anti-alucinação; atalho de backup reusa `SyncSettings`. `about.html` emitido no export.
- [x] **Cromo via t() com paridade pt/en; CC-BY VERBATIM pelo allowlist inalteradas.**
      `test:i18n-coverage` PASS (225 chaves, mesmos conjuntos pt↔en, todas em namespace de CROMO,
      CC-BY isentas). `about` adicionado ao conjunto de namespaces de cromo em `i18n.test.mjs` e
      `i18n-coverage.test.mjs`. As 2 atribuições são valores `t()` FIXOS idênticos pt/en.
- [x] **Gates verdes.** `tsc --noEmit` 0; `test:i18n-coverage`/`test:a11y-scan`/`test:web:contrast`
      PASS; `expo export --platform web` sucesso. (Também `test:about-attr`, `test:i18n`,
      `test:web:perf-budget` verdes.)
- [x] **the-light intocado (`225b8c9`); sem segredo/rede.** the-light @ `225b8c929...` limpo;
      `about.tsx` sem `fetch`/`console.*`/segredo/URL.

## Saída da verificação (re-rodada de forma independente, a partir de `app/`)

```
tsc --noEmit                 → TSC_EXIT=0
test:about-attr              → EXIT 0 — PASS (guarda fonte-da-verdade CC-BY)
   about.xrefAttribution == XREF_ATTRIBUTION (OpenBible CC-BY), idêntico pt/en
   about.stepAttribution == STEP_ATTRIBUTION (STEP/Tyndale CC BY 4.0), idêntico pt/en
test:i18n-coverage           → EXIT 0 — PASS (225 chaves paridade; cromo sem PT/hex; CC-BY isento)
test:i18n                    → EXIT 0 — PASS (paridade + anti-alucinação: 225 chaves cromo)
test:a11y-scan               → EXIT 0 — PASS (73 interativos: role/label/alvo ≥44)
test:web:contrast            → EXIT 0 — PASS (WCAG AA, tokens de tema; guarda não-vacuosa)
test:web:perf-budget         → EXIT 0 — PERF BUDGET LOCKED OK
   moduleCount 840 (budget 840, EXATO)
   eagerBytes 1340803 (±1024) · eagerGzip 338708 (±2048) · eagerBrotli 267845 (±1024)
expo export --platform web   → EXPO_EXIT=0 · dist/about.html emitido (24764 B)
git status (pós re-run)      → só ` M .claude/settings.json` (baseline regenerou byte-idêntico)
```

## Achados dos 6 checks adversariais

1. **Drift guard REAL (não vacuoso).** `about-attributions.test.mjs` extrai por texto as
   constantes fonte `XREF_ATTRIBUTION` (`ReaderXrefPanel.tsx:30`) e `STEP_ATTRIBUTION`
   (`ReaderStudyPanel.tsx:75`) e compara byte-a-byte contra AMBAS as ocorrências (pt E en) do
   catálogo, além de exigir pt==en. Confirmado idênticas: xref = `Cross references courtesy of
   OpenBible.info (CC-BY)`; step = `Credit it to 'STEP Bible' linked to www.STEPBible.org (data
   based on work at Tyndale House, Cambridge; CC BY 4.0)`. Self-test (§1) prova o extrator
   (aspas simples/duplas/multi-linha) e que o comparador distingue strings diferentes
   (`assert.notEqual('CC-BY','CC BY 4.0')`). FALHA em drift de qualquer lado.
2. **Claims precisos.** offline-first / BYOK / anti-alucinação declarados com verdade. Provedores
   = Claude (Anthropic), GPT (OpenAI), Gemini (Google), Ollama (modelos locais, sem chave) =
   `SUPPORTED_PROVIDERS = ['anthropic','openai','gemini','ollama']` (`lib/keystore.ts:20`). Nenhum
   provedor inventado; Ollama incluído (corrige omissão do README). Rótulos de vendor corretos.
3. **Backup reusa SyncSettings (mesmo padrão lazy).** `about.tsx:38` usa
   `await import('../components/SyncSettings')` — idêntico ao da Home (`index.tsx:83`). No bundle,
   AMBOS referenciam o MESMO chunk async `SyncSettings-94a2edd...js` (path 840); nenhuma superfície
   duplicada.
4. **Re-baseline de perf legítimo.** moduleCount 839→840 = as +1 rota eager `about` (web
   `output: static` ⇒ toda rota é eager). Confirmei que os motores pesados NÃO entraram no entry:
   `SyncSettings` e `createDriveAuth` estão em chunks ASYNC separados (`SyncSettings-...js`,
   `driveAuth-...js`); o entry só tem as referências de `import()`. `measure-web-bundle.sh` e
   `web-bundle-budget.json` batem exatamente (nominais idênticos: eagerBytes 1340803, gzip 338708,
   brotli 267845, moduleCount 840); **tolerâncias INALTERADAS** (1024/2048/1024). Aceitável pelo
   precedente F5.10/F5.19/F5.26. Único toque em `loop/` = `web-bundle-budget.json` + baseline
   regenerado (determinístico — zero drift no working tree pós re-run). Nada de STATUS/JOURNAL/
   queue/HALT tocado.
5. **a11y/contrast/i18n cobrem a nova tela.** Interativo (`about-open-sync`) com role/label/alvo;
   link da Home com role/label. Zero hex (tokens de tema). Todo cromo via `t()` com paridade
   pt/en (nav.about, home.about, a11y.openAbout, a11y.openSync, home.syncBackup presentes nos 2
   catálogos). ADR-0055 presente e coerente (fecha a varredura; próximo livre = ADR-0056).
6. **Não negociáveis OK.** the-light intocado @ `225b8c9` (limpo). `.claude/settings.json` NÃO
   commitado (só modificado no working tree). Commit `89c34ad` toca apenas
   produto/i18n/teste/scripts/DECISIONS + o JSON de perf — nada sob `core/`. Sem
   segredo/rede/console em `about.tsx`.

## Decisões / ADRs

ADR-0055 (F5.35) registrado em `DECISIONS.md`: tela Sobre consolidada, atribuições CC-BY como
fonte-da-verdade verbatim travadas por `test:about-attr`, e re-baseline de perf deliberado
(+1 rota eager) com tolerâncias inalteradas.

## Bloqueios / perguntas

Nenhuma. Última tarefa da varredura de refinamento (F5.28+) concluída e aceita.

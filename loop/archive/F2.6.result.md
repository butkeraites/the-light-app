---
id: F2.6
status: validated
gate: true
reviewer: humano (validação com a própria chave, fora do loop)
finished_at: 2026-07-01
---

# F2.6 — Validação REAL do `ask` com a chave BYOK: **VALIDADA**

## Resumo
O humano rodou o harness `core/examples/ask_real.rs` com a **própria chave** (numa env
var, nunca no chat/git/log) e obteve uma resposta **REAL** do provedor, provando a cadeia
BYOK ponta a ponta com **anti-alucinação intacta**.

## Resultado (reportado pelo humano; a chave nunca passou pelo loop)
- **provider = gemini**, **model = gemini-2.5-flash** (2ª tentativa; ver abaixo).
- **CITED_TEXT** (do STORE, verbatim): `16 For God so loved the world, that he gave his
  only begotten Son, that whosoever believeth in him should not perish, but have
  everlasting life.` → **João 3:16 KJV verbatim do banco local** (idêntico ao `TLA_READ`/
  `TLA_SEARCH`). **Anti-alucinação: o texto bíblico vem do store; o LLM só interpreta.**
- **INTERPRETATION** (IA): parágrafo real do Gemini sobre João 3:16 (amor de Deus,
  sacrifício, vida eterna) — claramente do modelo, não do mock.
- **Chave nunca exibida** ("(presente — não exibida)") nem logada/commitada.

## Critério de aceite
- [x] Interpretação REAL do provedor (não o mock).
- [x] CITED_TEXT = João 3:16 verbatim do STORE (anti-alucinação).
- [x] Sem erro; a chave não apareceu em nenhum output.
- [x] Humano reportou provedor/modelo + "funcionou" sem colar a chave.

## Nota (1ª tentativa) — achado registrado
A 1ª execução (default `gemini-2.0-flash`) deu **HTTP 429 free_tier limit: 0**. Pesquisa
web confirmou que **`gemini-2.0-flash` foi RETIRADO em 3/mar/2026** → o default que a F2.3
pôs no `the-light-core` (`providers.rs` `default_model`) está OBSOLETO. A 2ª tentativa
com `AI_MODEL=gemini-2.5-flash` (free-tier atual) passou.

## Consequência / TODO
- **the-light PR focado (batch com F2.7/D2):** atualizar `default_model` do Gemini de
  `gemini-2.0-flash` → `gemini-2.5-flash` no `the-light-core` (o default atual aponta p/
  modelo morto). Enquanto não mergeado, usar Gemini exige passar `model` explícito.
- Rede só nesta validação opt-in; offline-first intacto; `the-light` em `133077a`.

## Desfecho
F2.6 **validada** → o Driver arquiva, remove `loop/HALT`, e segue para **F2.7** (paridade
web de IA, que começa pelo **PR D2/wasm ao core** — no qual entra também o fix do
`default_model`) → **F2.8 (Marco 2)**.

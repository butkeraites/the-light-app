---
id: F6.3
status: passed
commit: 4316a22
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

O gate do wasm da fronteira foi endurecido: `useWasmReady()` (web) não engole mais a
falha de `uniffiInitAsync()` — expõe `{ ready, error, retry }`; o `WasmGate` renderiza
erro VISÍVEL + botão "Tentar de novo" em vez de spinner infinito; `retry()` reseta a
`initPromise` memoizada para uma nova tentativa REAL. Novo fluxo de smoke
`wasm-error-ui` (só sob `SMOKE_WASM_WRONG_MIME=1`) prova a UI de erro E a recuperação
por retry end-to-end. Todos os gates verdes; os 7 fluxos da F6.2 seguem verdes em
dev+dist. Aprovado.

## Critério de aceite (todos cumpridos)

- [x] Falha de init da fronteira → UI de erro VISÍVEL + retry, nunca spinner infinito —
      provado no dist com `SMOKE_WASM_WRONG_MIME=1`: `TLA_WEB_wasm-error-ui ok`. O smoke
      assevera `wasm-error` + `wasm-retry` presentes E spinner AUSENTE, e que o texto de
      leitura NÃO vazou (gate bloqueou os children).
- [x] Caminho feliz inalterado: `test:web:smoke` verde nos 7 fluxos da F6.2 em dev E dist.
- [x] `wasm.web.ts` não engole mais o erro (`.catch` preenche `error`); `WasmGate` mostra
      erro+retry; `retry()` re-tenta de verdade (reset da promise + re-dispara o effect) —
      provado pela recuperação real no smoke.
- [x] Cromo via `t()` pt/en (`wasm.loadError`/`wasm.retry`); a11y no botão (role/label);
      sem hex. `tsc --noEmit` 0; i18n/i18n-coverage/a11y-scan verdes.
- [x] `app/web/generated/*` intocado; the-light em `225b8c9`; sem segredo.

## Saída da verificação (re-rodada pelo reviewer, de `app/`)

```
=== tsc ===            TSC_EXIT=0
=== test:i18n ===      PASS — paridade pt↔en: 235 chaves (inclui wasm.loadError/wasm.retry)
=== test:i18n-coverage === PASS — paridade 235; cromo 25 arquivos sem PT fora de t(); sem hex
=== test:a11y-scan === PASS — 75 interativos (Pressable×60) com role/label/alvo (retry ok)

=== test:web:smoke (dev) ===
TLA_WEB_open-chapter ok
TLA_WEB_parallel ok
TLA_WEB_search-xref ok
TLA_WEB_notes-persist ok
TLA_WEB_plans-persist ok
TLA_WEB_export-import ok
TLA_WEB_ai-reachability ok
==> SMOKE OK (target=dev)   DEV_EXIT=0

=== test:web:smoke (dist) ===
TLA_WEB_open-chapter ok
TLA_WEB_parallel ok
TLA_WEB_search-xref ok
TLA_WEB_notes-persist ok
TLA_WEB_plans-persist ok
TLA_WEB_export-import ok
TLA_WEB_ai-reachability ok
==> SMOKE OK (target=dist)

=== test:web:smoke-wasm-error (SMOKE_WASM_WRONG_MIME=1, dist) ===
    modo SMOKE_WASM_WRONG_MIME=1 → só o fluxo wasm-error-ui (fronteira corrompida de propósito)
  [wasm-error-ui] init da fronteira falhou → UI de erro VISÍVEL + retry (spinner ausente)
  [wasm-error-ui] retry re-tentou de verdade → leitura recuperou (John 3:16 renderizado)
TLA_WEB_wasm-error-ui ok
==> SMOKE OK (target=dist)
```

Sem processos órfãos após a corrida: portas do harness (8099/8100) livres, sem node
solto de `static-server.mjs`/`smoke.browser.mjs`. O `:8081` (expo pré-existente) é
irrelevante para esta tarefa.

## Verificação adversarial (5 checagens)

1. **O engolimento sumiu de verdade.** `wasm.web.ts:70-75`: o `.catch` agora seta
   `error` (não descarta); o comentário antigo "reaparece ao chamar listBooks()" foi
   removido. `WasmGate.tsx:41-58`: quando `error`, renderiza `<Text role=alert>` +
   `<Pressable role=button>` (testIDs `wasm-error`/`wasm-retry`), NUNCA o
   `ActivityIndicator` infinito (esse só no ramo `!ready`). Confirmado lendo ambos.
2. **Retry re-tenta de verdade.** `retry()` chama `resetWasmInit()` (`initPromise = null`)
   e bumpa `attempt`, cuja mudança re-dispara o `useEffect([attempt])` → `ensureWasmReady()`
   vê `initPromise === null` → novo `uniffiInitAsync()`. O smoke prova end-to-end (não é
   no-op): corrompe a fronteira → UI de erro → `GET /__smoke/fix-frontier-wasm` → clica
   retry → `waitBodyIncludes(John 3:16)` (texto VERBATIM + spinner ausente) PASSA. Ou seja,
   a 2ª tentativa re-instanciou com bytes válidos — recuperação real.
3. **Caminho feliz inalterado.** Os 7 fluxos da F6.2 verdes em dev E dist. O fluxo
   `wasm-error-ui` roda SÓ sob o flag: driver filtra `flows.filter(f => f.name === 'wasm-error-ui')`
   sob o flag e `!== 'wasm-error-ui'` sem o flag (`smoke.browser.mjs:163-169`) — não vaza
   para as corridas normais. Filtro correto.
4. **Switch legítimo + escopado.** `SMOKE_WASM_WRONG_MIME=1` corrompe SÓ o
   `generated/wasm-bindgen/index_bg` (regex `isFrontierWasm`, NÃO casa `vendor/` do wa-sqlite);
   o control route `/__smoke/fix-frontier-wasm` só existe sob o flag e apenas PARA de corromper.
   Só pode fazer o init FALHAR (ou, após o fix, servir bytes reais) — nunca forjar um verde.
4b. **generated/ intocado.** A fronteira é embrulhada em `wasm.web.ts` (`ensureWasmReady`
   → `uniffiInitAsync` de `generated/index.web`), sem editar `generated/*` (confirmado no
   `--name-only` do commit). Nativo `wasm.ts` segue `ready:true` incondicional.
5. **Não negociáveis.** the-light em `225b8c9`; `.claude/settings.json` NÃO commitado (só
   modificação de working tree); sem segredo (a `DUMMY_KEY` do fluxo de IA é pré-existente e
   explicitamente falsa). Commit `4316a22` toca apenas produto (WasmGate/wasm.ts/wasm.web.ts),
   i18n, guards de teste, harness e package.json.

## Decisões / ADRs

Nenhuma. Endurecimento de UX + cobertura de smoke; sem mudança de arquitetura/ADR.

## Bloqueios / perguntas

Nenhuma.

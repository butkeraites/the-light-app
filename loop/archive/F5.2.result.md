---
id: F5.2
status: passed
commit: 05f8102
reviewer: reviewer
finished_at: 2026-07-02
---

## Resumo

Tracer vertical do workstream de i18n/a11y/temas. O executor entregou: (a)
`app/lib/i18n.ts` — camada de i18n de CROMO de UI dependency-free (molde
`theme.ts`): `Locale = 'pt' | 'en'`, catálogos `pt`/`en` tipados como
`Record<MessageKey, string>` (paridade forçada em compile-time), `translate()`
puro com interpolação por `split/join`, `detectDeviceLocale()`/`normalizeLocale()`
offline, `I18nProvider`/`useI18n()` com `{ locale, t, setLocale, isSystem }`; (b)
`app/lib/prefs.ts` + `prefs.web.ts` — KV de preferências OFFLINE com
`PrefsBackend` injetável (nativo=arquivo `prefs.json` sob documentDirectory via
`expo-file-system/legacy` lazy; web=localStorage; namespace `tla.pref.<key>`); (c)
`LanguageToggleButton` no header ao lado do tema; (d) `app/app/index.tsx` migrada
ponta a ponta (100% strings via `t()`, a11y, zero hex → tokens de tema); (e) prova
headless `i18n.test.mjs` + ADR-0038.

Re-executei TODOS os gates de forma independente: `tsc --noEmit` (0), prova
headless (PASS), `expo export --platform web` (exit 0, 7 rotas), regressão
`test:web:reading`/`test:web:search`/`test:keystore` (todos exit 0). eslint é N/A
(repo sem config, confirmado). Inspeção de código confirma anti-alucinação
(versículo verbatim, `translation` ≠ `locale`), offline-first (sem rede),
`the-light` intocado em `2fc2dab`, `core/` inalterado e não no commit, e nenhum
arquivo/segredo espúrio (`.claude/settings.json` NÃO commitado). **Decisão: passed.**

## Critério de aceite

- [x] **`app/lib/i18n.ts` com catálogos pt/en tipados, Provider/hook e detecção
  offline (sem rede, sem dep nova).** Lido o arquivo: `Locale`, `MessageKey`
  (union de 16 chaves), `const pt/en: Record<MessageKey, string>`, `translate()`
  puro, `I18nProvider`/`useI18n()`. `detectDeviceLocale()` usa `navigator.language`
  (web) / `Intl.DateTimeFormat().resolvedOptions().locale` (nativo), fallback `pt`;
  `grep` de `fetch/http/XMLHttpRequest` em i18n/prefs = vazio (offline).
  `package.json` NÃO ganhou dependência de runtime (i18next/expo-localization/
  AsyncStorage ausentes; `dependencies` inalteradas — só o script `test:i18n` foi
  adicionado).
- [x] **Idioma persiste offline (nativo=arquivo; web=localStorage) via prefs com
  backend injetável; reabrir mantém.** `prefs.ts`/`prefs.web.ts` expõem
  `createPrefs(backend?)` com `PrefsBackend` injetável; `I18nProvider` re-hidrata o
  override salvo no boot (`useEffect` → `getPref`) e `setLocale` persiste
  (`setPref`/`removePref`). A prova headless assevera round-trip que **sobrevive a
  uma NOVA instância de `Prefs` sobre o mesmo storage** (simula reabrir) — PASS.
- [x] **`LanguageToggleButton` no header, alterna PT⇄EN re-renderizando via `t()`;
  `testID='language-toggle'`, `accessibilityRole='switch'`.** Confirmado no
  componente (`testID="language-toggle"`, `accessibilityRole="switch"`,
  `accessibilityState.checked`, label via `t('language.switchToOther')`, cor por
  token). Inserido em `_layout.tsx` (`HeaderControls`) dentro dos providers, logo
  `setLocale` atualiza o estado do `I18nProvider` e re-renderiza o cromo.
- [x] **`index.tsx`: 100% strings via `t()`, interativos com role+label, ZERO
  hex.** Todas as strings via `t()` (título, placeholder, hint, links, erros);
  `TextInput`/`Link`/título/resultado com `accessibilityRole`/`accessibilityLabel`
  e testIDs; estilos derivados de `useTheme()` via `makeStyles(colors)`.
  `grep -RnE '#[0-9a-fA-F]{6}' app/app/index.tsx` = **vazio** (também vazio em
  `LanguageToggleButton.tsx` e `_layout.tsx`).
- [x] **Prova headless verde: paridade catálogo pt↔en, `t()` correto, round-trip,
  fallback; script `test:i18n`.** `node web/__tests__/i18n.test.mjs` → PASS (16
  chaves, mesmos conjuntos; `home.readBible` diverge PT≠EN; interpolação
  `{message}`; round-trip sobrevive a reabrir; `pt-BR`→pt/`en-US`→en/
  desconhecido→pt). Script `"test:i18n"` presente no `package.json`.
- [x] **`translation` (versão bíblica) e `locale` (UI) distintos; nenhuma string
  CC-BY alterada.** `grep 'translation' app/lib/i18n.ts` → aparece SÓ em
  comentários (linhas 15/16/34); nunca como identificador. Verso renderizado
  verbatim (`index.tsx:60` `passage.verses.map((v) => v.text).join('\n')`), nunca
  via `t()`. O commit não toca nenhuma tela portadora de atribuição CC-BY (só
  `_layout.tsx`/`index.tsx`), logo nenhuma atribuição foi alterada.
- [x] **`core/src/lib.rs` e `core/Cargo.toml` INALTERADOS; the-light `2fc2dab`
  intacto; tsc 0, eslint 0, expo export exit 0; sem regressão.** `git status` do
  `the-light` limpo em `2fc2dab`; pin `2fc2dab` presente em `core/Cargo.toml`;
  `core/` ausente da lista de arquivos do commit `05f8102`. tsc=0, expo export=0,
  regressão verde (ver saída). eslint: repo SEM config (`ls app/.eslintrc*
  app/eslint.config.*` → nenhum) → N/A, não reprova.

## Saída da verificação

```
$ ls app/.eslintrc* app/eslint.config.* 2>/dev/null
NONE (eslint N/A)   # sem config → eslint corretamente N/A

$ git show --name-only 05f8102   # somente app-side + DECISIONS.md
DECISIONS.md
app/app/_layout.tsx
app/app/index.tsx
app/components/LanguageToggleButton.tsx
app/lib/i18n.ts
app/lib/prefs.ts
app/lib/prefs.web.ts
app/package.json
app/web/__tests__/i18n-headless-entry.ts
app/web/__tests__/i18n.test.mjs
# .claude/settings.json NÃO commitado (só M no working tree)

$ node web/__tests__/i18n.test.mjs
PASS — i18n de CROMO (PT/EN) + KV de prefs OFFLINE (backend fake, sem device/rede):
  paridade de catálogo pt↔en: 16 chaves, mesmos conjuntos (sem órfã)
  translate(): PT/EN corretos; home.readBible DIVERGE (troca observável); interpola {message}
  persistência: setPref→getPref; SOBREVIVE a nova instância (reabrir); removePref volta ao default
  detecção offline: 'pt-BR'→pt, 'en-US'→en, desconhecido/''→pt; detectDeviceLocale() válido
  anti-alucinação: todas as 16 chaves são CROMO (home/ref/a11y/language); nenhuma de versículo
  higiene: i18n.ts / prefs.ts / prefs.web.ts sem console.* (prefs nunca loga)
EXIT=0

$ npx tsc --noEmit
EXIT=0

$ npx expo export --platform web
› Static routes (7): / /_sitemap /+not-found /read /read/[book] /search /read/[book]/[chapter]
Exported: dist
EXPO_EXIT=0

$ npm run -s test:web:reading  -> EXIT=0  (paridade books=66, john3_v16, john_chapters=21)
$ npm run -s test:web:search   -> EXIT=0  (paridade João 3:16 no conjunto)
$ npm run -s test:keystore     -> EXIT=0  (sem cross-leak; chave nunca em console; fonte sem console.*)

$ grep -RnE '#[0-9a-fA-F]{6}' app/app/index.tsx           -> (vazio) OK
$ grep -nE 'fetch\(|https?://|XMLHttpRequest' app/lib/i18n.ts app/lib/prefs*.ts -> (vazio) OK
$ grep -n 'translation' app/lib/i18n.ts                   -> só comentários (15,16,34)

$ git -C /Users/butkeraites/Documents/the-light status --short  -> (vazio, limpo)
$ git -C /Users/butkeraites/Documents/the-light rev-parse HEAD  -> 2fc2dabbe1e8ea14d9aab07ba774221595b65b7d
$ grep -n 2fc2dab core/Cargo.toml                          -> pin presente (rev 2fc2dab), core/ fora do commit
```

## Decisões / ADRs

- **ADR-0038** (DECISIONS.md) — camada de i18n de CROMO de UI PT/EN
  dependency-free + KV de preferências OFFLINE + detecção de idioma do device;
  home migrada ponta a ponta. Numeração verificada: `0036` reservado ao gate de
  sync (F5.22) e `0037` à PR `ai-pure` de planos (F5.10); o último ADR realmente
  existente antes deste é o `0035`. **`ADR-0038` é livre e NÃO colide** com nenhum
  ADR existente (o cabeçalho aparece uma vez; a segunda ocorrência de "ADR-0038" é
  a auto-referência do arquivo-fonte). O ADR documenta corretamente a distinção
  `locale` (UI) × `translation` (versão bíblica), a garantia anti-alucinação e o
  offline-first.

## Escopo tracer

Confirmado tracer-shaped: apenas a home (`index.tsx`) foi migrada ponta a ponta;
os títulos estáticos das demais telas (`read/*`, `search`) permanecem PT com
comentário explícito de que a migração é das tarefas F5.x seguintes. Isso é
aceitável para um tracer, não uma falha.

## Bloqueios / perguntas

Nenhuma.

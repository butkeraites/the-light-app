---
id: F5.11
title: i18n dos painéis de IA (Ask, Chat, Compare, Study): strings + labels de acessibilidade
phase: 5
status: passed
gate: false
depends_on: [F5.2]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-03
---

## Objetivo

Migrar para t() as strings visíveis e os accessibilityLabel dos quatro painéis de IA, preservando a separação anti-alucinação (cited_text do store separado da interpretation) e mantendo verbatim as atribuições/licenças (STEP CC-BY) mesmo com a UI traduzida.

> **Herda F5.2/F5.5 (aceitas):** camada i18n + o `lang={locale}` já ligado aos 4 painéis
> (F5.5) que faz a RESPOSTA da IA seguir o idioma. Esta tarefa migra o CROMO dos painéis de IA
> (Ask/Chat/Compare/Study): rótulos, botões, estados, `sourceLabel` (o `bookNamePt`/`En` de exibição
> deferido em F5.5), labels de acessibilidade. NÃO traduzir `cited_text`/versículo (store) nem a
> `interpretation` (modelo); `reference` canônica permanece EN.

## Contexto / arquivos

_Workstream:_ Acessibilidade + i18n + temas · _Depende de:_ F5.2 (aceita)

- `app/lib/i18n.ts`
- `app/components/ReaderAskPanel.tsx`
- `app/components/ReaderChatPanel.tsx`
- `app/components/ReaderComparePanel.tsx`
- `app/components/ReaderStudyPanel.tsx`

## Passos sugeridos

1. Migrar títulos, rótulos de botão, placeholders, estados (carregando/erro/vazio), rótulos de 'Texto citado' vs 'Interpretação' e os accessibilityLabel (ex.: 'Perguntar à IA sobre esta passagem') dos 4 painéis para t().
2. Manter VERBATIM os textos de atribuição/licença (STEP CC-BY etc.) — traduzir apenas o rótulo ao redor, nunca o identificador de licença; manter a atribuição VISÍVEL.
3. Não alterar o pipeline de IA: cited_text vem do store/ai-pure, interpretation do modelo; i18n só no cromo. Confirmar que o prop `lang` (locale, de i18n-2) continua sendo respeitado.
4. Gates: tsc/eslint/expo export web verdes.

## Critério de aceite

- [ ] Strings e accessibilityLabel dos 4 painéis via t(); alternam PT⇄EN.
- [ ] Blocos de citação/interpretação continuam separados (anti-alucinação visível); cited_text do store inalterado.
- [ ] Atribuições (STEP CC-BY) permanecem visíveis e com o identificador de licença VERBATIM.
- [ ] Nenhuma string PT hardcoded remanescente nesses arquivos; tsc 0, eslint 0, expo export web exit 0; core intacto.

## Comando(s) de verificação

```sh
set -e; cd /Users/butkeraites/Documents/the-light-app/app
npx tsc --noEmit && npx eslint . && npx expo export --platform web
```

## Restrições

Painéis com mais a11y já existente (ReaderVersePanel tem muitos labels — fica em i18n-5). Não espelhar prompt/citação/aparato em TS (ADR-0029). BYOK: nenhuma chave nova; i18n não toca segredos.

- **Offline-first:** i18n local (reusa F5.2); nada exige rede.
- **Anti-alucinação (crítico p/ painéis de IA):** i18n só traduz CHROME (rótulos/botões/estados). O `cited_text`/versículo citado vem do store verbatim — NUNCA via `t()`. A `interpretation` é a saída do modelo (não traduzir pós-hoc). `sourceLabel`/`reference` canônica EN preservados. `lang={locale}` (F5.5) controla o idioma da RESPOSTA, não o texto citado.
- **the-light só via PR+ADR:** `the-light-core` pinado (`2fc2dab`); app-side.
- **Qualidade:** `tsc --noEmit`; test:i18n + self-tests de IA (ai/ai-stream/ai-multiprovider/study/compare/session) sem regressão; paridade de catálogo pt/en em compile-time; prova de troca de idioma no cromo do painel.

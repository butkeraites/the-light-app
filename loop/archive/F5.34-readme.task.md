---
id: F5.34
title: README real (propósito · offline-first/BYOK · anti-alucinação · licenças · como rodar) — subsume NOTICE
status: passed
gate: false
depends_on: []
phase: 5
trilha: Refinamento (varredura F5.28+)
---

## Objetivo

`README.md` tem 15 bytes (só o título). Para o marco de abertura/shippable o repo não tem
ponto de entrada para usuário nem contribuidor. Escrever um README documentando o que é o app,
seus princípios não-negociáveis, o licenciamento/atribuições do conteúdo embarcado (servindo
como o manifesto de atribuição autoritativo, dobrando a necessidade de um NOTICE separado) e
como buildar os três alvos. As strings de atribuição VERBATIM DEVEM casar com as constantes
in-app. Só docs, sem tocar o `the-light`, sem segredo.

## Aceite

- [ ] `README.md` documenta: (1) o que é o app (estudo bíblico bilíngue PT/EN, offline-first);
      (2) regras offline-first + BYOK; (3) anti-alucinação (texto do versículo sempre do store
      local; o LLM só interpreta); (4) licenciamento do conteúdo embarcado como tabela de
      atribuição — KJV & Almeida 1911 domínio público, cross-references OpenBible.info CC-BY,
      léxico STEP/Tyndale CC BY 4.0; (5) passos de build/run para web/iOS/Android.
- [ ] As strings CC-BY VERBATIM aparecem EXATAMENTE como as constantes in-app:
      `Cross references courtesy of OpenBible.info (CC-BY)` (XREF_ATTRIBUTION) e
      `Credit it to 'STEP Bible' linked to www.STEPBible.org (data based on work at Tyndale House, Cambridge; CC BY 4.0)` (STEP_ATTRIBUTION).
- [ ] Um grep confirma ambas as strings exatas presentes em `README.md` e que ainda casam com
      `app/components/ReaderXrefPanel.tsx:30` e `app/components/ReaderStudyPanel.tsx:75-76`.
- [ ] Nenhuma mudança de código exigida; the-light intocado (`225b8c9`); sem segredo; guardas existentes seguem verdes.

## Verificação

```sh
cd /Users/butkeraites/Documents/the-light-app
grep -qF 'Cross references courtesy of OpenBible.info (CC-BY)' README.md && echo XREF_OK
grep -qF "Credit it to 'STEP Bible' linked to www.STEPBible.org" README.md && echo STEP_OK
# confirmar que as constantes in-app ainda contêm as mesmas strings (fonte da verdade):
grep -qF 'Cross references courtesy of OpenBible.info (CC-BY)' app/components/ReaderXrefPanel.tsx && echo XREF_SRC_OK
grep -qF "Credit it to 'STEP Bible' linked to www.STEPBible.org" app/components/ReaderStudyPanel.tsx && echo STEP_SRC_OK
cd app && npx tsc --noEmit
```

## Origem

`README.md` (15 bytes); `app/components/ReaderXrefPanel.tsx:30` (XREF_ATTRIBUTION);
`app/components/ReaderStudyPanel.tsx:75-76` (STEP_ATTRIBUTION). Varredura de refinamento
(dedupe README + THIRD_PARTY_NOTICES). Notas: **primeiro grep as constantes reais no código**
e copie-as verbatim (não confie na memória); a tabela de atribuição do README é a fonte única
(o NOTICE separado foi dobrado aqui). Consultar `VISION_AND_ARCHITECTURE.md`/`IMPLEMENTATION_PLAN.md`
para descrever propósito/princípios com fidelidade; não inventar recursos que não existem.
Documentar os comandos de build reais (ver `scripts/` e `app/package.json`).

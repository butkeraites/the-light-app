---
id: F0.8
status: passed
commit: 36af016
reviewer: reviewer
finished_at: 2026-06-30
---

## Resumo

F0.8 liga o core no **Android NATIVO**, espelhando a F0.7 (iOS, ADR-0008). O
Executor entregou no commit `36af016`: `ubrn build android --and-generate`
(cargo-ndk → staticlib Rust em `jniLibs/arm64-v8a/libthe_light_app_core.a` + glue
do Turbo Module Gradle/CMake/Kotlin/JNI), integrado ao app Expo via
`expo prebuild` + autolink (mesmo `app/react-native.config.js` da F0.7, raiz como
library) + New Arch codegen; dois patches de template em arquivos GERADOS
(`AndroidManifestNew.xml` p/ AGP 8; resolução do runtime C++ no `CMakeLists.txt`
Node≥20-safe), aplicados de dentro de `gen-bindings-android.sh`. A prova é
**headless e real**: re-executei `scripts/run-android-selftest.sh`, que bootou o
AVD `thelight_avd` headless, fez `gradlew :app:installDebug` (build completo,
não SKIP_BUILD), subiu o Metro, lançou o app e capturou do `adb logcat` **ambos**
os marcadores PT e EN com `book=43 chapter=3 verse=16` — `parse_reference` pelo
**Rust nativo** via Turbo Module (PT==EN). Saída 0.

**ANTI-FAKE confirmado (leitura do código):**
- `app/web/reference.ts` **delega** ao barrel gerado do Turbo Module
  (`./native-generated/src/index` → `parseReferenceNative` → JSI → Rust). NÃO faz
  parsing TS nem eco; só embrulha o retorno numa Promise.
- `app/web/selftest.ts` compõe o marcador a partir do **retorno real** de
  `parseReference` (`ref.book` / `ref.chapter` / `verseField(ref)`). 43/3/16 NÃO
  estão hardcoded no selftest nem no glue — só aparecem como a asserção esperada
  no `run-android-selftest.sh` (`MARK_PT`/`MARK_EN`), que é a expectativa, não a
  fonte.
- `git show 36af016 --stat` NÃO lista `reference.web.ts`, `core/src` nem
  `../the-light` — fronteira e core intactos; caminho web preservado.

Decisão: **passed**.

## Critério de aceite

- [x] **Módulo nativo Android construído pelo `ubrn`** — `gen-bindings-android.sh`
  (encapsula `ubrn build android --and-generate --targets aarch64-linux-android`)
  produziu o staticlib `android/src/main/jniLibs/arm64-v8a/libthe_light_app_core.a`
  (261 MB, verificado presente) + a glue do Turbo Module (build.gradle, CMakeLists,
  cpp-adapter, AndroidManifest, Kotlin Module/Package). O build subsequente embarcou
  o `.so` no APK (`.../lib/arm64-v8a/libthe-light-app-core.so`). Gerados e ignorados.
- [x] **App roda no emulador Android (run real)** — `run-android-selftest.sh`:
  prebuild (`app/android/gradlew` presente) + `gradlew :app:installDebug`
  (`-PreactNativeArchitectures=arm64-v8a`) + launch no AVD `thelight_avd` headless,
  todos sem erro (script saiu 0).
- [x] **`parse_reference` pelo Rust nativo, provado headless** — `adb logcat`
  capturou AMBOS `parseReference("Jo 3.16")` (PT) e `parseReference("John 3:16")`
  (EN) resolvendo p/ `book=43, chapter=3, verse=16` via Turbo Module (JNI/JSI) →
  Rust. Exit 0 só porque PT **e** EN bateram.
- [x] **Tela ligada ao binding nativo** — `app/app/index.tsx` → `app/web/reference.ts`
  → `./native-generated/src/index` (Turbo Module), reutilizado da F0.7 sem reescrever
  lógica. `reference.web.ts` (wasm) intacto (Metro resolve por extensão).
- [x] **Sem regressão web (F0.6b) nem iOS (F0.7)** — `git show 36af016 --stat` não
  toca blocos `web:`/`ios:`/`turboModule:`, scripts iOS, nem `reference.web.ts`.
  `cd app && npx tsc --noEmit` → exit 0.
- [x] **`the-light` e fronteira INTACTOS** — `git show 36af016 --stat` não lista
  `core/src` nem `../the-light`; `parse_reference` segue delegando ao core.
- [x] **Working tree limpo** — `git status --porcelain` vazio após o run;
  `android/` e `app/android/` aparecem como ignorados (`!!`), não untracked.
- [x] **ADR-0009 registrado** em `DECISIONS.md` (linhas 702–843): caminho Android
  do `ubrn` (`build android`, config `android:`, cargo-ndk → jniLibs, integração
  Expo prebuild/autolink/New Arch codegen, dois patches de template, prova headless
  via `adb logcat`) com contexto → decisão → consequências.

## Saída da verificação

**0) Ambiente (via `zsh -ic`):**
```
ANDROID_NDK_HOME=/opt/homebrew/share/android-commandlinetools/ndk/27.1.12297006  (dir OK)
adb 1.0.41 | cargo-ndk 4.1.2 | openjdk 17.0.19
rustup target: aarch64-linux-android (instalado)
emulator -list-avds: thelight_avd PRESENT
```

**1) ANTI-FAKE / fronteira (`git show 36af016 --stat`):**
```
 .gitignore                                 |  19 +++-
 DECISIONS.md                               | 144 ++++++++++++++++++++++++
 app/app.json                               |   3 +
 loop/queue/F0.8-ligar-core-android.task.md |   2 +-
 scripts/gen-bindings-android.sh            | 160 +++++++++++++++++++++++++++
 scripts/run-android-selftest.sh            | 172 +++++++++++++++++++++++++++++
 ubrn.config.yaml                           |  45 ++++++++
 7 files changed, 543 insertions(+), 2 deletions(-)
```
→ NÃO lista `reference.web.ts`, `core/src`, `../the-light`, `reference.ts`,
`selftest.ts` (compartilhados da F0.7, intocados). Confirmado.

**2) PROVA HEADLESS (`run-android-selftest.sh`, exit 0 — build completo, emulador real):**
```
==> [1/5] Bootando AVD thelight_avd (headless)
    serial: emulator-5554 — aguardando boot_completed (até 300s)
==> [2/5] gradlew :app:installDebug (-PreactNativeArchitectures=arm64-v8a)
==> [3/5] Metro (EXPO_PUBLIC_TLA_SELFTEST=1) em background
==> [4/5] logcat -c + launch + captura (tag ReactNativeJS)
==> [5/5] Aguardando marcadores PT+EN (até 240s)
----- marcadores capturados (emulador) -----
TLA_SELFTEST DONE
TLA_SELFTEST EN book=43 chapter=3 verse=16
TLA_SELFTEST PT book=43 chapter=3 verse=16
TLA_SELFTEST START
--------------------------------------------
TLA_SELFTEST PT book=43 chapter=3 verse=16
TLA_SELFTEST EN book=43 chapter=3 verse=16
==> OK — parse_reference provado pelo Rust nativo via Turbo Module Android (PT==EN).
OK: killing emulator, bye bye
OK
```
Asserção do bloco de verificação:
```
grep "TLA_SELFTEST PT book=43 chapter=3 verse=16" → PT MARKER OK
grep "TLA_SELFTEST EN book=43 chapter=3 verse=16" → EN MARKER OK
```

**3) Sem regressão (`cd app && npx tsc --noEmit`):**
```
tsc exit: 0
```

**4) Working tree limpo (`git status --porcelain` após o run):**
```
(vazio)
git status --ignored: !! android/   !! app/android/   (gerados ignorados)
```

## Decisões / ADRs

- **ADR-0009** (`DECISIONS.md`) — Caminho Android nativo do `ubrn`: subcomando
  `ubrn build android --and-generate`, bloco `android:` no `ubrn.config.yaml`
  (`directory: android`, `jniLibs: src/main/jniLibs`, `targets: [arm64-v8a]`,
  `platform: 24`, `packageName: com.thelight.core`, `useSharedLibrary: false`),
  cargo-ndk → staticlib em `jniLibs/<abi>`, integração Expo via prebuild + autolink
  (mesmo `app/react-native.config.js` da F0.7) + New Arch codegen, dois patches de
  template em arquivos gerados (`AndroidManifestNew.xml` p/ AGP 8; resolução do
  runtime C++ no `CMakeLists.txt` Node≥20-safe), prova headless via `adb logcat`
  (tag `ReactNativeJS`). Apenas EXTENSÃO — blocos `rust:`/`bindings:`/`web:`/`ios:`/
  `turboModule:` intactos. Coerente com o entregue (verificado no código e no run).
- Só o ABI `arm64-v8a` é construído (casa o AVD arm64) — risco registrado no ADR.
- Offline-first preservado: rede só em dev/build (cargo-ndk/Gradle/npm/Metro); o
  runtime do self-test é offline. Nenhum segredo em git/log.

## Bloqueios / perguntas

Nenhuma. Aceite cumprido, prova headless real verde (PT==EN via Rust nativo), sem
regressão web/iOS, working tree limpo, `the-light`/fronteira intactos.

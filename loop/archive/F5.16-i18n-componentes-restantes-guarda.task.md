---
id: F5.16
title: i18n dos componentes restantes (VersePanel, Xref, Parallel, VersionPicker, toggles) + GUARDA de cobertura i18n/tema
phase: 5
status: passed
gate: false
depends_on: [F5.5, F5.8, F5.11]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-03
---

## Objetivo

Fechar a cobertura de i18n nos componentes de leitura restantes e adicionar uma GUARDA headless determinística que impede regressão: catálogos pt↔en em paridade total, nenhuma string PT hardcoded em JSX/placeholder/accessibilityLabel nos arquivos migrados, e nenhum hex hardcoded fora de theme.ts/vendor.

> **Fecha a cobertura i18n** (após F5.5 leitura, F5.8 busca/nav, F5.11 painéis IA): migra os
> componentes RESTANTES (VersePanel, Xref, Parallel, VersionPicker, toggles, etc.) e adiciona uma
> **GUARDA de cobertura** headless — um teste que FALHA se aparecer string de cromo PT hardcoded ou
> hex fora de token de tema nos componentes cobertos, prevenindo regressão. REUSA F5.2. NÃO traduzir
> versículo/refs/nomes de livro (STORE; `locale` só seleciona campo).

## Contexto / arquivos

_Workstream:_ Acessibilidade + i18n + temas · _Depende de:_ F5.5, F5.8, F5.11 (aceitas)

- `app/lib/i18n.ts`
- `app/components/ReaderVersePanel.tsx`
- `app/components/ReaderXrefPanel.tsx`
- `app/components/ReaderParallelView.tsx`
- `app/components/ReaderVersionPicker.tsx`
- `app/components/ThemeToggleButton.tsx`
- `app/components/LanguageToggleButton.tsx`
- `app/web/__tests__/i18n.test.mjs`

## Passos sugeridos

1. Migrar strings + accessibilityLabel de ReaderVersePanel.tsx (muitos labels: 'Editor de nota do versículo','Marcar com …','Perguntar à IA…', etc.), ReaderXrefPanel.tsx (inclui a atribuição OpenBible CC-BY — manter VERBATIM), ReaderParallelView.tsx, ReaderVersionPicker.tsx, ThemeToggleButton.tsx ('Mudar para tema claro/escuro'), LanguageToggleButton.tsx → t().
2. Criar a guarda app/web/__tests__/i18n-coverage.test.mjs (scanner determinístico de fonte, sem runtime React): (a) paridade total de catálogo (todo MessageKey em pt E en); (b) varrer app/components/*.tsx e app/app/**/*.tsx e falhar se houver literal com letra acentuada/palavra PT em texto de JSX, `placeholder=`"..." ou `accessibilityLabel=`"..." que NÃO seja `t(...)` (allowlist explícita p/ testID, nomes técnicos, identificadores de licença/atribuição verbatim); (c) falhar se houver `#[0-9a-fA-F]{6}` fora de app/lib/theme.ts e de app/web/vendor.
3. Adicionar script `test:i18n-coverage` em app/package.json.
4. Gates: tsc/eslint/expo export web verdes; a guarda passa.

## Critério de aceite

- [ ] Componentes restantes migrados para t() com a11y labels traduzidos; atribuições CC-BY visíveis e verbatim.
- [ ] Guarda headless (test:i18n-coverage) verde: paridade pt↔en total; nenhuma string PT hardcoded nos arquivos migrados (fora da allowlist); nenhum hex fora de theme.ts/vendor.
- [ ] tsc 0, eslint 0, expo export web exit 0; core intacto; the-light 2fc2dab.
- [ ] **GUARDA de cobertura** headless que reprova cromo PT hardcoded / hex fora de token nos componentes cobertos (previne regressão futura).

## Comando(s) de verificação

```sh
set -e; cd /Users/butkeraites/Documents/the-light-app/app
node web/__tests__/i18n-coverage.test.mjs
npx tsc --noEmit && npx eslint . && npx expo export --platform web
```

## Restrições

A guarda só é significativa depois que todas as telas foram migradas (por isso depende de i18n-2/3/4). É um lint-like determinístico (sem chave/rede). A allowlist deve isentar identificadores de licença/atribuição (CC-BY, 'STEP Bible', 'OpenBible') que ficam verbatim.

- **Offline-first:** i18n/tema local (reusa F5.2/F5.14).
- **Anti-alucinação:** i18n só CHROME — versículo/refs/nomes de livro do STORE (locale só seleciona campo); CC-BY verbatim; NUNCA traduzir conteúdo bíblico via `t()`. A GUARDA de cobertura deve reforçar isto (namespaces são só cromo).
- **the-light só via PR+ADR:** `the-light-core` pinado (`2fc2dab`); app-side.
- **Qualidade:** `tsc --noEmit`; a GUARDA headless deve FALHAR se houver string de cromo hardcoded/hex fora de token de tema nos componentes cobertos; test:i18n + self-tests sem regressão; paridade pt/en compile-time.

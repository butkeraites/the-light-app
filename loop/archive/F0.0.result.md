---
id: F0.0
status: passed
commit: efe334f
reviewer: reviewer
finished_at: 2026-06-29
---

## Resumo

Tarefa de descoberta de ambiente (sem código de produto). O Reviewer rodou de
forma independente todos os comandos de verificação e confirmou que o relatório
do Executor é fiel ao ambiente real — cada versão e cada status batem exatamente
com a saída observada, sem nada inventado. Os quatro itens do critério de aceite
estão cumpridos: tabela ferramenta→versão→status completa, `wasm32-unknown-unknown`
marcado como pendência, gerador de bindings UniFFI identificado por nome e status,
e as lacunas (Xcode, Android SDK/NDK, targets, ubrn) listadas como bloqueios
explícitos de tarefas futuras. Nenhuma dessas lacunas bloqueia a própria F0.0,
conforme o aceite permite explicitamente "marcado como pendência" e "lacunas
como bloqueios". Decisão: **passed**.

## Critério de aceite

- [x] **Tabela com cada ferramenta e versão detectada.** Presente nas notas do
  Executor e verificada item a item. Todos os valores conferem com a saída real
  (rustc/cargo/rustup 1.96.0/1.29.0, node v25.8.1, npm 11.11.0, pnpm 11.3.0,
  Expo SDK 56 via resolução npm, targets, ubrn/uniffi-bindgen/cargo-ndk, Xcode,
  Android).
- [x] **`wasm32-unknown-unknown` instalado OU marcado como pendência.** Confirmado
  AUSENTE pela verificação independente (`rustup target list --installed` mostra
  apenas `aarch64-apple-darwin`) e corretamente marcado como **pendência a
  instalar** via `rustup target add`.
- [x] **Nome e versão (ou status) do gerador de bindings UniFFI confirmados.** Nome
  confirmado: `uniffi-bindgen-react-native` (CLI `ubrn`), em transição para
  `uniffi-bindgen-javascript`. Status verificado: **não instalado** (`which ubrn`
  e `which uniffi-bindgen` falham). Versão exata a fixar na tarefa que instalar a
  ferramenta (provável F0.2). O aceite admite "ou status" — satisfeito.
- [x] **Lacunas (Xcode/NDK/targets ausentes) listadas como bloqueios explícitos.**
  Seção "Bloqueios futuros explícitos" das notas do Executor enumera: targets Rust,
  ubrn/uniffi-bindgen, cargo-ndk + Android SDK/NDK (bloqueio real de build Android,
  ex.: F0.8) e Xcode completo (bloqueio real de build iOS, ex.: F0.7). Cada um
  atribuído à tarefa futura que bloqueia, sem bloquear a F0.0.

## Saída da verificação

Comandos rodados pelo Reviewer de forma independente (2026-06-29, macOS Darwin 25.5.0):

### Rust
```
$ rustc --version
rustc 1.96.0 (ac68faa20 2026-05-25)

$ cargo --version
cargo 1.96.0 (30a34c682 2026-05-25)

$ rustup --version
rustup 1.29.0 (28d1352db 2026-03-05)

$ rustup target list --installed
aarch64-apple-darwin

$ rustup target list --installed | grep -E "wasm32|ios|android"
(no wasm32/ios/android targets installed)
```

### Node / JS
```
$ node --version
v25.8.1

$ npm --version
11.11.0

$ pnpm --version
11.3.0

$ which yarn
yarn not found

$ which bun
bun not found
```

### Expo
```
$ which expo
expo not found globally

$ npx --no-install expo --version
npm error npx canceled due to missing packages and no YES option: ["expo@56.0.12"]
```
Interpretação confirmada: Expo não instalado localmente (esperado — não há projeto;
o app nasce na F0.1). O resolvedor do npm aponta `expo@56.0.12`, indicando linha
atual = Expo SDK 56. Nenhuma instalação foi disparada.

### UniFFI / bindgen / cargo-ndk
```
$ which ubrn
ubrn not found

$ which uniffi-bindgen
uniffi-bindgen not found

$ which cargo-ndk
cargo-ndk not found
```

### Mobile (Xcode / Android)
```
$ xcode-select -p
/Library/Developer/CommandLineTools

$ xcodebuild -version
xcode-select: error: tool 'xcodebuild' requires Xcode, but active developer
directory '/Library/Developer/CommandLineTools' is a command line tools instance

$ echo "ANDROID_HOME='${ANDROID_HOME}'"
ANDROID_HOME=''

$ echo "ANDROID_SDK_ROOT='${ANDROID_SDK_ROOT}'"
ANDROID_SDK_ROOT=''

$ which adb
adb not found
```

### Commit
```
$ git log --oneline -1
efe334f chore(F0.0): confirmar toolchain e registrar versões do ambiente
```

**Conferência de fidelidade:** todos os 21 pontos de dado do relatório do Executor
foram reproduzidos de forma idêntica pelo Reviewer. Nenhuma divergência. Nenhum
valor inventado. O commit efe334f existe e corresponde à F0.0.

## Decisões / ADRs

- Tabela de versões do ambiente fica registrada neste resultado (e nas notas do
  Executor) e deverá ser migrada para `DECISIONS.md` na F0.1, conforme a restrição
  da própria tarefa. Não há `DECISIONS.md` ainda — correto para esta fase.
- Stack confirmada para fixar: rustc/cargo 1.96.0, Node v25.8.1 (npm 11.11.0 /
  pnpm 11.3.0), Expo SDK 56. Gerador de bindings: `uniffi-bindgen-react-native`
  (CLI `ubrn`), versão exata a pinar no momento da instalação.
- Nenhuma regra não negociável foi violada: nada foi instalado, o repositório
  `the-light` não foi tocado, nenhum segredo foi lido, e a resolução de versão do
  Expo via npx é descoberta (não embarca nada no produto). Offline-first/BYOK
  preservados.

## Bloqueios / perguntas

Nenhum bloqueio para a F0.0 — o aceite admite explicitamente "marcado como
pendência" e "lacunas listadas como bloqueios", e ambos foram cumpridos. As
lacunas abaixo são documentadas aqui e **bloquearão tarefas futuras específicas,
não a F0.0**:

1. **Targets Rust ausentes** (`wasm32-unknown-unknown`, `aarch64-apple-ios`,
   `aarch64-apple-ios-sim`, `aarch64-linux-android`, `armv7-linux-androideabi`):
   pendência leve, resolvível via `rustup target add` na tarefa que compilar para
   cada alvo. Não exige ação humana.
2. **`ubrn` / `uniffi-bindgen` ausentes do PATH**: pendência a instalar (npm/cargo)
   na tarefa da fronteira UniFFI (provável F0.2); nome de pacote e versão a fixar
   nesse momento. Não exige ação humana além da instalação roteirizável.
3. **Build Android (ex.: F0.8) — BLOQUEIO REAL futuro**: `cargo-ndk` ausente +
   Android SDK/NDK não configurados (`ANDROID_HOME` e `ANDROID_SDK_ROOT` vazios,
   `adb` ausente, sem diretório NDK). Exigirá instalar Android SDK + NDK e definir
   `ANDROID_HOME` — ação humana/setup de máquina.
4. **Build iOS (ex.: F0.7) — BLOQUEIO REAL futuro**: apenas Command Line Tools
   presentes (`/Library/Developer/CommandLineTools`); Xcode completo ausente
   (`xcodebuild` indisponível). Exigirá instalar o Xcode (App Store) e apontar
   `xcode-select`/`xcodebuild` — ação humana.

Quando o loop chegar às tarefas F0.7 (iOS) e F0.8 (Android), os itens 3 e 4
deverão disparar HALT por ferramenta/ambiente ausente, a menos que o setup humano
já tenha sido feito até lá.

---
id: F6.9
title: Léxico — expandir LEXICON_BOOKS ao Novo Testamento inteiro (interlinear além dos 3 de amostra)
status: passed
gate: false
depends_on: []
phase: 6
trilha: Dados/perf
---

## Objetivo (RE-ESCOPADO)

O interlinear de estudo profundo é uma AMOSTRA de 3 livros (`LEXICON_BOOKS=[1,19,43]`
Gênesis/Salmos/João). Expandir para o **Novo Testamento inteiro** (livros 40..66) — Strong's,
lemma, translit, gloss, tokens originais por versículo — para que o estudo interlinear funcione em
TODO o NT (Mateus, Romanos, etc.), não só João. É asset ON-DEMAND (não entra no first-paint).

**NOTA (re-escopo):** o deliverable original de popular `morph_legend` foi REMOVIDO daqui — o
executor da 1ª tentativa provou que `morph_legend` está VAZIA na PRÓPRIA fonte (`bible.sqlite`
= 0 linhas; o importador `import-scholarly` do `the-light` só CRIA a tabela, nunca a popula; o
dataset de legenda de morfologia da STEP nunca foi importado). Popular exigiria FABRICAR (viola
anti-alucinação) OU um PR ao `the-light` + ADR + re-pin (decisão humana) → virou **F6.9b (gate,
deferido)**. Esta tarefa faz SÓ a parte 100% viável e verbatim: expandir os livros.

## Escopo

1. **`core/examples/gen_reading_sample_db.rs:48`** — trocar `LEXICON_BOOKS = &[1, 19, 43]` por o NT
   inteiro `40..=66` (Mateus..Apocalipse). (Manter Gênesis/Salmos? Opcional — se quiser preservar o
   AT de amostra, usar `[1, 19, 40..=66]`; senão só o NT. Decidir e documentar; preferir incluir os
   3 originais + o NT p/ não regredir o estudo do AT amostrado.)
2. **Regenerar os assets** via `./scripts/gen-reading-sample-db.sh` (cargo example, feature `embedded`,
   alguns min + split sqlite3). `lexicon-sample.sqlite` cresce (todos os `original_tokens`/`lexicon` do
   NT — ~142k tokens, ~5.580 Strong's do NT). `reading-lite.sqlite` (leitura, SEM léxico) NÃO muda.
   `morph_legend` segue vazia (é F6.9b) — mas continua NÃO-dropada não muda nada (0 linhas); manter o
   comportamento atual de drop ou não é indiferente (não gastar esforço nisso aqui).
3. **Re-baseline do budget** (`scripts/measure-web-bundle.sh` + `loop/perf/web-bundle-budget.json`):
   só `lexiconDbBytes` (asset ON-DEMAND) muda; `moduleCount`/entry NÃO. Nota `noteF69` justificando +
   o tamanho medido. Se ficar grande demais (>~40MB) e for preocupante, escopar a um subconjunto do NT
   e DOCUMENTAR — mas o NT inteiro verbatim é o alvo.
4. **Verificar** que `deepStudy`/`lexicalEntries` retornam léxico para um livro NOVO do NT (ex.:
   Romanos) além de João; atualizar asserções de sanidade do gerador se necessário (verbatim do store).

## Aceite

- [ ] `LEXICON_BOOKS` cobre o NT inteiro (40..66); `SELECT COUNT(DISTINCT book_number) FROM original_tokens`
      reflete o NT (+ os de amostra se mantidos); Romanos (e outros do NT) retornam léxico via `deepStudy`/`lexicalEntries`.
- [ ] Assets regenerados; `reading-lite.sqlite` inalterado (66 livros, leitura); `lexicon-sample.sqlite` maior (tamanho reportado).
- [ ] `test:web:perf-budget` LOCKED OK após re-baseline SÓ do `lexiconDbBytes` (moduleCount inalterado); nota justificando.
- [ ] Léxico VERBATIM do `bible.sqlite` (anti-alucinação); `tsc` 0; `test:web:study`/`test:web:lexicon`/`test:web:reading` verdes; the-light `225b8c9`; sem `.sqlite` commitado; sem segredo.
- [ ] `morph_legend` (resolução de códigos de morfologia) permanece **F6.9b deferido** (gate: PR ao the-light + ADR).

## Verificação

```sh
cd /Users/butkeraites/Documents/the-light-app && ./scripts/gen-reading-sample-db.sh
sqlite3 assets/data/lexicon-sample.sqlite "SELECT COUNT(DISTINCT book_number) FROM original_tokens; SELECT COUNT(*) FROM lexicon;"
sqlite3 assets/data/reading-lite.sqlite "SELECT COUNT(DISTINCT number) FROM books;"   # segue 66
cd app && npx tsc --noEmit && npm run test:web:study && npm run test:web:lexicon && npm run test:web:reading && npm run test:web:perf-budget
```

## Notas

- `bible.sqlite` (fonte) tem o NT completo: `original_tokens` = 142.096 linhas (27 livros), 5.580 Strong's do NT (confirmado). O gerador é app-side (`core/examples`), NÃO toca o `the-light`.
- Léxico completo AT+NT (~90MB) segue DEFERIDO (produto). Aqui é NT (verbatim, viável).
- Reusar o molde `INSERT..SELECT FROM src` já usado p/ `original_tokens`/`lexicon` (verbatim).

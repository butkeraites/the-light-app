---
id: F2.8
status: approved
gate: true
reviewer: humano (sign-off)
finished_at: 2026-07-01
---

# F2.8 — Marco 2: APROVADO (sign-off humano)

## Resumo
Sign-off humano do **Marco 2 — IA BYOK ancorada, multiplataforma**. Todas as tarefas da
Fase 2 (F2.1–F2.7b) foram aceitas e verificadas de forma independente.

## Decisões do sign-off
1. **Fase 2 APROVADA.**
2. **Próxima direção: Fase 3 — estudo profundo** (léxico Strong + pesquisa assistida). O
   core já tem `ai::study`/`ai::lexicon`/`ai::research` (embedded) — a decompor em
   `backlog/PHASE-3.md` no molde das fases anteriores.

## Capacidades aceitas
- `ask` ancorado + streaming (fronteira, callback UniFFI): F2.1/F2.3a.
- GeminiProvider + `complete_stream` no core (PR sancionado → `133077a`): F2.3.
- Chave BYOK nativa (Keychain via `expo-secure-store`): F2.4.
- UI nativa de `ask` (streaming + citado/interpretação): F2.5.
- **Validação REAL** (Gemini `gemini-2.5-flash`, `cited_text` = João 3:16 do store +
  interpretação real, chave nunca no loop): F2.6.
- IA no web (`ai-pure` wasm p/ prompt/citação + `fetch`; chave session-only; PR sancionado
  → `c8ecb2f`; **zero drift** nativo↔web): F2.7/F2.7b.

## Regras não negociáveis — mantidas
Anti-alucinação (texto do store; LLM só interpreta; `citation` mesma impl Rust nativo↔web);
BYOK (chave nunca em git/log; **login-de-conta OAuth rejeitado** por pesquisa — ADR-0023);
offline-first (IA opt-in); `the-light` só via PR+ADR (2 PRs mergeados por sign-off + re-pin;
wasm puro). ADRs 0023–0025.

## Consequência
`loop/HALT` removido; loop retomado. O `planner` decompõe a **Fase 3** e semeia a 1ª tarefa.

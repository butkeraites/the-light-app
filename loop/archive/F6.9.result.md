---
id: F6.9
status: passed
commit: e99434f
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

Verificação independente **PASSED**. O léxico interlinear de estudo profundo deixou
de ser uma amostra de 3 livros ({Gn,Sl,Jo}) e passou a cobrir o **Novo Testamento
inteiro** (livros 40..=66) + Gn/Sl do AT amostrado — `LEXICON_BOOKS = {1,19} ∪ 40..=66`
= 29 livros distintos. Dados **verbatim** do `bible.sqlite` (STEP CC BY 4.0) via
`INSERT..SELECT FROM src` — sem fabricação. `reading-lite.sqlite` (leitura) inalterado
em 66 livros / 40.308.736 B: o léxico é o asset on-demand separado. `morph_legend`
corretamente deferido a F6.9b (0 linhas na PRÓPRIA fonte — não fabricado). Ambos os
re-baselines de perf são legítimos e honestamente anotados. Commit toca só o gerador
app-side + JSONs de perf + measure script; sem `.sqlite`/segredo commitado; the-light
intocado em `225b8c9`.

## Critério de aceite (verificado contra os assets regenerados vivos)

- [x] **LEXICON_BOOKS cobre o NT inteiro (40..66) + amostra AT.** `SELECT COUNT(DISTINCT
      book_number) FROM original_tokens` = **29**; lista = `1,19,40..66`. Romanos (45) =
      **7.261** tokens com Strong; Mateus (40) = 18.898; João (43) segue presente (16.076).
      Asserções de sanidade do gerador (`romans_tokens>=1`, `lex_books==29`) rodaram na
      regeneração (o DB só existe porque os asserts passaram). `deepStudy`/`lexicalEntries`
      verdes.
- [x] **Assets regenerados; reading-lite inalterado; lexicon-sample maior.**
      `reading-lite.sqlite` = 66 livros, **40.308.736 B** (byte-exato, inalterado);
      `lexicon-sample.sqlite` = **27.869.184 B** (era 9.502.720). Reading não afetado.
- [x] **perf-budget LOCKED OK após re-baseline de lexiconDbBytes; moduleCount inalterado;
      nota justificando.** `PERF BUDGET LOCKED OK` (após limpar cache Metro — ver ressalva).
      `lexiconDb 27.869.184 EXATO`; `moduleCount 842 (budget 842, EXATO)`; lock cross-check
      (baseline == contrato congelado) passou; `noteF69` presente e detalhado.
- [x] **Léxico VERBATIM (anti-alucinação); tsc 0; study/lexicon/reading verdes; the-light
      225b8c9; sem .sqlite commitado; sem segredo.** Hash SHA1 de TODOS os tokens de Romanos
      (surface/strongs/strongs_raw/lemma/translit/morph/gloss) IDÊNTICO entre `bible.sqlite`
      (fonte) e `lexicon-sample.sqlite` (gerado): `dac837e8…`. Cópia via
      `INSERT INTO original_tokens(...) SELECT ... FROM src.original_tokens WHERE book_number IN (...)`.
      `tsc --noEmit` = 0; 3 testes verdes; the-light `225b8c9`; `.sqlite` gitignored (não
      commitado); scan de segredo no diff = limpo.
- [x] **morph_legend permanece F6.9b deferido.** 0 linhas no asset E 0 linhas na fonte
      (`bible.sqlite`) — corretamente NÃO fabricado, não tocado.

## Saída da verificação (real)

DADOS (assets vivos regenerados):
```
SELECT COUNT(DISTINCT book_number) FROM original_tokens;  -> 29   (1,19,40..66)
Romanos(45) tokens com Strong;                            -> 7261
João(43) tokens;                                          -> 16076   (segue presente)
Mateus(40) tokens;                                        -> 18898
morph_legend (asset)   -> 0        morph_legend (fonte bible.sqlite) -> 0
reading-lite books     -> 66       reading-lite.sqlite bytes -> 40308736 (inalterado)
lexicon-sample.sqlite bytes -> 27869184
```

VERBATIM (anti-alucinação) — hash de todos os tokens de Romanos, fonte vs gerado:
```
source:    dac837e8ced2db5611c9367d42242191cf9b399a
generated: dac837e8ced2db5611c9367d42242191cf9b399a   (IDÊNTICO)
Rm 1:1 amostra: Παῦλος/G3972G/Παῦλος/Paulos/Paul … (bit-a-bit igual)
```

GATES (de app/):
```
npx tsc --noEmit            -> 0
test:web:study              -> PASS (João 3:16 store + estudo profundo; anti-alucinação)
test:web:lexicon            -> PASS (léxico de lexicon-sample.sqlite on-demand; João 3:16 -> 21 Strong)
test:web:reading            -> PASS (66 livros; Mateus 1:1 KJV = NT além de João; paridade nativa)
test:web:perf-budget        -> PERF BUDGET LOCKED OK  (lexiconDb 27.869.184 EXATO; moduleCount 842;
                               eager vivo 1.359.039 vs budget 1.358.977±1024; lock cross-check OK)
test:web:smoke              -> SMOKE OK (reading John 3:16 / settings / study-chat / wasm-error-ui verdes)
```

Export limpo confirmou o asset por hash: `lexicon-sample.55291c09…sqlite` (== hash do
baseline) e `wa-sqlite.6a09cc3e…wasm` (== hash congelado). Baseline re-escrito é
determinístico (sem drift git).

## Os 5 checks adversariais

1. **Expansão real + verbatim — OK.** Romanos retorna léxico (7.261 Strong); hash
   bit-a-bit idêntico à fonte; gerador copia via `INSERT..SELECT FROM src`, nunca fabrica.
2. **Reading não afetado — OK.** `reading-lite.sqlite` inalterado (66 livros, 40.308.736 B);
   o léxico é o asset on-demand separado (chunk `sqlite-lexicon-opfs`).
3. **Re-baseline legítimo (2 partes) — OK.** (a) `lexiconDbBytes` cresceu porque o asset
   on-demand genuinamente cresceu (byte-exato 27.869.184; moduleCount inalterado pois NÃO é
   eager). (b) `eagerBytes` 1.354.253→1.358.977 é débito de cromo de F6.7/F6.8 (specs sem
   perf-budget, mesmo padrão do noteF537), NÃO F6.9 — provado: o valor vivo medido por mim
   (1.359.039) bate EXATO com o "asset-NT" citado na noteF67F68, e o asset-velho de 3 livros
   dá o mesmo eager (1.358.914), demonstrando que a drift é anterior/independente do léxico
   on-demand. moduleCount 842 inalterado; gzip/brotli dentro da banda (nominais inalterados);
   `measure-web-bundle.sh` + `web-bundle-budget.json` concordam (lock cross-check passa).
   Ambos honestamente anotados (noteF69 + noteF67F68).
4. **morph_legend deferido a F6.9b — OK.** 0 linhas na fonte; não fabricado; não tocado.
5. **Não-negociáveis — OK.** the-light `225b8c9` (gerador é app-side, `core/examples`);
   `e99434f` toca só `gen_reading_sample_db.rs` + `web-bundle-baseline.json` +
   `web-bundle-budget.json` + `measure-web-bundle.sh`; nenhum `.sqlite` commitado (gitignored);
   `.claude/settings.json` NÃO commitado; scan de segredo limpo.

## Decisões / ADRs

Nenhuma nova. Referências: ADR-0056 (léxico amostrado, base que F6.9 expande), F6.9b
(gate deferido — popular `morph_legend` exigiria PR ao the-light + ADR + re-pin, decisão
humana; fabricar violaria anti-alucinação).

## Bloqueios / perguntas

Nenhum bloqueio ao aceite. Ressalva de reprodutibilidade (não é falha da tarefa): a 1ª
execução de `test:web:perf-budget` falhou (EXIT=1) por um FLAKE de cache do Metro — o
`expo export` reaproveitou cache stale e NÃO emitiu o vendor `wa-sqlite-fts5.wasm`, fazendo
o passo de pré-compressão dar ENOENT no hash esperado. F6.9 não toca bundling de wasm.
Após limpar cache app-local (`node_modules/.cache`, `.expo`) + `expo export --clear`, o
export emitiu o wasm com o hash congelado EXATO e o gate deu `PERF BUDGET LOCKED OK`.
Recomendação (opcional, follow-up de tooling): o enforcer poderia rodar o export com
`--clear` para blindar contra esse flake de cache do Metro.

---
id: F5.1
status: passed
commit: eb81a72
reviewer: reviewer
finished_at: 2026-07-02
---

## Resumo

F5.1 (re-escopo native-first) expõe a geração de planos de leitura JÁ EXISTENTE
no core (`the_light_core::userdata::plans`) pela fronteira do app, com corpo
NATIVO (`#[cfg(not(target_arch = "wasm32"))]`) + stub web
(`#[cfg(target_arch = "wasm32")]`) e 6 host tests determinísticos. Verifiquei
independentemente: reli o diff de `eb81a72`, a fonte do core em `2fc2dab`, rodei
todos os gates (test/fmt/clippy/tsc/wasm) e confirmei cada critério de aceite
lendo os testes + o código (não só os nomes). Zero drift: a fronteira Rust
delega a `plans::{available_plans, plan_by_id, PlanProgress::day_index_for}` e a
`reference::format_reference`; o TS só reexporta os bindings. `the-light`
intocado (`2fc2dab`), `core/Cargo.toml` fora do commit, sem segredos.
**Veredito: passed.**

## Critério de aceite

- [x] **`list_reading_plans()` (nativo) retorna 3 summaries (annual/365, nt/90,
  gospels/30) com nomes PT verbatim do core (sem hardcode).**
  Verificado no teste `plans_tests::lists_three_plans_with_core_pt_names_verbatim`
  (lib.rs): `assert_eq!(plans.len(), 3)`, `days_of("annual")==365`,
  `days_of("nt")==90`, `days_of("gospels")==30`, e — crucial contra hardcode — os
  nomes são comparados contra um `HashMap` construído de
  `available_plans()` (`assert_eq!(&p.name, core.get(p.id.as_str()).unwrap())`).
  O corpo nativo de `list_reading_plans` (lib.rs) usa `plans::available_plans()`
  + `plans::plan_by_id(id).map(|p| p.days.len())` — nenhum nome literal. Confirmei
  na fonte do core (`plans.rs:46-55`) que o CATALOG traz `"Bíblia em 1 ano"` (365),
  `"Novo Testamento em 90 dias"` (90), `"Evangelhos em 30 dias"` (30). Teste passou.

- [x] **`reading_plan_day("gospels",0)` → refs de capítulo inteiro começando em
  Mateus (book=40, chapter=1) + label não-vazio; fora do intervalo/desconhecido →
  vazio, sem panic.**
  Teste `plans_tests::gospels_day0_starts_at_matthew_1_whole_chapter_with_label`:
  `first.book==40`, `first.chapter==1`, `matches!(first.verses, VerseRange::WholeChapter)`,
  `!day.label.is_empty()`. Teste `unknown_plan_or_out_of_range_day_is_empty_no_panic`
  cobre id inexistente e dia `10_000` → `references`/`label` vazios sem panic. O
  corpo nativo delega a `plan_by_id(&plan_id).map(|p| p.reading(day).to_vec())`
  (herda `None`/`&[]` do core, `plans.rs:40-42,58-66`) e monta `label` via
  `reference::format_reference`. Confirmado que gospels = `chapters_for_books(40,43)`
  no core → começa em Mateus 1 WholeChapter. Ambos testes passaram.

- [x] **`reading_plan_day_index` == `PlanProgress::day_index_for`, satura em
  [0,len-1], ISO inválida → CoreError, delegado (sem reimplementar).**
  O corpo nativo (lib.rs) constrói `PlanProgress { plan_id: String::new(),
  start_date, completed: 0 }` e chama `.day_index_for(today, len as usize)` — NÃO
  há lógica de clamp/subtração na fronteira; toda a matemática vive em
  `plans.rs:109-115`. `NaiveDate` obtido por inferência via `start_date.parse()`
  (FromStr ISO), SEM `use chrono` (grep confirmou: só há a palavra em comentário).
  Datas inválidas mapeadas para `CoreError::Generic` via `.map_err`. Teste
  `day_index_matches_core_and_clamps_to_range`: `idx==0`, `idx("...01-11")==10`,
  satura em `0` (antes) e `364` (muito depois). Teste
  `day_index_invalid_iso_date_is_core_error`: `"not-a-date"` e `"31/12/2026"` →
  `.is_err()`. Ambos passaram.

- [x] **Fronteira NATIVA + stub web; build web + `tsc` verdes; zero drift TS/Rust.**
  Cada uma das 3 fns tem corpo `#[cfg(not(target_arch = "wasm32"))]` (delega ao core)
  + bloco `#[cfg(target_arch = "wasm32")]` de stub (list→`Vec::new()`, day→vazio,
  index→`CoreError`) — confirmado no diff. Records (`ReadingPlanSummary`,
  `ReadingPlanDay`) são cfg-free (`String`/`u32`/`Vec<Reference>`), presentes em
  ambos alvos. `app/web/reading.ts` e `reading.web.ts` apenas reexportam os wrappers
  gerados (`listReadingPlansNative`/`...Wasm`); grep por `chunk|CATALOG|clamp|
  whole_chapter|num_days` só encontrou comentários (nenhuma lógica espelhada).
  `tsc --noEmit` exit 0 (bindings nativos + web presentes na superfície).

- [x] **`the-light` NÃO tocado (`2fc2dab`; `core/Cargo.toml` inalterado); grafo
  wasm puro.**
  `git -C the-light status` limpo, `rev-parse HEAD` = `2fc2dabbe1e8...`. O commit
  `eb81a72` toca SÓ 3 arquivos (`core/src/lib.rs`, `app/web/reading.ts`,
  `app/web/reading.web.ts`) — `core/Cargo.toml` NÃO está no commit e mantém o pin
  `rev = 2fc2dab...` (ai-pure para wasm, embedded para host). `.claude/settings.json`
  NÃO foi commitado. `cargo tree --target wasm32` (rust_modules/wasm) → nenhum
  `reqwest`/`rusqlite`: **WASM_PURE_OK**.

## Saída da verificação

```
# cargo test -p the-light-app-core
test result: ok. 75 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.32s
Doc-tests: ok. 0 passed; 0 failed

# cargo test -p the-light-app-core plans_tests  (os 6 novos)
test plans_tests::day_index_invalid_iso_date_is_core_error ... ok
test plans_tests::day_index_matches_core_and_clamps_to_range ... ok
test plans_tests::unknown_plan_or_out_of_range_day_is_empty_no_panic ... ok
test plans_tests::gospels_day0_starts_at_matthew_1_whole_chapter_with_label ... ok
test plans_tests::lists_three_plans_with_core_pt_names_verbatim ... ok
test plans_tests::annual_days_sum_to_1189_whole_bible_chapters ... ok
test result: ok. 6 passed; 0 failed; 0 ignored; 69 filtered out

# cargo fmt --check           -> exit 0 (sem diffs)
# cargo clippy -p the-light-app-core --all-targets -- -D warnings  -> CLIPPY_EXIT=0 (Finished, 0 warnings)
# npx tsc --noEmit            -> TSC_EXIT=0
# cargo tree --target wasm32-unknown-unknown | grep -E 'reqwest|rusqlite'  -> vazio -> WASM_PURE_OK

# the-light: git status limpo; HEAD = 2fc2dabbe1e8ea14d9aab07ba774221595b65b7d
# commit eb81a72 --name-only: app/web/reading.ts, app/web/reading.web.ts, core/src/lib.rs (só 3)
# grep 'use chrono' core/src/lib.rs -> só em comentário ("SEM use chrono"); nenhum import
```

## Decisões / ADRs

- Re-escopo native-first legítimo. Confirmei na fonte do core que `pub mod userdata`
  é `#[cfg(feature = "embedded")]`, logo `userdata::plans` não existe sob `ai-pure`/wasm;
  tornar as fns cfg-free exigiria PR `ai-pure` ao `the-light` (gate estratégico +
  ADR = F5.10). O padrão adotado (corpo nativo + stub web + host test) é o mesmo de
  F1.10/`deep_study`. Nenhum ADR novo criado nem exigido nesta tarefa.
- O executor NÃO burlou um critério cfg-free: a tarefa foi explicitamente
  re-escopada para native+stub (`rescoped_at: 2026-07-02`), e o critério de aceite
  vigente pede exatamente a fronteira NATIVA + stub — atendido.

## Bloqueios / perguntas

Nenhuma.

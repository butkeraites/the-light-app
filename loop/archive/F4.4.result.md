---
id: F4.4
status: passed
gate: false
commit: 69b369f0293bbdc232db6ade3a612c67876dbeaa
reviewer: reviewer
finished_at: 2026-07-02
---

## Resumo

F4.4 habilita o 2º backend de pesquisa web — **Tavily (BYOK)** — no estudo profundo
WEB, como paridade da F4.3 nativa. Verificação INDEPENDENTE (rodei eu mesmo todos os
comandos): a entrega é 100% app-side (glue TS `research.web.ts`/`study.web.ts` + UI
`ReaderStudyPanel.tsx` + self-test MOCK), **NÃO toca `the-light`/`core/`/Rust**, o pin
`04b9b24` permanece intacto e o grafo wasm segue puro. O transporte `tavilySearch` faz
`POST https://api.tavily.com/search` com a chave BYOK **só no corpo** (`api_key`), a
chave é **session-only** (`useState`, nunca persistida/logada/em git), e as citações
`[W:n]`/`kind="Web"` continuam montadas pelo Rust `study_web_finalize` (ZERO-DRIFT). Todos
os critérios de aceite foram satisfeitos e a verificação está verde. **Veredito: passed.**

## Critério de aceite (verificado item a item)

- [x] **Transporte Tavily** — `research.web.ts::tavilySearch` faz `POST
      https://api.tavily.com/search`, header `content-type: application/json`, corpo
      `JSON.stringify({ api_key: key, query, max_results, search_depth: 'basic' })`.
      `max_results = Math.min(10, Math.max(1, limit ?? 4))` (clamp 1..10);
      `DEFAULT_TAVILY_LIMIT = 4` exportado. Parse de `results[]` → `{title (default "(sem
      título)"), url, snippet (=`content`), site (host via `url.split('://')[1]`),
      fetchedAt}`, pulando itens sem `url` — mesmo `StudyWebSourceInput` do
      `wikipediaSearch`. A **chave vai SÓ no CORPO**: nunca na URL, nunca em header, e o
      erro HTTP cita só o status (`Tavily respondeu HTTP ${res.status}`). O self-test
      confirma por parse do body do fetch mock.
- [x] **Zero-drift** — grep em `web/*.ts` (fora de testes) por `kind:"Web"`/`Citation`/
      `[W:`/`academic_markdown=`/`apparatus`: **NENHUM** constructo TS. As citações vêm de
      `studyWebFinalize` (wasm `ai-pure`); `study.web.ts` só entrega `web_sources` cruas a
      `studyWebPrepare`. `passageText` = João 3:16 verbatim do store, separado da
      `interpretation`.
- [x] **Fronteira web** — `deepStudyOnHandle` removeu o `void researchKey` e agora repassa
      `researchKey` a `resolveWebSources`; o ramo `"tavily"` injeta as fontes nos
      `web_sources`. Self-test (A) prova ≥1 citação `Web` da URL Tavily.
- [x] **BYOK/erro sem chave** — ramo `"tavily"` sem `researchKey` → `throw new Error(
      'pesquisa web tavily exige uma chave (BYOK) — nenhuma configurada')` (cita só
      "tavily", 0 fetch). Self-test bloco (B): `assert.equal(callsNoKey.length, 0)` e a
      mensagem nunca inclui chave. Chave session-only via `useState` (ADR-0025).
- [x] **Wikipedia preservado** — bloco (C) do self-test: backend `"wikipedia"` keyless
      gera citação `Web` da URL wikipedia, faz fetch keyless, NÃO chama Tavily, sem
      segredo na URL. Regressão `test:web:research` (Wikipedia) verde.
- [x] **Offline por padrão** — bloco (D): sem `researchBackend` → 0 citação `Web`, 0 fetch
      Tavily/Wikipedia; `passageText` = `16 <João 3:16 KJV>` verbatim do store.
- [x] **UI** — `ReaderStudyPanel` troca o toggle booleano por seletor 3-vias
      (`off`/`wikipedia`/`tavily`, testIDs `study-web-research-{off,wikipedia,tavily}`);
      Tavily exibe `TextInput` `secureTextEntry` session-only (`tavilyKey` em `useState`,
      testID `study-web-research-tavily-key`) + aviso de privacidade/atribuição (chave só
      na sessão, na memória, viaja só no corpo; citações Web montadas pelo app das URLs).
      A chave não persiste.
- [x] **MOCK verde** — self-test headless (fetch MOCK) prova: URL exata
      `https://api.tavily.com/search`, método POST, `api_key` no corpo (= dummy), chave
      nunca na URL/header/log, `search_depth:"basic"`, `max_results` 1..10, ≥1 citação
      `kind="Web"` com URL `britannica.com`, `academicMarkdown` cita `[W`, `passageText` ≠
      `interpretation`. Backend=tavily sem chave → erro só "tavily", 0 fetch.
- [x] **Qualidade / integridade** — `tsc --noEmit` exit 0; `expo export --platform web`
      exit 0; self-tests web (research/study/compare/export) sem regressão; grafo wasm
      PURO (sem rusqlite/reqwest); pin `04b9b24` intacto (2 ocorrências); `core/Cargo.toml`
      NÃO no diff; nenhum arquivo `core/`/the-light tocado.
- [x] **Chave nunca em git/log** — diff usa só valores fictícios (`DUMMY_TAVILY_KEY =
      'test-only-not-a-real-tavily-key'`, `DUMMY_LLM_KEY`); scan por `tvly-`/`api_key:"tvly`/
      `Bearer <20+>` no diff de código: NADA. Sem `setKey`/`AsyncStorage`/`localStorage`/
      `sessionStorage`/`IndexedDB` para a chave Tavily.

## Saída da verificação (saídas REAIS observadas por mim)

Escopo/integridade (`git show 69b369f --name-only`):
```
app/components/ReaderStudyPanel.tsx
app/package.json
app/web/__tests__/research-tavily.web.test.mjs
app/web/research.web.ts
app/web/study.web.ts
loop/queue/F4.4-tavily-web-fetch.task.md
```
- `grep "^core/"` no diff → nenhum arquivo `core/`; `core/Cargo.toml` NÃO no diff.
- Pin em `core/Cargo.toml`: linha 37 (`ai-pure`) e linha 44 (`embedded`) →
  `rev = "04b9b24a990bb78c5e7cf2fffd9cc1571dc68376"` (2 ocorrências, intacto).

Grafo wasm puro:
```
OK grafo wasm puro (sem rusqlite/reqwest)
```

Self-test Tavily (`npm run test:web:research-tavily`):
```
PASS — pesquisa web TAVILY OPT-IN (BYOK) no estudo web (fetch MOCK Tavily + LLM):
  endpoint / método        -> POST https://api.tavily.com/search
  chave BYOK               -> NO CORPO (api_key); NUNCA na URL/header/log
  citações Web (URLs)      -> 1 (url contém britannica — do Rust)
  academicMarkdown         -> cita [W:n]/fonte web; passagem do store presente
  BYOK sem chave           -> erro citando só "tavily", 0 fetch
  regressão Wikipedia      -> keyless preservado (citação Web, sem chave)
  off                      -> 0 citação Web, 0 fetch de pesquisa
  ANTI-ALUCINAÇÃO: passageText = João 3:16 do store; [W:n]/citações do Rust, das URLs.
```

Regressão web (sem regressão):
```
=== research ===  PASS — pesquisa web Wikipedia OPT-IN no estudo web (fetch MOCK Wikipedia + LLM)
=== study ===     PASS — paridade web do estudo profundo (study_web_prepare/finalize + fetch MOCK)
=== compare ===   PASS — comparação multi-IA ancorada no web (2× askAnchored mock, mesma âncora)
=== export ===    PASS — export acadêmico web (buildStudyExport sobre o retorno real de deepStudy)
```

TypeScript + export web:
```
TSC real exit=0
expo exit=0
```

Scan de segredos (diff de código `app/`): `OK sem chave real`. Persistência da chave:
`tavilyKey` só em `useState` (linha 149) → `researchKey` (linha 181) → `value` do
`TextInput` (linha 363); nenhum `setKey`/`*Storage`/`IndexedDB` para a chave (os matches
de `OPFS` são comentários sobre carregamento do DB, não da chave).

## Decisões / ADRs

- **ADR-0035** (Tavily web: transporte `fetch` a `api.tavily.com/search` + chave BYOK
  session-only só no corpo + toggle UI opt-in) — registrado pela tarefa. Chave session-only
  implementada pela **opção (a)** da task: `useState` no componente (in-memory, perdida no
  reload), evitando o `assertProvider` de LLM do keystore. Conforme ADR-0025/0028/0029.

## Bloqueios / perguntas

Nenhuma. Verificação verde e todos os invariantes preservados.

## Consequência

`passed` → o Driver arquiva `F4.4` (tarefa + resultado) para `loop/archive/`, atualiza
`STATUS.md`/`PROGRESS.md` e registra no `JOURNAL.md`. **Próxima tarefa: ⛔ gate F4.5 —
validação real com chaves reais (streaming / multi-IA / Tavily), BLOQUEANTE.** O Driver
deve escrever `loop/HALT` com motivo "aguardando sign-off do marco F4.5" e parar para
sign-off humano (rede/segredos reais — fora do escopo autônomo).

---
id: F6.7
status: passed
commit: df57b3f
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

Des-mock dos painéis **Study** (`ReaderStudyPanel.tsx`) e **Chat**
(`ReaderChatPanel.tsx`) verificado de forma independente e adversarial. Ambos
deixaram de hardcodar `MOCK_PROVIDER`: ganharam um seletor de provedor
`[mock, anthropic, openai, gemini, ollama]` (molde `ReaderAskPanel`, badges de
`useConfiguredAiProviders`) e passam o provedor SELECIONADO + a chave lida SOB
DEMANDA (`getKey(provider)`) às fronteiras existentes (`deepStudy` /
`askSessionAnchored`). Provedor real sem chave → `ask.needKeyError` + CTA
`/settings`, envio desabilitado (não chama a fronteira). `mock` segue default
offline. Anti-alucinação intacta; transporte/core inalterados. Todos os 9 gates
verdes. **Veredicto: PASSED.**

## Critério de aceite (todos satisfeitos)

- [x] **Seletor `[mock, anthropic, openai, gemini, ollama]` em Study e Chat.**
  `PROVIDER_OPTIONS = [MOCK_PROVIDER, ...SUPPORTED_PROVIDERS]`;
  `SUPPORTED_PROVIDERS = ['anthropic','openai','gemini','ollama']` (keystore.ts).
  Escolher provedor real → `getKey(provider)` + fronteira real; sem chave →
  `ask.needKeyError` + CTA `/settings` (`onConfigureProvider`), envio bloqueado
  (`studyDisabled`/`sendDisabled` incluem `needsKey`), sem travar. `mock` default
  offline (`useState(MOCK_PROVIDER)`).
- [x] **Anti-alucinação intacta.** O diff NÃO tocou os blocos de renderização
  (`passageText`/`interpretation`/`citedText`/`citations`/`lexicon`/`attribution`
  — única linha alterada nessa vizinhança foi reescrita de um comentário). Texto
  bíblico/citações/léxico vêm do retorno da fronteira (STORE, verbatim); só
  `interpretation` é do modelo; blocos citação vs. interpretação continuam
  separados e rotulados.
- [x] **Gates verdes:** `tsc` 0; `test:web:study`/`session`/`compare`,
  `test:i18n`/`i18n-coverage`, `test:a11y-modals` (`panels=6`, `labels=ok`),
  `test:web:contrast`, `test:web:smoke` (inclui `study-chat-selector`) — todos PASS.
- [x] **Sem chave/segredo real; the-light `225b8c9`; transporte/core inalterados.**
  Commit `df57b3f` toca só `ReaderChatPanel.tsx` + `ReaderStudyPanel.tsx` +
  `assertions.mjs`. `reading.ts`/`ai-anchored.web.ts` não tocados. the-light em
  `225b8c9`. `.claude/settings.json` NÃO commitado (working-tree apenas).

## Os 5 checks adversariais

1. **Un-mock é real + ordem dos args correta.** Diff confirma `- MOCK_PROVIDER`
   → `+ provider, + key` em AMBOS os call sites. Conferido contra as assinaturas
   em `reading.ts`:
   - `deepStudy(dbPath, translation, book, chapter, verse, mode, lens, depth,
     lang, providerName, key, model, researchBackend, researchKey)` — a chamada
     passa `provider` na posição de `providerName` e `key` na de `key` (model
     `undefined`, research separado). ✓
   - `askSessionAnchored(dbPath, translation, book, chapter, verse, lang, turns,
     studyMode, studyLens, providerName, key, model)` — `lang` ANTES de `turns`;
     `studyMode`/`studyLens` (`undefined`) ANTES de `providerName`; `provider` +
     `key` nas posições certas. ✓
   Um provedor real selecionado alcança a transport com a chave (provado headless
   pelos testes mock-fetch: `study`/`session`/`compare` verdes).
2. **Caminho sem chave é seguro.** Provedor real sem chave → `needsKey=true` →
   `studyDisabled`/`sendDisabled` (envio desabilitado) + bloco `study-provider-needkey`
   / `chat-provider-needkey` com `ask.needKeyError` + CTA `/settings`. Além disso,
   `onStudy`/`onSend` fazem `getKey` e lançam antes de chamar a fronteira se a chave
   for `null` — a IA NÃO é chamada (sem crash/hang; input preservado no Chat). `mock`
   (default) roda offline sem chave (`isMock` pula o `getKey`).
3. **Anti-alucinação PRESERVADA (crítico).** `grep` do diff nas referências de
   renderização (`passageText`/`interpretation`/`citedText`/`citations`/`lexicon`/
   `result.`/`answer.`) → nenhuma mudança de lógica (só um comentário reescrito).
   Blocos separados intactos: `study-passage-text` (STORE) vs. `study-interpretation`
   (modelo); `chat-cited-text` (âncora STORE) vs. thread de turnos (modelo).
   Nenhuma lógica de render enfraquecida.
4. **Chave nunca vaza.** `getKey` só flui para a fronteira; sem `console.*` nos
   painéis; `key`/`stored` nunca renderizados em JSX; sem string secreta hardcoded
   (`sk-`/`Bearer`/`api_key=`). A chave Tavily (web-research, session-only `useState`)
   é OUTRA e permanece separada/intocada. Smoke roda no caminho mock/offline (sem
   chave real).
5. **Transport/core intocados.** `df57b3f` = 3 arquivos (2 painéis +
   `assertions.mjs`). `reading.ts`/`ai-anchored.web.ts`/`the-light` inalterados;
   the-light `225b8c9`; `.claude/settings.json` não commitado.

## Saída da verificação (real)

```
$ npx tsc --noEmit
TSC_EXIT=0

$ npm run test:web:study     → PASS (passageText STORE / interpretation MOCK; chave dummy só no header)  EXIT=0
$ npm run test:web:session   → PASS (citedText STORE / interpretation MOCK; multi-turno; "mock"=0 fetch) EXIT=0
$ npm run test:web:compare   → PASS (2 providers; cited_match true; "mock"=0 fetch)                       EXIT=0
$ npm run test:i18n          → PASS (249 chaves pt↔en, sem órfã)                                          EXIT=0
$ npm run test:i18n-coverage → PASS (249 chaves; 26 arquivos sem PT/hex fora de t())                      EXIT=0
$ npm run test:a11y-modals   → PASS (TLA_A11Y modal=true labels=ok scale=ok focus=ok panels=6 locks=0)    EXIT=0
$ npm run test:web:contrast  → PASS (18 pares AA)                                                          EXIT=0

$ npm run test:web:smoke
  ...
  TLA_WEB_open-chapter ok
  TLA_WEB_parallel ok
  TLA_WEB_search-xref ok
  TLA_WEB_notes-persist ok
  TLA_WEB_plans-persist ok
  TLA_WEB_export-import ok
  TLA_WEB_import-fresh ok
  TLA_WEB_settings ok
  [study-chat] painel de ESTUDO abre com seletor de provedor (mock default + BYOK reais)
  [study-chat] painel de CONVERSA abre com seletor de provedor (mock default + BYOK reais)
  TLA_WEB_study-chat-selector ok
  TLA_WEB_ai-reachability ok
  ==> SMOKE OK (target=dist)
  TLA_WEB_wasm-error-ui ok
  ==> SMOKE OK (target=dist)
SMOKE_EXIT=0
```

## Decisões / ADRs

Nenhum ADR novo. F6.7 é fiação de UI (troca de args + cromo de seletor), sem
mudança de fronteira/core/transport. Resposta REAL de provedor (chave BYOK + rede)
segue como gate humano por definição — o loop só prova a FIAÇÃO (mock-fetch +
smoke offline), como a task determina. O CORS do Anthropic segue esperado até F6.8
(visto no ai-reachability, alheio a esta task).

## Bloqueios / perguntas

Nenhuma.

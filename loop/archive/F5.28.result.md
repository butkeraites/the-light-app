---
id: F5.28
status: passed
commit: 32d06f2
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

Verificação independente APROVADA. Os 4 nomes de exibição das cores de destaque
deixaram de ser hardcoded em PT (`label: 'Amarelo'…`) e agora são resolvidos via
`t(\`highlight.${c.name}\`)` no swatch do painel de versículo. Sob EN o leitor de
tela anuncia "Highlight with Yellow" (não mais "Amarelo"); sob PT segue "Marcar
com Amarelo". Todos os gates verdes; the-light intocado; sem segredo/rede.

## Critério de aceite (verificado)

- [x] **Novas chaves i18n em AMBOS os catálogos** — `highlight.yellow/green/blue/pink`
      presentes em `pt` (Amarelo/Verde/Azul/Rosa) e `en` (Yellow/Green/Blue/Pink)
      em `i18n.ts` (linhas 486-489 pt, 713-716 en) e na união `MessageKey` (256-259).
      `Record<MessageKey,string>` obriga ambos os lados → `tsc` 0 é prova de paridade
      em compilação; o test:i18n confirma em runtime (set-equality, 203 chaves).
- [x] **`ReaderVersePanel.tsx` resolve via `t(\`highlight.${c.name}\`)`** — linha 291-293:
      `accessibilityLabel={t('versePanel.highlightWith', { color: t(\`highlight.${c.name}\`) })}`.
      Templates: pt `'Marcar com {color}'` (474), en `'Highlight with {color}'` (701).
      Composição real: en → "Highlight with Yellow", pt → "Marcar com Amarelo".
      Não lê mais nenhum `c.label`.
- [x] **`highlightColors.ts` só dados persistidos** — `label` removido; `HighlightColor`
      mantém `name`/`light`/`dark`. `name` estreitado p/ união literal
      `'yellow'|'green'|'blue'|'pink'` (novo tipo `HighlightColorName`).
      `resolveHighlightColor(name: string, …)` mantém parâmetro `string` → highlights
      persistidos com cor arbitrária continuam resolvendo (fallback devolve o próprio
      nome). Exclusão de `lib/` do guarda i18n-coverage segue correta (o guarda varre
      components/*.tsx + app/**/*.tsx, não lib/; e highlightColors.ts nem tem mais PT).
- [x] **Gates verdes** — tsc 0; test:i18n PASS; test:i18n-coverage PASS; test:a11y-scan PASS.
- [x] **the-light intocado** (`225b8c9…`, working tree limpo); sem rede/segredo.

## Saída da verificação (re-executada por mim, a partir de app/)

```
$ npx tsc --noEmit
EXIT tsc: 0

$ npm run test:i18n            → exit 0
PASS — i18n de CROMO (PT/EN) …
  paridade de catálogo pt↔en: 203 chaves, mesmos conjuntos (sem órfã)
  (inclui highlight.yellow/green/blue/pink pt=Amarelo/Verde/Azul/Rosa, en=Yellow/Green/Blue/Pink)
  (compõe en "Highlight with Yellow" e pt "Marcar com Amarelo")

$ npm run test:i18n-coverage   → exit 0
PASS — GUARDA de cobertura i18n/tema …
  (1) paridade pt↔en: 203 chaves, mesmos conjuntos; todas em namespace de CROMO
  (2) cromo: 23 arquivos sem string PT/acentuada fora de t()
  (3) tema: 23 arquivos sem hex hardcoded

$ npm run test:a11y-scan       → exit 0
PASS — GUARDA de a11y de INTERATIVOS …
  varreu 71 interativos em 23 arquivos … (3) alvo de toque … swatch+hitSlop

$ npm run test:web:notes       → exit 0   (sibling de highlights: sem regressão)
```

## Verificação de substância (adversarial — 5 checagens)

1. **Bug corrigido** — Composição confirmada em `ReaderVersePanel.tsx:291-293`; não há
   mais leitura de `c.label`. Templates + chaves produzem en "Highlight with Yellow",
   pt "Marcar com Amarelo". O teste `i18n.test.mjs` assere AMBOS os locais: 8 asserts
   individuais (pt+en das 4 cores) MAIS 2 asserts de composição do a11y
   (`versePanel.highlightWith` com `{color}` traduzido) para en e pt — não é mera presença.
2. **Paridade real** — As 4 chaves existem nos dois catálogos com os valores corretos.
   Não é vacuosa: `translate('en','highlight.yellow')` deve dar exatamente 'Yellow'
   (chave ausente cairia no fallback e reprovaria), e o teste de set-equality pt↔en
   falharia se um lado faltasse. `Record<MessageKey,string>` também trava em compilação.
3. **Remoção de `label` segura** — grep no repo: nenhum consumidor de `HighlightColor.label`
   (únicos consumidores: `ReaderVersePanel` via `HIGHLIGHT_COLORS.map`/`c.name` e
   `read/[book]/[chapter].tsx` via `resolveHighlightColor(name,…)` com string do store).
   União `name` estreitada type-checa (tsc 0); `resolveHighlightColor` ainda aceita
   strings livres do store (persistidos arbitrários não quebram — fallback devolve o nome).
4. **Anti-alucinação intacta** — só CROMO mudou (nomes de exibição Amarelo/Yellow).
   Texto de versículo e o DADO do highlight (`name`='yellow' persistido) intocados.
   Adicionar `highlight` a CHROME_NAMESPACES é legítimo: são rótulos de UI, não conteúdo
   bíblico. `versePanel.highlightWith` continua interpolando `{color}` como cromo.
5. **Não-negociáveis** — the-light em `225b8c929cf388e29dc148fec3975bf05a884b07`
   (limpo). `.claude/settings.json` NÃO está no commit (0 ocorrências no `git show --stat`;
   fica só como modificação não-commitada na working tree). Commit `32d06f2` toca apenas
   5 arquivos sob `app/` (produto + testes), nada sob `loop/` ou `core/`. Sem segredo/rede.

## Decisões / ADRs

Nenhum ADR novo necessário — fecha o follow-up de i18n da F5.16 sem mudança de contrato
externo nem do core. HEAD do repo = `32d06f2` (reportado pelo Executor confere).

## Bloqueios / perguntas

Nenhuma.

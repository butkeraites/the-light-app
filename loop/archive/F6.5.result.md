---
id: F6.5
status: passed
commit: ed2654d
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

Verificação independente APROVADA. O self-test on-device foi consertado e nivelado:
(1) a asserção de busca do `search-selftest.ts` agora escopa a query "God" ao livro 43
(João) na fronteira `search` real — João 3:16 fica determinístico no retorno da Bíblia
completa (F5.36), sem PROOF_LIMIT cego nem hardcode; (2) os scripts iOS/Android resolvem
um device/AVD disponível (env-overridable) em vez de depender do fixo "iPhone 17"
inexistente; (3) `run-android-selftest.sh` ficou em paridade `TLA_*` total com o iOS.
Reproduzi o iOS on-device com SKIP_BUILD (build F6.4 reusado, JS atual via Metro) →
exit 0 com a bateria completa incl. `TLA_SEARCH ...ok` (hits=69) e `TLA_DBUP adopted=true`.
Android on-device fica PENDENTE (device-gated — precisa de build gradle fresco; não é
problema de código da F6.5).

## Critério de aceite

- [x] `TLA_SEARCH` volta a `ok` (João 3:16 determinístico na Bíblia completa) — provado
  on-device: `TLA_SEARCH query="God" hits=69 first_ref="John 3:16" first_text="For God so
  loved the world..."`. A correção escopa `book=43` na chamada `search(dbPath,'God','kjv',
  SEARCH_BOOK,PROOF_LIMIT)` (SEARCH_BOOK=43), NÃO aumenta cegamente o limite nem hardcoda o
  resultado. O texto vem de `hit.text` (store); a constante "For God so loved" só na
  asserção do shell (MARK_SEARCH_TEXT). Cross-check forte: o headless `search.web.test.mjs`
  independente retorna EXATAMENTE 69 hits ao filtrar livro=43 — mesmo número que o nativo.
- [x] `run-ios-selftest.sh` VERDE ponta a ponta (exit 0), bateria `TLA_*` completa +
  `TLA_DBUP adopted=true`, sem depender de DEVICE_NAME inexistente. Prova on-device iOS
  capturada por mim (não só do executor) — ver "Saída da verificação".
- [x] `run-android-selftest.sh` assere a MESMA bateria `TLA_*` + `TLA_DBUP` — os 24 padrões
  grep são idênticos aos do iOS (diff textual dos blocos `grep -q` = zero diferença);
  `bash -n` limpo. On-device Android: PENDENTE device-gated (script pronto/validado).
- [x] `tsc --noEmit` 0; nenhum `test:web:*`/smoke regride; the-light `225b8c9`; sem segredo;
  commit toca só os 3 arquivos de self-test; `.claude/settings.json` NÃO commitado.

## Saída da verificação

### Gates headless (rodados por mim)
```
npx tsc --noEmit                 -> exit 0
npm run test:web:search          -> PASS (busca web wa-sqlite FTS5)
    search("God","kjv",-,5000) -> 3892 hits (Bíblia completa)
    filtro livro=43 (João)     -> 69 hits, todos no livro 43   <-- casa com o nativo (hits=69)
npm run test:web:reading         -> exit 0 (João 3:16, books=66, john_chapters=21)
npm run test:web:xref            -> exit 0 (Romanos 5:8 1º por votos=871)
npm run test:web:smoke           -> exit 0 (SMOKE OK, wasm-error-ui ok)
```

### iOS on-device (DEVICE_NAME="iPhone 17 Pro" SKIP_BUILD=1 ./scripts/run-ios-selftest.sh -> exit 0)
Device resolvido: `iPhone 17 Pro = D6C39B9C-... — bootando`. Marcadores emitidos pelo Rust
nativo via Turbo Module:
```
TLA_A11Y modal=true labels=ok scale=ok focus=ok panels=6 locks=0
TLA_SELFTEST PT/EN book=43 chapter=3 verse=16
TLA_DBUP adopted=true matt1_verses=25 matt1_v1_len=81
TLA_READ books=66 john3_v16="For God so loved the world..." john_chapters=21
TLA_PARALLEL kjv_john3_16="For God so loved..." alm_john3_16="Porque Deus amou o mundo..."
TLA_SEARCH query="God" hits=69 first_ref="John 3:16" first_text="For God so loved the world..."
TLA_XREF verse="John 3:16" count=20 first_ref="Romans 5:8" first_votes=871
TLA_NOTES note_ref="John 3:16" highlights=1 persisted=true
TLA_ASK ... streamed=true cited_prefix="For God so loved..." interp_len=94
TLA_STUDY ... passage_prefix="For God so loved..." lexicon=21 attribution_ok=true
TLA_CHAT ... turns=3 cited_prefix="For God so loved..." interp_len=94
TLA_COMPARE ... providers=2 cited_match=true first_provider="mock"
TLA_EXPORT ... md_len=980 has_passage=true has_attribution=true
TLA_PLANS plan_id="gospels" days=30 persisted=true
==> OK — bateria completa provada pelo Rust nativo via Turbo Module.
```

### Verificação adversarial (5 checks)
1. **Fix é escopo, não trapaça** — CONFIRMADO. `SEARCH_BOOK=43` passado à `search(...)`
   real; João 3:16 localizado por `.find()` no retorno; texto = `hit.text` do store. Glue
   de produto (`app/web/reading.ts`, `sqlite-search.web.ts`) NÃO alterado (último commit
   deles: d670ee6 / b2f1d8f — não ed2654d).
2. **Defaults de device resilientes** — CONFIRMADO. iOS: sem env, resolve 1º iPhone
   disponível (booted preferido) em [1/5]; com env exige o device. Android: análogo
   ("thelight_avd" se existir, senão 1º AVD). Override por env preservado nos dois.
3. **Paridade Android real** — CONFIRMADO. Os 24 padrões `grep -q[EF]` do loop de asserção
   Android == iOS (comparação textual: zero diferença), incl. `TLA_DBUP`. `bash -n
   scripts/run-android-selftest.sh` limpo.
4. **iOS full-green reproduzido** — CONFIRMADO (ver acima), exit 0 com `TLA_SEARCH ...ok` +
   `TLA_DBUP adopted=true` + bateria completa.
5. **Não-negociáveis** — CONFIRMADO. the-light em `225b8c9`; `.claude/settings.json` só no
   working tree (M), NÃO commitado; sem segredo no diff; `git show --stat ed2654d` = só
   `app/web/search-selftest.ts`, `scripts/run-android-selftest.sh`, `scripts/run-ios-selftest.sh`.

## Decisões / ADRs

Nenhum ADR novo. A correção é da ASSERÇÃO do self-test (não da busca de produto), alinhada
ao padrão já existente do headless `search.web.test.mjs` e da F6.2 (escopo por livro).

## Bloqueios / perguntas

Android on-device: PENDENTE (device-gated). O APK debug reusado no emulador tem módulo
nativo incompatível com o JS atual → precisa de um `gradle install` fresco (build pesado),
alheio ao código da F6.5. O script está pronto, validado (`bash -n` limpo, paridade `TLA_*`
total) e o commit reporta Android como PENDENTE — NÃO alega verde silenciosamente. Follow-up
para o Driver/humano rodar o Android quando um build fresco estiver disponível. Nenhum outro
bloqueio.

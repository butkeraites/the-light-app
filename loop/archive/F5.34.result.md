---
id: F5.34
status: passed
commit: 838cf45
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

README real (6850 bytes, era 15) que documenta propósito, princípios não-negociáveis,
anti-alucinação, tabela de atribuição autoritativa (subsume NOTICE) e build/run dos três
alvos. Revisão adversarial: NENHUMA alegação de recurso inventada/falsa encontrada; toda
feature concreta foi conferida contra o repo. As duas strings CC-BY casam byte-a-byte com
as constantes in-app. Só `README.md` no commit; the-light intocado; sem segredo; tsc verde.
**Passa.**

## Critério de aceite (conferido)

- [x] Documenta (1) o que é (estudo bíblico bilíngue PT/EN, offline-first) — abertura + seção Features.
- [x] (2) offline-first + BYOK — seção "Non-negotiable principles" (keys em secure storage, nunca git/log).
- [x] (3) anti-alucinação — seção dedicada: texto do versículo sempre do store local; LLM só interpreta; citation-stripping em todos os alvos.
- [x] (4) tabela de atribuição — KJV & Almeida 1911 domínio público, xrefs OpenBible.info CC-BY, léxico STEP/Tyndale CC BY 4.0.
- [x] (5) build/run web/iOS/Android — seção "Build & run" com comandos reais.
- [x] Strings CC-BY VERBATIM presentes e idênticas às constantes in-app.
- [x] grep confirma ambas em README.md e casam com `ReaderXrefPanel.tsx:30` / `ReaderStudyPanel.tsx:76`.
- [x] Nenhuma mudança de código; the-light em `225b8c9`; sem segredo; guardas verdes.

## Saída da verificação (real)

```
XREF_OK
STEP_OK
XREF_SRC_OK
STEP_SRC_OK
# fonte da verdade (idêntica, byte-a-byte):
README.md:79 | ... `Credit it to 'STEP Bible' linked to www.STEPBible.org (data based on work at Tyndale House, Cambridge; CC BY 4.0)` |
ReaderStudyPanel.tsx:76:  "Credit it to 'STEP Bible' linked to www.STEPBible.org (data based on work at Tyndale House, Cambridge; CC BY 4.0)";
ReaderXrefPanel.tsx:30:export const XREF_ATTRIBUTION = 'Cross references courtesy of OpenBible.info (CC-BY)';

$ cd app && npx tsc --noEmit
tsc exit: 0

$ git diff-tree --name-only -r 838cf45  ->  README.md   (único arquivo)
$ the-light HEAD  ->  225b8c929cf388e29dc148fec3975bf05a884b07
$ git status --short  ->  ' M .claude/settings.json' (não commitado no 838cf45)
$ README secret scan  ->  NO_SECRETS
```

## Achados de acurácia (feature-by-feature, adversarial)

- **Idiomas PT/EN** — VERDADEIRO. `app/lib/i18n.ts:35` `type Locale = 'pt' | 'en'`. README não alega mais idiomas.
- **Temas light/dark** — VERDADEIRO. `themePalettes.ts:103` `PALETTES: Record<'light' | 'dark', ...>`. Nenhuma menção a sepia/outros.
- **Provedores IA Claude/GPT/Gemini** — VERDADEIRO. `keystore.ts:20` suporta `['anthropic','openai','gemini','ollama']` → anthropic=Claude, openai=GPT, gemini=Gemini, todos existem. Compare multi-provider real (`ReaderComparePanel.tsx`, scripts `test:web:compare`/`test:web:ai-multiprovider`).
  - *Observação (não-falha):* o código suporta um 4º provedor, `ollama` (modelos locais), que o README omite. Isso é uma sub-alegação (menciona menos do que existe), não uma alegação falsa — nenhum recurso inexistente foi prometido.
- **Sync Google-Drive opt-in/OFF por padrão** — VERDADEIRO e preciso. `syncPrefs.ts:3-5,34,49-51` "default OFF", "ZERO rede/conta sem ação explícita"; README diz "opt-in and OFF by default … fully functional offline without it". Casa com F5.26.
- **Anti-alucinação / citation em todos os alvos** — VERDADEIRO. Camada existe: `the-light/crates/the-light-core/src/ai/citation.rs`; `core/src/lib.rs` delega a `ai::citation::rewrite_anchors` e afirma rodar em "todos os alvos". Léxico verificado vem do store local.
- **Offline-first + BYOK (keys em secure storage)** — VERDADEIRO. `keystore.ts` guarda no cofre seguro (Keychain/Keystore/vault web); README idem.
- **Comandos de build reais** — CONFEREM: `scripts/gen-bible-db.sh`, `gen-bindings-web.sh`, `gen-bindings-ios.sh`, `gen-bindings-android.sh`, `run-ios-selftest.sh`, `run-android-selftest.sh` existem; `npm run web/ios/android` e `test:web:reading` existem em `app/package.json`.
- **Versão concreta "Expo SDK 56"** — VERDADEIRO. `app/package.json` `expo: ~56.0.0`.
- **Fontes de conteúdo (KJV scrollmapper, Almeida 1911, TSK/OpenBible.info, léxico STEP)** — CONFEREM com `scripts/gen-bible-db.sh`.
- **Outras features spot-check** — parallel/side-by-side (`ReaderParallelView.tsx`), FTS5 search, planos+reminders, léxico Strong's, export SBL/Markdown: todos têm código/scripts correspondentes.

## Julgamento do idioma

README em inglês enquanto outros docs de topo são PT. A tarefa NÃO exigiu PT; para um README
público é convenção razoável e as strings de atribuição são em inglês (casando byte-a-byte).
Não é motivo de falha — registrado apenas como observação.

## Decisões / ADRs

Nenhuma. Documentação pura; a tabela de atribuição do README passa a ser a fonte única (dobra o NOTICE).

## Bloqueios / perguntas

Nenhuma.

---
id: F6.2
status: passed
commit: 16e998f
reviewer: reviewer-subagent
finished_at: 2026-07-04
---

## Resumo

Harness de smoke em browser REAL (F6.1) estendido com os 6 fluxos críticos que só
tinham cobertura headless/in-memory (OPFS, DOM, download/upload via Blob/`<input>`,
rede de IA). Re-rodei os gates de forma independente: `tsc --noEmit` = 0 e
`test:web:smoke` VERDE em dev E dist (exit 0), com os 7 marcadores `TLA_WEB_*`.
Só código de teste/harness mudou (commit toca apenas `app/web/__browser__/*`);
produto e `the-light` (225b8c9) intocados; sem segredo (chave DUMMY). **passed.**

## Critério de aceite (verificado contra o estado real)

- [x] `assertions.mjs` cobre os 6 fluxos + abrir-capítulo; `smoke-dev` E `smoke-dist`
  verdes com todos os `TLA_WEB_*`. → 7 marcadores × 2 alvos, todos `ok`, exit 0.
- [x] Notas e planos provados persistindo em **reload real** (OPFS); caso anônimo
  gracioso. → `[notes] ...persistiram no reload real (OPFS)`; anônimo `GRACIOSO="persisted"`
  (sem hang); `[plans] ...persistiram no reload real (completed=1/365)`.
- [x] Export→download→import round-trip no browser (Blob + `<input>` via CDP/fileChooser).
  → `notes 1→1, highlights 1→1`. Merge idempotente (`0 applied`) porque o store já tem
  os Records; round-trip provado pela re-exportação com contagens intactas.
- [x] IA-reachability registra o estado real por provedor sem travar (ver abaixo).
- [x] Determinístico (userDataDir limpo `mkdtemp`, `waitForFunction`, sem órfãos);
  Chrome-ausente → vermelho (`chrome.mjs` sai 1 se ausente; skip só com
  `SMOKE_SKIP_IF_NO_CHROME=1`, que o loop nunca seta).
- [x] `tsc --noEmit` 0; nenhum `test:web:*` regride; the-light `225b8c9`; sem chave
  real (grep de segredos → NONE; `DUMMY_KEY='tla-smoke-DUMMY-not-a-real-key-...'`).

## Saída da verificação (real)

`npx tsc --noEmit` → EXIT=0.

`npm run test:web:smoke` → `SMOKE_EXIT=0`. Marcadores (idênticos em dev@:8099 e dist@:8100):

```
TLA_WEB_open-chapter ok
  [parallel] KJV + Almeida renderizados lado a lado (2º getChapter real)
TLA_WEB_parallel ok
  [search] "God" → 10 hits ; João 3:16 encontrado; 20 referências cruzadas renderizadas
TLA_WEB_search-xref ok
  [notes] nota+marcação persistiram no reload real (OPFS); anônimo GRACIOSO="persisted"
TLA_WEB_notes-persist ok
  [plans] plano iniciado + 1 dia marcado persistiram (completed=1/365)
TLA_WEB_plans-persist ok
  [export] backup via Blob/CDP: notes=1, highlights=1 ; round-trip notes 1→1, highlights 1→1
TLA_WEB_export-import ok
TLA_WEB_ai-reachability ok
==> SMOKE OK (target=dev)  /  ==> SMOKE OK (target=dist)
```

Pós-run: nenhum listener em 8099/8100, 0 procs órfãos (chrome/target), 0 temp
`tla-smoke*` remanescente. O `:8081` (expo do usuário) segue de pé — não tocado.

## Verificação adversarial (5 checagens)

1. **Fluxos exercitam o caminho real, sem atalho.** Confirmado por leitura +
   asserção de efeito downstream: `parallel-toggle` é um `Pressable` real com
   `onPress=setParallel` e o fluxo assevera o texto Almeida VERBATIM junto do KJV;
   busca digita queries reais e abre 20 xrefs; notas gravam e o reload re-lê o
   marcador `✎` do OPFS; planos avançam a `completed=1`; export usa CDP
   `setDownloadBehavior` e re-injeta o Blob via `waitForFileChooser`/`accept`.
   Esperas usam `waitForFunction` (o `sleep()` só aparece em loops de polling com
   `deadline` — não como espera-de-render fixa). Timeout = a própria falha "hang".
2. **Reachability de IA registrada corretamente (âncora F6.8).** Só reprova em hang
   (`term.hung`) ou swallow silencioso (`!netDone && !visible`); CORS-wall gera
   `requestfailed` → `netDone=true` → NÃO reprova. Chave DUMMY, sem segredo real.
3. **Bug de produto real, corretamente NÃO corrigido.** `userdata-opfs.web.ts`
   L37-38: `getDirectoryHandle(OPFS_ROOT_DIR, {create:false})` e `(OPFS_USERDATA_DIR,
   {create:false})` estão FORA do `try/catch` (só o loop de segmentos L42-49 é
   guardado). `readFile` (L67-79) depende de `resolveParent(...,false)` devolver
   `null` p/ o desfecho gracioso "ausente → null", mas num OPFS fresco (root/userdata
   ainda inexistentes) L37/38 LANÇAM `NotFoundError`. O executor NÃO tocou produto —
   contornou no TESTE exercitando o import no contexto PRINCIPAL que já tem userdata
   gravado (merge idempotente). É um bug real de import "fresh-device" → follow-up.
4. **Determinismo / no-false-green.** `userDataDir` via `mkdtemp` por run;
   `finalize` (trap SIGINT/SIGTERM + fim/erro) faz teardown; 0 órfãos pós-run
   confirmado. O `el.click()` sintético (para evitar o hang de `page.click` no
   `protocolTimeout` em Pressables RNW) é escolha de harness razoável e AUTO-VALIDADA:
   cada fluxo assevera um efeito real downstream (texto paralelo, `✎`, `completed=1`,
   arquivo de export, requests de rede da IA) — um clique no-op reprovaria. Chrome-
   ausente → vermelho herdado da F6.1.
5. **Não negociáveis.** Commit `16e998f` toca só `app/web/__browser__/{assertions,
   smoke.browser}.mjs`; produto/gerado intocado; the-light `225b8c9`;
   `.claude/settings.json` NÃO commitado (só M no working tree, pré-existente);
   sem segredo.

## IA reachability — estado por provedor (âncora de regressão p/ F6.8)

Idêntico em dev e dist, com chave DUMMY, desfecho VISÍVEL, sem hang:

- **anthropic** = `reached provider at network (HTTP 400) but CORS-blocked from JS
  (net::ERR_FAILED)` — parede CORS/R7 ESPERADA até a F6.8. Registrada, não reprovada.
- **openai** = `reached provider (HTTP 200/401) — CORS ok`.
- **gemini** = `reached provider (HTTP 200/400) — CORS ok`.

## Decisões / ADRs

Nenhuma. Tarefa de teste/harness; sem decisão de arquitetura.

## Bloqueios / perguntas

Nenhum bloqueio p/ a F6.2 (aceite cumprido, verificação verde).

**Follow-up recomendado (bug de produto real, fora do escopo desta tarefa):**
import "fresh-device" em OPFS vazio quebra por `NotFoundError` — `userdata-opfs.web.ts`
`resolveParent` L37-38 chama `getDirectoryHandle(..., {create:false})` sem guarda,
enquanto `readFile`/`deleteFile` assumem retorno `null` gracioso. O round-trip da
F6.2 só cobre o import sobre store JÁ populado (contorno no teste). Sugiro nova
tarefa (ex.: F6.x) para guardar L37-38 (retornar `null` em ausência quando
`create=false`, como o loop de segmentos já faz) e o smoke exercitar import em
`BrowserContext` anônimo/OPFS vazio para provar o caminho fresh-device.

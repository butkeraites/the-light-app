---
id: F5.20
status: passed
commit: b8538ce
reviewer: reviewer
finished_at: 2026-07-03
---

## Resumo

F5.20 fecha a varredura de a11y dos elementos INTERATIVOS (Pressable/Touchable*/
Switch/TextInput/Link) das telas e painéis e a transforma em uma GUARDA headless
determinística (`web/__tests__/a11y-scan.test.mjs` + script `test:a11y-scan`,
molde F5.16/F5.18 — Node puro, sem device/rede/chave). A guarda varre 61
interativos em 22 arquivos e verifica, por tag, três coisas: (1) papel
(`accessibilityRole` obrigatório só p/ touchable genérico), (2) rótulo
(`accessibilityLabel`/`placeholder`/texto-filho), (3) alvo de toque (hitSlop/
flex/tamanho; dim fixa <44 sem hitSlop reprova). As correções mínimas
não-visuais fecham as poucas lacunas reais (6 backdrops de modal, 6 botões Close,
swatch 34×34, chips, toggle/preset, ReaderVersionPicker, atribuição xref, readLink
da home) — todas via hitSlop/padding/label, nenhum redesenho. Inclui o follow-up
da F5.18 (ADR-0048): o "—" de versículo ausente foi promovido de `faint`
(1.61:1) para `muted` (AA), restringindo `faint` ao chevron decorativo.

Reproduzi TODOS os gates de forma independente (tsc, a11y-scan, contrast,
i18n-coverage, reading/search/notes, expo export web) e PROVEI a guarda com duas
injeções de violação (role e size), ambas pegas com arquivo:linha+motivo e
revertidas. Decisão do em-dash julgada correta. Veredito: **passed**.

## Critério de aceite

- [x] **Todo interativo tem role + label + alvo ≥44 (ou hitSlop).** A guarda é o
  mecanismo objetivo: `test:a11y-scan` varre 61 interativos (Pressable×49,
  TextInput×8, Switch×1, Link×3) em 22 arquivos com 0 violações. Spot-check dos
  alvos: swatch 34×34 recebeu hitSlop `{top:8,bottom:8,left:5,right:5}` (→50×44
  real, não fake); botões Close hitSlop 12; chips (paddingVertical 6–7) + hitSlop
  `{top:8,bottom:8}` (→~46–48 real). Fixes honestos.
- [x] **Guarda headless verde.** `test:a11y-scan` exit 0 (self-test embutido +
  varredura real). Confirmei o script em `package.json`.
- [x] **Labels via t()/store; tsc 0; expo export web 0; core intacto.** tsc 0;
  expo export exit 0; the-light @ `2fc2dab` limpo; 0 arquivos `core/` no commit.
  Labels de cromo via `t()` (`ai.close`/`common.close`/`versePanel.highlightWith`/
  `a11y.reminderTime` — todas existem em PT+EN e são typechecked pelo union
  `TranslationKey`); labels de conteúdo do store como dado
  (`${t.abbrev} ${t.language}` no version picker, `XREF_ATTRIBUTION` verbatim) —
  anti-alucinação preservada.
- [x] **Guarda FALHA se faltar role/label ou alvo insuficiente (PROVADO).**
  Probe 1 (removi `accessibilityRole` do Pressable de `ReaderSearchResultItem`):
  FALHA `components/ReaderSearchResultItem.tsx:54 <Pressable> [role] sem
  accessibilityRole`, exit 1. Probe 2 (adicionei `height:34` sem hitSlop ao estilo
  `row`): FALHA `components/ReaderSearchResultItem.tsx:54 <Pressable> [size]
  height:34 < 44 sem hitSlop`, exit 1. Ambas revertidas → guarda exit 0, `git
  status` de `app/` limpo. Política por tag SÓLIDA (não loophole): touchable
  genérico exige role (renderiza View sem papel), enquanto TextInput(textbox)/
  Switch(switch)/Link(link) são corretamente ISENTOS do role (papel nativo
  implícito — forçar role quebraria a semântica).
- [x] **Follow-up F5.18 registrado (em-dash faint→muted), decisão correta.** Ver
  veredito abaixo; ADR-0048 registra com precisão.

### Prova da guarda (probe-and-revert)

A guarda genuinamente pega violações reais, não é rubber-stamp:
- **[role]** `<Pressable>` genérico sem `accessibilityRole` → reprovado com
  arquivo:linha. TextInput/Switch/Link isentos (papel implícito) — verificado no
  código-fonte da guarda (`roleRequired = GENERIC_TOUCHABLES.includes(tag)`), não
  é brecha que deixe passar gap real.
- **[size]** dim fixa numérica <44 sem hitSlop → reprovado. `hitSlop`/`flex`-fill
  contam como afordância. Limitação honesta disclosed no ADR: não mede px em
  runtime — exige afordância explícita. Fixes reais (hitSlop de verdade), não
  afordância declarada-mas-falsa.

### Veredito do follow-up F5.18 (em-dash `—`)

CORRETO promover, não manter a exclusão:
- (a) O `—` é renderizado em `ReaderParallelView` quando `primaryText == null` /
  `secondaryText == null` — sinaliza que a passagem NÃO EXISTE naquela tradução →
  CONVEY informação → NÃO-decorativo → deve cumprir contraste. Promover é o certo.
- (b) `faint` (#cccccc/#555555) fica usado APENAS no chevron `›` da
  `ReaderBookList`, dentro de um `Pressable` já rotulado (`accessibilityRole=
  "button"` + `accessibilityLabel={t('a11y.openBook',…)}`) → afordância
  redundante/decorativa → a exclusão 1.4.11 do ADR-0046 agora é precisa.
- (c) NENHUM token mudou de valor: `themePalettes.ts` não está no commit; só o
  estilo `missing` troca `colors.faint` → `colors.muted`. `muted` é AA
  (light #6b6b6b 5.33:1, dark #9a9a9a 6.75:1); `faint` inalterado.
- ADR-0048 registra tudo isso corretamente (número livre, header único).

## Saída da verificação

```
$ git -C the-light rev-parse HEAD        → 2fc2dab… (limpo, git status vazio)
$ git show --name-only b8538ce           → 14 arquivos; 0 em core/; 0 .claude/*

$ npx tsc --noEmit                        → TSC_EXIT=0

$ npm run test:a11y-scan
PASS — GUARDA de a11y de elementos INTERATIVOS (lint-like determinístico, sem device/rede):
  varreu 61 interativos em 22 arquivos (Pressable×49, TextInput×8, Switch×1, Link×3)
  (1) role … (2) label … (3) alvo de toque … (4) self-test …            → exit 0

$ npm run test:web:contrast
PASS — GUARDA de contraste WCAG AA … muted/bg 5.33:1 (#6b6b6b) …
  decorativos (faint/divider/border): 6 reportados, não bloqueiam (1.4.11)   → exit 0

$ npm run test:i18n-coverage
PASS … (1) paridade pt↔en: 162 chaves … (2) cromo … (3) tema … (4) self-test  → exit 0

$ node web/__tests__/reading.web.test.mjs → exit 0
$ node web/__tests__/search.web.test.mjs  → exit 0
$ node web/__tests__/notes.web.test.mjs   → exit 0

$ npx expo export --platform web          → Exported: dist   EXPO_EXIT=0

# PROBE 1 (role): remover accessibilityRole do Pressable de ReaderSearchResultItem
$ npm run test:a11y-scan
FAIL — GUARDA a11y: components/ReaderSearchResultItem.tsx:54 <Pressable> [role] sem accessibilityRole   → exit 1
$ git checkout -- … ; npm run test:a11y-scan → exit 0

# PROBE 2 (size): height:34 sem hitSlop no estilo `row`
$ npm run test:a11y-scan
FAIL — GUARDA a11y: components/ReaderSearchResultItem.tsx:54 <Pressable> [size] height:34 < 44 sem hitSlop → exit 1
$ git checkout -- … ; npm run test:a11y-scan → exit 0 ; git status app/ limpo

# eslint: sem config (ADR-0038, N/A no repo) — gate de qualidade TS é tsc + guardas lint-like
$ npx eslint .  → "ESLint couldn't find an eslint.config file" (condição pré-existente, não regressão)
```

## Decisões / ADRs

- **ADR-0048** (aceito, 2026-07-03) — varredura de a11y dos interativos como guarda
  headless + follow-up F5.18 (em-dash `—` de versículo ausente `faint`→`muted`).
  Número livre, header único, sem tocar `the-light`/`core`. Registra a política
  por tag, a limitação honesta do alvo ≥44 (afordância explícita, não px runtime)
  e a restrição de `faint` ao decorativo — tudo confere com o código.
- eslint permanece N/A por ADR-0038 (sem config); o gate de qualidade TS deste
  repo é `tsc --noEmit` + guardas lint-like determinísticas (i18n-coverage,
  contrast, a11y-scan), todas verdes.

## Bloqueios / perguntas

Nenhuma. Todos os critérios de aceite cumpridos e verificados de forma
independente; a guarda comprovadamente pega violações reais (role e size) e a
decisão do em-dash está correta. Probes revertidas — árvore de `app/` limpa,
HEAD em `b8538ce`.

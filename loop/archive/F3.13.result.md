---
id: F3.13
status: approved
gate: true
reviewer: humano (sign-off)
finished_at: 2026-07-02
---

# F3.13 — Marco 3: APROVADO (sign-off humano)

## Resumo
Sign-off do **Marco 3 — estudo profundo assistido por IA, multiplataforma**. Fase 3
(F3.1–F3.12b) aceita e verificada; validada com IA real (F3.10). Encerra o roteiro
planejado das Fases 0–3.

## Decisões do sign-off
1. **Fase 3 APROVADA.**
2. **Próxima direção: Polimento da IA** — streaming web real (token-a-token via fetch),
   comparação multi-IA de conteúdo real (chaves), Tavily BYOK, mais provedores. A decompor
   em `backlog/PHASE-4.md`.

## Capacidades aceitas
Léxico Strong (STEP CC-BY) · estudo profundo (modos×lentes×profundidades) · conversa/
refinamento · comparação multi-IA · export acadêmico SBL · pesquisa web (Wikipedia keyless
opt-in) — nativo + web (zero drift), com validação real (Gemini + Wikipedia; 26 citações de
fontes locais/verificadas; WARNINGS vazio).

## Regras não negociáveis — mantidas
Anti-alucinação zero-drift (mesma impl Rust ai-pure nativo↔web, `assert_eq!`); BYOK (chave
nunca em git/log); offline-first (IA/pesquisa opt-in); `the-light` só via PR+ADR (3 PRs
sancionados em `04b9b24`, nunca forkado; wasm puro); STEP/OpenBible CC-BY + domínio público.
ADRs 0026–0032.

## Consequência
`loop/HALT` removido; loop retomado. O `planner` decompõe o **Polimento da IA** (Fase 4) e
semeia a 1ª tarefa.

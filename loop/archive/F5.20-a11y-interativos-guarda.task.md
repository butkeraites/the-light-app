---
id: F5.20
title: Varredura de acessibilidade de elementos interativos (labels/roles/alvos de toque) + guarda headless
phase: 5
status: passed
gate: false
depends_on: [F5.16]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-03
---

## Objetivo

Garantir que TODO elemento interativo (Pressable/TouchableOpacity/Link) das telas e componentes tenha accessibilityRole + accessibilityLabel (via t()) e alvo de toque adequado (≥44x44 ou hitSlop), e adicionar uma guarda headless que falha se algum interativo ficar sem esses atributos.

> **Herda F5.16 (i18n cobertura + guarda) + F5.18 (contraste).** Varredura de a11y dos elementos
> INTERATIVOS: garante `accessibilityRole`+`accessibilityLabel` e alvo de toque adequado (≥44x44
> iOS / 48 Android) em todo touchable/botão/switch/campo; vira uma GUARDA headless (molde F5.16/F5.18).
> **Inclui o follow-up da F5.18:** reconsiderar o token `faint` no em-dash de "versículo ausente"
> (1.61:1) — decidir se promove a ≥3:1 (não-decorativo) ou mantém a exclusão com justificativa.

## Contexto / arquivos

_Workstream:_ Acessibilidade + i18n + temas · _Depende de:_ F5.16 (aceita)

- `app/app/index.tsx`
- `app/app/search/index.tsx`
- `app/app/read/[book]/[chapter].tsx`
- `app/components/ReaderChapterView.tsx`
- `app/components/ReaderSearchResultItem.tsx`
- `app/components/ReaderVersePanel.tsx`
- `app/components/ReaderVersionPicker.tsx`

## Passos sugeridos

1. Auditar todos os interativos (grep por Pressable/TouchableOpacity/Link em app/app e app/components), preencher accessibilityRole + accessibilityLabel (t()) e garantir hitSlop/tamanho mínimo (≥44) onde faltar — priorizar os de menor cobertura hoje (ReaderChapterView, ReaderSearchResultItem, home).
2. Criar app/web/__tests__/a11y-scan.test.mjs (scanner de fonte determinístico): para cada `<Pressable`/`<TouchableOpacity`/`<Link` nos arquivos de app/components e app/app, falhar se não houver accessibilityRole e (accessibilityLabel OU texto filho) dentro do bloco do elemento; reportar arquivo:linha dos faltantes.
3. Adicionar script `test:a11y-scan` em app/package.json.
4. Gates: tsc/eslint/expo export web verdes; a guarda passa.

## Critério de aceite

- [ ] Todo interativo tem accessibilityRole + accessibilityLabel e alvo de toque ≥44 (ou hitSlop equivalente).
- [ ] Guarda headless (test:a11y-scan) verde (nenhum interativo sem role/label).
- [ ] Labels vêm de t() (traduzíveis); tsc 0, eslint 0, expo export web exit 0; core intacto.
- [ ] **Guarda de a11y** headless: FALHA se um elemento interativo ficar sem `accessibilityRole`/label ou com alvo de toque insuficiente.
- [ ] **Follow-up F5.18:** decisão registrada sobre o `faint` no em-dash de versículo ausente (promover a ≥3:1 ou manter exclusão documentada).

## Comando(s) de verificação

```sh
set -e; cd /Users/butkeraites/Documents/the-light-app/app
node web/__tests__/a11y-scan.test.mjs
npx tsc --noEmit && npx eslint . && npx expo export --platform web
```

## Restrições

Depende de i18n-5 porque os labels são fornecidos via t() (já migrados). Scanner é lint-like determinístico (sem device). Complementa a11y-contrast (cor) e a11y-dynamic-focus (foco/escala).

- **Offline-first:** varredura/guarda local; nada exige rede.
- **Anti-alucinação:** só a11y de cromo — não tocar texto bíblico; labels de conteúdo do store (nome de livro/ref) via seleção de campo, não `t()` (mas PODEM ganhar a11y label de cromo).
- **the-light só via PR+ADR:** `the-light-core` pinado (`2fc2dab`); app-side.
- **Qualidade:** `tsc --noEmit`; varredura HEADLESS determinística que garante que TODO elemento interativo tem `accessibilityRole`+label e alvo de toque adequado (≥44x44 iOS / 48 Android); vira guarda (falha se um interativo ficar sem role/label); self-tests sem regressão.

---
id: F5.11
status: passed
commit: 8b8cd74
reviewer: reviewer
finished_at: 2026-07-03
---

## Resumo

F5.11 migra para `t()` o CROMO (títulos, botões, estados, dicas/placeholders,
badges, `sourceLabel`, labels de a11y) dos 4 painéis de IA (Ask/Chat/Compare/
Study) e localiza o `sourceLabel` deferido na F5.5, estendendo a camada i18n da
F5.2 (ADR-0038). Verifiquei independentemente: re-rodei todos os gates (verdes),
inspecionei o diff dos 7 arquivos e escrutinei a fronteira anti-alucinação. A LEI
está preservada: nenhum conteúdo bíblico (versículo citado, interpretação do
modelo, `reference` canônica, atribuições CC-BY) passa por `t()`; só cromo e
dados técnicos INTERPOLADOS (`{source}`/`{provider}`/`{model}`/`{count}`/`{tokens}`).
Decisão: **passed**.

## Critério de aceite

- [x] **Strings e accessibilityLabel dos 4 painéis via t(); alternam PT⇄EN.**
  Grep confirma migração: `t('ask.title'|'chat.title'|'compare.title'|'study.title')`,
  botões (`ask.submit`/`chat.send`/`compare.submit`/`study.submit`), estados/dicas
  e a11y (`a11y.providerWithKey`/`a11y.byokKey`/`a11y.questionField`/`a11y.chatField`/
  `a11y.compareField`/`a11y.tavilyKey`). `test:i18n` prova divergência observável:
  `ask.submit` Perguntar↔Ask, `study.submit` Estudar↔Study, `ai.interpTitle` PT≠EN.
  Nenhum literal PT/acentuado remanescente FORA de comentário nos 5 arquivos (grep
  varreu `[áàâãéêíóôõúç…]`: todos os hits são `//`/`/* */`/JSX-comment).
- [x] **Blocos de citação/interpretação separados; cited_text do store inalterado.**
  Conteúdo renderiza VERBATIM e direto (nunca via `t()`): Ask `{answer.citedText}`/
  `{interpretationText}`; Chat `{answer.citedText}`/`{turn.content}`; Compare
  `{anchorText}`/`{col.answer.interpretation}`; Study `{result.passageText}`/
  `{result.interpretation}`. Só o RÓTULO (`ai.citedTitle`/`ai.interpTitle`) é cromo.
  Único `t()` que toca `interpretation` é `t('ask.estimate',{tokens: approxTokens(...)})`
  — interpola a CONTAGEM (número), não o texto. Os 6 self-tests de IA seguem
  logando `cited_text = João 3:16 KJV verbatim` e `interpretation = MOCK` — zero drift.
- [x] **Atribuições (STEP CC-BY) visíveis e com identificador de licença VERBATIM.**
  `STEP_ATTRIBUTION`/`attributionLines` renderizadas direto (`{s}`), nunca via `t()`;
  citações do estudo exibem `c.license`/`c.attribution`/`c.kind`/`c.key`/`c.title`
  verbatim do retorno — só o título "Citações"/"Léxico" é cromo. Nenhuma chave de
  namespace de conteúdo bíblico/licença no catálogo (guard anti-alucinação do
  `test:i18n` cobre todas as 130 chaves como CROMO).
- [x] **Nenhuma string PT hardcoded; tsc 0, eslint 0, expo export web exit 0; core intacto.**
  `tsc --noEmit` exit 0; `test:i18n` PASS (130 chaves, paridade pt↔en); os 6 self-tests
  de IA PASS sem regressão; `expo export --platform web` exit 0. `eslint` = N/A (repo
  sem `eslint.config.*`/`.eslintrc*` e sem dep de eslint — `npx` pega um global 10.6.0
  que aborta por falta de config; precedente estabelecido em F5.2/F5.5/F5.7/F5.8,
  ADR-0038; gate honesto = tsc + testes node + expo export). `the-light` intacto em
  `2fc2dab`.

## Extras verificados (fora do checklist, exigidos pela tarefa)

- **`reference` canônica permanece EN:** Ask (L471) e Compare (L532) mantêm
  `${bookNameEn(bookNumber)} …` inalterados; só o `sourceLabel` mudou para `bookLabel`.
- **`bookLabel` seleciona campo do STORE, não traduz:** `listBooks().find(...)` →
  `locale==='en' ? b.nameEn : b.namePt`; fallback `t('read.bookFallback',{number})`
  é cromo (livro ausente do store). `read.bookFallback` já existia (F5.5).
- **Study Option refactor:** `label` → `labelKey: MessageKey`; `value` (enum da
  fronteira) e `key`/testID inalterados (`study-mode-${o.key}`/`study-lens-${o.key}`/
  `study-depth-${o.key}`/`study-web-research-${o.key}`). `test:web:study` PASS drives
  os mesmos modos (2 seções/22 citações). Fronteira segue fonte única.
- **Escopo:** commit tem exatamente os 7 arquivos, todos sob `app/` (sem `core/`,
  sem `.claude/settings.json`). `ReaderVersePanel`/xref sourceLabel deferidos p/
  F5.16 — buraco esperado, não desta tarefa.

## Saída da verificação (real)

```
$ npx tsc --noEmit         → tsc exit=0
$ npm run test:i18n        → PASS — paridade pt↔en 130 chaves; ask.submit/study.submit/
    ai.interpTitle DIVERGEM PT≠EN; ask.title/ai.meta/compare.consistencyOk interpolam
    {source}/{provider}/{model}/{count} VERBATIM; anti-alucinação: 130 chaves são CROMO
$ npm run test:web:ai              → PASS (cited_text = João 3:16 KJV verbatim; interpretation = MOCK)
$ npm run test:web:ai-stream       → PASS (cited_text do store inalterado; tokens só da interpretação)
$ npm run test:web:ai-multiprovider→ PASS (anthropic/openai/ollama; cited_text separado, verbatim)
$ npm run test:web:study           → PASS (passageText = João 3:16 KJV do store; interpretation = MOCK)
$ npm run test:web:compare         → PASS (cited_match=true, mesma âncora do store)
$ npm run test:web:session         → PASS (citedText do store; interpretation = MOCK; 3 turnos)
$ npx eslint .                     → N/A (ESLint 10.6.0: "couldn't find an eslint.config file"; repo sem config/dep — ADR-0038)
$ npx expo export --platform web   → expo exit=0 (Exported: dist)
$ git -C ../the-light rev-parse HEAD → 2fc2dab (limpo)
$ git show --name-only 8b8cd74     → 7 arquivos, todos app/; sem core/; sem settings.json
```

## Decisões / ADRs

Sem ADR novo. Estende F5.2/ADR-0038 (camada i18n; eslint N/A neste repo). Nenhum
arquivo de ADR tocado no commit. Confirmado que o executor não introduziu ADR.

## Bloqueios / perguntas

Nenhuma.

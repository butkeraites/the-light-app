---
id: F5.33
status: passed
commit: 6f8186f
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

Seletor tri-estado de tema (claro / escuro / seguir o sistema) entregue como
`ThemeModeSelector` (3 chips segmentados no header), aposentando o
`ThemeToggleButton` binário. Surfa a capacidade já existente (`setMode`/`isSystem`
da ADR-0043) sem criar 2ª camada de persistência. Todas as 7 verificações
re-executadas de forma independente a partir de `app/` passaram. the-light
intocado (`225b8c9`), commit `6f8186f` toca só product/i18n/test. VERDICT: PASSED.

## Critério de aceite (verificado)

- [x] Controle no header deixa escolher claro/escuro/sistema; "sistema" chama
      `setMode(null)`. Rastreado: `ThemeModeSelector.tsx:40` `onPress={() => setMode(option.value)}`
      com `OPTIONS` = `light` / `dark` / `null` (linhas 20-22). `theme.ts:91-104`:
      `setMode(next)` com `next == null` → `removePref(THEME_PREF_KEY)` (limpa
      `tla.pref.theme.mode`) → `isSystem = override === null` (linha 111) → base volta a
      `useColorScheme()`. Cada escolha persiste via o MESMO KV da F5.2 (re-hidratação no
      boot, `theme.ts:73-88`) — sobrevive a reinício. Não é cosmético.
- [x] Reflete o estado real: `active = option.value === null ? isSystem : !isSystem && mode === option.value`
      (`ThemeModeSelector.tsx:35`) — sistema ativo quando não há override; senão casa o modo
      efetivo. Nativo+web: header comum via `_layout.tsx:41-47` (`HeaderControls` → `ThemeModeSelector`).
      Cromo 100% via `t()` (glyph decorativo; label = `t(labelKey)`), paridade pt/en.
- [x] Gates verdes (saídas abaixo): `tsc` 0; `test:web:theme`/`test:i18n`/`test:i18n-coverage`/
      `test:a11y-scan`/`test:web:firstpaint` PASS; `expo export --platform web` OK.
- [x] a11y por chip: `accessibilityRole="button"` + `accessibilityState={{selected: active}}` +
      `accessibilityLabel={t(option.labelKey)}` + `hitSlop` (`ThemeModeSelector.tsx:42-45`).
      the-light intocado (`225b8c9`, working tree limpo); sem segredo/rede/console.

## Saída da verificação (real, re-executada de app/)

```
===== tsc =====                TSC_OK
===== test:web:theme =====     PASS — persistência do MODO DE TEMA no KV de prefs OFFLINE
  removePref volta a seguir o esquema do sistema; ... chave namespaceada tla.pref.theme.mode
  (theme.test.mjs:101-103: removePref → getPref==null → store SEM 'tla.pref.theme.mode')
===== test:i18n =====          PASS — paridade de catálogo pt↔en: 204 chaves, mesmos conjuntos (sem órfã)
===== test:i18n-coverage ===== PASS — (1) paridade pt↔en: 204 chaves; (3) 23 arquivos sem hex hardcoded
===== test:a11y-scan =====     PASS — varreu 71 interativos (Pressable×57, TextInput×9, Switch×2, Link×3)
===== test:web:firstpaint ===== PASS — Stack montadas síncrono; ensureWasmReady() 1x (stub renomeado p/ ThemeModeSelector)
===== expo export web =====    EXPO_OK
```

## Achados dos 6 checks adversariais

1. **Tri-state funciona (não cosmético):** confirmado o traço `setMode(null)` →
   `removePref(THEME_PREF_KEY)` → limpa `tla.pref.theme.mode` → `isSystem` → `useColorScheme`
   (`theme.ts:91-104,111`). O chip ativo deriva de `isSystem`/`mode`, refletindo estado real.
2. **Sem 2ª camada de persistência:** reusa `ThemeProvider.setMode` + prefs KV da F5.2
   (`getPref/removePref/setPref` de `./prefs`, chave `THEME_PREF_KEY`). Nenhum novo mecanismo
   de storage. Cada escolha (incl. "sistema") sobrevive a reinício pela re-hidratação existente.
3. **Aposentadoria limpa do ThemeToggleButton:** arquivo DELETADO (−37 linhas no stat). Grep no
   repo INTEIRO (app+tests+stubs, excl. node_modules/dist): ÚNICA ocorrência restante é um
   COMENTÁRIO histórico em `ThemeModeSelector.tsx:6` — nenhum import/referência de código.
   `_layout.tsx` renderiza o novo seletor; `LanguageToggleButton` só mudou comentário
   (diff confirmado: renomeia menção, zero mudança funcional). tsc/build/a11y-scan verdes.
4. **Paridade i18n limpa:** `theme.switchToLight/switchToDark` REMOVIDAS de ambos catálogos
   (grep exit 1, zero ocorrências). Novas presentes em ambos: pt `i18n.ts:375-377`
   (Tema claro / Tema escuro / Seguir o sistema), en `603-605` (Light theme / Dark theme /
   Follow system), + união `MessageKey:130-132`. Paridade 204 (i18n + i18n-coverage). Sem órfã.
5. **a11y:** 3 chips como interativos próprios (role button + selected + label via `t()` +
   hitSlop). Cada chip anuncia o modo que define (label) e o estado selecionado; glyph decorativo
   não interfere. a11y-scan verde (71 interativos, sem falha de role/label/alvo de toque).
6. **Não negociáveis:** the-light `225b8c9` intocado (tree limpo). `.claude/settings.json` NÃO
   commitado (só modificado no working tree). `app/dist` gitignored (não commitado). Commit
   `6f8186f` = 7 arquivos, todos product/i18n/test; nada sob `core/`, `loop/` ou the-light.
   Sem rede/segredo/console no código novo.

## Decisões / ADRs

Nenhuma nova. Consome ADR-0043 (F5.14, `setMode`/`isSystem`/persistência) — UX escolhida:
3 chips segmentados no header (opção sugerida na tarefa). Sem 2ª camada de persistência.

## Bloqueios / perguntas

Nenhuma.

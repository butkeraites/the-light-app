---
id: F5.4
title: Fronteira: persistência nativa do progresso do plano (PlanStore fs) + host test round-trip
phase: 5
status: passed
gate: false
depends_on: [F5.1]
attempts: 0
created_by: planner
created_at: 2026-07-02
seeded_at: 2026-07-02
---

## Objetivo

Expose native reading-plan progress persistence through the frontier by delegating to the core's `PlanStore` (fs), reusing the same userdata data_dir convention as notes (F1.10). Native-only body (`cfg(not(target_arch="wasm32"))`) + web stub (web persistence = rp-4). Covers start/read/advance/clear of the single active plan (`reading-plans/active.json`).

> **Contexto native-first (herda F5.1):** a F5.1 expôs a GERAÇÃO de planos pela fronteira
> NATIVA (`#[cfg(not wasm32)]` + stub web), pois `userdata::plans` é `#[cfg(feature="embedded")]`
> no core `2fc2dab`. Esta tarefa (persistência via `PlanStore` fs) é **inerentemente nativa**
> (fs) → segue o MESMO padrão: impl nativa + stub web. A paridade web do progresso (OPFS) é
> a F5.10 (que também depende da PR `ai-pure` de planos ao core — gate à parte). NÃO reimplementar
> `PlanStore`/serialização em TS/Rust (drift).

## Contexto / arquivos

_Workstream:_ Planos de leitura · _Depende de:_ F5.1 (aceita)

- `/Users/butkeraites/Documents/the-light-app/core/src/lib.rs`
- `/Users/butkeraites/.cargo/git/checkouts/the-light-9eb8809a6d68281a/2fc2dab/crates/the-light-core/src/userdata/plans.rs`
- `/Users/butkeraites/.cargo/git/checkouts/the-light-9eb8809a6d68281a/2fc2dab/crates/the-light-core/src/userdata/mod.rs`
- `/Users/butkeraites/Documents/the-light-app/app/web/reading.ts`
- `/Users/butkeraites/Documents/the-light-app/app/web/reading.web.ts`

## Passos sugeridos

1. Add a pure Record `ReadingPlanProgress{ plan_id:String, start_date:String /*ISO YYYY-MM-DD*/, completed:u32 }` with `From<the_light_core::userdata::plans::PlanProgress>` mapping NaiveDate↔ISO String (pure Record so it exists on all targets, like Note/Highlight).
2. Add frontier fns with native body `#[cfg(not(target_arch="wasm32"))]` + web stub `#[cfg(target_arch="wasm32")]` (molde put_note/list_notes): `reading_plan_progress(data_dir:String) -> Option<ReadingPlanProgress>` via `PlanStore::new(<data_dir>/"reading-plans"/"active.json").load()`; `start_reading_plan(data_dir:String, plan_id:String, start_date:String) -> ReadingPlanProgress` (validate plan_id via `plans::plan_by_id`, save `PlanProgress{plan_id, start_date parsed, completed:0}`); `set_reading_plan_completed(data_dir:String, completed:u32) -> ReadingPlanProgress` (load-or-error, set completed, save); `clear_reading_plan(data_dir:String) -> bool` (PlanStore.clear).
3. Build the path EXACTLY as `<data_dir>/reading-plans/active.json` (mirror core `reading_plans_dir()` join) so native fs and the web OPFS mirror (rp-4) agree byte-for-byte on layout.
4. Web stubs return a clear CoreError/None (persistence web = rp-4), keeping the `userdata`/fs off the wasm graph (grafo wasm puro).
5. Regenerate native bindings (`./scripts/gen-bindings.sh`) + web bindings (`./scripts/gen-bindings-web.sh` — the fns appear but web body is the stub); add glue wrappers in `app/web/reading.ts` (native, delegate) and `app/web/reading.web.ts` (web, throw/stub for now).
6. Add deterministic host test on an ISOLATED temp dir (molde notes F1.10 host test, e.g. tempfile): no active plan → None; start('gospels', '2026-06-16') → completed=0 + a SECOND independent load reads it back; set_completed(5) persists; clear → true then false; unknown plan_id → CoreError.

## Critério de aceite

- [ ] start→load→set_completed→clear round-trips on an isolated temp data_dir via the core PlanStore (persisted across independent store handles).
- [ ] Path is `<data_dir>/reading-plans/active.json` (matches core layout; web mirror in rp-4 will read the same file).
- [ ] Unknown plan_id → CoreError (no panic); no-active-plan set_completed → CoreError.
- [ ] Web body is a stub; `userdata`/fs stays OUT of the wasm graph (grafo wasm puro).
- [ ] the-light NOT touched (2fc2dab); no network, no keys.

## Comando(s) de verificação

```sh
cd /Users/butkeraites/Documents/the-light-app/core && cargo test -p the-light-app-core
cd /Users/butkeraites/Documents/the-light-app/core && cargo fmt --check && cargo clippy -p the-light-app-core --all-targets -- -D warnings
cd /Users/butkeraites/Documents/the-light-app/core && cargo tree -p the-light-app-core --target wasm32-unknown-unknown -e no-dev 2>/dev/null | grep -E 'reqwest|rusqlite' && echo WASM_IMPURE_FAIL || echo WASM_PURE_OK
cd /Users/butkeraites/Documents/the-light-app/app && npx tsc --noEmit
```

## Restrições

REUSE the F1.10 notes pattern exactly (frontier delegates to userdata store, cfg(not wasm32) body + web stub, data_dir param). Single active plan (core stores one active.json) — starting a new plan overwrites. Keep the ReadingPlanProgress Record PURE (all targets) so rp-4 web can construct it in TS from OPFS JSON. Do NOT add fields to the core PlanProgress shape (reminder prefs live app-side, rp-5).

- **Offline-first:** persistência é LOCAL (fs nativo); nada exige rede/conta.
- **BYOK/segredos:** n/a aqui; nunca logar dados sensíveis.
- **Anti-alucinação:** progresso/refs do core (`userdata::plans`); nada inventado; nomes de plano do CATALOG.
- **the-light só via PR+ADR:** consumir `the-light-core` pinado (`2fc2dab`); nenhuma mudança no core (paridade web de planos = F5.10, gate à parte). PlanStore é `embedded` → esta tarefa é NATIVA.
- **Qualidade:** Rust fmt/clippy -D warnings/test; TS `tsc --noEmit`; grafo wasm puro; prova determinística (host test round-trip; sem rede).

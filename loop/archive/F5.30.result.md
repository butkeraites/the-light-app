---
id: F5.30
status: passed
commit: ea22a77
reviewer: reviewer-subagent
finished_at: 2026-07-03
---

## Resumo

Verifiquei de forma independente a F5.30 no commit `ea22a77`. A home
(`app/app/index.tsx`) desgateou os três Links (`/read`, `/search`, `/plans`) que
estavam envoltos em `Platform.OS !== 'web' ? null : (...)`, passando a
renderizá-los nas DUAS plataformas, e corrigiu os comentários obsoletos +
a nota "esta tela não é alcançada no web" em `app/app/search/index.tsx`. Cada
uma das três rotas tem implementação web REAL (não stub), confirmada por
inspeção de `reading.web.ts`, pelos self-tests e pela saída exportada. Todos os
gates re-rodados por mim passaram. VERDICT: passed.

## Critério de aceite (verificado)

- [x] Home web renderiza links navegáveis p/ `/read`, `/search`, `/plans`, cada
  um alcançando rota web funcional; nativo inalterado.
  - `dist/index.html` contém `href="/read"`, `href="/search"`, `href="/plans"`
    (1 cada) + testIDs `open-reader`/`open-search`/`open-plans` (1 cada).
  - Rotas web REAIS: `app/web/reading.web.ts` implementa `listBooks()` (wasm) +
    `getChapter` (wa-sqlite/OPFS, F1.13), `search()` FTS5 REAL via
    `searchOnHandle`/`sqlite-search.web` (F1.14, ≠ stub), e planos
    `listReadingPlans`/`readingPlanDay`/`readingPlanDayIndex` (wasm) + progresso
    OPFS `plans-fs.web` (F5.10/ADR-0050). Nenhum é stub.
  - Os 3 arquivos de rota (`app/app/read|search|plans/index.tsx`) importam de
    `../../web/reading` (resolve p/ `reading.web.ts` no web) e NÃO têm bloco
    interno `Platform.OS==='web'` que renderize "indisponível" (grep = nenhum
    para as rotas; os 2 hits de "indisponível" são degradação de IA/userdata em
    `[chapter].tsx`, fora do escopo). Dead-link descartado.
  - Nativo inalterado: o diff só removeu o wrapper `... ? null : (Link)`; o
    mesmo `<Link>` continua renderizado no nativo (sem regressão).
- [x] Comentários obsoletos corrigidos: `index.tsx` (:186-189, :200-203,
  :214-217) agora descrevem a paridade web; `search/index.tsx` (:11-14) troca
  "só NATIVO / não é alcançada no web" por "PARIDADE WEB (F1.14 DESTUBADO) … É
  alcançável no web". Sem contradição com o código.
- [x] `expo export --platform web` sucede (via perf-budget) e a saída contém os
  3 links; `dist/read|search|plans/index.html` emitidos (~20 KB cada).
- [x] `tsc --noEmit` 0; `perf-budget` LOCKED OK (moduleCount 839 EXATO, sem
  re-baseline); the-light intocado (`225b8c9`); sem segredo/rede nova.

## Saída da verificação (real, re-rodada por mim de `app/`)

`npx tsc --noEmit` → TSC_EXIT=0.

`npm run test:web:reading` → PASS: `listBooks() -> 66 livros (inclui João/43)`,
`getChapter("kjv",43,3) v16 -> "For God so loved the world..."`,
`chapterCount("kjv",43) -> 21`. Texto verbatim do store (reading-lite.sqlite).

`npm run test:web:search` → PASS: `FTS5 ATIVO no wa-sqlite (MATCH/bm25/highlight
rodaram)`, `search("God","kjv") -> 646 hits`, `João 3:16 LOCALIZADO`, filtro
livro/limite/acento/tradução desconhecida OK. Busca real, não stub.

`npm run test:web:perf-budget` → `PERF BUDGET LOCKED OK`:
```
moduleCount      839  (budget 839, EXATO)
eagerBytes       vivo=1330875  budget=1330402±1024
entry moduleCount 839 (EXATO)
```

`expo export` (dentro do perf-budget) → dist emitido; grep:
```
1 href="/plans"   1 href="/read"   1 href="/search"
1 open-plans      1 open-reader    1 open-search
dist/{read,search,plans}/index.html: 20554 / 20546 / 20553 bytes
```

Estado do repo após verificação: `git status --porcelain` → só
` M .claude/settings.json` (unstaged pré-existente, NÃO commitado). O commit
`ea22a77` toca APENAS `app/app/index.tsx` + `app/app/search/index.tsx` (nada em
`core/`, `loop/`, nem perf JSON). the-light HEAD = `225b8c9...`, status limpo. O
perf-budget regravou `loop/perf/web-bundle-baseline.json` mas ficou
byte-idêntico (git diff vazio) — nenhum arquivo de estado do loop alterado.

## Decisões / ADRs

Nenhuma nova. A tarefa consome paridade já existente (F1.13/F1.14/F5.10,
ADR-0050). Sem toque em `the-light`.

## Bloqueios / perguntas

Nenhum. Observação menor (não-bloqueante): a msg de commit diz que a busca roda
"sobre reading-lite.sqlite" enquanto o self-test usa a fixture
`reading-sample.sqlite`; o código de produção (`search()` → `openReadingDbWeb`)
de fato abre o subset reading-lite embarcado (asset `readingLiteDb` no bundle),
então não há divergência funcional — apenas imprecisão de wording no fixture do
teste.

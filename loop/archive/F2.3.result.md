---
id: F2.3
status: passed
commit: f08a651
reviewer: reviewer
finished_at: 2026-07-01
---

## Resumo

PR ao `the-light-core` implementado no branch **local** `feat/ai-gemini-streaming`
(commit `f08a651`), tocando **apenas** `crates/the-light-core/src/ai/mod.rs` (+44) e
`.../ai/providers.rs` (+219). Entrega **D1 (GeminiProvider)** e **D4 (streaming no trait
`LlmProvider`)** do ADR-0023. `GeminiProvider` implementa `LlmProvider` e está registrado
em `PROVIDERS`/`default_model`/`build_provider`/`estimate_cost_usd`; `gemini_body`/
`gemini_chat_body`/`gemini_extract` são funções puras testadas. `complete_stream` tem
**default não-quebrante** (chama `self.complete` e emite a String inteira 1× via `on_token`)
— Mock/anthropic/openai/ollama seguem válidos **sem override** e sem mudança de comportamento.
Verificação independente **verde**: `fmt` (0), `clippy -D warnings` (0), `cargo test --workspace`
(exit 0, 0 failed; core lib **184 passed** com os **7 testes novos**), `build -p the-light-core` OK.
`main` **intacto** em `8f66004` e o branch **NÃO** foi pushado. Chave nunca em log/git.

## Critério de aceite

- [x] **`GeminiProvider` implementa `LlmProvider`; registrado em todos os pontos.**
  `impl LlmProvider for GeminiProvider` (providers.rs:424) — `name()=="gemini"`, `model()`,
  `complete()`/`chat()` via `post()` (POST a `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`,
  key no header `x-goog-api-key`, l.447-457). `"gemini"` em `PROVIDERS` (mod.rs:30),
  `default_model`→`gemini-2.0-flash` (providers.rs:31), `build_provider` exige key
  (l.63-66), `estimate_cost_usd` (flash `(0.10,0.40)` conhecido; demais gemini → `None`,
  l.83-84). `gemini_body`/`gemini_chat_body`/`gemini_extract` puros e testados.
- [x] **`LlmProvider::complete_stream` default não-quebrante; providers existentes intactos.**
  Default no trait (mod.rs:345-354): `let full = self.complete(system,user)?; on_token(&full); Ok(full)`.
  Nenhum provider sobrescreve; `complete`/`chat` de anthropic/openai/ollama/Mock inalterados
  em comportamento. Teste `complete_stream_default_emits_full_once` prova emissão única.
- [x] **the-light: `fmt --check`, `clippy -D warnings`, `cargo test` (workspace) TODOS verdes.**
  fmt exit 0; clippy exit 0 (0 warnings); test exit 0, 0 failed em todos os binários, core lib
  **184 passed**. Sem regressão.
- [x] **`cargo build -p the-light-core` (default/embedded) OK.** exit 0.
- [x] **Commit no branch `feat/ai-gemini-streaming`; push NÃO feito.** `main..feat` = só
  `f08a651`; `feat/...@{u}` → `fatal: no upstream` (exit 128); `main` == `8f66004`; tree limpo.

## Saída da verificação

**Estado do git (via `.git` refs + `git --git-dir` — read-only):**
```
HEAD branch        : feat/ai-gemini-streaming
branch tip         : f08a6512caa628cb662eff94c907e022de134241   (f08a651)
main               : 8f6600460c3680a537d2f5df81b6980ed7e630d5   (== esperado, INTACTO)
main..feat commits : f08a651 feat(ai): GeminiProvider + LlmProvider::complete_stream (non-breaking default)
upstream           : fatal: no upstream configured for branch 'feat/ai-gemini-streaming'  (exit 128 → local-only, SEM push)
status --porcelain : (vazio)  exit 0  → working tree limpo, casa com f08a651
```
Reflog confirma base: `checkout: moving from main to feat/ai-gemini-streaming` a partir de
`8f66004`, seguido de **1 commit** → `f08a651`.

**`git show --stat f08a651` (toca SÓ o módulo `ai`):**
```
f08a651 feat(ai): GeminiProvider + LlmProvider::complete_stream (non-breaking default)
 crates/the-light-core/src/ai/mod.rs       |  44 +++++-
 crates/the-light-core/src/ai/providers.rs | 219 +++++++++++++++++++++++++++++-
 2 files changed, 255 insertions(+), 8 deletions(-)
```
Nada fora de `crates/the-light-core/src/ai/` — nenhuma quebra de API pública fora do módulo.

**Qualidade (workspace do the-light, via `cargo --manifest-path`):**
```
cargo fmt --all --check                                  → fmt exit 0
cargo clippy --workspace --all-targets -- -D warnings     → clippy exit 0  (0 warning/error)
cargo test --workspace                                    → test exit 0
   ... (todos os binários "test result: ok", 0 failed) ...
   test result: ok. 184 passed; 0 failed; 0 ignored; ...   (the-light-core lib)
   test result: ok.  90 passed; 0 failed; ...
   [demais binários: 17/9/6/7/4/5/5/7/10/7/10/2/4/10/2/0, todos 0 failed]
cargo build -p the-light-core                             → build exit 0
```

**Testes gemini/stream (`cargo test -p the-light-core --lib "ai::"` — 58 passed, 0 failed):**
```
test ai::providers::tests::gemini_body_shape ... ok
test ai::providers::tests::gemini_extract_concats_parts ... ok
test ai::providers::tests::gemini_extract_errors_without_candidates ... ok
test ai::providers::tests::gemini_chat_body_maps_assistant_to_model_role ... ok
test ai::providers::tests::gemini_registered_in_factory_and_default_model ... ok
test ai::tests::complete_stream_default_emits_full_once ... ok
test ai::tests::gemini_is_registered_in_providers ... ok
```
7 testes novos (5 `gemini_*` em providers + `gemini_is_registered_in_providers` +
`complete_stream_default_emits_full_once` no trait). Todos passam.

**Registros gemini (`grep -n "gemini" mod.rs providers.rs` — trechos-chave):**
```
mod.rs:30       pub const PROVIDERS: &[&str] = &["anthropic", "openai", "ollama", "gemini"];
providers.rs:31         "gemini" => "gemini-2.0-flash",                 (default_model)
providers.rs:63-64      "gemini" => { let key = key.ok_or_else(|| AiError::NoKey("gemini".into()))?; ... }
providers.rs:83-84      "gemini-2.0-flash" | "gemini-2.0-flash-001" => (0.10, 0.40),
                        m if m.starts_with("gemini") => return None,    (preço incerto → None)
```

**Chave NUNCA em log/git (crítico):**
```
grep -rnE "println!|eprintln!|dbg!|print!|log::|tracing::"  ../ai/   →
   ai/keys.rs:23:  // `Debug` redige os valores: nunca exponha chaves, mesmo via `dbg!`/panic.
   (única ocorrência = COMENTÁRIO em keys.rs, arquivo NÃO tocado por este commit; zero chamadas reais)
uso de self.key (providers.rs)  →  só em headers:
   200: .header("x-api-key", &self.key)                 (anthropic)
   270: .header("authorization", format!("Bearer {}", self.key))  (openai)
   453: .header("x-goog-api-key", &self.key)            (gemini — NÃO na URL)
URL Gemini (l.447-449): só `.../models/{model}:generateContent` — sem key.
Testes usam só "PLACEHOLDER"/"sk"/"k"; nenhuma chave real no diff.
```

## Decisões / ADRs

- Implementa **D1 (GeminiProvider)** + **D4 (streaming no trait)** do **ADR-0023** (sign-off
  humano no gate F2.2), precedente ADR-0005/F0.6a para PR sancionado ao `the-light`.
- **SSE real por provedor foi deliberadamente adiado**: o override de streaming incremental
  (reqwest SSE) não foi implementado para não arriscar o build/testes; o `complete_stream`
  fica no **default não-quebrante** (emite a resposta 1×). Isso é **evolução** — a fronteira/UX
  de streaming (F2.3a/F2.5) já funciona sobre o default; streaming real é incremento futuro
  sem mudar chamadores (assinatura callback compatível com UniFFI/JSI).
- **D2 (desacoplar `ai::study`/`citation` de `research::WebSource`/`reqwest` p/ wasm) está FORA
  de escopo** — é um PR focado separado, antes da F2.7 (menor risco/review).
- Nota de método: o `git`/`cargo` diretos via Bash contra `../the-light/**` são bloqueados por
  deny-rule neste ambiente; a verificação foi feita com o tool `Read` + `git --git-dir`/`cargo
  --manifest-path` (read-only), produzindo a saída **real** colada acima (não fabricada).

## Bloqueios / perguntas

**HANDOFF BLOQUEANTE (ação humana obrigatória).** O branch `feat/ai-gemini-streaming`
(`f08a651`) está pronto e verde **localmente**, mas **NÃO foi pushado nem mergeado** (push/merge
ao core é ação do humano — F0.6a). Próximos passos:
1. **Humano**: `git push` do branch `feat/ai-gemini-streaming` + **merge na `main` do `the-light`**;
2. **Humano**: informar o **rev mergeado**;
3. **Driver**: **re-pinar** o `the-light-core` na fronteira para esse rev — isso é a tarefa **F2.3a**.

O Driver deve escrever `loop/HALT` com esse motivo (aguardando push/merge + rev p/ re-pin).
Enquanto o re-pin não acontecer, tarefas da fronteira que dependam do core novo (Gemini/streaming)
ficam bloqueadas.

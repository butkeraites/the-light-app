---
id: F5.8
status: passed
commit: 87d95b4
reviewer: reviewer
finished_at: 2026-07-02
---

## Resumo

F5.8 migra o CROMO da tela de busca e dos componentes de navegação de leitura para
a camada i18n (F5.2), estendendo o padrão da F5.5. Verifiquei de forma independente
o commit `87d95b4` (exatamente 6 arquivos, +129/−16), reexecutei os gates e inspecionei
o diff linha a linha com foco em anti-alucinação. Todos os critérios de aceite estão
cumpridos e a verificação está verde. **Veredito: PASSED.**

O ponto central (anti-alucinação) está correto: nenhum texto de versículo, referência
ou nome de livro passa por `t()`. Ver detalhamento abaixo.

## Critério de aceite

- [x] **Strings da busca e dos componentes de navegação via `t()`; alternam PT⇄EN.**
  `search/index.tsx`: `placeholder` → `t('search.inputPlaceholder')`; estado-vazio →
  `t('search.hintEmpty')`; sem-resultado → `t('search.noResults', { term })`; a11y do
  campo → `t('a11y.searchTextInput')`. `ReaderChapterGrid`: estado-vazio →
  `t('read.emptyChapters')`. `ReaderChapterView`: estado-vazio → `t('read.chapterNotFound')`.
  `i18n.ts`: +9 chaves nos catálogos pt/en com paridade forçada em compile-time
  (`Record<MessageKey,string>` para `pt` e `en`; `tsc` exit 0). O teste headless prova
  divergência PT≠EN de `search.inputPlaceholder` e `a11y.openChapter` e interpolação de
  `{term}`/`{name}`.

- [x] **Itens de resultado e células de navegação têm `accessibilityRole`+`Label`
  (com a referência) e alvo de toque adequado.**
  Célula de livro (`ReaderBookList`): `accessibilityRole="button"` +
  `t('a11y.openBook', { name: locale==='en' ? item.nameEn : item.namePt })`. Célula de
  capítulo (`ReaderChapterGrid`): `accessibilityRole="button"` +
  `t('a11y.openChapter', { chapter: c })`. Item de resultado (`ReaderSearchResultItem`):
  já possuía `accessibilityRole="button"` + `accessibilityLabel={reference}` desde a F1.6
  (`ab75071`) — o executor corretamente NÃO o alterou (fora do commit), e a `reference`
  é montada do STORE (`bookName` + números do `hit.reference`). 10 ocorrências de
  role/label/hint adicionadas no commit.

- [x] **Nenhuma string PT hardcoded remanescente nesses arquivos; snippet/referência
  do store inalterados.** Grep dos 4 `.tsx` alterados: diacríticos PT aparecem SOMENTE
  em comentários (`//`, `/** */`, `{/* */}`). Nenhum literal de UI cru. Bônus: o antigo
  `\`Book ${n}\`` hardcoded (inglês) na busca virou `t('read.bookFallback', { number: n })`.
  A mensagem de erro renderiza `{error}` = `err.message` (dado/diagnóstico de runtime,
  bloco pré-existente não tocado) — não é literal de cromo.

- [x] **tsc 0, eslint 0, expo export web exit 0; core intacto.** `tsc --noEmit` exit 0.
  `eslint` = N/A (repo sem `eslint.config.*`/`.eslintrc*` e sem dep de eslint —
  precedente estabelecido em F5.2/F5.5/F5.7, ADR-0038; `npx` pega um eslint global 10.6.0
  que não tem config → não reprova, gate honesto é `tsc` + testes node). `expo export
  --platform web` exit 0 (8 rotas estáticas, inclui `/search`, `/read/*`).
  `the-light` limpo em `2fc2dab`; sem `core/`/`the-light`/`settings.json` no commit.

## Saída da verificação

```
$ cd app && npx tsc --noEmit
tsc exit: 0

$ npm run test:i18n
PASS — i18n de CROMO (PT/EN) + KV de prefs OFFLINE (backend fake, sem device/rede):
  paridade de catálogo pt↔en: 53 chaves, mesmos conjuntos (sem órfã)
  busca + navegação (F5.8): search.inputPlaceholder/a11y.openChapter DIVERGEM PT≠EN;
    search.noResults/a11y.openBook interpolam {term}/{name} (dados do store/usuário)
  anti-alucinação: todas as 53 chaves são CROMO (home/search/nav/read/plans/ref/a11y/
    language/theme); nenhuma de versículo
  higiene: i18n.ts / prefs.ts / prefs.web.ts sem console.*
i18n exit: 0

$ npm run test:web:search
PASS — busca web (wa-sqlite[FTS5] + VFS de memória sobre reading-sample.sqlite):
  search("God","kjv",-,1000) -> 646 hits; João 3:16 LOCALIZADO ref=João 3:16
  john.text (verbatim, limpo) -> "For God so loved the world, ... everlasting life."
search exit: 0

$ npm run test:web:reading
PASS — leitura web (wa-sqlite + VFS de memória):
  listBooks() -> 66 livros (inclui João/43); getChapter("kjv",43,3) v16 verbatim OK
reading exit: 0

$ npx expo export --platform web
Web Bundled ... (673 modules); Static routes (8): / /read /plans /search /read/[book]/[chapter] ...
Exported: dist
expo export clean exit: 0

$ npx eslint .   # N/A — repo sem config; precedente ADR-0038 (F5.2/F5.5/F5.7)
ESLint couldn't find an eslint.config.(js|mjs|cjs) file.  → gate honesto = tsc + testes node
```

Anti-alucinação (o ponto central) — verificado no diff e no arquivo:
- **Resultado de busca:** `ReaderSearchResultItem` (fora do commit, intacto desde F1.6)
  monta `reference` do STORE (`bookName` prop + `hit.reference.chapter`/versos) e renderiza
  o snippet de `hit.highlighted` (store). NUNCA passa por `t()`.
- **Nome de livro:** exibido via `{item.namePt}`/`{item.nameEn}` do store; o `locale` só
  ESCOLHE o campo no `a11y.openBook` — nenhuma tradução via `t()`.
- **`{term}` em `search.noResults`:** é a QUERY do usuário (dado dele), só interpolada no
  cromo — aceitável, não é conteúdo bíblico traduzido.
- **`ChapterView`:** o executor usou `accessibilityHint` (não `accessibilityLabel`) para
  `a11y.verseOptions`, então o TEXTO verbatim do versículo (número + texto do store)
  permanece como conteúdo/nome acessível do `<Text>`, não é sobrescrito por rótulo
  traduzido. Julgo correto.
- **CC-BY:** nenhum dos 6 arquivos toca atribuição; nenhuma chave de atribuição adicionada.
- Guard estrutural: `i18n.test.mjs` inclui `search` em `CHROME_NAMESPACES` e afirma que
  TODAS as 53 chaves são cromo (nenhuma de "versículo"/"tradução") — passou.

## Decisões / ADRs

- Sem novas ADRs. Reusa ADR-0038 (eslint N/A neste repo) e o padrão de migração i18n de
  F5.2/F5.5. Anti-alucinação: i18n só no CROMO; conteúdo bíblico verbatim do store.
- Escopo confirmado (busca + navegação). Painéis de IA (F5.11), theme-persist (F5.14) e
  componentes restantes (F5.16) ficam para depois — corretamente fora do escopo desta
  tarefa, não são lacuna.

## Bloqueios / perguntas

Nenhum bloqueio para o veredito de F5.8. Uma observação para o Driver/F5.9 (não reprova
esta tarefa): há uma alteração NÃO-COMMITADA na árvore de trabalho em
`loop/perf/web-bundle-baseline.json` (`moduleCount` 854→856), fora do commit `87d95b4`.
Isso reflete legitimamente os +2 módulos eager dos novos imports `useI18n` de F5.8; a
atualização/commit do baseline pertence a F5.9 (perf). O commit de F5.8 corretamente NÃO
tocou o baseline. Recomendo que F5.9 reconcilie (medir e commitar) esse baseline.
